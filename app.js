const STORAGE_KEY = "anjwa-career-platform-plan-v1";
const SELF_EVAL_STORAGE_KEY = "anjwa-career-platform-self-eval-v1";
const CREATIVE_EVAL_STORAGE_KEY = "anjwa-career-platform-creative-eval-v1";
const SELF_EVAL_IDENTITY_STORAGE_KEY = "anjwa-career-platform-self-eval-identity-v1";
const COURSE_DESIGNER_STORAGE_KEY = "anjwa-career-platform-course-designer-v1";
const curriculumData = window.ANJWA_CURRICULUM_DATA || { plans: {} };
const recommendationData = window.ANJWA_RECOMMENDATION_DATA || { records: [] };
const universityRecommendationData = window.ANJWA_UNIVERSITY_RECOMMENDATION_DATA || { records: [] };
const topicData = window.ANJWA_TOPIC_DATA || { topics: [] };
const subjectGuideData = window.ANJWA_SUBJECT_GUIDE || { areaProfiles: {}, groupProfiles: {} };
const courseDesignerData = window.ANJWA_COURSE_DESIGNER || { interests: [], optionProfiles: [] };
const curriculumPlanOrder = ["current2026", "incoming2026", "incoming2025", "incoming2024"];
const plannerPlanOrder = ["incoming2026", "incoming2025", "incoming2024"];
const curriculumPlanLabels = {
  current2026: "전체 학년(2026학년도 현재)",
  incoming2026: "1학년(2026학년도 신입생)",
  incoming2025: "2학년(2025학년도 신입생)",
  incoming2024: "3학년(2024학년도 신입생)"
};
const choiceGroupStartOverrides = {
  incoming2026: {
    "3-1": [75]
  },
  incoming2025: {
    "3-1": [76]
  },
  incoming2024: {
    "3-1": [58]
  }
};
const recommendationCurriculumPlans = [
  { key: "incoming2026", label: "1학년(2026 신입)", shortLabel: "26신입", standard: "2022", className: "grade-1" },
  { key: "incoming2025", label: "2학년(2025 신입)", shortLabel: "25신입", standard: "2022", className: "grade-2" },
  { key: "incoming2024", label: "3학년(2024 신입)", shortLabel: "24신입", standard: "2015", className: "grade-3" }
];
const curriculumSemesterOrder = ["1-1", "1-2", "2-1", "2-2", "3-1", "3-2"];
const commonRecommendationSubjects = [
  "공통국어", "공통국어1", "공통국어2",
  "공통수학", "공통수학1", "공통수학2",
  "공통영어", "공통영어1", "공통영어2",
  "통합사회", "통합사회1", "통합사회2",
  "통합과학", "통합과학1", "통합과학2",
  "과학탐구실험", "과학탐구실험1", "과학탐구실험2",
  "한국사", "한국사1", "한국사2"
];
const genericRecommendationSubjectLabels = [
  "국어", "영어", "수학", "과학", "사회", "일반사회", "역사", "지리", "윤리"
];
const genericRecommendationSubjectPhrases = [
  "국어영어수학",
  "국어수학영어",
  "국어영어수학과학",
  "교과군",
  "과목군",
  "다양한교과",
  "다양한과목",
  "여러교과",
  "여러과목",
  "계열구분없이",
  "교과목선택이수",
  "진로및적성",
  "적성과진로",
  "전공을고려"
];
const recommendationSubjectCategories = [
  { key: "math", label: "수학 계열", keywords: ["수학", "대수", "미적분", "기하", "확률", "통계"] },
  { key: "science", label: "과학 계열", keywords: ["물리", "화학", "생명", "생물", "지구", "과학", "역학", "전자기", "물질", "에너지", "세포", "유전", "행성", "우주"] },
  { key: "other", label: "기타 선택과목", keywords: [] }
];

const additionalCourseCatalogSeed = [
  { name: "경제 수학", area: "수학", category: "진로선택" },
  { name: "인공지능 수학", area: "수학", category: "진로선택" },
  { name: "직무 수학", area: "수학", category: "진로선택" },
  { name: "실용 통계", area: "수학", category: "융합선택" },
  { name: "수학적 모델링", area: "수학", category: "융합선택" },
  { name: "자료와 통계", area: "수학", category: "융합선택" },
  { name: "직무 의사소통", area: "국어", category: "진로선택" },
  { name: "고전 읽기", area: "국어", category: "진로선택" },
  { name: "심화 국어", area: "국어", category: "진로선택" },
  { name: "기본 영어", area: "영어", category: "일반선택" },
  { name: "직무 영어", area: "영어", category: "진로선택" },
  { name: "영어권 문화", area: "영어", category: "진로선택" },
  { name: "국제 관계의 이해", area: "사회", category: "진로선택" },
  { name: "비교 문화", area: "사회", category: "진로선택" },
  { name: "국제 경제", area: "사회", category: "진로선택" },
  { name: "국제 정치", area: "사회", category: "진로선택" },
  { name: "법과 사회", area: "사회", category: "진로선택" },
  { name: "금융과 경제생활", area: "사회", category: "융합선택" },
  { name: "전자기와 양자", area: "과학", category: "진로선택" },
  { name: "화학 반응의 세계", area: "과학", category: "진로선택" },
  { name: "세포와 물질대사", area: "과학", category: "진로선택" },
  { name: "생물의 유전", area: "과학", category: "진로선택" },
  { name: "행성우주과학", area: "과학", category: "진로선택" },
  { name: "과학의 역사와 문화", area: "과학", category: "융합선택" },
  { name: "생활과학 탐구", area: "기술·가정", category: "융합선택" },
  { name: "로봇과 공학세계", area: "기술·가정", category: "진로선택" },
  { name: "창의 공학 설계", area: "기술·가정", category: "진로선택" },
  { name: "데이터 과학", area: "정보", category: "진로선택" },
  { name: "소프트웨어와 생활", area: "정보", category: "융합선택" },
  { name: "인공지능과 미래사회", area: "정보", category: "융합선택" },
  { name: "음악 감상과 비평", area: "예술", category: "진로선택" },
  { name: "미술 감상과 비평", area: "예술", category: "진로선택" },
  { name: "연극", area: "예술", category: "일반선택" },
  { name: "무용", area: "예술", category: "일반선택" },
  { name: "스포츠 과학", area: "체육", category: "진로선택" },
  { name: "체육 전공 실기 기초", area: "체육", category: "진로선택" },
  { name: "일본어", area: "제2외국어·한문", category: "일반선택" },
  { name: "중국어", area: "제2외국어·한문", category: "일반선택" },
  { name: "스페인어", area: "제2외국어·한문", category: "일반선택" },
  { name: "프랑스어", area: "제2외국어·한문", category: "일반선택" },
  { name: "독일어", area: "제2외국어·한문", category: "일반선택" },
  { name: "한문", area: "제2외국어·한문", category: "일반선택" },
  { name: "인간과 심리", area: "교양", category: "진로선택" },
  { name: "교육의 이해", area: "교양", category: "일반선택" },
  { name: "교육학", area: "교양", category: "일반선택" },
  { name: "철학", area: "교양", category: "일반선택" }
];

const majorOptionGroups = [
  {
    label: "공학·AI·자연",
    options: ["공학계열", "기계공학", "전기전자공학", "반도체공학", "화학공학", "컴퓨터공학", "인공지능", "정보보안", "물리학", "화학", "신소재", "지구과학", "천문우주", "대기과학", "해양학"]
  },
  {
    label: "생명·의학·보건",
    options: ["의학·보건계열", "의예", "약학", "치의예", "한의예", "수의예", "간호학과", "보건관리학과", "물리치료학과", "작업치료학과", "임상병리학과", "방사선학과", "응급구조학과", "치위생학과", "생명과학", "생명공학", "식품공학", "환경생태"]
  },
  {
    label: "인문·사회·상경",
    options: ["인문·사회계열", "국어국문학과", "영어영문학과", "문헌정보학과", "문화콘텐츠학과", "정치외교학과", "사회학과", "사회복지학과", "심리학과", "행정학과", "법학과", "경영학과", "경제학과", "국제통상학과", "빅데이터경영", "지리학과"]
  },
  {
    label: "교육",
    options: ["교육계열", "교육학과", "초등교육과", "유아교육과", "국어교육과", "영어교육과", "사회교육과", "일반사회교육과", "역사교육과", "지리교육과", "윤리교육과", "수학교육과", "과학교육과", "물리교육과", "화학교육과", "생물교육과", "지구과학교육과", "환경교육과"]
  },
  {
    label: "미디어·어문·문화",
    options: ["미디어·광고·어문계열", "미디어", "광고", "신문방송", "언론", "영어영문", "어문", "문화콘텐츠"]
  }
];

const courses = [
  { id: "kor_reading", name: "주제 탐구 독서", area: "국어", category: "진로 선택", credits: 3, offered: true, grades: [1, 2, 3], tracks: ["humanities", "media"] },
  { id: "kor_debate", name: "독서 토론과 글쓰기", area: "국어", category: "진로 선택", credits: 3, offered: true, grades: [2, 3], tracks: ["humanities", "media"] },
  { id: "kor_media", name: "매체 의사소통", area: "국어", category: "융합 선택", credits: 3, offered: false, grades: [2, 3], tracks: ["media"] },
  { id: "kor_film", name: "문학과 영상", area: "국어", category: "융합 선택", credits: 3, offered: false, grades: [2, 3], tracks: ["media"] },
  { id: "math_algebra", name: "대수", area: "수학", category: "일반 선택", credits: 4, offered: true, grades: [1, 2], tracks: ["engineering", "medical"] },
  { id: "math_calc1", name: "미적분Ⅰ", area: "수학", category: "일반 선택", credits: 4, offered: true, grades: [2, 3], tracks: ["engineering", "medical"] },
  { id: "math_calc2", name: "미적분Ⅱ", area: "수학", category: "진로 선택", credits: 3, offered: true, grades: [2, 3], tracks: ["engineering", "medical"] },
  { id: "math_geometry", name: "기하", area: "수학", category: "진로 선택", credits: 3, offered: true, grades: [2, 3], tracks: ["engineering"] },
  { id: "math_ai", name: "인공지능 수학", area: "수학", category: "진로 선택", credits: 3, offered: false, grades: [2, 3], tracks: ["engineering"] },
  { id: "math_stats", name: "확률과 통계", area: "수학", category: "일반 선택", credits: 3, offered: true, grades: [2, 3], tracks: ["engineering", "medical", "humanities"] },
  { id: "soc_politics", name: "정치", area: "사회", category: "진로 선택", credits: 3, offered: true, grades: [2, 3], tracks: ["humanities"] },
  { id: "soc_economics", name: "경제", area: "사회", category: "진로 선택", credits: 3, offered: true, grades: [2, 3], tracks: ["humanities"] },
  { id: "soc_culture", name: "사회와 문화", area: "사회", category: "일반 선택", credits: 3, offered: true, grades: [2, 3], tracks: ["humanities", "media"] },
  { id: "soc_ethics", name: "윤리와 사상", area: "사회", category: "진로 선택", credits: 3, offered: true, grades: [2, 3], tracks: ["humanities"] },
  { id: "soc_issue", name: "사회문제 탐구", area: "사회", category: "융합 선택", credits: 3, offered: false, grades: [2, 3], tracks: ["humanities", "media"] },
  { id: "sci_physics", name: "물리학", area: "과학", category: "일반 선택", credits: 3, offered: true, grades: [2, 3], tracks: ["engineering"] },
  { id: "sci_chemistry", name: "화학", area: "과학", category: "일반 선택", credits: 3, offered: true, grades: [2, 3], tracks: ["engineering", "medical"] },
  { id: "sci_life", name: "생명과학", area: "과학", category: "일반 선택", credits: 3, offered: true, grades: [2, 3], tracks: ["medical"] },
  { id: "sci_earth", name: "지구과학", area: "과학", category: "일반 선택", credits: 3, offered: true, grades: [2, 3], tracks: ["engineering"] },
  { id: "sci_energy", name: "물질과 에너지", area: "과학", category: "진로 선택", credits: 3, offered: false, grades: [2, 3], tracks: ["medical", "engineering"] },
  { id: "sci_cell", name: "세포와 물질대사", area: "과학", category: "진로 선택", credits: 3, offered: false, grades: [2, 3], tracks: ["medical"] },
  { id: "sci_gene", name: "생물의 유전", area: "과학", category: "진로 선택", credits: 3, offered: false, grades: [2, 3], tracks: ["medical"] },
  { id: "eng_presentation", name: "영어 발표와 토론", area: "영어", category: "융합 선택", credits: 3, offered: true, grades: [1, 2, 3], tracks: ["engineering", "medical", "humanities", "media"] },
  { id: "career_project", name: "진로 주제 프로젝트", area: "교양", category: "융합 선택", credits: 2, offered: true, grades: [1, 2, 3], tracks: ["engineering", "medical", "humanities", "media"] }
];

const competencies = [
  {
    label: "학업역량",
    title: "수업을 통해 배우고 탐구하는 힘",
    body: "과목 성취도, 학습 태도, 문제 해결 과정, 자료 분석, 발표와 토론 참여가 함께 드러난다."
  },
  {
    label: "진로역량",
    title: "관심 분야를 확장하는 흐름",
    body: "희망 전공과 관련된 과목 선택, 독서, 탐구활동, 동아리, 진로활동이 연결될수록 설득력이 커진다."
  },
  {
    label: "공동체역량",
    title: "함께 배우고 실천하는 태도",
    body: "협업, 의사소통, 역할 수행, 갈등 조정, 학교와 지역사회 안에서의 실천 경험이 중요하다."
  },
  {
    label: "세특",
    title: "활동 목록보다 사고 과정",
    body: "세특은 무엇을 했는지보다 수업 안에서 어떻게 생각하고 탐구하며 성장했는지를 보여주는 자료다."
  },
  {
    label: "과목 선택",
    title: "진로와 학업 흐름의 증거",
    body: "전공 관련 과목을 왜 선택했는지, 그 과목에서 어떤 탐구를 했는지가 서류 평가에서 의미를 갖는다."
  },
  {
    label: "면접 대비",
    title: "기록을 설명할 수 있어야 함",
    body: "학생이 작성한 탐구활동과 자기평가서는 면접에서 자신의 활동을 구체적으로 설명하는 기초 자료가 된다."
  }
];

const sechukExamples = {
  motivation: {
    title: "탐구 동기",
    text: "예: 통합과학 수업에서 감염병 확산 그래프를 보며, 같은 감염병도 지역의 의료 접근성에 따라 피해가 달라질 수 있다는 점이 궁금해졌습니다."
  },
  question: {
    title: "탐구 질문",
    text: "예: 의료 접근성이 낮은 지역에서는 어떤 요인이 감염병 대응을 어렵게 만들까? 교통, 병원 수, 고령 인구 비율 중 어떤 변수가 더 크게 작용할까?"
  },
  concept: {
    title: "수업 개념 연결",
    text: "예: 확률과 통계의 자료 분석, 통합사회에서 배운 지역 격차, 생명과학의 감염과 면역 개념을 함께 사용해 질문을 정리했습니다."
  },
  process: {
    title: "탐구 과정",
    text: "예: 공공데이터를 찾아 지역별 병원 수와 고령 인구 비율을 표로 정리하고, 친구들과 변수별 영향 가능성을 토론한 뒤 그래프로 비교했습니다."
  },
  result: {
    title: "결과",
    text: "예: 단순히 병원 수만 보는 것보다 이동 시간, 고령 인구 비율, 예방 정보 접근성이 함께 작용한다는 점을 알게 되었습니다."
  },
  limit: {
    title: "한계",
    text: "예: 사용한 자료가 시·군 단위라 개인별 이동 거리나 실제 진료 경험을 충분히 반영하지 못했습니다. 통계 자료만으로 판단하는 데 한계가 있었습니다."
  },
  follow: {
    title: "후속 활동",
    text: "예: 다음 진로활동에서는 보건소 접근성, 원격 진료, 지역 의료 정책을 조사하고, 사회문제 탐구나 동아리 활동에서 개선 방안을 제안해 볼 수 있습니다."
  }
};

const selfEvalFields = [
  {
    key: "motivation",
    label: "탐구 동기",
    placeholder: "수업 중 어떤 개념, 사례, 자료에서 호기심이 생겼는지 적어보세요."
  },
  {
    key: "question",
    label: "탐구 질문",
    placeholder: "내가 해결하고 싶은 질문을 한 문장으로 적어보세요."
  },
  {
    key: "concept",
    label: "수업 개념 연결",
    placeholder: "수업에서 배운 개념, 용어, 원리와 어떻게 연결되는지 적어보세요."
  },
  {
    key: "process",
    label: "탐구 과정",
    placeholder: "자료 조사, 실험, 비교, 토론, 분석, 발표 등 실제 과정을 적어보세요."
  },
  {
    key: "result",
    label: "탐구 결과",
    placeholder: "알게 된 점, 해석한 내용, 정리한 결론을 적어보세요."
  },
  {
    key: "limit",
    label: "한계와 보완",
    placeholder: "자료나 방법의 한계, 더 확인해야 할 점을 적어보세요."
  },
  {
    key: "followUp",
    label: "후속 탐구·실천",
    placeholder: "다음 수업, 다른 과목, 동아리, 진로활동, 실천으로 어떻게 이어갈지 적어보세요."
  },
  {
    key: "book",
    label: "독서 기록",
    placeholder: "예: 팩트풀니스(한스 로슬링)",
    rows: 2
  },
  {
    key: "bookReflection",
    label: "책을 읽고 느낀 점",
    placeholder: "책을 통해 새롭게 알게 된 점, 생각이 달라진 부분을 적어보세요."
  },
  {
    key: "bookConnection",
    label: "독서와 과목의 연결",
    placeholder: "책의 내용이 수업 개념, 탐구 질문, 후속 활동과 어떻게 연결되는지 적어보세요."
  }
];

const creativeEvalTypes = {
  autonomy: {
    label: "자율활동",
    shortGuide: "학교와 학급 안에서 발견한 문제, 맡은 역할, 함께 만든 변화를 정리합니다.",
    competencies: ["공동체역량", "학업역량"],
    fields: [
      { key: "activityName", label: "주요 활동", rows: 2, placeholder: "학급·학교 프로그램이나 프로젝트의 이름과 핵심 내용을 적어보세요.", help: "활동명만 나열하지 말고 어떤 문제나 필요를 다룬 활동인지 함께 적습니다." },
      { key: "motivation", label: "참여 계기와 문제의식", placeholder: "왜 참여했고, 학교생활에서 어떤 문제나 필요를 발견했는지 적어보세요.", help: "진로를 억지로 붙이기보다 실제로 불편했던 점, 궁금했던 점, 바꾸고 싶었던 점에서 출발합니다." },
      { key: "role", label: "내가 맡은 역할", placeholder: "공식 직책뿐 아니라 조사, 의견 수렴, 기록, 조정 등 실제로 맡은 일을 적어보세요.", help: "반장·부장 같은 직책보다 내가 직접 판단하고 실행한 일이 드러나야 합니다." },
      { key: "process", label: "실행 과정", placeholder: "의견 조사, 회의, 자료 분석, 캠페인, 제작, 발표 등 실제 과정을 순서대로 적어보세요.", help: "처음 계획과 실제 실행이 어떻게 달라졌는지, 무엇을 근거로 방법을 바꾸었는지 적으면 좋습니다." },
      { key: "collaboration", label: "협력과 문제 해결", placeholder: "의견 차이, 어려움, 피드백을 어떻게 조정하고 해결했는지 적어보세요.", help: "모두와 잘 지냈다는 표현보다 의견 차이를 듣고 조정한 구체적인 장면을 씁니다." },
      { key: "result", label: "결과와 변화", placeholder: "활동 전후 달라진 점, 결과물, 참여 반응 등 확인할 수 있는 변화를 적어보세요.", help: "성공했다는 감상보다 설문, 참여율, 결과물, 규칙 변화처럼 확인 가능한 근거를 남깁니다." },
      { key: "reflection", label: "배운 점과 성장", placeholder: "공동체 안에서 무엇을 배우고 나의 태도나 판단이 어떻게 달라졌는지 적어보세요.", help: "리더십이라는 단어 대신 경청, 책임, 조정, 재시도처럼 성장한 행동을 설명합니다." },
      { key: "book", label: "독서 기록", rows: 2, placeholder: "제목(저자)의 형태로 적어보세요.", help: "활동을 이해하거나 문제를 새롭게 바라보는 데 실제로 도움을 준 책을 적습니다." },
      { key: "bookReflection", label: "독서로 달라진 생각", placeholder: "책을 읽기 전후 생각이 어떻게 달라졌는지 적어보세요.", help: "감명 깊었다는 표현보다 책에서 얻은 관점이 판단 기준을 어떻게 바꾸었는지 씁니다." },
      { key: "bookConnection", label: "독서와 활동의 연결", placeholder: "책에서 얻은 질문이나 관점을 활동에 어떻게 적용했는지 적어보세요.", help: "독서를 별도 실적으로 두지 말고 조사 문항, 실행 방법, 결과 해석과 연결합니다." },
      { key: "followUp", label: "다음 실천", placeholder: "보완하거나 다음 학년에 이어가고 싶은 활동을 적어보세요.", help: "막연히 더 열심히 하겠다고 쓰지 말고 다음에 바꿀 방법이나 이어갈 문제를 구체화합니다." }
    ]
  },
  career: {
    label: "진로활동",
    shortGuide: "진로에 대한 질문이 조사·상담·독서·교과 학습으로 이어진 과정을 정리합니다.",
    competencies: ["진로역량", "학업역량"],
    fields: [
      { key: "activityName", label: "진로 질문 또는 활동", rows: 2, placeholder: "탐색한 분야와 해결하고 싶었던 진로 질문을 적어보세요.", help: "직업 이름만 쓰기보다 그 분야에서 알고 싶었던 문제를 질문으로 적습니다." },
      { key: "motivation", label: "관심을 갖게 된 계기", placeholder: "수업, 독서, 상담, 강연, 경험 중 무엇이 관심을 만들었는지 적어보세요.", help: "어릴 때부터 꿈이었다는 표현보다 생각이 시작되거나 달라진 실제 계기를 씁니다." },
      { key: "process", label: "탐색 과정", placeholder: "조사한 자료, 읽은 책, 상담 내용, 비교한 학과·직업, 직접 해본 활동을 적어보세요.", help: "강연을 들었다는 사실보다 강연 뒤 어떤 자료를 추가로 확인하고 질문을 확장했는지 적습니다." },
      { key: "learningConnection", label: "교과·탐구 연결", placeholder: "관심 분야가 어떤 과목의 개념이나 탐구활동과 연결되었는지 적어보세요.", help: "진로활동을 별도 행사로 두지 말고 수업 개념, 선택과목, 독서와 연결합니다." },
      { key: "insight", label: "새롭게 이해한 점", placeholder: "분야의 실제 문제, 필요한 역량, 예상과 달랐던 점을 적어보세요.", help: "좋은 직업이라는 감상보다 그 분야가 해결하는 문제와 필요한 학업을 구체적으로 정리합니다." },
      { key: "change", label: "진로 생각의 변화", placeholder: "관심 분야가 넓어지거나 좁아진 과정, 나에게 맞는 점과 더 확인할 점을 적어보세요.", help: "진로가 확정되지 않아도 괜찮습니다. 무엇을 확인하면서 선택 기준이 달라졌는지가 중요합니다." },
      { key: "reflection", label: "나의 강점과 보완점", placeholder: "현재 갖춘 역량과 앞으로 보완해야 할 학습·경험을 적어보세요.", help: "적성검사 결과를 옮기기보다 실제 활동에서 확인한 강점과 부족한 점을 근거로 씁니다." },
      { key: "book", label: "독서 기록", rows: 2, placeholder: "제목(저자)의 형태로 적어보세요.", help: "직업 소개보다 진로 분야가 다루는 문제와 관점을 이해하는 데 도움을 준 책을 적습니다." },
      { key: "bookReflection", label: "독서로 달라진 진로 생각", placeholder: "책을 읽고 해당 분야나 직업을 보는 관점이 어떻게 달라졌는지 적어보세요.", help: "진로가 확고해졌다는 결론보다 새롭게 알게 된 역할, 쟁점, 필요한 역량을 씁니다." },
      { key: "bookConnection", label: "독서와 진로 탐색의 연결", placeholder: "책에서 생긴 질문을 상담, 교과 학습, 학과 조사로 어떻게 이어갔는지 적어보세요.", help: "책의 관점을 다음 조사나 선택과목 검토에 실제로 적용한 과정을 씁니다." },
      { key: "followUp", label: "다음 탐색 계획", placeholder: "다음 과목 선택, 독서, 상담, 탐구에서 무엇을 확인할지 적어보세요.", help: "다음 학년에 어떤 질문을 더 깊게 탐구할지, 어떤 과목이나 활동으로 확인할지 씁니다." }
    ]
  },
  club: {
    label: "동아리활동",
    shortGuide: "동아리의 이름보다 주제 선택, 탐구 방법, 역할, 결과와 심화 과정을 정리합니다.",
    competencies: ["학업역량", "진로역량", "공동체역량"],
    fields: [
      { key: "activityName", label: "동아리명과 활동 주제", rows: 2, placeholder: "동아리명과 가장 의미 있었던 탐구·프로젝트 주제를 적어보세요.", help: "여러 활동을 모두 적기보다 한 해의 성장 흐름을 가장 잘 보여주는 주제를 중심으로 씁니다." },
      { key: "motivation", label: "주제 선택 이유", placeholder: "수업, 독서, 이전 활동에서 어떤 질문이 생겨 이 주제를 골랐는지 적어보세요.", help: "진로와 관련 있어서라는 말보다 어떤 현상이나 개념이 궁금했는지 구체화합니다." },
      { key: "process", label: "탐구·제작 과정", placeholder: "자료 조사, 실험, 설계, 토론, 제작, 발표 과정을 구체적으로 적어보세요.", help: "사용한 자료와 방법, 변인을 통제하거나 기준을 정한 과정이 드러나면 좋습니다." },
      { key: "role", label: "나의 역할과 협업", placeholder: "팀 안에서 맡은 일, 의견을 조정하거나 동료의 기여를 연결한 과정을 적어보세요.", help: "혼자 한 일과 함께 한 일을 구분하고, 협업이 결과를 어떻게 바꾸었는지 씁니다." },
      { key: "result", label: "결과와 근거", placeholder: "실험 결과, 분석 내용, 결과물, 발표 피드백처럼 확인 가능한 내용을 적어보세요.", help: "성공 여부뿐 아니라 예상과 달랐던 결과와 그 결과를 어떻게 해석했는지 적습니다." },
      { key: "limit", label: "한계와 보완", placeholder: "자료·방법·시간의 한계와 다시 한다면 바꾸고 싶은 점을 적어보세요.", help: "한계는 실패의 고백이 아니라 탐구 방법을 점검하고 다음 질문을 만드는 과정입니다." },
      { key: "reflection", label: "배운 점과 역량", placeholder: "학업, 진로, 협업 측면에서 무엇이 성장했는지 실제 행동과 함께 적어보세요.", help: "흥미로웠다는 감상보다 개념 이해, 자료 해석, 의사소통 방식이 어떻게 달라졌는지 씁니다." },
      { key: "followUp", label: "다음 탐구 계획", placeholder: "다음 학년이나 다른 과목에서 어떻게 심화할지 적어보세요.", help: "같은 활동을 반복하기보다 새로운 변수, 자료, 교과 개념을 추가해 심화 방향을 제시합니다." }
    ]
  }
};

const creativeEvalExamples = {
  autonomy: [
    {
      grade: 1,
      title: "학급 학습환경 개선",
      values: {
        activityName: "학급 학습환경 개선 프로젝트 - 소음과 좌석 이용 문제 조사",
        motivation: "자습 시간마다 소음 기준이 달라 갈등이 생기는 모습을 보고, 구성원이 함께 지킬 수 있는 기준이 필요하다고 생각했다.",
        role: "학급 구성원의 의견을 익명 설문으로 모으고, 서로 다른 요구를 분류해 회의 자료로 정리했다.",
        process: "소음이 불편한 시간과 이유를 조사하고, 집중 구역과 대화 가능 구역을 나누는 방안을 제안해 일주일간 시범 운영했다.",
        collaboration: "조용한 환경과 자유로운 협업을 원하는 의견이 충돌해 시간대별 기준을 두는 절충안을 만들었다.",
        result: "시범 운영 뒤 설문에서 불편하다는 응답이 줄었고, 학급회의를 거쳐 자습 시간 규칙으로 정리되었다.",
        reflection: "좋은 규칙은 한쪽 의견을 따르는 것이 아니라 자료를 통해 요구를 확인하고 합의 가능한 기준을 만드는 과정임을 배웠다.",
        followUp: "다음에는 규칙이 잘 지켜지지 않는 이유도 조사해 학생이 스스로 점검할 수 있는 방법을 제안하고 싶다."
      }
    },
    {
      grade: 2,
      title: "학교 정보 접근성 개선",
      values: {
        activityName: "교내 행사·지원 프로그램 안내 방식 개선",
        motivation: "신청 기간을 놓쳐 프로그램에 참여하지 못한 친구들이 있다는 점에서 정보가 전달되는 방식에 문제가 있다고 판단했다.",
        role: "기존 안내 경로를 조사하고 학생들이 자주 확인하는 매체와 필요한 정보를 설문으로 확인했다.",
        process: "공지 유형을 신청·상담·행사로 분류하고, 마감일과 대상이 먼저 보이는 주간 안내 양식을 제작해 시범 배포했다.",
        collaboration: "학생회와 담당 선생님의 의견을 반영해 개인정보가 포함되지 않도록 게시 기준을 수정했다.",
        result: "시범 기간 중 문의가 반복되는 항목이 줄었고, 마감일 중심 안내가 유용하다는 피드백을 받았다.",
        reflection: "정보를 많이 제공하는 것보다 이용자의 상황에 맞게 구조화하는 것이 접근성을 높인다는 점을 알게 되었다.",
        followUp: "학년별로 필요한 정보가 다른지 비교하고, 접근성이 낮은 학생을 위한 보완 방법도 조사하고 싶다."
      }
    },
    {
      grade: 3,
      title: "후배 과목 선택 지원",
      values: {
        activityName: "후배를 위한 선택과목 경험 공유 활동",
        motivation: "과목 이름만 보고 선택했다가 수업 방식이 예상과 달랐다는 친구들의 경험을 보며 실제 수업 정보가 필요하다고 생각했다.",
        role: "과목별 수업 방식과 준비 경험을 수집할 질문을 만들고, 답변을 과장 없이 정리하는 역할을 맡았다.",
        process: "재학생 인터뷰를 통해 수업에서 다룬 개념, 활동 방식, 필요한 준비를 정리하고 공식 교육과정 정보와 구분해 안내했다.",
        collaboration: "개인 성적이나 교사 평가로 오해될 수 있는 표현은 제외하고, 여러 학생의 공통 경험만 남기도록 기준을 합의했다.",
        result: "후배 상담 시간에 자료가 활용되었고, 과목 선택 이유를 먼저 생각해 보게 되었다는 반응을 확인했다.",
        reflection: "경험을 전달할 때도 정확성, 개인정보, 해석의 한계를 함께 고려해야 책임 있는 정보가 된다는 점을 배웠다.",
        followUp: "후배들이 실제 선택 뒤 느낀 점을 다시 조사해 안내 자료의 부족한 부분을 보완하고 싶다."
      }
    }
  ],
  career: [
    {
      grade: 1,
      title: "보건 문제에서 진로 질문 찾기",
      values: {
        activityName: "지역에 따라 의료 이용이 달라지는 이유는 무엇일까?",
        motivation: "통합사회에서 지역 격차를 배우고, 거주 지역에 따라 병원까지 이동하는 시간이 다르다는 자료를 보며 관심이 생겼다.",
        process: "보건의료 직업을 조사한 뒤 병원 수, 교통, 고령 인구가 의료 접근성에 어떤 영향을 주는지 공공자료로 비교했다.",
        learningConnection: "통합사회의 지역 격차와 수학의 자료 해석을 연결해 단순히 의사라는 직업보다 보건정책과 의료서비스 구조를 함께 살펴보았다.",
        insight: "의료 문제는 치료 기술뿐 아니라 교통, 정보, 인구 구조와도 연결되며 여러 직무가 함께 해결한다는 점을 알게 되었다.",
        change: "의료계열을 하나의 직업으로 보던 생각에서 벗어나 임상, 보건정책, 의료데이터 분야를 구분해 탐색하게 되었다.",
        reflection: "자료를 읽고 비교하는 데 흥미가 있었지만 직무별 실제 업무는 충분히 확인하지 못했다.",
        followUp: "2학년에는 생명과학과 사회 과목의 관련 개념을 배우며 의료데이터와 보건정책 중 어떤 분야에 더 관심이 있는지 확인하고 싶다."
      }
    },
    {
      grade: 2,
      title: "도시·교통 분야 좁혀 보기",
      values: {
        activityName: "대중교통 노선은 누구의 이동을 우선해야 할까?",
        motivation: "지리 수업에서 생활권과 공간 불평등을 배우며 통학 시간과 교통 접근성의 차이가 궁금해졌다.",
        process: "교통공학, 도시계획, 지리정보 관련 학과의 교육과정을 비교하고 버스 정류장 분포와 인구 자료를 지도에 표시했다.",
        learningConnection: "한국지리의 생활권 개념과 수학의 좌표·통계 개념을 활용해 정류장 간 거리와 이용 인구를 비교했다.",
        insight: "교통 계획은 노선을 만드는 기술뿐 아니라 이용자 자료, 안전, 비용, 형평성을 함께 판단하는 일임을 이해했다.",
        change: "건축 중심의 관심이 도시공학과 교통계획으로 구체화되었고, 공간 자료를 다루는 역량이 필요하다고 판단했다.",
        reflection: "공간 관계를 시각화하는 데 강점이 있었지만 비용과 정책 기준을 해석하는 지식은 부족했다.",
        followUp: "경제 과목의 비용·편익 개념을 학습하고 실제 교통 정책 사례를 비교해 대안의 현실성을 검토하고 싶다."
      }
    },
    {
      grade: 3,
      title: "AI·미디어 진로의 쟁점 검토",
      values: {
        activityName: "추천 알고리즘은 이용자의 선택을 넓힐까 좁힐까?",
        motivation: "미디어 콘텐츠 추천이 편리한 동시에 비슷한 정보만 반복해 보여준다는 문제를 접하고 기술과 사회의 관계가 궁금해졌다.",
        process: "컴퓨터공학, 미디어학, 심리학의 접근을 비교하고 추천 결과가 달라지는 간단한 데이터 분류 실험과 이용자 설문을 진행했다.",
        learningConnection: "정보 과목의 데이터 처리, 사회문화의 미디어 효과, 확률과 통계의 표본 해석을 하나의 질문에 연결했다.",
        insight: "알고리즘의 성능만으로 좋은 추천을 판단하기 어렵고 다양성, 설명 가능성, 이용자의 선택권도 평가 기준이 되어야 함을 알게 되었다.",
        change: "개발 자체에 머물던 관심이 인간 중심 AI와 디지털 미디어 정책까지 확장되었다.",
        reflection: "기술 원리를 설명하는 강점은 확인했지만 이용자 경험을 해석할 때 표본의 편향을 더 주의해야 했다.",
        followUp: "대학에서는 데이터과학과 미디어 연구가 결합되는 전공을 비교하고, 추천의 다양성을 측정하는 지표를 더 탐구하고 싶다."
      }
    }
  ],
  club: [
    {
      grade: 1,
      title: "과학 실험의 기초 다지기",
      values: {
        activityName: "과학실험 동아리 - 렌즈 종류에 따른 초점 거리 비교",
        motivation: "과학 수업에서 빛의 굴절을 배운 뒤 렌즈의 모양이 상의 위치에 어떤 차이를 만드는지 직접 확인하고 싶었다.",
        process: "볼록렌즈의 두께와 물체 거리를 바꾸며 상이 선명해지는 위치를 반복 측정하고 측정값을 표로 정리했다.",
        role: "측정 기준을 통일하고 팀원의 기록을 비교해 값의 차이가 큰 실험은 다시 수행하도록 제안했다.",
        result: "렌즈와 물체 사이의 조건에 따라 초점 위치가 달라짐을 확인했고 반복 측정값의 평균을 사용해 오차를 줄였다.",
        limit: "렌즈의 정확한 곡률을 알 수 없어 렌즈 모양과 초점 거리의 관계를 정량적으로 설명하는 데 한계가 있었다.",
        reflection: "실험 결과보다 측정 기준을 맞추고 오차 원인을 확인하는 과정이 과학적 탐구에서 중요하다는 점을 배웠다.",
        followUp: "2학년에는 렌즈 공식과 오차율을 적용해 측정 결과를 수학적으로 설명해 보고 싶다."
      }
    },
    {
      grade: 2,
      title: "경제 자료로 주장 검증하기",
      values: {
        activityName: "경제탐구 동아리 - 기준금리 변화와 가계대출의 관계 분석",
        motivation: "금리가 오르면 대출이 항상 감소한다는 설명이 실제 자료에서도 같은지 궁금했다.",
        process: "기준금리, 가계대출, 주택가격 자료의 시점을 맞춰 그래프로 비교하고 시차를 두었을 때 관계가 달라지는지 확인했다.",
        role: "자료 출처와 단위를 검토하고 서로 다른 해석을 비교할 수 있도록 그래프 기준을 통일했다.",
        result: "금리 변화 직후 모든 지표가 같은 방향으로 움직이지 않았고 소득, 주택시장 상황 등 다른 요인을 함께 봐야 한다고 해석했다.",
        limit: "관찰 자료만으로 인과관계를 확정하기 어렵고 분석 기간에 따라 결과가 달라질 수 있었다.",
        reflection: "경제 현상을 하나의 원인으로 설명하지 않고 자료의 시차와 다른 변수를 함께 살펴보는 태도가 필요함을 배웠다.",
        followUp: "3학년에는 물가와 고용 지표를 추가하고 국가별 금리 정책 사례를 비교해 분석 범위를 넓히고 싶다."
      }
    },
    {
      grade: 3,
      title: "인문사회 쟁점의 관점 통합",
      values: {
        activityName: "인문사회 토론 동아리 - 생성형 AI 창작물의 책임과 권리",
        motivation: "AI가 만든 결과물의 오류와 저작권 문제를 접하며 창작자, 이용자, 개발자의 책임 기준이 궁금해졌다.",
        process: "관련 법·윤리 사례를 당사자별로 분류하고, 권리 보호와 기술 활용을 함께 고려한 토론 쟁점을 설계했다.",
        role: "상반된 주장의 근거를 정리하고 토론 중 합의된 기준과 끝까지 남은 쟁점을 기록해 최종 제안문을 작성했다.",
        result: "결과물의 사용 목적, 공개 범위, 인간의 검토 여부에 따라 책임을 다르게 보아야 한다는 단계별 기준을 제안했다.",
        limit: "기술과 제도가 빠르게 변해 현재 사례만으로 보편적인 기준을 정하기 어려웠다.",
        reflection: "찬반 중 하나를 고르는 것보다 이해관계자의 권리와 실제 적용 조건을 구분해야 설득력 있는 대안이 된다는 점을 배웠다.",
        followUp: "대학에서는 미디어 윤리와 정보법 사례를 더 살펴보고 실제 서비스 약관을 비교해 기준을 보완하고 싶다."
      }
    }
  ]
};

const creativeEvalDomains = {
  korean: {
    label: "국어·미디어",
    careers: "국어국문·언론정보·문헌정보·미디어",
    stages: [
      {
        title: "학교 안내문의 표현과 이해도",
        trigger: "같은 학교 안내문을 읽고도 친구마다 준비해야 할 내용을 다르게 이해하는 모습을 보고 문장의 구조가 정보 전달에 미치는 영향이 궁금해졌다.",
        question: "제목, 문장 길이, 핵심어의 위치에 따라 안내문의 이해도가 달라질까?",
        concept: "국어의 요약, 정보 구조, 독자 고려 개념을 활용했다.",
        method: "안내문 세 편의 문장 길이와 핵심 정보 위치를 비교하고, 친구들이 한 번 읽은 뒤 기억한 내용을 익명으로 확인했다.",
        role: "비교 기준과 확인 문항을 만들고 응답을 표로 정리했다.",
        collaboration: "친구들의 해석이 다른 지점을 함께 확인해 어려운 어휘만이 아니라 정보 배열도 검토했다.",
        evidence: "날짜와 대상이 첫 문단에 제시된 안내문의 핵심 정보 회상률이 높았고, 이를 바탕으로 짧은 안내 양식을 제안했다.",
        limit: "참여 인원이 적고 안내 내용의 난이도가 달라 문장 구조만의 효과로 단정하기 어려웠다.",
        learning: "좋은 글은 내용을 줄이는 데 그치지 않고 독자가 필요한 정보를 찾는 순서를 설계해야 함을 배웠다.",
        next: "2학년에는 기사 제목의 표현이 독자의 판단에 미치는 영향을 자료로 비교하고 싶다.",
        book: "생각을 빼앗긴 세계(프랭클린 포어)",
        bookInsight: "정보를 전달하는 형식도 독자의 선택과 판단에 영향을 준다는 관점으로 학교 안내문을 다시 보게 되었다."
      },
      {
        title: "기사 제목의 프레이밍 비교",
        trigger: "같은 사건을 다룬 기사도 제목에 따라 책임의 주체와 문제의 원인이 다르게 보인다는 점을 발견했다.",
        question: "행위자, 수치, 감정어를 제목에 넣는 방식은 독자가 중요하다고 판단하는 쟁점을 어떻게 바꿀까?",
        concept: "화법과 언어의 표현 효과, 매체의 관점, 논증의 근거를 연결했다.",
        method: "동일 사건 기사 열두 편의 제목을 행위자·수치·감정어 기준으로 코딩하고 본문 근거와 일치하는지 교차 확인했다.",
        role: "코딩 기준표를 만들고 팀원 간 분류가 다른 제목을 다시 검토했다.",
        collaboration: "주관적 판단을 줄이기 위해 두 명 이상이 독립적으로 분류한 뒤 불일치 사례의 기준을 수정했다.",
        evidence: "감정어가 강한 제목일수록 본문의 복합적인 원인이 축소되는 사례가 많아 제목과 본문을 함께 확인하는 읽기 기준을 제안했다.",
        limit: "선정한 언론사와 사건 수가 제한되어 전체 기사 작성 관행으로 일반화할 수 없었다.",
        learning: "비판적 읽기는 매체를 막연히 불신하는 것이 아니라 표현과 근거가 일치하는지 확인하는 과정임을 이해했다.",
        next: "3학년에는 생성형 AI 요약문까지 비교해 정보가 축약될 때 사라지는 관점을 분석하고 싶다.",
        book: "팩트풀니스(한스 로슬링)",
        bookInsight: "극적인 표현이 사실 판단을 흐릴 수 있다는 내용을 기사 제목의 감정어와 수치 제시 방식 분석에 적용했다."
      },
      {
        title: "생성형 AI 요약과 출처 신뢰성",
        trigger: "긴 글을 AI로 요약하면 편리하지만 원문의 조건과 반론이 빠지는 경우를 확인해 요약의 신뢰 기준이 필요하다고 생각했다.",
        question: "AI 요약문은 원문의 핵심 주장, 조건, 반론, 출처를 어느 정도 보존하며 이를 어떻게 검증할 수 있을까?",
        concept: "독서의 핵심 정보 선별, 논증 구조, 매체 윤리를 통합했다.",
        method: "칼럼과 보고서 원문을 직접 요약한 결과와 두 종류의 AI 요약을 비교하고 주장·근거·조건·출처 보존 여부를 평가표로 검토했다.",
        role: "평가 기준을 설계하고 누락 사례를 원문 문단과 연결해 검증 기록을 남겼다.",
        collaboration: "평가자마다 핵심 정보 판단이 달라 먼저 원문의 논증 구조를 합의한 뒤 결과를 다시 비교했다.",
        evidence: "AI 요약은 중심 주장은 잘 남겼지만 제한 조건과 반론을 자주 생략해 출처 링크와 검토 질문을 포함한 사용 지침을 제작했다.",
        limit: "프롬프트와 모델에 따라 결과가 달라 재현성을 확보하기 어려웠다.",
        learning: "도구의 정확도를 믿는 것보다 검증 가능한 기준을 만들고 원문으로 되돌아가는 태도가 중요함을 배웠다.",
        next: "대학에서는 디지털 리터러시와 자연어 처리의 평가 방법을 함께 탐구하고 싶다.",
        book: "AI 지도책(케이트 크로퍼드)",
        bookInsight: "AI 결과도 데이터와 설계자의 선택에서 자유롭지 않다는 관점을 바탕으로 요약문을 중립적인 결과로 보지 않게 되었다."
      }
    ]
  },
  math: {
    label: "수학·데이터",
    careers: "수학·통계·데이터과학·산업공학·경제",
    stages: [
      {
        title: "평균과 분포로 보는 학습시간",
        trigger: "학급의 평균 학습시간만으로 친구들의 학습 모습을 설명하기 어렵다는 생각이 들었다.",
        question: "평균이 같은 집단도 분포와 이상치가 다르면 어떻게 다르게 해석해야 할까?",
        concept: "평균, 중앙값, 범위와 자료의 분포를 활용했다.",
        method: "익명 학습시간 자료를 구간별로 나누고 평균과 중앙값을 비교해 극단값이 결과에 미치는 영향을 확인했다.",
        role: "개인을 식별하지 않는 설문 문항을 만들고 계산 결과를 그래프로 표현했다.",
        collaboration: "자료 공개 범위에 대한 의견을 반영해 원자료 대신 구간별 빈도만 공유했다.",
        evidence: "소수의 긴 학습시간이 평균을 높이는 것을 확인해 중앙값과 분포를 함께 제시하는 학급 자료를 만들었다.",
        limit: "자기보고식 자료라 실제 시간과 차이가 있을 수 있고 학습의 질은 반영하지 못했다.",
        learning: "대표값 하나보다 자료가 만들어진 방식과 흩어진 정도를 함께 보아야 함을 배웠다.",
        next: "2학년에는 두 변수의 관계를 분석하면서 상관관계를 원인으로 해석하는 오류를 살펴보고 싶다.",
        book: "통계의 미학(최제호)",
        bookInsight: "대표값은 자료 전체를 대신하지 못한다는 설명을 읽고 평균과 중앙값을 함께 비교하게 되었다."
      },
      {
        title: "상관관계와 교란변수 점검",
        trigger: "수면시간이 길면 성적이 높다는 설문 결과를 보고 두 변수만으로 원인을 설명할 수 있는지 의문이 들었다.",
        question: "수면시간과 집중도 사이의 관계는 학년, 학습시간, 스마트폰 사용시간을 고려해도 유지될까?",
        concept: "산점도, 상관관계, 표본과 교란변수 개념을 적용했다.",
        method: "익명 설문 자료를 산점도로 나타내고 학년과 스마트폰 사용시간에 따라 집단을 나누어 관계의 변화를 비교했다.",
        role: "결측값 처리 기준과 그래프 축을 통일하고 과도한 인과 표현을 수정했다.",
        collaboration: "팀원이 제시한 다른 설명을 변수 목록에 추가하고 분석 결과와 해석을 분리해 작성했다.",
        evidence: "전체 자료에서는 관계가 보였지만 집단을 나누자 약해져 단순 상관만으로 원인을 설명할 수 없다는 결론을 냈다.",
        limit: "표본이 작고 집중도를 주관적 척도로 측정해 결과의 안정성이 낮았다.",
        learning: "그래프가 관계를 보여주더라도 어떤 변수를 빠뜨렸는지 질문해야 책임 있는 해석이 된다는 점을 배웠다.",
        next: "3학년에는 제한된 조건에서 여러 대안을 비교하는 최적화 모형을 만들고 가정의 영향을 검토하고 싶다.",
        book: "벌거벗은 통계학(찰스 윌런)",
        bookInsight: "상관관계가 인과관계를 뜻하지 않는 사례를 읽고 설문 결과에 영향을 줄 다른 변수를 먼저 찾게 되었다."
      },
      {
        title: "학교 공간 배정 최적화와 한계",
        trigger: "한정된 자율학습 공간을 선착순으로 배정하면 이동이 불편한 학생에게 불리할 수 있다는 문제를 발견했다.",
        question: "좌석 수, 이동거리, 이용시간, 형평성을 함께 고려한 배정 기준을 수학적으로 표현할 수 있을까?",
        concept: "함수, 가중치, 경우의 수와 최적화의 목적함수를 활용했다.",
        method: "조건별 가중치를 달리한 세 가지 배정 모형을 만들고 가상의 이용 자료에 적용해 총 이동거리와 미배정 인원을 비교했다.",
        role: "변수와 제약조건을 정의하고 가중치 변화에 따른 결과를 표로 검증했다.",
        collaboration: "효율과 형평성 중 무엇을 우선할지 토론하고 단일 정답 대신 기준별 결과를 함께 제시했다.",
        evidence: "이동거리만 최소화한 모형은 특정 집단의 미배정률이 높아져 최소 보장 조건을 추가한 수정안을 제시했다.",
        limit: "가상의 자료를 사용했고 실제 이용자의 선호와 갑작스러운 일정 변경을 반영하지 못했다.",
        learning: "수학 모형은 중립적인 답이 아니라 어떤 가치를 변수와 조건에 넣었는지 설명해야 하는 도구임을 배웠다.",
        next: "대학에서 최적화와 의사결정 모형을 학습하며 실제 자료로 민감도 분석을 수행하고 싶다.",
        book: "수학이 필요한 순간(김민형)",
        bookInsight: "수학이 답을 계산하는 기술을 넘어 판단의 전제와 관계를 명확히 하는 언어라는 관점을 모형 설계에 적용했다."
      }
    ]
  },
  science: {
    label: "과학·공학",
    careers: "물리·화학·환경·전기전자·기계공학",
    stages: [
      {
        title: "교실 조도와 측정 오차",
        trigger: "같은 교실에서도 자리와 시간에 따라 책상이 어둡게 느껴지는 차이가 궁금했다.",
        question: "창문과 조명으로부터의 거리가 책상 조도에 어떤 차이를 만들까?",
        concept: "빛의 세기, 거리, 반복 측정과 평균 개념을 적용했다.",
        method: "같은 시간대에 교실 격자 위치별 조도를 세 번씩 측정하고 평균과 측정 범위를 좌석도에 표시했다.",
        role: "측정 높이와 기기 방향을 통일하고 이상값이 나온 위치를 다시 측정했다.",
        collaboration: "팀별 측정값 차이를 비교해 측정 자세와 외부 빛 조건을 기록 항목에 추가했다.",
        evidence: "창가와 복도 쪽의 차이가 예상보다 컸고 흐린 날에는 특정 구역이 권장 수준보다 낮아 보조 조명 위치를 제안했다.",
        limit: "스마트폰 센서의 정확도를 검교정하지 못했고 계절에 따른 일조 변화를 반영하지 못했다.",
        learning: "측정값을 얻는 것보다 조건을 통일하고 오차 원인을 기록하는 과정이 중요함을 배웠다.",
        next: "2학년에는 조도와 소비전력을 함께 측정해 밝기와 에너지 효율을 비교하고 싶다.",
        book: "떨림과 울림(김상욱)",
        bookInsight: "일상에서 느끼는 현상도 측정 가능한 물리량으로 바꾸어 볼 수 있다는 관점으로 교실의 빛을 살펴보았다."
      },
      {
        title: "조명 방식과 에너지 효율",
        trigger: "밝은 조명이 항상 효율적인 것은 아니라는 점에서 같은 조도를 만드는 데 필요한 전력이 궁금해졌다.",
        question: "조명 종류와 배치에 따라 단위 전력당 확보되는 유효 조도는 어떻게 달라질까?",
        concept: "전력, 에너지 전환, 효율과 오차율 개념을 연결했다.",
        method: "LED 조명의 거리와 각도를 바꾸며 조도와 소비전력을 측정하고 조도/전력 비율을 계산했다.",
        role: "독립변인과 통제변인을 정하고 측정표와 안전 수칙을 관리했다.",
        collaboration: "측정 순서에 따른 기기 온도 변화를 확인해 충분한 냉각 시간을 포함하도록 실험 절차를 수정했다.",
        evidence: "정면 밝기가 높은 배치보다 빛을 고르게 분산한 배치가 작업면 전체의 효율이 높아 교실 배치 모형을 제작했다.",
        limit: "소형 조명으로 실험해 실제 교실 천장 높이와 반사면의 영향을 그대로 재현하지 못했다.",
        learning: "공학적 개선은 최고값보다 사용 목적, 공간 전체의 균일성, 에너지 비용을 함께 고려해야 함을 알게 되었다.",
        next: "3학년에는 센서의 보정과 자동 제어를 추가해 실제 환경 변화에 대응하는 장치를 설계하고 싶다.",
        book: "에너지란 무엇인가(바츨라프 스밀)",
        bookInsight: "효율은 장치 하나의 성능이 아니라 전체 시스템과 사용 조건에서 판단해야 한다는 관점을 실험 해석에 적용했다."
      },
      {
        title: "환경센서 자동 제어의 신뢰성",
        trigger: "자동 조명 장치가 사람의 움직임이나 햇빛 변화에 잘못 반응하는 사례를 보고 센서 기준의 신뢰성이 궁금해졌다.",
        question: "조도와 움직임 센서의 오차를 고려하면서 불필요한 점등을 줄이는 제어 기준은 무엇일까?",
        concept: "센서 보정, 임계값, 피드백 제어와 에너지 보존을 통합했다.",
        method: "기준 조도계와 센서 값을 비교해 보정식을 만들고 임계값과 대기시간을 달리한 제어 알고리즘을 모형 공간에서 시험했다.",
        role: "오류 상황을 설계하고 로그 데이터를 분석해 거짓 점등과 미점등 비율을 계산했다.",
        collaboration: "에너지 절감과 이용자 안전의 우선순위가 충돌해 최소 조도 보장 조건을 함께 넣었다.",
        evidence: "단일 임계값보다 일정 시간의 평균과 움직임 정보를 함께 사용한 방식이 오류를 줄여 개선 알고리즘을 제안했다.",
        limit: "짧은 기간의 실내 실험이라 계절, 다양한 이용자 행동, 센서 노화를 반영하지 못했다.",
        learning: "기술의 성능은 정상 작동뿐 아니라 오류가 발생했을 때 누구에게 어떤 영향을 주는지까지 평가해야 함을 배웠다.",
        next: "대학에서는 제어공학과 센서공학을 학습해 실제 교실 데이터로 장기 검증하고 싶다.",
        book: "공학이란 무엇인가(성풍현 외)",
        bookInsight: "공학은 제약조건 속에서 안전하고 검증 가능한 해결책을 만드는 과정이라는 내용을 제어 기준 설계에 반영했다."
      }
    ]
  },
  social: {
    label: "사회·지리",
    careers: "지리·도시계획·행정·사회학·경제",
    stages: [
      {
        title: "학교 주변 보행 안전 지도",
        trigger: "등하교 때 같은 횡단보도에서도 보행자가 느끼는 위험이 시간대별로 다르다는 점을 발견했다.",
        question: "차량 통행량, 신호시간, 시야 방해 요소 중 보행 위험과 가장 밀접한 조건은 무엇일까?",
        concept: "생활공간, 안전권, 관찰 조사와 지도 표현을 활용했다.",
        method: "학교 주변 세 지점의 차량 수, 신호시간, 불법 주정차 여부를 같은 기준으로 관찰해 지도에 표시했다.",
        role: "관찰표와 안전 동선을 만들고 조사자가 차도에 접근하지 않도록 역할을 배치했다.",
        collaboration: "체감 위험도만으로 판단하지 않도록 관찰 자료와 학생 의견을 구분해 정리했다.",
        evidence: "통행량보다 불법 주정차로 시야가 가려지는 시간대의 위험 응답이 높아 학교에 안전 안내 위치 변경을 제안했다.",
        limit: "며칠간의 관찰이라 날씨와 행사에 따른 변화가 반영되지 않았다.",
        learning: "공간 문제를 해결하려면 개인의 느낌을 존중하면서도 관찰 가능한 조건으로 바꾸어 확인해야 함을 배웠다.",
        next: "2학년에는 대중교통 접근성을 지역별 인구 특성과 함께 지도화하고 싶다.",
        book: "도시는 무엇으로 사는가(유현준)",
        bookInsight: "공간의 구조가 사람의 행동과 관계를 바꾼다는 관점으로 통학로를 단순한 이동 경로가 아닌 생활공간으로 보게 되었다."
      },
      {
        title: "대중교통 취약지역 분석",
        trigger: "같은 지역 안에서도 정류장까지 이동하기 어려운 주민이 있다는 점에서 정류장 수만으로 접근성을 판단하기 어렵다고 생각했다.",
        question: "정류장 거리, 배차 간격, 고령 인구 비율을 함께 보면 교통 취약지역은 어떻게 달라질까?",
        concept: "생활권, 공간 불평등, 인구구조와 자료의 표준화를 연결했다.",
        method: "공공데이터의 정류장 위치와 배차 간격을 지도화하고 고령 인구 자료를 같은 공간 단위로 맞춰 중첩했다.",
        role: "좌표 자료를 정리하고 지표 단위가 다른 값을 구간화해 비교 지도를 제작했다.",
        collaboration: "교통 편의의 기준을 두고 의견이 달라 이동거리와 배차 간격을 별도 지표로 먼저 제시했다.",
        evidence: "정류장이 가까워도 배차가 드문 지역이 확인되어 거리 중심 평가에 시간 접근성 지표를 추가했다.",
        limit: "실제 승하차 수와 경사도, 보도 상태를 포함하지 못해 이동의 어려움을 완전히 설명하지 못했다.",
        learning: "정책 지표는 무엇을 측정하는지에 따라 취약한 집단을 보이게도 가리게도 한다는 점을 이해했다.",
        next: "3학년에는 예산과 형평성을 함께 고려한 노선 개선 대안을 비교하고 싶다.",
        book: "도시의 승리(에드워드 글레이저)",
        bookInsight: "도시의 연결성이 기회를 만든다는 관점을 교통 접근성과 교육·의료 이용 가능성의 관계로 확장했다."
      },
      {
        title: "교통정책 대안의 형평성 평가",
        trigger: "노선을 늘리자는 제안이 필요하지만 한정된 예산에서 누구의 이동을 우선할지 기준이 필요하다고 생각했다.",
        question: "이용자 수, 이동시간 절감, 교통약자 접근성, 비용을 함께 고려하면 어떤 대안이 타당할까?",
        concept: "공공재, 비용·편익, 사회적 형평성과 공간 분석을 통합했다.",
        method: "노선 증편, 수요응답형 교통, 환승 지원 세 대안의 수혜집단과 비용을 비교하는 다기준 평가표를 만들었다.",
        role: "평가 기준의 근거를 조사하고 가중치 변화에 따라 대안 순위가 달라지는지 분석했다.",
        collaboration: "효율을 중시한 팀원과 형평성을 중시한 팀원의 기준을 모두 반영해 단일 순위 대신 조건별 권고안을 작성했다.",
        evidence: "이용자 수만 보면 증편이 유리했지만 교통약자 접근성을 높이면 수요응답형 교통의 우선순위가 올라가 혼합안을 제시했다.",
        limit: "실제 운영비와 주민 수요 자료가 부족해 정책의 실행 가능성을 정확히 계산하지 못했다.",
        learning: "정책 제안은 좋은 의도보다 기준, 자료, 불이익을 받는 집단, 실행 조건을 투명하게 설명해야 함을 배웠다.",
        next: "대학에서 도시정책과 지리정보 분석을 학습해 주민 참여 자료까지 포함한 평가를 수행하고 싶다.",
        book: "정의란 무엇인가(마이클 샌델)",
        bookInsight: "효율적인 선택이 항상 공정한 선택은 아니라는 질문을 교통정책의 가중치와 수혜집단 분석에 적용했다."
      }
    ]
  },
  english: {
    label: "영어·국제",
    careers: "영어영문·국제학·통번역·외교·글로벌미디어",
    stages: [
      {
        title: "영문 기사 핵심어 비교",
        trigger: "같은 환경 문제를 다룬 영문 기사에서 반복되는 단어와 책임 주체가 다르다는 점을 발견했다.",
        question: "기사의 핵심어 선택은 독자가 문제의 원인과 해결 주체를 이해하는 방식에 어떤 차이를 만들까?",
        concept: "영어의 어휘 뉘앙스, 주어 선택, 요약 읽기를 활용했다.",
        method: "같은 사건을 다룬 영문 기사 세 편의 반복 어휘와 문장 주어를 표시하고 한국어 요약의 차이를 비교했다.",
        role: "문맥에 따른 어휘 의미를 사전과 원문 예문으로 확인하고 비교표를 만들었다.",
        collaboration: "번역이 다른 표현은 직역과 의역을 나누어 토론하고 원문의 강조점을 우선했다.",
        evidence: "개인 행동을 강조한 기사와 기업·정부 책임을 강조한 기사의 해결책 제시가 달라 핵심어-주장 연결표를 제작했다.",
        limit: "기사 수가 적고 영어 숙련도에 따라 뉘앙스 해석이 달라질 수 있었다.",
        learning: "영어 읽기는 단어를 바꾸는 작업이 아니라 누가 무엇을 강조하는지 맥락을 해석하는 과정임을 배웠다.",
        next: "2학년에는 국가별 기사와 인터뷰를 비교해 문화적 관점이 쟁점 구성에 미치는 영향을 살펴보고 싶다.",
        book: "언어의 온도(이기주)",
        bookInsight: "단어 선택이 관계와 판단을 바꿀 수 있다는 관점을 영문 기사의 핵심어와 책임 주체 분석에 적용했다."
      },
      {
        title: "문화적 관점을 담은 영어 팟캐스트",
        trigger: "국가별 청소년 기사에서 같은 사회 문제를 설명하는 사례와 해결 방식이 다르다는 점이 궁금했다.",
        question: "문화적 배경이 다른 청중에게 같은 쟁점을 설명할 때 어떤 맥락과 표현을 보완해야 할까?",
        concept: "영어 담화 구성, 청중 고려, 문화적 맥락과 출처 인용을 연결했다.",
        method: "해외 기사와 기관 보고서를 비교해 쟁점 설명문을 작성하고 두 버전의 영어 팟캐스트를 제작해 청취 피드백을 받았다.",
        role: "자료 출처를 검토하고 대본의 주장·근거·예시 순서를 편집했다.",
        collaboration: "발음의 유창성보다 내용 이해가 우선이라는 피드백을 반영해 문장을 줄이고 배경 설명을 추가했다.",
        evidence: "지역 배경을 짧게 설명한 버전에서 핵심 주장 이해 응답이 높아 청중별 설명 전략을 정리했다.",
        limit: "청취자 수가 적고 영어 능력 차이가 결과에 영향을 주었을 수 있었다.",
        learning: "국제적 의사소통은 영어를 어렵게 쓰는 것이 아니라 상대가 모르는 맥락을 예측하고 정확히 설명하는 능력임을 배웠다.",
        next: "3학년에는 상반된 국가 자료를 종합한 영문 정책 제안서를 만들고 근거의 균형을 검토하고 싶다.",
        book: "타인의 고통(수전 손택)",
        bookInsight: "다른 사회의 문제를 전달할 때 자극적인 장면보다 맥락과 당사자의 관점을 존중해야 한다는 기준을 대본에 반영했다."
      },
      {
        title: "근거 기반 영문 정책 브리프",
        trigger: "국제 쟁점을 소개하는 글이 문제 제기에 그치고 실행 주체와 반론을 충분히 다루지 않는 경우를 발견했다.",
        question: "서로 다른 국가 자료를 종합해 실행 조건과 반론을 포함한 영문 정책 제안서를 어떻게 작성할 수 있을까?",
        concept: "영어 논증 구조, 반론·재반론, 자료 인용과 국제 비교를 통합했다.",
        method: "정부·국제기구·언론 자료의 목적과 통계 기준을 비교하고 문제-근거-대안-한계 구조의 영문 브리프를 작성했다.",
        role: "통계의 조사연도와 단위를 검증하고 출처별 이해관계를 주석으로 표시했다.",
        collaboration: "초안에 빠진 반론을 동료 검토로 찾고 대안의 비용과 예상 부작용 문단을 추가했다.",
        evidence: "근거의 수보다 출처의 기준을 설명한 수정본이 더 신뢰된다는 피드백을 받아 검증 체크리스트를 함께 제시했다.",
        limit: "영어 자료 중심이라 비영어권 당사자의 목소리가 충분히 포함되지 못했다.",
        learning: "글로벌 관점은 여러 나라를 나열하는 것이 아니라 자료 접근의 불균형과 번역 과정의 손실까지 성찰하는 태도임을 배웠다.",
        next: "대학에서 국제학과 미디어 번역을 공부하며 다언어 자료를 포함한 비교 연구를 해보고 싶다.",
        book: "왜 세계의 절반은 굶주리는가(장 지글러)",
        bookInsight: "국제 문제를 개인의 선택만으로 설명하지 않고 제도와 무역 구조를 함께 살펴야 한다는 관점을 정책 대안 구성에 반영했다."
      }
    ]
  },
  pe: {
    label: "체육·스포츠",
    careers: "체육교육·스포츠과학·운동재활·스포츠경영",
    stages: [
      {
        title: "준비운동과 운동 자각도 기록",
        trigger: "같은 체육 활동에서도 준비운동 방식에 따라 몸이 풀리는 정도가 다르게 느껴졌다.",
        question: "동적 준비운동과 정적 스트레칭 뒤 운동 자각도와 간단한 수행 기록은 어떻게 달라질까?",
        concept: "운동 강도, 근육의 기능, 자기 신체 인식과 안전을 연결했다.",
        method: "안전한 범위에서 두 준비운동 뒤 제자리 뛰기 기록과 운동 자각도를 측정하고 개인별 변화만 비교했다.",
        role: "무리한 경쟁을 막기 위해 측정 중단 기준을 정하고 기록지를 관리했다.",
        collaboration: "친구마다 유연성과 컨디션이 다르다는 의견을 반영해 개인 간 순위 대신 각자의 변화율을 보았다.",
        evidence: "동적 준비운동 뒤 몸이 준비되었다는 응답과 수행 기록이 좋아지는 경향을 확인해 수업 전 준비운동 순서를 제안했다.",
        limit: "측정 횟수가 적고 수면과 피로 상태를 통제하지 못해 일반화하기 어려웠다.",
        learning: "운동 결과는 의지뿐 아니라 준비 과정과 개인 상태의 영향을 받으므로 안전한 비교 기준이 필요함을 배웠다.",
        next: "2학년에는 운동 뒤 회복시간과 심박수 변화를 기록해 개인별 운동 계획을 만들고 싶다.",
        book: "운동화 신은 뇌(존 레이티)",
        bookInsight: "운동이 기록 향상뿐 아니라 집중과 정서에도 영향을 준다는 내용을 읽고 운동 전후 상태를 함께 기록하게 되었다."
      },
      {
        title: "회복시간을 반영한 운동 계획",
        trigger: "같은 운동량을 수행해도 회복 속도가 다른 친구에게 동일한 훈련 계획을 적용하는 것이 적절한지 의문이 들었다.",
        question: "운동 강도와 휴식시간에 따라 심박수 회복과 운동 자각도는 어떻게 달라질까?",
        concept: "심박수, 에너지 대사, 과부하와 회복의 원리를 적용했다.",
        method: "교사의 안전 지침 아래 일정한 운동 뒤 1·3·5분 심박수와 자각도를 기록하고 개인별 회복 곡선을 비교했다.",
        role: "측정 시간 신호와 기록을 담당하고 이상 반응이 있으면 즉시 중단하도록 점검했다.",
        collaboration: "회복이 느린 학생을 부족하다고 평가하지 않도록 절대 수치보다 개인 기준선 대비 변화를 사용했다.",
        evidence: "운동 강도가 높을수록 개인별 회복 차이가 커져 강도와 휴식시간을 조절한 선택형 계획표를 만들었다.",
        limit: "웨어러블 기기의 오차와 일시적인 컨디션 영향을 충분히 분리하지 못했다.",
        learning: "좋은 운동 지도는 동일한 과제를 주는 것이 아니라 개인의 반응을 관찰하고 조절하는 과정임을 이해했다.",
        next: "3학년에는 체력과 장애 여부가 다른 학생도 참여할 수 있는 포용적 체육 프로그램을 설계하고 싶다.",
        book: "스포츠 유전자(데이비드 엡스타인)",
        bookInsight: "수행 차이를 재능 하나로 설명할 수 없다는 내용을 통해 훈련 이력과 환경, 회복 조건을 함께 보게 되었다."
      },
      {
        title: "포용적 체육 프로그램 설계",
        trigger: "기능 중심 경기에서 일부 학생이 반복적으로 소외되는 모습을 보며 참여의 기준을 바꿀 필요가 있다고 생각했다.",
        question: "기능 수준과 신체 조건이 다른 학생 모두가 역할을 갖고 참여하려면 규칙과 평가를 어떻게 설계해야 할까?",
        concept: "운동 기능, 협동학습, 보편적 설계와 스포츠 윤리를 통합했다.",
        method: "참여 장벽을 익명 조사하고 공의 크기, 이동 범위, 득점 방식, 역할 순환을 달리한 게임을 시범 운영했다.",
        role: "안전 기준과 역할 카드를 만들고 참여 시간과 만족도 변화를 관찰했다.",
        collaboration: "경기성이 약해진다는 의견과 참여가 늘었다는 의견을 조정해 기본 규칙과 선택 규칙을 함께 두었다.",
        evidence: "역할 순환과 다양한 득점 방식을 적용한 뒤 공을 접하지 못한 학생 수가 줄어 수정 규칙안을 정리했다.",
        limit: "짧은 시범 수업이라 장기적인 기능 향상과 팀 관계 변화를 확인하지 못했다.",
        learning: "공정함은 모두에게 같은 조건을 주는 것만이 아니라 참여를 막는 장벽을 줄이는 것임을 배웠다.",
        next: "대학에서 특수체육과 운동재활을 공부하며 참여와 안전을 함께 평가하는 프로그램을 개발하고 싶다.",
        book: "공정하다는 착각(마이클 샌델)",
        bookInsight: "같은 규칙이 모두에게 같은 기회를 주는지 질문하게 되어 체육 활동의 참여 장벽과 역할 배분을 다시 살펴보았다."
      }
    ]
  },
  health: {
    label: "보건·생명",
    careers: "간호·보건·생명과학·의료행정·공중보건",
    stages: [
      {
        title: "건강정보의 출처 판별",
        trigger: "SNS에서 같은 건강 습관을 두고 서로 반대되는 조언이 확산되는 것을 보고 무엇을 믿어야 할지 궁금했다.",
        question: "건강정보의 신뢰성을 판단할 때 출처, 연구 대상, 표현 방식 중 무엇을 확인해야 할까?",
        concept: "건강 의사결정, 생명과학적 근거, 정보 출처 평가를 연결했다.",
        method: "온라인 게시물과 공공기관 자료를 출처·근거 링크·연구 대상·과장 표현 기준으로 비교했다.",
        role: "판별 체크리스트를 만들고 사실 확인이 어려운 표현을 별도로 표시했다.",
        collaboration: "유명인이 소개한 정보도 근거가 될 수 있다는 의견을 검토해 전문성보다 검증 가능한 원자료 유무를 기준으로 합의했다.",
        evidence: "효과를 단정하는 게시물은 연구 대상과 조건을 생략하는 경우가 많아 교내 건강정보 확인 카드뉴스를 제작했다.",
        limit: "전문 논문의 연구 설계를 완전히 해석하기 어려워 공공기관 해설 자료에 의존했다.",
        learning: "건강정보는 맞고 틀림만 판단하기보다 누구에게 어떤 조건에서 적용되는지 확인해야 함을 배웠다.",
        next: "2학년에는 학생들이 자주 접하는 건강 오해를 조사해 근거 기반 캠페인을 설계하고 싶다.",
        book: "우리 몸이 세계라면(김승섭)",
        bookInsight: "건강은 개인 습관뿐 아니라 환경과 정보 접근의 영향을 받는다는 관점으로 건강 조언의 전제를 살펴보게 되었다."
      },
      {
        title: "학생 건강 캠페인의 효과 확인",
        trigger: "건강 캠페인이 정보를 전달해도 실제 행동 변화로 이어지지 않는 경우가 많다는 점이 궁금했다.",
        question: "수면 위생 정보를 어떤 방식으로 제시해야 학생이 자신의 습관을 점검하고 실천할까?",
        concept: "항상성, 생체리듬, 건강행동과 의사소통을 연결했다.",
        method: "사전 설문으로 잘못 알려진 내용을 찾고 설명형·체크리스트형 자료를 제작해 이해도와 실천 의향을 비교했다.",
        role: "개인 건강정보를 수집하지 않도록 문항을 수정하고 공공기관 자료로 내용을 검증했다.",
        collaboration: "친구들의 피드백을 받아 훈계조 표현을 줄이고 실천 가능한 작은 행동을 선택하도록 구성했다.",
        evidence: "체크리스트형 자료에서 자신의 습관을 점검했다는 응답이 높았지만 실제 실천 여부는 확인하지 못했다.",
        limit: "의향을 행동 변화로 해석할 수 없고 수면 문제에는 개인이 해결하기 어려운 환경 요인도 있었다.",
        learning: "보건 활동은 정보를 많이 주는 것보다 대상자의 상황과 선택 가능성을 고려해야 함을 이해했다.",
        next: "3학년에는 건강정보 접근성이 낮은 사람을 고려한 안내와 지원체계를 조사하고 싶다.",
        book: "우리는 왜 잠을 자야 할까(매슈 워커)",
        bookInsight: "수면을 의지의 문제가 아니라 생체리듬과 환경의 문제로 이해하며 캠페인의 비난 표현을 수정했다."
      },
      {
        title: "건강정보 접근성과 돌봄 설계",
        trigger: "고령자나 외국인에게 의료 안내문이 어렵다면 필요한 서비스를 알고도 이용하지 못할 수 있다는 문제를 발견했다.",
        question: "읽기 수준과 언어 배경이 다른 이용자에게 안전 정보를 정확히 전달하려면 어떤 장치가 필요할까?",
        concept: "건강 문해력, 환자 안전, 의사소통과 공공보건의 접근성을 통합했다.",
        method: "의료기관 안내문을 문장 길이·전문용어·행동 지침 기준으로 분석하고 쉬운 글과 그림 안내 시안을 사용자에게 검토받았다.",
        role: "의학적 의미가 달라지지 않도록 원문과 수정문을 대조하고 확인 질문을 설계했다.",
        collaboration: "쉬운 표현이 부정확할 수 있다는 의견을 반영해 핵심 전문용어는 남기고 바로 설명을 붙였다.",
        evidence: "행동 순서를 번호와 그림으로 제시한 시안에서 이해가 어려운 지점이 줄어 수정 원칙을 정리했다.",
        limit: "실제 환자 집단이 아닌 소수의 학생·성인 피드백이라 의료현장 효과를 확인하지 못했다.",
        learning: "돌봄의 전문성은 지식을 아는 것뿐 아니라 상대가 안전하게 이해하고 행동하도록 설명하는 책임을 포함함을 배웠다.",
        next: "대학에서 간호·공중보건·보건행정을 비교하며 건강 문해력 개선 연구를 이어가고 싶다.",
        book: "아픔이 길이 되려면(김승섭)",
        bookInsight: "질병 경험은 개인의 몸뿐 아니라 제도와 언어의 영향을 받는다는 관점을 의료정보 접근성 문제로 확장했다."
      }
    ]
  },
  education: {
    label: "교육·심리",
    careers: "교육학·교과교육·심리·상담·교육공학",
    stages: [
      {
        title: "학습 습관과 기억 전략 비교",
        trigger: "같은 시간을 공부해도 다음 날 기억나는 정도가 달라 학습 방법의 차이가 궁금했다.",
        question: "다시 읽기와 스스로 질문하며 회상하기는 단기 기억에 어떤 차이를 만들까?",
        concept: "기억, 인출, 자기점검과 학습전략을 연결했다.",
        method: "짧은 학습 자료를 두 방식으로 익힌 뒤 동일한 확인 문항을 풀고 개인별 결과와 느낌을 기록했다.",
        role: "난이도가 비슷한 자료와 문항을 만들고 점수보다 사용 전략의 차이를 익명으로 정리했다.",
        collaboration: "친구마다 선호 방식이 달라 단일 우수 전략을 정하지 않고 언제 어떤 방법이 유용했는지 인터뷰했다.",
        evidence: "회상 방식에서 틀린 부분을 더 빨리 발견했다는 응답이 많아 수업 복습용 자기질문 틀을 만들었다.",
        limit: "하루 동안의 활동이라 장기 기억 효과를 확인하지 못했고 자료 친숙도도 달랐다.",
        learning: "학습은 오래 보는 시간이 아니라 무엇을 모르는지 확인하고 방법을 조절하는 과정임을 배웠다.",
        next: "2학년에는 또래 피드백이 글 수정과 학습 동기에 미치는 영향을 살펴보고 싶다.",
        book: "어떻게 공부할 것인가(헨리 뢰디거 외)",
        bookInsight: "익숙하게 읽히는 느낌이 실제로 기억한다는 뜻은 아니라는 내용을 바탕으로 회상 질문을 활용했다."
      },
      {
        title: "또래 피드백의 구체성과 수정 행동",
        trigger: "좋았다는 피드백보다 어느 부분을 어떻게 바꿀지 알려준 피드백이 글 수정에 도움이 된 경험에서 출발했다.",
        question: "평가형 피드백과 질문·근거 중심 피드백은 글 수정 행동에 어떤 차이를 만들까?",
        concept: "형성평가, 메타인지, 의사소통과 동기 개념을 적용했다.",
        method: "같은 초안에 두 유형의 익명 피드백을 제공하고 수정된 문장의 종류와 작성자의 수용 이유를 비교했다.",
        role: "사람을 평가하지 않고 글의 근거와 구조를 묻는 피드백 문장 틀을 설계했다.",
        collaboration: "피드백이 부담스럽다는 의견을 반영해 장점-질문-제안 순서와 선택권을 포함했다.",
        evidence: "구체적인 질문이 포함된 피드백에서 근거 추가와 문단 재구성 같은 실제 수정이 더 다양하게 나타났다.",
        limit: "글의 주제와 작성자의 관계에 따라 피드백 수용 정도가 달라질 수 있었다.",
        learning: "좋은 피드백은 정답을 대신 주기보다 학습자가 스스로 판단할 근거와 다음 행동을 찾게 해야 함을 배웠다.",
        next: "3학년에는 후배 학습지원 프로그램에서 피드백 틀을 적용하고 참여자의 변화를 장기적으로 살펴보고 싶다.",
        book: "교사와 학생 사이(하임 기너트)",
        bookInsight: "사람의 성격을 평가하는 말보다 관찰한 행동과 해결 가능한 문제를 말해야 한다는 원칙을 피드백 문장에 적용했다."
      },
      {
        title: "후배 학습지원 프로그램 평가",
        trigger: "멘토링이 설명을 많이 해주는 활동으로 끝나면 후배가 스스로 공부하는 힘을 기르기 어렵다고 생각했다.",
        question: "목표 설정, 자기질문, 피드백을 포함한 학습지원이 후배의 자기점검 행동을 어떻게 바꿀까?",
        concept: "자기조절학습, 동기, 형성평가와 상담 윤리를 통합했다.",
        method: "참여자와 주간 목표를 정하고 회상 질문과 피드백 기록지를 사용한 뒤 스스로 수정한 학습 행동을 면담으로 확인했다.",
        role: "후배 대신 답을 정하지 않고 질문으로 계획을 점검하며 개인정보가 기록되지 않도록 양식을 관리했다.",
        collaboration: "멘토별 방식 차이를 줄이기 위해 관찰-질문-선택의 공통 원칙을 합의하고 어려운 사례를 교사와 상의했다.",
        evidence: "일부 참여자가 도움을 요청하기 전에 틀린 이유를 먼저 기록하기 시작했지만 모든 학생에게 같은 변화가 나타나지는 않았다.",
        limit: "참여 기간이 짧고 멘토와의 관계 자체가 변화에 영향을 주어 프로그램 효과만 분리하기 어려웠다.",
        learning: "교육은 지식을 전달하는 것보다 학습자의 선택과 속도를 존중하면서 스스로 조절할 수 있는 환경을 만드는 일임을 배웠다.",
        next: "대학에서 교육학과 상담심리를 학습하며 학습지원의 효과와 윤리를 함께 연구하고 싶다.",
        book: "배움의 발견(타라 웨스트오버)",
        bookInsight: "배움이 지식 습득을 넘어 자신의 관점을 점검하고 선택을 넓히는 과정이라는 관점을 멘토링 목표에 반영했다."
      }
    ]
  }
};

const creativeEvalAdditionalSeries = {
  korean: [
    {
      id: "local-memory",
      label: "문학과 지역 기억",
      books: ["아몬드(손원평)", "기억 전달자(로이스 로리)", "타인의 고통(수전 손택)"],
      stages: [
        ["시 속 공간과 감정 읽기", "문학 작품의 공간 표현은 인물의 감정을 어떻게 드러낼까?", "시 세 편의 공간어와 감정어를 표시하고 화자의 시선 변화를 비교했다.", "같은 장소도 화자의 기억과 관계에 따라 다르게 의미화됨을 확인했다.", "지역 주민의 구술 기록과 문학의 공간 표현을 비교한다."],
        ["지역 구술사의 관점 비교", "같은 지역 사건을 세대별 주민은 왜 다르게 기억할까?", "공개 구술자료를 세대·역할별로 분류하고 공통점과 누락된 관점을 분석했다.", "기억은 사실의 복사본이 아니라 경험과 위치에 따라 선택적으로 구성됨을 확인했다.", "기록되지 않은 목소리를 존중하는 디지털 아카이브 원칙을 설계한다."],
        ["디지털 기억 아카이브의 윤리", "지역의 기억을 공개할 때 정확성·사생활·당사자 동의를 어떻게 조정할까?", "공개 범위와 익명화 수준이 다른 아카이브 사례를 비교해 수집·검증·삭제 요청 기준을 만들었다.", "많이 공개하는 것보다 당사자가 기록의 맥락과 공개 범위를 통제할 장치가 중요함을 제안했다.", "문학·기록학·디지털 인문학을 연결해 기억의 재현 방식을 탐구한다."]
      ]
    },
    {
      id: "public-language",
      label: "토론과 공공언어",
      books: ["말이 칼이 될 때(홍성수)", "논증의 탄생(조셉 윌리엄스 외)", "선량한 차별주의자(김지혜)"],
      stages: [
        ["학교 규칙 문장의 명확성", "학교 규칙의 모호한 표현은 학생의 행동 판단에 어떤 혼란을 만들까?", "규칙 문장에서 주체·조건·예외가 빠진 부분을 찾고 학생별 해석 차이를 조사했다.", "금지 표현만 있는 문장보다 이유와 적용 조건이 제시된 문장이 일관되게 이해되었다.", "규칙을 둘러싼 상반된 주장의 근거를 비교한다."],
        ["쟁점 토론의 근거 지도", "찬반 토론에서 사실 판단과 가치 판단을 구분하면 합의 가능 지점이 보일까?", "토론 자료를 주장·사실근거·가치기준·반론으로 표시한 근거 지도를 만들었다.", "가치가 달라도 사실관계를 먼저 확인하면 쟁점을 좁히고 절충안을 만들 수 있었다.", "다양한 독자가 이해하는 공공 제안문의 표현 기준을 만든다."],
        ["포용적인 공공 제안문", "정확하면서도 특정 집단을 배제하지 않는 정책 언어는 어떻게 작성해야 할까?", "공공문서의 전문용어·수동표현·대상 호칭을 분석하고 쉬운 글과 당사자 검토를 반영해 수정했다.", "문장을 쉽게 쓰는 것뿐 아니라 정책의 영향을 받는 사람을 주어로 드러내야 책임이 명확해졌다.", "국어학·법학·행정학에서 공공언어의 책임을 탐구한다."]
      ]
    }
  ],
  math: [
    {
      id: "risk-probability",
      label: "확률과 위험 판단",
      books: ["수학이 필요한 순간(김민형)", "숫자에 약한 사람들을 위한 통계학 수업(데이비드 스피겔할터)", "대량살상 수학무기(캐시 오닐)"],
      stages: [
        ["게임 규칙과 기대값", "승률이 비슷해 보이는 게임도 기대값은 왜 다를까?", "간단한 주사위 게임의 경우를 나누어 이론 확률과 반복 실험 결과를 비교했다.", "적은 시행에서는 승률이 크게 흔들리고 보상 크기까지 포함해야 선택의 유불리를 설명할 수 있었다.", "건강정보의 조건부확률 표현을 분석한다."],
        ["조건부확률과 건강 위험", "검사 정확도가 높아도 양성 결과가 실제 질병을 뜻하지 않을 수 있는 이유는 무엇일까?", "유병률과 민감도·특이도를 달리한 가상 인원표로 양성예측도를 계산했다.", "검사 성능만 제시하면 위험을 과장할 수 있어 기준집단의 발생률을 함께 알려야 했다.", "확률을 사용하는 자동 분류의 공정성과 오류 비용을 검토한다."],
        ["위험 예측모형의 공정성", "예측 정확도가 같은 모형도 집단별 오류율이 다르면 공정하다고 볼 수 있을까?", "가상 대출자료로 전체 정확도·거짓양성·거짓음성을 집단별로 계산하고 기준값을 바꾸었다.", "하나의 공정성 지표를 개선하면 다른 오류가 커질 수 있어 사용 목적과 피해를 함께 공개해야 했다.", "통계·데이터과학·금융수학의 책임 있는 모형 설계를 탐구한다."]
      ]
    },
    {
      id: "space-modeling",
      label: "기하와 공간 모델링",
      books: ["이상한 수학책(벤 올린)", "도시는 무엇으로 사는가(유현준)", "수학의 쓸모(닉 폴슨·제임스 스콧)"],
      stages: [
        ["축척으로 보는 교실 동선", "교실 가구 배치에 따라 이동거리와 충돌 지점은 어떻게 달라질까?", "교실을 축척 도면으로 옮기고 주요 이동 경로의 길이와 교차 횟수를 비교했다.", "최단거리 배치가 출입구 혼잡을 키울 수 있어 거리와 통로 폭을 함께 보아야 했다.", "휠체어·목발 이용자의 접근성을 기하 조건으로 분석한다."],
        ["접근 가능한 공간의 기하", "경사도·회전반경·통로 폭을 함께 고려하면 접근 가능한 경로는 어떻게 달라질까?", "학교 평면도에 경사와 폭 자료를 표시하고 이동 조건별 가능한 경로를 비교했다.", "거리만 짧은 경로보다 회전과 문턱 조건을 충족하는 경로가 실제 접근성에 중요했다.", "재난 상황의 대피 흐름을 모형화한다."],
        ["대피 동선 시뮬레이션", "최단 대피경로 안내가 인원이 몰릴 때도 가장 안전할까?", "공간을 네트워크로 단순화하고 출구 용량과 이동속도를 달리해 병목 변화를 계산했다.", "모두가 같은 최단경로를 택하면 병목이 커져 분산 안내와 이동약자 우선 조건이 필요했다.", "산업공학·건축·재난안전 분야의 공간 최적화를 탐구한다."]
      ]
    }
  ],
  science: [
    {
      id: "water-chemistry",
      label: "물 환경과 화학",
      books: ["물의 세계사(스티븐 솔로몬)", "침묵의 봄(레이첼 카슨)", "플라스틱 바다(찰스 무어)"],
      stages: [
        ["생활 주변 물의 pH 비교", "장소와 용도에 따라 물의 pH는 어떻게 다르며 측정값은 얼마나 안정적일까?", "수돗물·빗물·연못물의 pH를 반복 측정하고 온도와 보관시간을 함께 기록했다.", "한 번의 측정값보다 반복 범위와 시료 보관 조건이 해석에 더 중요했다.", "간이 정화 재료의 제거 효과를 비교한다."],
        ["정화 재료와 수질 지표", "모래·활성탄·섬유층의 조합은 탁도와 색도 개선에 어떤 차이를 만들까?", "같은 오염 모형수를 정화층별로 통과시켜 투과시간·탁도·색 변화를 비교했다.", "겉보기 투명도 개선이 화학적 안전을 뜻하지 않아 측정 가능한 지표의 범위를 명시했다.", "미세플라스틱 조사와 정화기술의 한계를 검토한다."],
        ["미세입자 검출과 정화 한계", "간이 관찰로 확인한 미세입자를 미세플라스틱이라고 단정하지 않으려면 어떤 검증이 필요할까?", "여과지 관찰과 대조시료를 사용하고 재질 확인에 필요한 분광분석 원리를 조사했다.", "형태 관찰만으로 재질을 확정할 수 없어 검출과 동정 단계를 분리한 조사 절차를 제안했다.", "환경화학·재료공학·정책을 연결한 오염 저감 방안을 탐구한다."]
      ]
    },
    {
      id: "heat-materials",
      label: "열과 지속가능한 재료",
      books: ["뜨거운 지구 열차를 멈추기 위해(장미정)", "물질의 물리학(한정훈)", "도넛 경제학(케이트 레이워스)"],
      stages: [
        ["단열재와 온도 변화", "재료의 종류와 두께에 따라 용기 내부 온도 변화는 어떻게 달라질까?", "종이·천·완충재의 두께를 맞춰 시간별 온도를 측정하고 냉각 곡선을 비교했다.", "초기 온도와 덮개 틈이 결과에 크게 영향을 주어 실험 조건 통일의 필요성을 확인했다.", "열전도율과 비용을 함께 비교한다."],
        ["단열 성능과 비용 비교", "성능이 높은 재료가 비용과 환경영향까지 고려해도 최선일까?", "열손실률·가격·재사용 가능성을 같은 기준으로 환산해 재료별 평가표를 만들었다.", "최고 성능 재료보다 적정 두께의 재사용 재료가 전체 조건에서 유리한 경우가 있었다.", "학교 공간에 적용할 지속가능한 단열 설계를 검증한다."],
        ["지속가능한 단열 시스템", "에너지 절감·화재 안전·폐기물 감소를 함께 만족하는 재료 조합은 무엇일까?", "재료 조합별 열성능을 시험하고 난연성 기준과 전과정 환경자료를 조사해 다기준 평가했다.", "단일 친환경 표시보다 사용기간·교체·폐기까지 포함한 설계가 필요하다고 제안했다.", "재료공학·건축환경·에너지정책의 통합 설계를 탐구한다."]
      ]
    }
  ],
  social: [
    {
      id: "consumption-labor",
      label: "소비와 노동",
      books: ["청소년을 위한 노동인권 에세이(구정화)", "플랫폼 자본주의(닉 서르닉)", "공정하다는 착각(마이클 샌델)"],
      stages: [
        ["상품표시와 소비 선택", "가격 외에 원산지·노동·환경 표시가 소비 선택에 어떤 영향을 줄까?", "같은 품목의 표시정보를 비교하고 어떤 정보가 선택 이유가 되는지 익명 조사했다.", "정보를 많이 제공해도 의미를 이해하기 어려우면 선택 기준으로 사용되지 않았다.", "플랫폼 서비스 뒤의 노동조건을 조사한다."],
        ["플랫폼 노동의 보이지 않는 비용", "빠른 배달과 낮은 가격의 편익 뒤에 어떤 노동과 위험이 포함될까?", "플랫폼 약관·노동자 인터뷰 기사·사고 통계를 이해관계자별로 비교했다.", "소비자 편익과 노동자 안전이 가격표에 함께 드러나지 않아 평가 기준을 넓혀야 했다.", "학교 구매에서 적용할 윤리적 조달 기준을 설계한다."],
        ["윤리적 조달 기준 제안", "공공 구매에서 가격·품질·노동·환경 조건을 어떻게 함께 평가할까?", "가상 구매안 세 개에 가격·내구성·노동 인증·폐기 비용의 가중치를 적용했다.", "낮은 구매가격이 장기비용까지 낮추지는 않아 최소 노동·환경 기준과 총비용 평가를 제안했다.", "경제·행정·노동정책에서 지속가능한 조달을 탐구한다."]
      ]
    },
    {
      id: "population-welfare",
      label: "인구와 복지",
      books: ["지방도시 살생부(마강래)", "고령화 가족(무레 요코)", "복지국가란 무엇인가(이상이)"],
      stages: [
        ["학령인구 변화 읽기", "지역별 학생 수 변화는 학교와 마을의 생활에 어떤 영향을 줄까?", "최근 인구자료와 학교 수 변화를 연도별로 비교하고 지도에 표시했다.", "학생 수 감소는 학급 규모뿐 아니라 통학거리와 지역 시설 유지에도 연결되었다.", "고령화와 생활서비스 접근성을 함께 분석한다."],
        ["고령화와 서비스 접근성", "고령인구 비율이 높은 지역에서 의료·교통·돌봄 접근성은 어떻게 다른가?", "고령인구와 병원·정류장·복지시설 위치를 같은 공간 단위로 비교했다.", "시설 수가 있어도 이동시간과 운영시간이 맞지 않으면 실제 이용이 어려웠다.", "한정된 복지예산의 배분 기준을 검토한다."],
        ["복지예산 배분의 기준", "필요 인원·서비스 격차·긴급성을 함께 고려한 예산 배분은 어떻게 설계할까?", "가상 지역자료에 균등·인구비례·필요도 기준을 적용해 수혜와 사각지대를 비교했다.", "단일 기준은 다른 취약집단을 놓칠 수 있어 최소보장과 필요도 가중을 결합한 안을 제시했다.", "사회복지·행정·지역학의 근거 기반 배분을 탐구한다."]
      ]
    }
  ],
  english: [
    {
      id: "translation-access",
      label: "번역과 정보 접근성",
      books: ["번역의 탄생(이희재)", "번역가의 길(김욱동)", "말하지 않는 세계사(최성락)"],
      stages: [
        ["학교 안내문의 영어 번역", "학교 안내를 영어로 옮길 때 직역만으로 행동 지침이 정확히 전달될까?", "원문 핵심 행동을 표시하고 직역본과 쉬운 영어 번역본의 이해도를 비교했다.", "문화적 배경 설명과 날짜·대상의 명시가 단어 수준의 정확성만큼 중요했다.", "영상 자막의 문화어 번역 전략을 비교한다."],
        ["문화어와 영상 자막", "한국 문화어를 자막에서 설명·음역·대체할 때 의미 손실은 어떻게 달라질까?", "영상 장면별로 세 번역 전략을 적용하고 영어 사용자의 이해와 몰입 피드백을 비교했다.", "설명을 길게 하면 의미는 정확하지만 장면을 놓칠 수 있어 목적별 전략 선택이 필요했다.", "재난·보건 공공정보의 다언어 전달 원칙을 설계한다."],
        ["다언어 공공정보 설계", "긴급정보를 언어와 문해 수준이 다른 사람에게 빠르고 정확하게 전달하려면 무엇이 필요할까?", "재난안내 사례를 핵심행동·문장길이·그림·언어전환 기준으로 분석해 다언어 시안을 시험했다.", "완전한 문장 번역보다 행동 순서와 그림, 검증된 핵심어를 함께 제시한 안이 빠르게 이해되었다.", "통번역·공공외교·언어정책에서 정보 접근권을 탐구한다."]
      ]
    },
    {
      id: "literature-identity",
      label: "영미문학과 정체성",
      books: ["원더(R. J. 팔라시오)", "앵무새 죽이기(하퍼 리)", "오리엔탈리즘(에드워드 사이드)"],
      stages: [
        ["인물 시점과 편견", "같은 사건을 다른 인물의 시점으로 읽으면 판단이 어떻게 달라질까?", "영어 소설의 동일 장면을 인물별 서술과 대사로 나누어 근거를 표시했다.", "처음의 인상은 제한된 시점에서 만들어져 다른 인물의 정보가 추가되면 판단이 수정되었다.", "시대와 문화가 다른 작품의 차별 표현을 비교한다."],
        ["문학 속 차별의 맥락 비교", "서로 다른 시대의 작품은 차별을 개인과 제도의 문제로 어떻게 다르게 그릴까?", "두 작품의 인물 갈등·법과 관습·서술자의 평가를 비교한 영문 토론 자료를 만들었다.", "개인의 선의만으로 해결되지 않는 제도적 조건이 작품의 결말과 인물 선택을 제한했다.", "타문화를 재현하는 서사의 권력관계를 분석한다."],
        ["재현과 타자화 비판", "영미권 텍스트가 다른 문화를 설명할 때 누구의 목소리를 중심에 두는가?", "여행기·소설·당사자 에세이의 묘사어와 발화 비중을 비교해 누락된 관점을 분석했다.", "다양한 인물을 등장시키는 것만으로 충분하지 않고 해석 권한과 목소리의 비중을 살펴야 했다.", "영문학·문화연구·국제관계의 재현 윤리를 탐구한다."]
      ]
    }
  ],
  pe: [
    {
      id: "injury-recovery",
      label: "부상 예방과 운동재활",
      books: ["운동화 신은 뇌(존 레이티)", "스포츠 유전자(데이비드 엡스타인)", "움직임의 뇌과학(캐럴라인 윌리엄스)"],
      stages: [
        ["자세와 반복동작 점검", "같은 동작을 반복할 때 자세 차이가 불편감과 수행 안정성에 어떤 영향을 줄까?", "교사의 안전지도 아래 기본 동작을 영상으로 확인하고 관절 정렬 체크 항목을 기록했다.", "기록 향상보다 무릎과 발의 정렬을 먼저 점검할 때 동작이 안정되었다.", "훈련량과 회복의 관계를 분석한다."],
        ["훈련부하와 회복", "운동시간만이 아니라 강도와 회복을 함께 기록하면 과부하 위험을 더 잘 살필 수 있을까?", "운동 자각도×시간으로 주간 부하를 계산하고 수면·통증 기록과 변화 추세를 비교했다.", "부하가 급격히 증가한 주에 피로 응답이 늘어 개인 기준선과 점진적 증가가 필요했다.", "개인차를 반영한 복귀 프로그램을 설계한다."],
        ["단계적 운동 복귀 설계", "부상 뒤 경기 복귀를 날짜가 아니라 기능 기준으로 판단하려면 무엇을 확인해야 할까?", "공공 재활지침을 참고해 통증·가동범위·균형·과제 수행의 단계별 체크안을 만들었다.", "하나의 기록을 통과하는 것보다 여러 기능과 당사자의 불안을 함께 확인해야 안전했다.", "운동재활·물리치료·체육교육의 안전한 복귀를 탐구한다."]
      ]
    },
    {
      id: "sports-management",
      label: "스포츠 데이터와 운영",
      books: ["머니볼(마이클 루이스)", "축구, 그 빛과 그림자(에두아르도 갈레아노)", "지속 불가능 자본주의(사이토 고헤이)"],
      stages: [
        ["경기기록과 팀 전략", "득점 외의 어떤 기록이 팀의 경기 흐름을 설명할까?", "패스 연결·공 소유 전환·수비 성공 위치를 기록해 득점 장면과 비교했다.", "개인 득점보다 연결 과정의 지표가 팀 전략을 설명하는 데 유용했다.", "관중과 참여자의 접근성을 조사한다."],
        ["모두가 이용하는 스포츠 행사", "경기장 정보·좌석·이동 동선은 다양한 관중의 참여를 어떻게 제한할까?", "행사 안내와 공간을 정보·이동·감각 접근성 기준으로 점검하고 이용자 관점의 동선을 그렸다.", "시설 설치뿐 아니라 예매와 안내 단계부터 접근성 정보가 제공되어야 했다.", "환경영향을 줄이는 스포츠 행사 운영안을 설계한다."],
        ["지속가능한 스포츠 이벤트", "교내 스포츠행사의 참여·안전·폐기물·비용을 함께 개선할 수 있을까?", "이동·물품·쓰레기·참여 자료를 바탕으로 대여컵·장비공유·분산일정 대안을 비교했다.", "환경 대안도 운영 인력과 안전을 고려해야 지속되어 단계별 시행안이 필요했다.", "스포츠경영·데이터분석·ESG 운영을 탐구한다."]
      ]
    }
  ],
  health: [
    {
      id: "nutrition-environment",
      label: "영양과 식생활 환경",
      books: ["식탁 위의 세계사(이영숙)", "음식의 언어(댄 주래프스키)", "육식의 종말(제러미 리프킨)"],
      stages: [
        ["식품표시 읽기", "당류·나트륨 표시를 1회 제공량과 총량 중 어느 기준으로 봐야 할까?", "음료와 간식의 1회 제공량·총내용량·영양성분을 실제 섭취 단위로 다시 계산했다.", "앞면의 강조 문구만 보면 총섭취량을 낮게 판단할 수 있어 제공량 기준 확인이 필요했다.", "개인의 선택을 둘러싼 식생활 환경을 조사한다."],
        ["건강한 선택과 학교 환경", "건강정보를 알아도 시간·가격·판매환경 때문에 선택이 달라질 수 있을까?", "익명 설문과 판매환경 관찰로 선택 이유를 맛·가격·접근성·정보로 나누어 비교했다.", "지식 부족보다 시간과 선택지의 제약이 큰 경우가 있어 개인교육만으로 해결하기 어려웠다.", "건강·비용·환경을 함께 고려한 식생활 제안을 만든다."],
        ["지속가능한 급식 선택", "영양 균형·기호·가격·탄소배출을 함께 고려한 식단 평가는 어떻게 가능할까?", "가상 식단의 영양기준·잔반·가격·환경자료를 다기준 표로 비교했다.", "한 요소의 최적화보다 학생 의견과 단계적 메뉴 변경을 포함한 혼합안이 현실적이었다.", "영양학·식품과학·공중보건의 식생활 정책을 탐구한다."]
      ]
    },
    {
      id: "infection-health",
      label: "감염과 공중보건",
      books: ["바이러스 폭풍의 시대(네이선 울프)", "전염병이 휩쓴 세계사(김서형)", "페스트(알베르 카뮈)"],
      stages: [
        ["손 위생 절차의 이해", "손 씻기 시간과 빠뜨리는 부위는 위생 효과와 어떤 관련이 있을까?", "형광로션 모형으로 손 씻기 전후 남은 부위를 관찰하고 절차별 누락을 기록했다.", "시간만 늘리는 것보다 손가락 사이와 손톱 등 반복 누락 부위를 안내하는 것이 중요했다.", "감염 위험을 전달하는 표현의 효과를 비교한다."],
        ["감염병 위험소통", "불안을 과도하게 높이지 않으면서 예방행동을 이끌려면 위험정보를 어떻게 제시해야 할까?", "확률·사례·행동지침 중심 안내문을 비교하고 이해도와 불안 반응을 익명 조사했다.", "위험 수치와 함께 개인이 할 수 있는 행동과 정보 갱신 시점을 제시한 자료가 더 신뢰되었다.", "예방정책의 접근성과 우선순위를 검토한다."],
        ["예방정책과 건강 형평성", "같은 예방 지침도 주거·노동·언어 조건이 다른 사람에게 동일하게 실행 가능할까?", "공공자료와 사례를 집단별 장벽·지원수단·예상효과로 분석해 보완 정책을 설계했다.", "정보 제공만으로 부족하며 유급휴가·언어지원·이동서비스 같은 실행 조건이 함께 필요했다.", "간호·역학·보건정책의 형평성 평가를 탐구한다."]
      ]
    }
  ],
  education: [
    {
      id: "reading-literacy",
      label: "독서교육과 문해력",
      books: ["책은 도끼다(박웅현)", "다시, 책으로(매리언 울프)", "유튜브는 책을 집어삼킬 것인가(김성우·엄기호)"],
      stages: [
        ["독서 선택과 지속성", "자유 선택과 추천 목록은 독서 지속성에 어떤 차이를 만들까?", "독서 선택 이유와 중단 지점을 익명 기록하고 책 난이도·관심 주제와 비교했다.", "책의 권위보다 읽을 목적과 현재 수준을 스스로 설명할 때 완독 가능성이 높았다.", "교과별 텍스트의 문해 요구를 비교한다."],
        ["교과 문해력의 차이", "과학 설명문과 사회 논증문을 읽을 때 필요한 읽기 전략은 어떻게 다를까?", "두 교과 글의 용어·도표·주장 구조를 표시하고 이해가 막힌 지점을 사고기록으로 비교했다.", "교과마다 핵심 정보를 찾는 표지가 달라 공통 독서법 하나만으로 충분하지 않았다.", "학교도서관의 맞춤형 읽기 지원을 설계한다."],
        ["학교도서관 읽기 지원", "관심·읽기수준·교과목적이 다른 학생에게 어떤 추천과 지원이 필요할까?", "가상의 이용자 유형별로 책·짧은 글·오디오·토론을 조합한 지원안을 만들고 피드백을 받았다.", "책 목록 제공보다 선택 이유와 읽은 뒤 질문을 나누는 지원이 학습 연결에 도움이 되었다.", "국어교육·문헌정보·교육공학의 문해력 지원을 탐구한다."]
      ]
    },
    {
      id: "digital-learning",
      label: "디지털 학습과 AI",
      books: ["도둑맞은 집중력(요한 하리)", "인공지능이 가르칠 수 있다는 착각(김재인·김성우·김현수)", "AI 지도책(케이트 크로퍼드)"],
      stages: [
        ["알림과 학습 집중", "스마트폰 알림의 유무는 과제 전환과 집중 체감에 어떤 영향을 줄까?", "짧은 학습과제 동안 알림 조건별 중단 횟수와 자기평가를 기록했다.", "알림을 보지 않아도 존재 자체가 확인 행동을 유발해 환경 설정의 중요성을 확인했다.", "디지털 학습도구의 피드백 방식을 비교한다."],
        ["학습도구와 피드백", "정답만 주는 도구와 힌트·질문을 주는 도구는 오류 수정에 어떤 차이를 만들까?", "같은 유형의 문제를 두 피드백 방식으로 풀고 다음 문제에서 수정된 전략을 비교했다.", "즉시 정답은 속도는 높였지만 오류 이유를 설명하는 능력은 질문형 피드백에서 더 잘 나타났다.", "AI 튜터의 도움과 의존을 평가한다."],
        ["AI 튜터의 교육적 책임", "AI 학습도구가 오답·편향·개인정보 위험을 줄이면서 학습자의 판단을 도울 수 있을까?", "AI 답변을 정확성·근거·힌트 수준·개인정보 요구 기준으로 점검하고 교사 검토가 필요한 상황을 분류했다.", "편리함보다 답을 검증하고 도움 수준을 선택하며 기록을 삭제할 권리가 학습자에게 필요했다.", "교육공학·컴퓨터교육·교육윤리의 인간 중심 AI를 탐구한다."]
      ]
    }
  ]
};

const creativeEvalTypeSeriesSeeds = {
  autonomy: {
    korean: [
      ["school-information", "학교 정보 접근성", ["안내문 이해도 조사", "주간 공지 양식 시범 운영", "다언어·쉬운 글 안내 기준 제안"]],
      ["discussion-culture", "학급 토론 문화", ["학급회의 발언 참여 관찰", "익명 의제 수렴 방식 개선", "숙의형 학급회의 운영안 제작"]],
      ["reading-community", "학교 독서 공동체", ["학급 독서 접근성 조사", "학생 주도 책 교환 체계 운영", "다양한 읽기 방식을 포함한 독서문화 제안"]]
    ],
    math: [
      ["data-display", "학교 데이터의 정확한 표현", ["학급 설문 그래프 오류 찾기", "학교 통계 시각화 기준 만들기", "오해를 줄이는 데이터 공개 원칙 제안"]],
      ["space-allocation", "공정한 공간 배정", ["자율학습 좌석 이용 조사", "공간 배정 기준별 결과 비교", "효율·형평성을 반영한 예약 규칙 제안"]],
      ["survey-privacy", "설문과 개인정보 보호", ["익명 설문의 식별 위험 점검", "최소 정보 수집 방식 시범 적용", "학생 조사 윤리 체크리스트 제작"]]
    ],
    science: [
      ["air-quality", "교실 공기질 개선", ["환기 불편 시간대 관찰", "이산화탄소 변화와 환기 방식 비교", "수업 방해를 줄이는 환기 운영안 제안"]],
      ["lab-safety", "과학실 안전문화", ["실험 준비 과정의 위험요소 점검", "안전 안내 이해도와 행동 비교", "학생 참여형 실험 안전 절차 개선"]],
      ["waste-reduction", "교내 폐기물 줄이기", ["분리배출 오류 유형 조사", "표시 방식별 분리배출 정확도 비교", "구매·사용·배출을 연결한 감축안 제안"]]
    ],
    social: [
      ["commute-safety", "통학 안전", ["등하교 위험지점 지도화", "시간대별 보행·차량 흐름 비교", "교통약자를 포함한 통학 안전안 제안"]],
      ["student-participation", "학생 의사결정 참여", ["학교 의견수렴 참여 장벽 조사", "온라인·대면 의견수렴 방식 비교", "결과 공개와 후속 답변 절차 제안"]],
      ["program-access", "학교 프로그램 접근성", ["프로그램 정보 누락 경험 조사", "학년별 신청 장벽과 정보 경로 분석", "참여 기회 격차를 줄이는 안내체계 제안"]]
    ],
    english: [
      ["bilingual-notice", "다언어 학교 안내", ["영문 안내의 직역 오류 찾기", "쉬운 영어와 그림 안내 시범 제작", "언어 배경을 고려한 긴급안내 기준 제안"]],
      ["culture-guide", "문화교류 안내", ["문화 차이로 생긴 오해 사례 수집", "교류학생용 학교생활 안내 제작", "상호문화 관점의 교류 운영안 제안"]],
      ["global-campaign", "국제 쟁점 캠페인", ["영문 환경 캠페인 표현 분석", "청중별 메시지 이해도 비교", "출처와 행동계획이 있는 국제 캠페인 제안"]]
    ],
    pe: [
      ["inclusive-pe", "모두가 참여하는 체육", ["체육활동 참여 장벽 조사", "역할 순환 규칙 시범 적용", "기능 차이를 고려한 포용적 경기안 제안"]],
      ["sports-safety", "교내 스포츠 안전", ["수업·행사 부상 위험요소 관찰", "준비운동과 안전안내 방식 비교", "학생이 점검하는 스포츠 안전체계 제안"]],
      ["active-break", "건강한 쉬는 시간", ["쉬는 시간 신체활동 요구 조사", "공간별 소규모 활동 시범 운영", "소음·안전·참여를 고려한 활동공간 제안"]]
    ],
    health: [
      ["health-literacy", "건강정보 문해력", ["교내 건강정보의 어려운 표현 찾기", "쉬운 글 건강안내 시범 제작", "근거와 행동지침을 갖춘 건강정보 기준 제안"]],
      ["sleep-environment", "학생 수면환경", ["수면을 방해하는 학교생활 요인 조사", "수면위생 안내 방식별 반응 비교", "비난 없이 실천을 돕는 수면지원안 제안"]],
      ["support-access", "마음건강 지원 접근성", ["상담 이용을 어렵게 하는 인식 조사", "상담정보 전달 경로 개선", "익명성과 위기대응을 고려한 지원 안내체계 제안"]]
    ],
    education: [
      ["study-environment", "학습환경 개선", ["자습시간 집중 방해요인 조사", "집중·협업 공간 분리 시범 운영", "학습유형을 고려한 공간 운영안 제안"]],
      ["peer-support", "또래 학습지원", ["도움 요청이 어려운 이유 조사", "질문형 또래 피드백 시범 적용", "의존을 줄이는 또래지원 원칙 제안"]],
      ["digital-focus", "디지털 집중환경", ["학습 중 알림 방해 관찰", "기기 사용규칙별 집중 반응 비교", "학습권과 자율성을 고려한 디지털 사용안 제안"]]
    ]
  },
  career: {
    korean: [
      ["editor", "편집자·출판기획자", ["독자에 맞게 글을 고치는 편집자의 역할", "출판기획과 편집 전공 학습 비교", "AI 출판환경에서 편집자의 검증 책임"]],
      ["journalist", "기자·미디어 연구자", ["사실을 기사로 구성하는 기자의 역할", "언론정보학과와 데이터저널리즘 비교", "공익·속보·정확성 사이의 직업윤리 검토"]],
      ["librarian", "사서·문헌정보 전문가", ["정보를 분류하고 연결하는 사서의 역할", "문헌정보학과의 정보조직·이용자 연구", "디지털 아카이브의 접근권과 개인정보 책임"]]
    ],
    math: [
      ["data-scientist", "데이터과학자", ["자료에서 패턴을 찾는 직무 탐색", "통계학·컴퓨터과학·데이터과학 전공 비교", "예측모형의 편향과 설명 책임 검토"]],
      ["actuary", "보험계리사·위험분석가", ["확률로 위험을 계산하는 직무 탐색", "수학·통계·경제 지식의 활용 비교", "위험등급과 보험 형평성의 직업윤리 검토"]],
      ["industrial-engineer", "산업공학자", ["시간·자원을 효율화하는 직무 탐색", "최적화·품질관리·인간공학 전공 학습 비교", "효율과 노동자 안전을 함께 보는 설계 책임"]]
    ],
    science: [
      ["environment-analyst", "환경분석가", ["물·공기의 오염을 측정하는 직무 탐색", "환경과학·화학·환경공학 전공 비교", "측정 불확실성과 주민 소통 책임 검토"]],
      ["materials-engineer", "재료공학자", ["물질의 성질을 설계하는 직무 탐색", "화학·물리·신소재공학의 접근 비교", "성능·안전·환경영향을 함께 보는 책임"]],
      ["medical-lab", "임상검사·생명연구 분야", ["검체에서 질병 정보를 찾는 직무 탐색", "생명과학·임상병리·의생명 전공 비교", "검사오류·개인정보·결과설명의 책임 검토"]]
    ],
    social: [
      ["urban-planner", "도시계획가", ["생활공간을 설계하는 직무 탐색", "지리·도시공학·건축 전공 비교", "개발이익과 주거권을 조정하는 책임 검토"]],
      ["public-admin", "공공정책·행정 전문가", ["사회문제를 정책으로 다루는 직무 탐색", "행정·경제·법 전공의 정책분석 비교", "예산·효율·형평성을 설명하는 책임 검토"]],
      ["social-researcher", "사회조사 연구자", ["사람들의 경험을 자료로 연구하는 직무 탐색", "사회학·심리학·통계학 연구방법 비교", "조사동의·표본편향·결과공개의 윤리 검토"]]
    ],
    english: [
      ["translator", "통번역 전문가", ["언어와 맥락을 옮기는 직무 탐색", "통번역·영어영문·언어학 전공 비교", "정확성·문화적 존중·기밀유지 책임 검토"]],
      ["diplomat", "외교·국제협력 전문가", ["국가와 기관을 연결하는 직무 탐색", "국제학·정치외교·지역학 전공 비교", "국익·인권·다자협력 사이의 책임 검토"]],
      ["global-content", "글로벌 콘텐츠 기획자", ["다른 문화권의 청중을 이해하는 직무 탐색", "미디어·문화콘텐츠·마케팅 전공 비교", "문화적 고정관념과 현지화의 책임 검토"]]
    ],
    pe: [
      ["pe-teacher", "체육교사", ["안전하고 참여적인 수업을 설계하는 역할", "체육교육과의 운동학·교육학 학습 비교", "기능 차이와 평가 공정성을 다루는 책임"]],
      ["rehab-specialist", "운동재활 전문가", ["회복과 기능 향상을 돕는 직무 탐색", "스포츠과학·물리치료·재활 관련 전공 비교", "과도한 복귀와 개인 건강정보의 윤리 검토"]],
      ["sports-manager", "스포츠경영 전문가", ["경기와 행사를 운영하는 직무 탐색", "스포츠경영·데이터·마케팅 학습 비교", "관중 접근성·선수권리·환경영향 책임 검토"]]
    ],
    health: [
      ["nurse", "간호사", ["환자 상태를 관찰하고 돌보는 역할 탐색", "간호학의 기초과학·임상·의사소통 학습", "환자안전·설명·비밀보장의 전문직 책임"]],
      ["public-health", "공중보건 전문가", ["개인이 아닌 집단의 건강을 다루는 직무", "보건학·역학·통계·정책 전공 비교", "예방정책과 건강 형평성의 책임 검토"]],
      ["health-admin", "보건의료행정 전문가", ["의료서비스를 연결하고 운영하는 직무", "보건행정·경영·정보관리 전공 비교", "효율·접근성·의료정보 보호의 책임 검토"]]
    ],
    education: [
      ["teacher", "교사", ["학생의 배움을 관찰하고 돕는 역할 탐색", "교과교육·교육학·평가 과목의 학습 비교", "공정한 평가와 학생 다양성을 존중하는 책임"]],
      ["counselor", "상담·심리 전문가", ["경청과 질문으로 변화를 돕는 직무 탐색", "심리·상담·교육 전공과 자격과정 비교", "비밀보장·위기개입·전문성 한계의 책임"]],
      ["edtech", "교육공학·에듀테크 전문가", ["학습도구와 환경을 설계하는 직무 탐색", "교육공학·컴퓨터교육·데이터 전공 비교", "AI 편향·개인정보·교사의 역할에 대한 책임"]]
    ]
  },
  club: {
    korean: [
      ["literary-criticism", "문학비평 동아리", ["시점에 따른 인물 해석 비교", "시대별 작품의 공간·정체성 비교", "문학 속 타자 재현과 독자 책임 탐구"]],
      ["media-factcheck", "미디어 팩트체크 동아리", ["기사 제목과 본문 근거 비교", "통계기사의 그래프·출처 검증", "AI 생성정보의 출처·맥락 검증 체계 제작"]],
      ["school-archive", "학교 기록·아카이브 동아리", ["학교 공간의 기억 수집", "세대별 구술기록 관점 비교", "동의·익명화·삭제권을 반영한 디지털 기록관"]]
    ],
    math: [
      ["statistics-model", "통계모형 동아리", ["평균과 분포로 학급자료 읽기", "상관관계와 교란변수 분석", "예측모형의 오차와 공정성 평가"]],
      ["probability-game", "확률게임 설계 동아리", ["주사위 게임의 이론확률과 실험", "기대값과 위험을 반영한 규칙 설계", "참가자 선택 편향을 고려한 게임 검증"]],
      ["optimization", "수학적 최적화 동아리", ["교실 동선의 거리 비교", "공간 배정의 제약조건 모델링", "효율·형평성을 반영한 최적화 모형 평가"]]
    ],
    science: [
      ["water-lab", "수질화학 탐구 동아리", ["생활 주변 물의 pH 반복 측정", "정화재료별 탁도·색도 비교", "미세입자 검출과 재질 판별 한계 분석"]],
      ["materials-lab", "열·재료 탐구 동아리", ["단열재별 냉각곡선 비교", "열성능·가격·재사용성 평가", "안전·환경영향을 포함한 단열시스템 설계"]],
      ["sensor-lab", "센서공학 동아리", ["스마트폰 센서의 반복오차 확인", "기준기기와 센서값 보정", "거짓작동을 줄이는 자동제어 알고리즘 검증"]]
    ],
    social: [
      ["gis", "지리정보 탐구 동아리", ["학교 주변 안전지도 제작", "교통 취약지역 공간분석", "형평성을 반영한 이동정책 대안 지도화"]],
      ["economics", "경제자료 분석 동아리", ["가격과 소비선택 설문", "금리·물가·대출 자료의 시차 분석", "정책효과와 집단별 영향을 비교한 경제 브리프"]],
      ["social-debate", "사회쟁점 토론 동아리", ["주장과 사실근거 구분", "이해관계자별 가치·비용 비교", "반론과 실행조건을 포함한 합의안 작성"]]
    ],
    english: [
      ["podcast", "영어 팟캐스트 동아리", ["영문기사 핵심어 비교", "문화적 배경을 설명하는 영어 대본 제작", "다국적 자료를 종합한 영어 정책방송 제작"]],
      ["translation", "번역 탐구 동아리", ["학교 안내문의 직역·의역 비교", "영상자막의 문화어 번역 실험", "다언어 공공정보의 정확성·접근성 검증"]],
      ["literature", "영미문학 동아리", ["인물 시점과 편견 분석", "시대별 차별 서사 비교", "영미문학의 타자화와 목소리 분석"]]
    ],
    pe: [
      ["sports-data", "스포츠 데이터 동아리", ["득점 외 경기지표 기록", "선수별·상황별 수행자료 분석", "지표의 편향을 고려한 팀전략 모형 평가"]],
      ["training", "운동과학 동아리", ["준비운동과 자각도 비교", "훈련부하와 회복곡선 분석", "개인차를 반영한 단계적 운동복귀 설계"]],
      ["inclusive-game", "포용적 스포츠 동아리", ["경기 참여 장벽 관찰", "도구·역할·득점규칙 변경 실험", "안전과 경기성을 반영한 포용적 게임 제작"]]
    ],
    health: [
      ["nutrition", "영양과 식생활 동아리", ["식품표시의 실제 섭취량 계산", "가격·접근성과 식품선택 관계 조사", "영양·비용·환경을 반영한 식단 평가"]],
      ["infection", "감염과 면역 동아리", ["손 위생 누락부위 모형실험", "감염 위험소통 자료 비교", "예방정책의 접근성과 형평성 분석"]],
      ["health-campaign", "건강소통 동아리", ["온라인 건강정보 출처 판별", "청중별 건강 캠페인 시범 제작", "행동변화와 부작용을 평가하는 캠페인 설계"]]
    ],
    education: [
      ["learning-strategy", "학습전략 연구 동아리", ["다시읽기와 회상학습 비교", "교과별 읽기전략 차이 분석", "자기조절학습 지원 프로그램 평가"]],
      ["feedback", "교육·피드백 동아리", ["칭찬과 구체적 피드백 비교", "질문형 또래피드백의 수정행동 분석", "학습자 선택권을 반영한 멘토링 체계 설계"]],
      ["ai-education", "AI·교육윤리 동아리", ["AI 답변과 교과서 설명 비교", "힌트형·정답형 AI 피드백 실험", "편향·개인정보·의존을 점검하는 AI 학습원칙"]]
    ]
  }
};

function expandCreativeEvalStage(tuple, grade, series) {
  const [title, question, method, evidence, next] = tuple;
  const gradeGuides = {
    1: {
      trigger: "학교생활과 수업에서 관찰한 작은 차이를 막연한 느낌으로 넘기지 않고 확인 가능한 질문으로 바꾸었다.",
      role: "관찰 기준과 기록 양식을 만들고 자료가 같은 조건에서 수집되는지 점검했다.",
      collaboration: "친구들의 다른 경험을 듣고 개인을 평가하지 않도록 조사 표현과 공개 범위를 조정했다.",
      limit: "관찰 기간과 참여 인원이 적어 결과를 전체 학생에게 일반화하기 어려웠다.",
      concept: "수업에서 배운 기초 개념을 실제 학교생활의 현상을 설명하는 데 적용했다."
    },
    2: {
      trigger: "1학년의 질문을 한 가지 원인으로 설명하기 어렵다는 점에서 비교 기준과 자료 범위를 넓혔다.",
      role: "자료 출처와 분류 기준을 정하고 팀원 사이에 해석이 다른 사례를 다시 검토했다.",
      collaboration: "서로 다른 주장을 근거표로 비교하고 피드백을 반영해 조사 방법을 한 차례 수정했다.",
      limit: "자료의 조사 시점과 단위가 달라 직접 비교하는 데 한계가 있었고 다른 변수의 영향도 남았다.",
      concept: "교과의 심화 개념과 자료 해석 방법을 연결해 단순 비교에서 관계 분석으로 발전시켰다."
    },
    3: {
      trigger: "앞선 분석이 실제 해결안으로 이어지려면 가치 충돌과 실행 조건까지 검토해야 한다고 판단했다.",
      role: "평가 기준과 예상되는 오류·부작용을 정리하고 대안별 결과를 검증했다.",
      collaboration: "효율·안전·형평성처럼 충돌하는 기준을 토론하고 단일 답 대신 조건별 대안을 합의했다.",
      limit: "가상 적용과 짧은 검토에 머물러 실제 운영의 장기 효과와 당사자 경험을 충분히 확인하지 못했다.",
      concept: "여러 교과 개념과 윤리적 판단을 통합해 근거·한계·대안이 함께 보이는 탐구로 확장했다."
    }
  };
  const guide = gradeGuides[grade];
  return {
    title,
    trigger: `${guide.trigger} ${question}`,
    question,
    concept: guide.concept,
    method,
    role: guide.role,
    collaboration: guide.collaboration,
    evidence,
    limit: guide.limit,
    learning: `${evidence} 이 과정에서 결과보다 판단 기준과 검증 과정을 설명하는 것이 중요함을 배웠다.`,
    next,
    book: series.books[grade - 1],
    bookInsight: `책에서 얻은 관점을 '${question}'이라는 질문의 배경과 결과 해석에 적용했다.`
  };
}

function getCreativeEvalBookPool(domainKey) {
  const domain = creativeEvalDomains[domainKey] || creativeEvalDomains.korean;
  const additional = creativeEvalAdditionalSeries[domainKey] || [];
  return [
    ...domain.stages.map((stage) => stage.book),
    ...additional.flatMap((series) => series.books)
  ].filter(Boolean);
}

function buildCreativeEvalTypeStage(type, domainKey, seriesIndex, gradeIndex, topic) {
  const domain = creativeEvalDomains[domainKey] || creativeEvalDomains.korean;
  const grade = gradeIndex + 1;
  const bookPool = getCreativeEvalBookPool(domainKey);
  const book = bookPool[(seriesIndex * 3 + gradeIndex) % bookPool.length];
  const typeGuides = {
    autonomy: [
      {
        question: `학교에서 다음 주제와 관련된 불편이나 참여 차이는 언제, 누구에게 나타날까? 주제: ${topic}`,
        method: `발생 시간과 장소를 관찰하고 익명 의견을 받아 불편 유형과 빈도를 표로 정리했다.`,
        evidence: `문제가 특정 시간과 이용 상황에 집중됨을 확인해 학급에서 바로 시험할 수 있는 작은 개선안을 만들었다.`,
        next: `다음 학년에는 운영자료와 이용자 집단을 넓혀 개선안의 효과를 비교한다.`
      },
      {
        question: `학생 의견과 운영자료를 함께 보면 '${topic}'의 원인과 해결 우선순위는 어떻게 달라질까?`,
        method: `기존 운영 방식과 학생 요구를 비교하고 두 가지 개선안을 일정 기간 시범 적용해 전후 반응을 확인했다.`,
        evidence: `반복되는 불편은 줄었지만 일부 학생에게 새로운 장벽이 생겨 이용자별 보완 조건을 추가했다.`,
        next: `3학년에는 안전·접근성·형평성과 장기 운영 가능성을 함께 검토한다.`
      },
      {
        question: `안전·접근성·형평성을 함께 고려해 '${topic}'을 지속 가능한 학교 운영안으로 만들 수 있을까?`,
        method: `학생·교사·담당자의 요구와 예상 부작용을 분류하고 대안별 비용·효과·실행 조건을 평가했다.`,
        evidence: `한 번에 전면 시행하기보다 최소 기준과 점검 시점을 둔 단계별 실행안이 현실적이라는 결론을 제안했다.`,
        next: `후속 이용자 의견과 장기 변화를 확인할 수 있도록 평가 문항과 인계자료를 남긴다.`
      }
    ],
    career: [
      {
        question: `'${topic}' 분야의 전문가는 어떤 문제를 해결하며 고등학교에서 어떤 공부가 필요할까?`,
        method: `직업정보와 학과 소개를 찾아 주요 업무·필요 지식·협업 대상을 비교하고 수업 개념과 연결했다.`,
        evidence: `직업명보다 해결하는 문제와 사용하는 지식이 진로 판단에 중요해 관련 전공과 과목을 넓게 탐색했다.`,
        next: `2학년에는 유사 학과의 전공과목과 연구방법을 비교해 관심 분야를 구체화한다.`
      },
      {
        question: `'${topic}'과 연결되는 학과들은 같은 문제를 어떤 지식과 방법으로 다르게 다룰까?`,
        method: `대학 교육과정과 전공과목, 직무 사례를 비교하고 내가 흥미를 느낀 학습과 보완할 역량을 기록했다.`,
        evidence: `비슷해 보이는 학과도 자료·실험·설계·의사소통의 비중이 달라 나의 선택 기준을 구체화했다.`,
        next: `3학년에는 실제 직무의 책임과 사회적 쟁점을 검토해 전공 적합성을 다시 판단한다.`
      },
      {
        question: `'${topic}' 전문가가 정확성·효율·안전·공정성이 충돌할 때 어떤 책임을 져야 할까?`,
        method: `직무 윤리 사례와 관련 제도를 검토하고 대안별 수혜·위험·한계를 비교해 나의 판단 근거를 정리했다.`,
        evidence: `관심만으로 진로를 정하기보다 필요한 학업, 실제 업무환경, 결정의 사회적 영향을 함께 고려하게 되었다.`,
        next: `대학 전공과 진로를 선택할 때 지속해서 탐구할 질문과 감당할 책임을 상담 기준으로 활용한다.`
      }
    ],
    club: [
      {
        question: `'${topic}'에서 관찰되는 차이에 영향을 주는 조건은 무엇이며 반복해서 확인할 수 있을까?`,
        method: `비교할 조건과 기록 단위를 정하고 같은 절차로 세 차례 이상 자료를 수집해 평균과 범위를 비교했다.`,
        evidence: `예상과 다른 값이 나온 조건을 다시 측정하면서 결과보다 측정 기준과 오차 기록이 중요함을 확인했다.`,
        next: `2학년에는 변인과 자료 범위를 넓혀 관계를 분석하고 다른 설명 가능성을 비교한다.`
      },
      {
        question: `조건과 자료 출처를 달리하면 '${topic}'의 결과와 해석은 어떻게 달라질까?`,
        method: `두 가지 방법이나 자료원을 적용하고 통제하지 못한 변수를 기록해 결과 차이의 원인을 팀별로 검토했다.`,
        evidence: `방법에 따라 결과가 달라지는 사례를 확인해 단일 결론 대신 적용 조건과 해석 범위를 제시했다.`,
        next: `3학년에는 모형·시제품·제안의 오류 상황을 설계해 신뢰성과 한계를 검증한다.`
      },
      {
        question: `'${topic}'의 결과를 실제 상황에 적용할 때 어떤 오류·부작용·윤리 문제가 생길 수 있을까?`,
        method: `정상 조건과 실패 조건을 나누어 검증하고 효율·안전·형평성 기준으로 대안을 평가했다.`,
        evidence: `성능이 가장 높은 안보다 오류를 설명하고 피해를 줄일 보완 장치를 갖춘 안이 타당하다고 판단했다.`,
        next: `대학 수준의 개념과 측정도구를 학습해 이번 탐구의 한계를 후속 연구 질문으로 발전시킨다.`
      }
    ]
  };
  const guide = typeGuides[type][gradeIndex];
  return {
    title: topic,
    trigger: guide.question,
    question: guide.question,
    concept: `${domain.label} 수업에서 배운 개념과 자료 해석 방법을 '${topic}'에 적용했다.`,
    method: guide.method,
    role: grade === 1
      ? "조사 기준과 기록 양식을 만들고 같은 조건에서 자료가 수집되는지 점검했다."
      : grade === 2
        ? "자료 출처와 비교 기준을 맡아 팀원 사이에 해석이 다른 사례를 다시 검토했다."
        : "검증 기준과 예상 오류를 정리하고 결과·한계·대안을 연결해 최종 자료를 구성했다.",
    collaboration: grade === 1
      ? "친구들의 다른 경험을 듣고 개인을 평가하지 않도록 조사 문항과 공개 범위를 조정했다."
      : grade === 2
        ? "서로 다른 해석을 근거표로 비교하고 피드백을 반영해 방법을 수정했다."
        : "충돌하는 기준을 토론하고 반대 의견과 예상 부작용을 최종안에 반영했다.",
    evidence: guide.evidence,
    limit: grade === 1
      ? "관찰 기간과 참여 인원이 적어 결과를 전체 학생과 모든 상황에 일반화하기 어려웠다."
      : grade === 2
        ? "자료의 시점과 단위가 다르고 통제하지 못한 변수가 있어 관계를 원인으로 단정할 수 없었다."
        : "가상 적용과 짧은 검토에 머물러 실제 운영의 장기 효과와 당사자 경험을 충분히 확인하지 못했다.",
    learning: `${guide.evidence} 이 과정에서 활동의 양보다 질문·근거·검증·수정의 흐름이 중요함을 배웠다.`,
    next: guide.next,
    book,
    bookInsight: `책에서 얻은 관점을 '${topic}'의 질문과 결과 해석에 적용하며 처음의 판단을 수정했다.`
  };
}

function getCreativeEvalSeries(domainKey, type = state.creativeEvalType) {
  const seeds = creativeEvalTypeSeriesSeeds[type]?.[domainKey]
    || creativeEvalTypeSeriesSeeds.autonomy.korean;
  return seeds.map(([id, label, topics], seriesIndex) => ({
    id,
    label,
    stages: topics.map((topic, gradeIndex) => buildCreativeEvalTypeStage(type, domainKey, seriesIndex, gradeIndex, topic))
  }));
}

function buildCreativeEvalSeriesExamples(type, domainKey, seriesId = "representative") {
  const domain = creativeEvalDomains[domainKey] || creativeEvalDomains.korean;
  const seriesList = getCreativeEvalSeries(domainKey, type);
  const series = seriesList.find((item) => item.id === seriesId) || seriesList[0];
  return series.stages.map((stage, index) => {
    const grade = index + 1;
    const common = {
      grade,
      domain: domain.label,
      careers: domain.careers
    };
    if (type === "career") {
      const careerProcess = [
        `관련 직업을 넓게 조사한 뒤 ${domain.careers} 계열의 학과 소개와 교육과정에서 이 질문을 어떤 과목으로 배우는지 찾아보았다. 이어 ${stage.method}`,
        `${domain.careers} 계열 안에서도 학과마다 접근 방법이 다름을 확인하기 위해 대학 교육과정, 전공 과목, 직무 사례를 비교했다. 그 뒤 ${stage.method}`,
        `관심 분야를 전공명으로만 정하지 않기 위해 실제 직무의 책임과 사회적 쟁점, 필요한 학업을 비교했다. 전공별 접근을 검토하며 ${stage.method}`
      ][index];
      const careerChange = [
        `${domain.label}에 관심이 있다는 막연한 생각에서 벗어나 '${stage.question}'을 다루는 여러 전공과 직업이 있음을 알게 되었다.`,
        `직업의 겉모습보다 문제를 해결하는 방식이 나와 맞는지 살펴보게 되었고, ${domain.careers} 계열 안에서 더 확인할 분야와 덜 맞는 분야를 구분했다.`,
        `진로를 확정하는 데 집중하기보다 내가 지속해서 탐구하고 싶은 질문, 필요한 학업, 직업이 감당해야 할 책임을 선택 기준으로 세우게 되었다.`
      ][index];
      const careerReflection = [
        `자료의 핵심을 정리하고 질문을 만드는 강점은 확인했지만 실제 전공 수업과 직무 환경은 충분히 알지 못했다. ${stage.limit}`,
        `서로 다른 자료를 비교하는 과정에는 흥미가 있었지만 낯선 전공 용어와 연구 방법을 정확히 이해하는 데 보완이 필요했다. ${stage.limit}`,
        `근거와 사회적 영향을 함께 검토하는 태도는 강점이었지만 실제 현장의 제약과 당사자 관점을 더 확인해야 했다. ${stage.limit}`
      ][index];
      return {
        ...common,
        title: `${stage.title} 진로 탐색`,
        values: {
          activityName: `${domain.careers} 계열 탐색 - ${stage.question}`,
          motivation: `수업과 독서에서 '${stage.title}' 주제를 접한 뒤, 이 문제를 실제로 연구하고 해결하는 사람은 누구이며 어떤 공부가 필요한지 궁금해졌다.`,
          process: careerProcess,
          learningConnection: `${stage.concept} 이 개념이 대학의 전공 과목과 실제 직무에서 어떻게 사용되는지 확인했다.`,
          insight: `${stage.learning} 또한 이 분야는 지식뿐 아니라 자료 검증, 이해관계자 설명, 결과에 대한 책임이 필요함을 알게 되었다.`,
          change: careerChange,
          reflection: careerReflection,
          book: stage.book,
          bookReflection: `${stage.bookInsight} 책을 읽기 전에는 직업의 업무만 생각했지만, 이후에는 그 일이 해결하려는 문제와 사회적 책임도 진로 판단 기준으로 보게 되었다.`,
          bookConnection: `책에서 생긴 다음 질문을 바탕으로 관련 학과의 교육과정과 직무 사례를 찾아보고 전공별 접근 차이를 비교했다. 질문: ${stage.question}`,
          followUp: `${stage.next} 다음 상담에서는 관련 선택과목과 대학 전공 과목을 함께 확인해 현재의 관심이 실제 학업과 맞는지 점검할 계획이다.`
        }
      };
    }
    if (type === "club") {
      const clubRoles = [
        `탐구 기준과 기록 양식을 만들고, 팀원이 같은 조건에서 자료를 수집하는지 점검했다. ${stage.collaboration}`,
        `자료 출처와 분석 기준을 맡아 분류가 다른 사례를 팀원과 다시 검토했다. ${stage.collaboration}`,
        `대안별 검증 기준과 예상 오류를 정리하고 결과 해석을 총괄했다. ${stage.collaboration}`
      ][index];
      return {
        ...common,
        title: `${stage.title} 탐구 프로젝트`,
        values: {
          activityName: `${domain.label} 동아리 - ${stage.title}`,
          motivation: `동아리에서 '${stage.title}' 주제와 관련된 현상을 관찰했다. 결과를 미리 정하지 않고 자료로 확인하기 위해 다음과 같이 탐구 질문을 정했다. ${stage.question}`,
          process: `먼저 ${stage.concept} 팀에서 변인·비교 기준·자료 출처를 정한 뒤 ${stage.method}`,
          role: clubRoles,
          result: stage.evidence,
          limit: stage.limit,
          reflection: `${stage.learning} 예상과 다른 결과를 지우지 않고 원인을 다시 확인하면서 탐구는 정답을 증명하는 일이 아니라 설명을 수정하는 과정임을 배웠다.`,
          followUp: `${stage.next} 다음 동아리 활동에서는 이번 한계를 보완할 자료나 변인을 먼저 정해 후속 탐구로 이어갈 계획이다.`
        }
      };
    }
    const autonomyRoles = [
      "불편을 겪는 학생들의 의견을 익명으로 모을 질문을 만들고, 관찰 자료와 개인 의견을 구분해 학급회의 자료로 정리했다.",
      "기존 방식의 문제를 확인할 조사 기준을 만들고, 학생회·담당 교사·이용 학생의 요구를 분류해 시범안 제작을 맡았다.",
      "앞선 활동의 한계를 바탕으로 개선 기준과 예상 부작용을 정리하고, 시범 운영 결과를 다음 담당자가 활용할 수 있게 문서화했다."
    ][index];
    const autonomyProcess = [
      `학교생활에서 이 문제를 경험한 상황을 먼저 관찰하고, 학생 의견을 익명으로 확인했다. 이후 ${stage.method} 결과를 바탕으로 학급에서 시도할 수 있는 작은 개선안을 만들었다.`,
      `기존 운영 방식과 학생 요구를 비교하고, ${stage.method} 결과를 반영한 개선안을 일정 기간 시범 운영한 뒤 전후 반응을 확인했다.`,
      `효율뿐 아니라 안전·접근성·형평성을 개선 기준으로 정했다. ${stage.method} 이어 실행 가능한 안과 보완 조건을 구분해 학교 구성원에게 제안했다.`
    ][index];
    const autonomyResults = [
      `${stage.evidence} 이를 바탕으로 학급에서 바로 실천할 수 있는 안내·규칙·배치 개선안을 제안하고 구성원의 의견을 다시 받았다.`,
      `${stage.evidence} 시범 운영 뒤 효과가 있었던 부분과 불편이 남은 부분을 나누어 수정안을 만들었다.`,
      `${stage.evidence} 결과와 한계를 함께 공개하고 담당자·이용자 의견을 반영한 단계별 실행안을 남겼다.`
    ][index];
    return {
      ...common,
      title: `${stage.title} 공동체 개선`,
      values: {
        activityName: `학교생활 개선 활동 - ${stage.title}`,
        motivation: `학교와 학급에서 '${stage.title}' 주제와 관련된 불편이나 참여 차이를 발견했다. 개인의 느낌만으로 결정하지 않고 구성원과 함께 다음 질문을 확인할 필요가 있다고 생각했다. ${stage.question}`,
        role: autonomyRoles,
        process: autonomyProcess,
        collaboration: `해결책을 먼저 정하지 않고 서로 다른 이용자의 요구를 들었다. ${stage.collaboration} 반대 의견도 실행 조건에 반영했다.`,
        result: autonomyResults,
        reflection: `${stage.learning} 공동체 문제는 좋은 아이디어만으로 해결되지 않고 의견 수렴, 시범 실행, 결과 공개와 수정이 필요함을 배웠다.`,
        book: stage.book,
        bookReflection: `${stage.bookInsight} 책을 읽고 개인의 태도만 지적하기보다 환경과 규칙이 행동에 미치는 영향도 살펴보게 되었다.`,
        bookConnection: `책에서 얻은 관점을 학생 의견을 묻는 문항과 개선안의 판단 기준에 반영하고, 다음 질문을 학교생활의 문제로 다시 조사했다. ${stage.question}`,
        followUp: `${stage.next} 다음에는 실제 이용자의 후속 의견과 장기적인 변화를 확인해 개선안이 지속될 조건을 보완하고 싶다.`
      }
    };
  });
}

const selfEvalHelp = {
  motivation: {
    title: "탐구 동기",
    guide: "수업과 수행평가에서 실제로 만난 개념, 자료, 질문에서 출발합니다. 책을 읽으며 생긴 의문도 수업 개념과 연결되면 탐구 동기가 될 수 있습니다.",
    good: "통합사회 수업에서 지역 격차를 배운 뒤, 책에서 본 의료 접근성 사례가 떠올랐다. 같은 질병이라도 거주 지역의 병원 수와 이동 시간에 따라 대응 능력이 달라질 수 있다는 점이 궁금해졌다.",
    avoid: "평소 의학에 관심이 있어서 조사했다.",
    sechuk: "지역 격차 개념과 책에서 얻은 의료 접근성 사례를 연결해 보건 격차 문제를 질문으로 발전시키고, 자료 기반 탐구의 출발점을 스스로 설정함.",
    how: "희망 진로부터 쓰기보다, 수업 개념이나 책의 내용 중 무엇이 내 질문을 만들었는지 먼저 씁니다."
  },
  question: {
    title: "탐구 질문",
    guide: "답을 찾아볼 수 있을 만큼 구체적인 질문으로 씁니다. 수업 시간 안에서 깊게 다루기 어렵다면 교과 선생님과 이야기해 개별 탐구 질문으로 다듬을 수 있습니다.",
    good: "지역별 병원 수, 응급실까지의 이동 시간, 고령 인구 비율 중 감염병 대응 취약성과 더 밀접하게 관련되는 요인은 무엇일까?",
    avoid: "의료 문제는 왜 생길까?",
    sechuk: "감염병 대응 취약성을 병원 수, 이동 시간, 고령 인구 비율이라는 변수로 나누어 질문하고, 비교 가능한 자료를 설정해 탐구 방향을 구체화함.",
    how: "좋은 질문은 비교 대상, 변수, 확인하고 싶은 관계가 드러납니다. 책을 읽으며 생긴 질문도 자료로 확인할 수 있는 형태로 바꾸면 좋습니다."
  },
  concept: {
    title: "수업 개념 연결",
    guide: "수업에서 배운 개념, 용어, 원리를 탐구 질문과 연결합니다.",
    good: "확률과 통계의 산포도와 이상치, 통합사회의 지역 격차 개념을 활용해 지역별 보건 지표를 비교했다.",
    avoid: "수업 내용과 관련이 있었다.",
    sechuk: "산포도, 이상치, 지역 격차 개념을 활용해 보건 지표를 비교하며, 수업 개념을 실제 사회 현상 해석에 적용함.",
    how: "개념 이름만 쓰지 말고, 그 개념으로 어떤 자료를 읽고 무엇을 해석했는지까지 적습니다."
  },
  process: {
    title: "탐구 과정",
    guide: "수업 활동이나 수행평가에서 무엇을 어떻게 조사·비교·분석했는지 과정이 보이게 씁니다. 추가 탐구는 교과 선생님과 상의해 수업 내용과 연결되도록 정리합니다.",
    good: "공공데이터에서 지역별 병원 수, 고령 인구 비율, 응급실까지의 이동 시간을 찾아 표로 정리하고 산점도로 비교했다. 병원 수가 많아도 이동 시간이 긴 지역이 있다는 점을 친구들과 토론하며 해석을 수정했다.",
    avoid: "인터넷을 조사하고 발표했다.",
    sechuk: "공공데이터를 표와 산점도로 정리하고, 단일 지표로 설명되지 않는 사례를 토론하며 자료 해석의 근거를 조정하는 모습을 보임.",
    how: "자료 출처, 비교 방식, 분석 도구, 토론·피드백 과정을 넣으면 탐구 과정이 구체적으로 보입니다."
  },
  result: {
    title: "탐구 결과",
    guide: "새롭게 알게 된 점과 자신의 해석을 구체적으로 씁니다.",
    good: "병원 수만으로 의료 접근성을 판단하기 어렵고, 이동 시간과 고령 인구 비율을 함께 보아야 한다는 점을 알게 되었다.",
    avoid: "많은 것을 알게 되어 유익했다.",
    sechuk: "의료 접근성 판단에서 단일 지표의 한계를 인식하고, 이동 시간과 인구 구조를 함께 고려해야 함을 근거 있게 설명함.",
    how: "결과는 감상이 아니라, 탐구 전 생각과 달라진 점이나 새롭게 해석한 내용을 씁니다."
  },
  limit: {
    title: "한계와 보완",
    guide: "자료나 방법의 부족함을 인정하고 보완 방향을 씁니다.",
    good: "시·군 단위 자료만 사용해 개인별 이동 거리는 반영하지 못했다. 다음에는 대중교통 접근성과 응급실 위치 자료를 함께 비교하고 싶다.",
    avoid: "한계는 없었다.",
    sechuk: "사용 자료의 단위를 비판적으로 검토하고, 대중교통 접근성과 응급실 위치 자료를 추가해야 한다는 보완 방향을 제시함.",
    how: "한계를 쓰는 것은 부족함을 드러내는 일이 아니라, 더 나은 탐구로 확장하는 사고를 보여주는 일입니다."
  },
  followUp: {
    title: "후속 탐구·실천",
    guide: "다음 수업, 독서, 동아리, 진로활동으로 어떻게 이어갈지 씁니다.",
    good: "다음 진로활동에서는 보건소 접근성, 원격 진료, 지역 의료 정책을 조사해 지역 격차 완화 방안을 제안해보고 싶다.",
    avoid: "앞으로 더 열심히 공부하고 싶다.",
    sechuk: "탐구 결과를 바탕으로 원격 진료와 지역 보건 정책으로 관심을 확장하며, 후속 진로활동에서 개선 방안을 제안하려는 계획을 세움.",
    how: "후속 활동은 다음 과목, 독서, 동아리, 진로활동 중 하나와 연결하면 좋습니다."
  },
  book: {
    title: "독서 기록",
    guide: "제목(저자) 형태로 쓰되, 독서는 탐구 동기·탐구 질문·후속 활동으로 이동할 수 있습니다. 읽은 사실보다 어떤 질문을 만들었는지가 중요합니다.",
    good: "팩트풀니스(한스 로슬링) - 평균만으로 사회 현상을 판단하면 차이와 예외가 가려질 수 있다는 점을 확인했다. 그래서 지역별 보건 자료도 평균뿐 아니라 분포와 이상치로 봐야겠다고 생각했다.",
    avoid: "책을 읽음.",
    sechuk: "책에서 얻은 평균과 분포의 차이에 대한 관점을 지역별 보건 자료 해석에 적용하며, 독서를 탐구 질문 형성의 계기로 활용함.",
    how: "책 제목만 남기지 말고, 책이 내 탐구 동기나 질문을 어떻게 바꾸었는지 같이 정리합니다."
  },
  bookReflection: {
    title: "책을 읽고 느낀 점",
    guide: "책을 읽으며 새롭게 생긴 생각이나 바뀐 관점을 씁니다.",
    good: "평균값은 전체 경향을 보여주지만, 지역별 차이와 예외 사례를 가릴 수 있다는 점을 알게 되었다. 이후 보건 자료를 볼 때 평균뿐 아니라 분포, 이상치, 자료가 수집된 맥락을 함께 확인해야겠다고 생각했다.",
    avoid: "감명 깊었다.",
    sechuk: "책을 읽으며 통계 자료를 단순 평균으로만 해석하는 태도의 한계를 인식하고, 분포와 예외 사례를 함께 검토하는 방향으로 사고를 확장함.",
    how: "느낌보다 관점 변화, 새로 생긴 질문, 탐구 방법의 변화가 보이면 좋습니다."
  },
  bookConnection: {
    title: "독서와 과목의 연결",
    guide: "책의 내용이 수업 개념, 탐구 질문, 후속 활동과 어떻게 이어지는지 씁니다.",
    good: "책에서 본 통계 해석의 오류를 확률과 통계 수업의 산포도와 이상치 개념에 연결했다. 지역별 보건 자료를 평균으로만 비교하지 않고, 특정 지역이 왜 다른 분포를 보이는지 다시 질문했다.",
    avoid: "과목과 관련이 있었다.",
    sechuk: "책에서 얻은 통계 해석 관점을 확률과 통계 수업의 산포도 개념과 연결하고, 기존 보건 자료를 다른 기준으로 재해석함.",
    how: "책의 내용, 수업 개념, 내 탐구가 서로 이어지게 씁니다."
  },
  competencies: {
    title: "역량 체크",
    guide: "과목별 자기평가서에서 어떤 역량이 드러나는지 표시합니다.",
    good: "자료를 분석하고 해석했다면 학업역량, 희망 전공 질문으로 확장했다면 진로역량, 토론과 협업 과정이 있었다면 공동체역량을 함께 체크할 수 있다.",
    avoid: "좋아 보이는 역량을 모두 체크하기.",
    sechuk: "자료 분석, 질문 확장, 토론과 협업처럼 실제 행동의 근거가 있을 때 역량이 설득력 있게 드러남.",
    how: "역량은 많이 체크하는 것보다, 작성 내용 안에 근거가 있는지 확인하는 것이 중요합니다."
  }
};

const selfEvalSubjectAreaExamples = {
  국어: {
    title: "국어·독서 과목 예시",
    focus: "텍스트 해석, 근거 찾기, 주장 구성, 표현 개선",
    self: "기후 위기 관련 비문학 글을 읽으며 필자의 주장과 근거를 분리해 정리했다. 이후 같은 쟁점을 다룬 통계 기사와 비교하면서, 감정적 표현보다 자료의 출처와 비교 기준이 설득력에 더 중요하다는 점을 확인했다.",
    sechuk: "텍스트의 주장과 근거를 분리해 분석하고, 같은 쟁점을 다룬 통계 자료와 비교하며 자신의 관점을 조정함. 자료의 출처와 비교 기준을 점검하며 비판적 읽기 태도를 드러냄."
  },
  수학: {
    title: "수학 과목 예시",
    focus: "개념 적용, 모델링, 자료 해석, 풀이 과정 설명",
    self: "로그 단원에서 큰 수를 간단히 표현하는 원리를 배운 뒤, 컴퓨터가 데이터를 저장할 때 이진법과 소수 표현을 어떻게 사용하는지 궁금해졌다. 진법 변환과 근삿값 표현을 조사하며 수학 개념이 정보 처리 방식과 연결된다는 점을 정리했다.",
    sechuk: "로그와 진법 개념을 컴퓨터의 수 표현 방식과 연결해 탐구 주제를 설정하고, 근삿값 표현과 이진법의 관계를 조사함. 수학 개념을 단순 계산에 머무르지 않고 정보 처리 원리로 확장해 이해하려는 태도를 보임."
  },
  영어: {
    title: "영어 과목 예시",
    focus: "자료 읽기, 핵심어 정리, 영어 표현으로 주장 전달",
    self: "인공지능 윤리를 다룬 영어 지문에서 bias, accountability, privacy 등의 핵심어를 정리했다. 이후 찬반 자료를 비교해 AI 활용에서 책임 소재가 왜 중요한지 영어 발표문으로 구성했다.",
    sechuk: "영어 지문의 핵심어와 논지를 파악하고, 찬반 자료를 비교해 자신의 의견을 영어 발표문으로 구성함. 내용 이해를 표현 활동으로 연결하며 근거 기반 의사소통 역량을 보임."
  },
  사회: {
    title: "사회 과목 예시",
    focus: "사례 분석, 쟁점 비교, 자료 근거, 대안 제시",
    self: "청년 빈곤 문제를 다룬 토론에서 개인의 노력 부족이라는 설명과 노동시장 구조 문제라는 설명을 비교했다. 청년 실업률, 주거비, 비정규직 비율 자료를 근거로 서로 다른 입장을 검토하고 내 주장을 수정했다.",
    sechuk: "청년 빈곤 문제를 사회 구조와 개인 선택의 관점에서 비교하고, 통계 자료를 근거로 자신의 논리를 조정함. 토론 과정에서 다른 의견을 반박하거나 수용하며 사고를 체계화하는 모습을 보임."
  },
  과학: {
    title: "과학 과목 예시",
    focus: "실험 설계, 변수 통제, 자료 해석, 한계 인식",
    self: "세포분열 관찰 실험에서 예상보다 분열 중인 세포가 적게 나타난 이유를 생각했다. 시료 준비 시간, 염색 정도, 관찰 위치가 결과에 영향을 줄 수 있다고 보고 추가 관찰 방법을 제안했다.",
    sechuk: "세포분열 실험에서 예상과 다른 결과가 나타난 원인을 스스로 분석하고, 시료 준비와 관찰 조건의 영향을 고려해 추가 탐구 방향을 제시함. 과학적 탐구에서 변인과 한계를 점검하는 태도를 보임."
  },
  체육: {
    title: "체육 과목 예시",
    focus: "수행 과정, 신체 변화 관찰, 협업, 피드백 반영",
    self: "운동 수행 기록을 비교하며 자세 교정 전후의 변화를 확인하고, 팀 활동에서 역할 분담이 경기 운영에 미치는 영향을 정리했다.",
    sechuk: "운동 수행 변화를 기록으로 확인하고 피드백을 반영해 자세를 개선함. 팀원과 역할을 조정하며 공동 목표를 위해 협력하는 태도를 보임."
  },
  예술: {
    title: "예술 과목 예시",
    focus: "표현 의도, 제작 과정, 피드백, 감상과 수정",
    self: "작품의 색채를 바꾸며 전달되는 분위기가 어떻게 달라지는지 비교하고, 친구들의 감상 의견을 반영해 표현 방식을 수정했다.",
    sechuk: "색채와 분위기의 관계를 탐색하며 표현 의도를 구체화하고, 동료 피드백을 반영해 작품을 수정하는 과정을 통해 표현력과 성찰 태도를 보임."
  },
  교양: {
    title: "교양·진로 과목 예시",
    focus: "자기 이해, 진로 탐색, 문제 발견, 실천 계획",
    self: "관심 직업의 역할을 조사한 뒤, 그 직업에서 필요한 역량을 현재 수업 활동과 연결했다. 특히 자료 해석, 의사소통, 협업 경험이 부족하다고 판단해 다음 활동에서 보완할 계획을 세웠다.",
    sechuk: "진로 정보를 단순 조사에 그치지 않고 자신의 학습 경험과 연결해 필요한 역량을 분석함. 앞으로의 과목 선택과 활동 계획을 구체화하는 모습을 보임."
  }
};

const selfEvalConceptProfiles = {
  국어: {
    label: "국어",
    concept: "관점, 근거, 표현 방식, 매체성, 독자와 맥락",
    question: "같은 쟁점을 다룬 두 글은 어떤 근거와 표현 방식으로 독자를 설득할까?",
    gradeDepth: {
      1: "글의 주장과 근거를 구분하고, 낯선 표현이나 자료에서 질문을 만든다.",
      2: "서로 다른 글·매체·자료를 비교하며 관점 차이와 설득 방식을 분석한다.",
      3: "사회 쟁점이나 진로 주제와 연결해 자신의 해석과 표현 전략을 정리한다."
    },
    self: "수업에서 읽은 글의 주장과 근거를 나누어 정리한 뒤, 같은 주제를 다룬 기사와 비교하며 표현 방식에 따라 독자의 판단이 달라질 수 있음을 확인했다.",
    sechuk: "텍스트의 주장과 근거를 분리해 읽고, 다른 매체 자료와 비교하며 관점과 표현 전략의 차이를 분석함."
  },
  수학: {
    label: "수학",
    concept: "함수, 변화, 구조, 모델링, 증명, 자료 해석",
    question: "수학 개념을 이용하면 실제 자료의 변화나 관계를 어떻게 더 정확하게 설명할 수 있을까?",
    gradeDepth: {
      1: "계산 결과보다 개념이 쓰이는 이유를 설명하고, 풀이 과정을 자신의 말로 정리한다.",
      2: "함수·확률·도형 개념을 자료나 현상에 적용해 비교하고 해석한다.",
      3: "모델의 조건과 한계를 따져 보고, 더 적절한 수학적 표현이나 후속 질문을 제안한다."
    },
    self: "자료를 평균만으로 비교하면 차이가 가려질 수 있다고 보고, 산포도와 이상치 개념을 활용해 지역별 자료를 다시 해석했다.",
    sechuk: "수학 개념을 실제 자료 해석에 적용하고, 단일 지표의 한계를 인식하며 더 적절한 분석 기준을 탐색함."
  },
  영어: {
    label: "영어",
    concept: "핵심어, 논지, 맥락, 문화, 의사소통 목적",
    question: "영어 지문 속 핵심어와 근거를 파악하면 사회·문화 쟁점을 어떻게 더 분명하게 설명할 수 있을까?",
    gradeDepth: {
      1: "핵심어와 중심 문장을 찾아 지문의 주장을 정확히 이해한다.",
      2: "관련 자료를 비교해 찬반 근거를 정리하고 영어로 자신의 의견을 표현한다.",
      3: "전공·사회 쟁점 자료를 읽고, 맥락과 관점을 반영해 발표나 글쓰기로 확장한다."
    },
    self: "AI 윤리를 다룬 지문에서 bias, privacy, accountability의 의미를 정리하고, 찬반 자료를 비교해 책임 소재에 대한 의견을 영어 발표문으로 구성했다.",
    sechuk: "영어 지문의 핵심어와 논지를 파악하고, 관련 자료를 비교해 근거 기반 의사소통으로 확장함."
  },
  사회: {
    label: "사회",
    concept: "제도, 구조, 갈등, 자료 해석, 대안, 시민성",
    question: "사회 문제는 개인의 선택과 사회 구조 중 어떤 요인이 더 크게 작용할까?",
    gradeDepth: {
      1: "사회 현상을 여러 관점으로 바라보고, 관련 자료를 찾아 질문을 만든다.",
      2: "통계·사례·신문 자료를 비교해 원인과 영향을 분석한다.",
      3: "정책이나 제도의 한계를 평가하고, 실현 가능한 대안과 후속 활동을 제안한다."
    },
    self: "청년 빈곤 문제를 개인 책임과 노동시장 구조의 관점에서 비교하고, 청년 실업률과 주거비 자료를 근거로 내 입장을 조정했다.",
    sechuk: "사회 현상을 여러 관점에서 비교하고, 통계 자료를 근거로 자신의 판단을 수정하며 쟁점 이해를 심화함."
  },
  역사: {
    label: "역사",
    concept: "원인과 결과, 변화와 지속, 사료 해석, 역사적 맥락",
    question: "같은 사건도 사료의 관점과 시대적 맥락에 따라 왜 다르게 해석될까?",
    gradeDepth: {
      1: "사건의 원인과 결과를 시간 순서로 정리하고 기본 맥락을 이해한다.",
      2: "서로 다른 사료나 해석을 비교하며 관점 차이를 찾는다.",
      3: "역사적 쟁점을 현재 사회 문제와 연결해 의미와 한계를 평가한다."
    },
    self: "독립운동 관련 사료를 비교하며 같은 사건도 작성 주체와 목적에 따라 강조점이 달라진다는 점을 확인했다.",
    sechuk: "사료의 작성 배경과 관점을 고려해 역사적 사건을 비교하고, 해석의 차이를 근거 있게 설명함."
  },
  윤리: {
    label: "윤리",
    concept: "가치 판단, 책임, 공동선, 권리, 규범, 딜레마",
    question: "서로 충돌하는 가치가 있을 때 어떤 기준으로 더 책임 있는 판단을 할 수 있을까?",
    gradeDepth: {
      1: "윤리 개념과 입장을 구분하고, 생활 속 사례에 적용한다.",
      2: "쟁점 상황에서 여러 입장의 근거와 한계를 비교한다.",
      3: "현대 사회의 윤리 문제를 전공·진로와 연결해 책임 있는 대안을 모색한다."
    },
    self: "AI 추천 알고리즘의 편리함과 개인정보 보호가 충돌하는 사례를 두고, 공리주의와 의무론 관점의 판단 기준을 비교했다.",
    sechuk: "윤리 이론을 실제 쟁점에 적용해 가치 충돌을 분석하고, 판단 기준의 차이를 근거 있게 설명함."
  },
  지리: {
    label: "지리",
    concept: "공간 분포, 지역성, 상호작용, 스케일, 지속가능성",
    question: "같은 사회 현상도 지역의 위치와 환경에 따라 왜 다르게 나타날까?",
    gradeDepth: {
      1: "지도와 통계 자료를 읽으며 지역별 차이를 관찰한다.",
      2: "자연환경, 인구, 산업, 교통 요인을 연결해 공간 분포를 분석한다.",
      3: "지역 문제의 원인과 대안을 여러 스케일에서 검토하고 지속가능성까지 평가한다."
    },
    self: "지역별 의료 접근성 자료를 지도와 함께 비교하며, 병원 수뿐 아니라 이동 시간과 고령 인구 비율이 함께 작용한다는 점을 확인했다.",
    sechuk: "공간 분포와 지역성 개념을 활용해 지역별 보건 격차를 분석하고, 단일 지표의 한계를 인식함."
  },
  경제: {
    label: "경제",
    concept: "희소성, 선택, 비용과 편익, 시장, 유인, 분배",
    question: "개인과 기업의 선택은 어떤 유인과 비용 구조에 따라 달라질까?",
    gradeDepth: {
      1: "희소성과 선택의 개념으로 생활 속 경제 사례를 설명한다.",
      2: "시장 자료와 정책 사례를 비교해 비용, 편익, 유인의 변화를 분석한다.",
      3: "정책 효과의 장단기 영향과 이해관계자별 차이를 평가한다."
    },
    self: "최저임금 논의를 노동자, 자영업자, 기업의 관점으로 나누고, 고용과 소득 자료를 비교하며 정책 효과를 단순하게 판단하기 어렵다는 점을 정리했다.",
    sechuk: "경제 개념을 활용해 정책 쟁점을 이해관계자별로 분석하고, 상반된 자료를 비교하며 판단을 구체화함."
  },
  "정치·법": {
    label: "정치·법",
    concept: "권리, 제도, 절차, 권력 분립, 시민 참여, 법적 책임",
    question: "제도와 법은 개인의 권리와 공동체의 안전 사이에서 어떤 균형을 만들어야 할까?",
    gradeDepth: {
      1: "권리와 의무, 제도 절차를 사례 중심으로 이해한다.",
      2: "정책·판례·법률 사례를 비교해 쟁점의 근거를 정리한다.",
      3: "사회 문제를 제도 개선 관점에서 검토하고 시민 참여 방안을 제안한다."
    },
    self: "청소년 보호와 표현의 자유가 충돌하는 사례를 두고, 관련 법 조항과 판례의 판단 기준을 비교했다.",
    sechuk: "법적 기준과 권리 충돌 사례를 비교하며 제도의 역할과 한계를 분석함."
  },
  과학: {
    label: "과학",
    concept: "관찰, 변인, 모형, 증거, 설명, 한계",
    question: "예상과 다른 실험 결과가 나왔을 때 어떤 변인과 조건을 다시 확인해야 할까?",
    gradeDepth: {
      1: "관찰 사실과 해석을 구분하고, 실험 조건을 정확히 기록한다.",
      2: "변인을 통제하며 자료를 비교하고, 예상과 다른 결과의 원인을 분석한다.",
      3: "모형의 설명력과 한계를 평가하고, 더 정교한 실험이나 자료 분석을 설계한다."
    },
    self: "실험 결과가 예상과 다르게 나온 이유를 시료 준비, 관찰 위치, 측정 방법으로 나누어 검토하고 보완 방법을 제안했다.",
    sechuk: "실험 과정에서 변인과 조건을 점검하고, 예상과 다른 결과를 과학적으로 설명하려는 태도를 보임."
  },
  물리: {
    label: "물리",
    concept: "힘과 운동, 에너지, 파동, 전자기, 모형화",
    question: "물체의 운동이나 에너지 변화는 어떤 조건에서 예측과 다르게 나타날까?",
    gradeDepth: {
      1: "힘, 속도, 에너지의 기본 관계를 생활 속 사례로 설명한다.",
      2: "실험 자료나 그래프를 활용해 운동·파동·전자기 현상을 분석한다.",
      3: "이상적 모형과 실제 조건의 차이를 비교하고, 오차와 한계를 설명한다."
    },
    self: "마찰을 무시한 계산 결과와 실제 측정값이 달라지는 이유를 생각하며, 접촉면과 측정 오차가 에너지 손실에 미치는 영향을 비교했다.",
    sechuk: "물리 모형과 실제 측정값의 차이를 분석하고, 오차 요인과 조건의 영향을 근거 있게 설명함."
  },
  화학: {
    label: "화학",
    concept: "입자, 결합, 반응, 에너지, 평형, 농도",
    question: "물질의 구조와 반응 조건이 결과물의 성질을 어떻게 바꿀까?",
    gradeDepth: {
      1: "물질의 성질을 입자와 결합의 관점에서 설명한다.",
      2: "농도, 온도, 촉매 등 조건 변화가 반응 결과에 미치는 영향을 비교한다.",
      3: "화학 반응의 효율, 안전성, 환경 영향을 함께 고려해 후속 탐구를 설계한다."
    },
    self: "반응 속도가 온도와 농도에 따라 달라지는 이유를 입자 충돌 관점으로 설명하고, 조건 변화에 따른 결과를 표로 정리했다.",
    sechuk: "화학 반응 조건과 입자 모형을 연결해 자료를 해석하고, 반응 결과의 차이를 근거 있게 설명함."
  },
  생명과학: {
    label: "생명과학",
    concept: "구조와 기능, 항상성, 유전 정보, 물질대사, 상호작용",
    question: "생명 현상은 구조, 기능, 환경 조건이 어떻게 맞물려 나타날까?",
    gradeDepth: {
      1: "생명 현상의 기본 구조와 기능을 관찰하고 개념을 정확히 설명한다.",
      2: "실험 조건과 변인을 바탕으로 세포, 유전, 항상성 자료를 해석한다.",
      3: "생명 현상의 한계와 응용 가능성을 의학·환경·윤리 쟁점과 연결해 검토한다."
    },
    self: "세포분열 관찰에서 예상보다 분열 중인 세포가 적게 보인 이유를 시료 준비 시간과 염색 정도, 관찰 위치의 차이로 나누어 분석했다.",
    sechuk: "생명과학 실험에서 예상과 다른 결과의 원인을 변인별로 검토하고, 관찰 조건의 한계를 바탕으로 후속 탐구를 제안함."
  },
  지구과학: {
    label: "지구과학",
    concept: "시스템, 순환, 시간 규모, 관측 자료, 상호작용",
    question: "지구 시스템의 변화는 어떤 관측 자료를 통해 더 정확하게 설명할 수 있을까?",
    gradeDepth: {
      1: "지권, 수권, 기권, 생물권의 기본 상호작용을 사례로 이해한다.",
      2: "관측 자료와 그래프를 활용해 변화 추세와 원인을 분석한다.",
      3: "긴 시간 규모의 자료와 불확실성을 고려해 기후·재해 문제의 대응 방안을 검토한다."
    },
    self: "강수량 자료와 침수 피해 사례를 비교하며, 같은 강수량이라도 지형과 도시 배수 구조에 따라 피해가 달라질 수 있음을 정리했다.",
    sechuk: "지구 시스템 자료를 지역 조건과 연결해 해석하고, 관측 자료의 의미와 한계를 함께 검토함."
  },
  정보: {
    label: "정보·AI",
    concept: "자료 구조, 알고리즘, 자동화, 모델, 데이터 윤리",
    question: "데이터를 어떤 기준으로 처리하느냐에 따라 결과와 판단은 어떻게 달라질까?",
    gradeDepth: {
      1: "문제를 절차로 나누고, 자료 입력과 출력의 관계를 이해한다.",
      2: "알고리즘이나 데이터 처리 방식이 결과에 미치는 영향을 비교한다.",
      3: "모델의 한계, 편향, 윤리 문제를 검토하고 개선 방향을 제안한다."
    },
    self: "AI가 제시한 설명을 그대로 쓰지 않고, 내가 이해한 정규분포 개념으로 알고리즘의 의미를 다시 설명해보았다.",
    sechuk: "생성형 AI 자료를 비판적으로 점검하고, 수업 개념에 맞게 자신의 언어로 재구성하려는 태도를 보임."
  },
  체육: {
    label: "체육",
    concept: "신체 움직임, 훈련 원리, 건강 관리, 협동, 피드백",
    question: "수행 기록과 피드백을 바탕으로 움직임을 어떻게 개선할 수 있을까?",
    gradeDepth: {
      1: "기본 동작과 규칙을 이해하고 수행 변화를 관찰한다.",
      2: "기록, 자세, 팀 전략을 비교하며 개선 요인을 찾는다.",
      3: "운동 수행과 건강 관리, 팀 운영을 연결해 자기 관리와 협업 역량을 정리한다."
    },
    self: "자세 교정 전후의 수행 기록을 비교하고, 팀 활동에서 역할 분담이 경기 흐름에 미치는 영향을 정리했다.",
    sechuk: "수행 변화를 기록으로 확인하고 피드백을 반영하며, 팀 목표를 위해 협력하는 태도를 보임."
  },
  예술: {
    label: "예술",
    concept: "표현 의도, 매체, 형식, 감상, 수정, 피드백",
    question: "표현 요소를 바꾸면 작품의 의미와 감상은 어떻게 달라질까?",
    gradeDepth: {
      1: "표현 요소와 감상 느낌을 연결해 작품의 특징을 설명한다.",
      2: "매체와 기법을 바꾸며 표현 효과를 비교하고 피드백을 반영한다.",
      3: "작품 의도, 사회적 맥락, 관객 반응을 고려해 표현을 심화한다."
    },
    self: "색채를 바꾸며 작품의 분위기가 어떻게 달라지는지 비교하고, 친구들의 감상 의견을 반영해 표현 방식을 수정했다.",
    sechuk: "표현 요소와 감상 반응을 연결해 작품을 수정하며, 의도와 효과를 성찰함."
  },
  교양: {
    label: "교양·진로",
    concept: "자기 이해, 문제 발견, 진로 탐색, 실천 계획, 성찰",
    question: "내가 발견한 문제와 관심은 어떤 수업 경험과 진로 탐색으로 이어질 수 있을까?",
    gradeDepth: {
      1: "자신의 관심과 강점을 수업 경험 속에서 찾아본다.",
      2: "관심 분야에 필요한 역량을 과목 활동과 연결해 점검한다.",
      3: "진로 목표와 현재 활동의 연결성을 평가하고 구체적 실천 계획을 세운다."
    },
    self: "관심 직업의 역할을 조사한 뒤, 자료 해석과 의사소통 경험이 필요하다고 판단해 다음 활동에서 보완할 계획을 세웠다.",
    sechuk: "진로 정보를 자신의 학습 경험과 연결하고, 필요한 역량과 후속 활동을 구체화함."
  }
};

const selfEvalConceptKeywordRules = [
  { profile: "생명과학", keywords: ["생명", "생물", "세포", "유전", "물질대사", "항상성"] },
  { profile: "물리", keywords: ["물리", "역학", "전자기", "양자", "파동"] },
  { profile: "화학", keywords: ["화학", "물질", "반응", "결합", "농도"] },
  { profile: "지구과학", keywords: ["지구", "행성", "우주", "기후", "해양"] },
  { profile: "역사", keywords: ["한국사", "세계사", "동아시아사", "역사"] },
  { profile: "윤리", keywords: ["윤리", "생활과 윤리", "윤리와 사상"] },
  { profile: "지리", keywords: ["지리", "세계시민", "기후변화", "지속가능"] },
  { profile: "경제", keywords: ["경제", "금융", "시장", "기업"] },
  { profile: "정치·법", keywords: ["정치", "법", "사회문제"] },
  { profile: "정보", keywords: ["정보", "인공지능", "데이터", "프로그래밍"] }
];

const selfEvalProfileGradeExamples = {
  국어: {
    1: {
      self: "공통국어 수업에서 환경 관련 논설문을 읽으며 주장과 근거를 분리해 정리했다. 이후 같은 주제를 다룬 뉴스 기사와 비교하면서, 감정적 표현보다 자료의 출처와 비교 기준이 설득력에 더 큰 영향을 준다는 점을 확인했다.",
      sechuk: "논설문의 주장과 근거를 구분해 읽고, 같은 쟁점을 다룬 기사와 비교하며 표현 방식과 자료 활용이 설득력에 미치는 영향을 분석함."
    },
    2: {
      self: "문학 수업에서 산업화 시기 소설을 읽고 인물의 갈등을 개인 성격이 아니라 시대적 배경과 연결해 해석했다. 작품 속 공간, 직업, 가족 관계를 근거로 인물의 선택이 사회 구조와 맞물려 있음을 정리했다.",
      sechuk: "작품 속 인물의 갈등을 시대적 배경과 사회 구조의 관점에서 해석하고, 구체적 장면을 근거로 자신의 해석을 설득력 있게 제시함."
    },
    3: {
      self: "독서 수업에서 인공지능 윤리를 다룬 글을 읽고 필자의 논증 구조를 분석했다. 이후 다른 칼럼과 비교하며 개인정보 보호와 편의성 사이의 가치 충돌을 정리하고, 내 글에서는 사례와 반론을 함께 제시해 주장 구조를 보완했다.",
      sechuk: "비문학 글의 논증 구조를 분석하고, 다른 관점의 자료와 비교하며 반론을 고려한 주장 구성으로 비판적 읽기와 쓰기 역량을 드러냄."
    }
  },
  수학: {
    1: {
      self: "공통수학에서 함수의 그래프를 배우며 같은 식도 그래프로 보면 변화 경향이 더 잘 보인다는 점이 흥미로웠다. 학교 매점 이용 시간대 자료를 가상의 표로 정리하고 증가·감소 구간을 그래프로 표현해 해석했다.",
      sechuk: "함수와 그래프 개념을 활용해 자료의 변화 경향을 표현하고, 표와 그래프의 장단점을 비교하며 수학적 표현의 의미를 설명함."
    },
    2: {
      self: "확률과 통계에서 평균만으로 자료를 판단할 때 생기는 한계를 배웠다. 지역별 보건 자료를 평균, 중앙값, 이상치로 나누어 비교하며 특정 지역의 취약성이 평균값에 가려질 수 있음을 확인했다.",
      sechuk: "평균, 중앙값, 이상치 개념을 실제 자료 해석에 적용하고, 단일 지표의 한계를 인식하며 자료를 다각도로 비교함."
    },
    3: {
      self: "미적분에서 변화율과 누적량의 관계를 배운 뒤, 감염병 확산 그래프에서 증가 속도가 언제 커지고 줄어드는지 해석했다. 단순 확진자 수보다 변화율을 함께 보아야 정책 판단이 달라질 수 있음을 정리했다.",
      sechuk: "미분 개념을 사회 현상 자료 해석에 적용하고, 변화율과 누적량을 함께 고려해 자료의 의미와 판단 기준을 심화함."
    }
  },
  영어: {
    1: {
      self: "공통영어 지문에서 climate justice라는 표현을 접하고 justice가 단순한 법적 정의가 아니라 사회적 책임과 연결될 수 있음을 알게 되었다. 핵심어를 정리한 뒤 지문의 주장과 근거를 한국어로 재구성했다.",
      sechuk: "영어 지문의 핵심어와 중심 내용을 파악하고, 낯선 개념을 문맥 속에서 해석하며 글의 주장과 근거를 정리함."
    },
    2: {
      self: "영어 독해와 작문에서 AI 활용에 관한 글을 읽고 bias, accountability, privacy의 의미를 정리했다. 찬반 자료를 비교한 뒤 AI 사용에서 책임 소재가 왜 중요한지 영어 문단으로 작성했다.",
      sechuk: "AI 윤리 관련 영어 자료의 핵심어를 분석하고, 찬반 근거를 비교해 자신의 의견을 영어 글쓰기로 구성함."
    },
    3: {
      self: "심화 영어 자료에서 의료 격차를 다룬 글을 읽고 health disparity와 accessibility의 의미를 정리했다. 국내 지역 의료 자료와 연결해 영어 발표문을 구성하며, 단어 번역보다 사회적 맥락 이해가 중요하다고 느꼈다.",
      sechuk: "전공 관련 영어 자료를 사회적 맥락과 연결해 해석하고, 국내 사례와 비교한 발표문을 구성하며 학문적 의사소통 역량을 보임."
    }
  },
  사회: {
    1: {
      self: "통합사회에서 인권과 불평등을 배우며 같은 제도가 사람마다 다르게 작용할 수 있다는 점이 궁금해졌다. 청소년 노동 사례를 찾아 권리 보장과 현실적 제약을 나누어 정리했다.",
      sechuk: "인권과 불평등 개념을 청소년 노동 사례에 적용하고, 제도와 현실의 차이를 구체적 사례로 설명함."
    },
    2: {
      self: "사회와 문화 수업에서 사회 계층과 이동을 배운 뒤, 교육 기회와 가구 소득의 관계를 통계 자료로 확인했다. 단순히 개인 노력으로 설명하기 어려운 구조적 요인을 토론 과정에서 정리했다.",
      sechuk: "사회 계층과 교육 기회 자료를 비교하며 구조적 요인을 분석하고, 토론을 통해 자신의 관점을 근거 있게 조정함."
    },
    3: {
      self: "사회문제 탐구에서 고령화와 지역 소멸 문제를 다루며 인구 구조, 일자리, 의료 접근성을 함께 보았다. 자료 단위가 시·군 수준이라 생활권 내부 차이를 설명하기 어렵다는 한계를 발견하고 후속 자료를 제안했다.",
      sechuk: "복합 사회 문제를 여러 지표로 분석하고, 자료 단위의 한계를 인식해 후속 탐구 방향을 제시함."
    }
  },
  역사: {
    1: {
      self: "한국사 수업에서 같은 사건도 교과서 서술과 당시 신문 자료의 강조점이 다르다는 점을 확인했다. 사건의 원인과 결과를 시간 순서로 정리하고, 자료가 만들어진 시점과 목적을 함께 살폈다.",
      sechuk: "역사 사건의 원인과 결과를 정리하고, 자료의 작성 시점과 목적을 고려해 사료 해석의 기초를 형성함."
    },
    2: {
      self: "세계사 수업에서 산업혁명의 영향을 배운 뒤 노동자의 생활 변화와 기술 발전의 긍정적 효과를 함께 비교했다. 서로 다른 계층의 입장을 나누어 보며 역사적 변화가 모두에게 같은 의미가 아니었음을 정리했다.",
      sechuk: "산업혁명의 영향을 계층별 관점으로 비교하고, 역사적 변화의 복합성을 구체적 사례로 설명함."
    },
    3: {
      self: "동아시아사에서 역사 갈등 사례를 배우며 같은 사건을 국가별 교과서가 어떻게 다르게 서술하는지 비교했다. 역사 인식의 차이가 현재 외교 문제와 연결될 수 있음을 확인하고, 공동 사료 읽기의 필요성을 정리했다.",
      sechuk: "국가별 역사 서술의 차이를 비교하고, 역사 인식이 현재 사회와 외교 문제에 미치는 영향을 비판적으로 탐구함."
    }
  },
  윤리: {
    1: {
      self: "통합사회 윤리 단원에서 행복과 정의를 배우며 개인의 이익과 공동체의 이익이 충돌하는 상황을 생각했다. 학교 규칙 사례를 들어 자유와 책임의 균형을 정리했다.",
      sechuk: "자유와 책임의 관계를 생활 속 사례에 적용하고, 공동체 속 개인의 선택 기준을 고민함."
    },
    2: {
      self: "생활과 윤리에서 생명윤리를 배우며 연명의료 결정 문제를 여러 입장에서 검토했다. 환자의 자기결정권, 가족의 부담, 의료진의 책임을 나누어 판단 기준을 비교했다.",
      sechuk: "생명윤리 쟁점을 다양한 입장에서 분석하고, 가치 충돌 상황에서 판단 기준을 비교하는 태도를 보임."
    },
    3: {
      self: "윤리와 사상에서 공리주의와 의무론을 배운 뒤 AI 추천 알고리즘의 편향 문제를 분석했다. 효율성과 권리 보호가 충돌할 때 어떤 윤리 기준이 필요한지 사례 중심으로 정리했다.",
      sechuk: "윤리 이론을 현대 기술 쟁점에 적용해 가치 충돌을 분석하고, 책임 있는 판단 기준을 제시함."
    }
  },
  지리: {
    1: {
      self: "통합사회에서 지역 격차를 배우며 우리 지역의 교통 접근성이 생활 기회에 어떤 영향을 주는지 궁금해졌다. 지도에서 학교, 병원, 버스 노선을 확인하고 이동 시간 차이를 정리했다.",
      sechuk: "지역 격차 개념을 생활권 자료와 연결해 이해하고, 공간 자료를 활용해 접근성 차이를 설명함."
    },
    2: {
      self: "세계지리 수업에서 기후와 농업의 관계를 배운 뒤 커피 생산 지역의 기후 조건과 노동 문제를 함께 조사했다. 자연환경만이 아니라 소비 구조와 국제 거래도 지역 변화에 영향을 준다는 점을 정리했다.",
      sechuk: "자연환경과 경제 구조를 함께 고려해 지역 특성을 분석하고, 세계적 상호의존 관계를 자료로 설명함."
    },
    3: {
      self: "한국지리 탐구에서 고령화 지역의 의료 접근성을 조사했다. 병원 수, 대중교통 노선, 고령 인구 비율을 함께 비교하며 같은 시·군 안에서도 생활권별 차이가 나타날 수 있음을 확인했다.",
      sechuk: "공간 분포와 지역성 개념을 활용해 의료 접근성 문제를 분석하고, 복수 지표를 연결해 지역 문제의 원인을 탐구함."
    }
  },
  경제: {
    1: {
      self: "통합사회에서 합리적 선택을 배우며 소비 선택이 가격뿐 아니라 시간, 정보, 만족도와도 관련된다는 점을 알게 되었다. 학교 주변 간식 구매 사례를 비용과 편익으로 나누어 정리했다.",
      sechuk: "희소성과 합리적 선택 개념을 생활 사례에 적용하고, 비용과 편익을 구분해 경제적 판단 과정을 설명함."
    },
    2: {
      self: "경제 수업에서 시장 균형을 배운 뒤 최저임금 인상이 고용과 소득에 미치는 영향을 조사했다. 노동자, 자영업자, 기업의 입장을 나누어 자료를 비교하며 정책 효과가 단순하지 않다는 점을 확인했다.",
      sechuk: "시장 균형과 유인 개념을 정책 사례에 적용하고, 이해관계자별 영향을 비교하며 경제 현상을 다각도로 분석함."
    },
    3: {
      self: "실용경제에서 금리 변화가 가계와 기업에 미치는 영향을 탐구했다. 대출 이자 부담, 소비 위축, 투자 결정 자료를 연결해 금리 정책의 효과와 한계를 정리했다.",
      sechuk: "금리 변화를 가계 소비와 기업 투자 관점에서 분석하고, 경제 정책의 효과와 한계를 자료 기반으로 설명함."
    }
  },
  "정치·법": {
    1: {
      self: "통합사회에서 민주주의 원리를 배우며 다수결이 항상 공정한지 궁금해졌다. 학급 의사결정 사례를 통해 다수의 선택과 소수 의견 보호가 함께 필요하다는 점을 정리했다.",
      sechuk: "민주주의 원리를 학급 의사결정 사례에 적용하고, 다수결과 소수 의견 보호의 균형을 설명함."
    },
    2: {
      self: "정치와 법 수업에서 표현의 자유와 명예훼손 문제를 배운 뒤 온라인 댓글 사례를 분석했다. 권리 보장과 피해 예방이 충돌할 때 법적 기준이 왜 필요한지 판례 요지를 바탕으로 정리했다.",
      sechuk: "권리 충돌 사례를 법적 기준과 연결해 분석하고, 표현의 자유와 책임의 관계를 구체적으로 설명함."
    },
    3: {
      self: "사회문제 탐구에서 청소년 참정권 논의를 다루며 정치 참여의 확대와 책임성 문제를 비교했다. 선거 연령 변화 사례와 청소년 정책 참여 사례를 살펴보고 제도 개선 방향을 제안했다.",
      sechuk: "정치 참여와 제도 개선 쟁점을 사례 중심으로 분석하고, 시민 참여의 의미와 책임을 균형 있게 검토함."
    }
  },
  과학: {
    1: {
      self: "통합과학에서 생태계 평형을 배우며 외래종 유입이 먹이 관계에 어떤 변화를 만드는지 궁금해졌다. 수업 사례를 바탕으로 개체 수 변화와 먹이사슬 변화를 표로 정리했다.",
      sechuk: "생태계 평형 개념을 외래종 사례에 적용하고, 한 종의 변화가 전체 관계망에 미치는 영향을 자료로 설명함."
    },
    2: {
      self: "과학 실험에서 예상과 다른 결과가 나온 이유를 단순 실패로 보지 않고 실험 조건별로 나누어 검토했다. 측정 시간, 기구 상태, 반복 횟수가 결과 해석에 영향을 줄 수 있음을 정리했다.",
      sechuk: "실험 결과의 차이를 변인과 조건의 관점에서 분석하고, 자료 해석에서 반복 측정과 조건 통제의 중요성을 인식함."
    },
    3: {
      self: "과학 탐구에서 기후 자료를 분석하며 장기 추세와 단기 변동을 구분해야 한다는 점을 확인했다. 특정 연도 자료만으로 결론을 내리기 어렵다는 한계를 발견하고 더 긴 기간의 자료 비교를 제안했다.",
      sechuk: "과학 자료의 시간 규모와 변동성을 고려해 해석하고, 자료 선택의 한계를 바탕으로 후속 분석 방향을 제시함."
    }
  },
  물리: {
    1: {
      self: "통합과학에서 에너지 전환을 배우며 놀이기구의 위치 에너지와 운동 에너지 변화를 생각했다. 마찰을 무시한 설명과 실제 운동이 다른 이유를 공기 저항과 마찰의 관점에서 정리했다.",
      sechuk: "에너지 전환 개념을 생활 속 운동 사례에 적용하고, 이상적 설명과 실제 조건의 차이를 인식함."
    },
    2: {
      self: "물리학에서 힘과 운동을 배우며 같은 힘을 받아도 물체의 질량에 따라 가속도가 달라지는 이유를 실험 자료로 확인했다. 측정값의 차이를 마찰과 측정 오차로 나누어 분석했다.",
      sechuk: "힘, 질량, 가속도의 관계를 실험 자료로 확인하고, 오차 요인을 구분해 물리 모형의 적용 조건을 설명함."
    },
    3: {
      self: "물리학Ⅱ에서 전자기 유도 개념을 배운 뒤 무선 충전의 원리를 조사했다. 코일의 거리와 정렬 상태가 효율에 영향을 줄 수 있음을 확인하고, 이상적 원리와 실제 기술 적용의 차이를 정리했다.",
      sechuk: "전자기 유도 개념을 실제 기술 사례에 연결하고, 효율에 영향을 미치는 조건을 분석하며 물리 개념의 응용 가능성을 탐구함."
    }
  },
  화학: {
    1: {
      self: "통합과학에서 물질의 상태 변화를 배우며 온도 변화가 입자 운동과 어떤 관련이 있는지 정리했다. 얼음이 녹는 과정에서 온도가 일정하게 유지되는 구간의 의미를 입자 배열 변화와 연결했다.",
      sechuk: "상태 변화와 입자 운동 개념을 연결해 해석하고, 온도 변화 그래프의 의미를 과학적으로 설명함."
    },
    2: {
      self: "화학 수업에서 반응 속도를 배우며 온도와 농도가 결과에 미치는 영향을 비교했다. 입자 충돌 관점으로 조건 변화의 이유를 설명하고, 실험 결과를 표로 정리했다.",
      sechuk: "반응 속도와 입자 충돌 개념을 활용해 실험 조건에 따른 결과 차이를 분석하고, 자료를 근거로 설명함."
    },
    3: {
      self: "화학Ⅱ에서 평형 이동을 배운 뒤 산업 공정에서 온도와 압력 조건을 조절하는 이유를 조사했다. 수율만 높이는 것이 아니라 안전성과 비용도 함께 고려해야 한다는 점을 정리했다.",
      sechuk: "화학 평형 개념을 산업 공정 사례에 적용하고, 수율, 안전성, 비용을 함께 고려해 화학 지식의 실제 적용을 탐구함."
    }
  },
  생명과학: {
    1: {
      self: "통합과학에서 세포가 생명 활동의 기본 단위라는 점을 배우며 세포 구조와 기능의 연결이 궁금해졌다. 세포 소기관의 역할을 정리하고, 기능 이상이 생명 활동에 미칠 영향을 사례로 생각했다.",
      sechuk: "세포 구조와 기능의 관계를 정리하고, 생명 활동을 구성 요소의 상호작용으로 이해하려는 태도를 보임."
    },
    2: {
      self: "생명과학 수업에서 세포분열 관찰 실험을 진행했으나 예상보다 분열 중인 세포가 적게 보였다. 이를 단순 실패로 보지 않고 염색 시간, 관찰 부위, 표본 준비 과정의 차이로 나누어 원인을 분석했다.",
      sechuk: "세포분열 실험에서 예상과 다른 결과를 변인별로 분석하고, 관찰 조건과 표본 준비 과정의 한계를 바탕으로 후속 탐구를 제안함."
    },
    3: {
      self: "생명과학Ⅱ에서 유전자 발현 조절을 학습한 뒤 같은 유전 정보를 가진 세포가 서로 다른 기능을 갖는 이유를 탐구했다. 전사 조절과 세포 분화 개념을 연결해 암세포의 비정상적 증식 사례를 조사했다.",
      sechuk: "유전자 발현 조절과 세포 분화 개념을 질병 사례에 연결하고, 생명 현상을 단일 원인보다 조절 체계의 관점에서 이해함."
    }
  },
  지구과학: {
    1: {
      self: "통합과학에서 지구 시스템을 배우며 비가 많이 와도 지역마다 침수 피해가 다르게 나타나는 이유가 궁금해졌다. 지형, 하천, 배수 시설의 차이를 사례로 정리했다.",
      sechuk: "지구 시스템의 상호작용을 지역 침수 사례에 적용하고, 자연 조건과 인공 환경이 함께 작용함을 설명함."
    },
    2: {
      self: "지구과학에서 대기와 해양의 상호작용을 배우며 엘니뇨가 기후에 미치는 영향을 조사했다. 해수면 온도 변화와 강수량 변화를 그래프로 비교하고 지역별 영향이 다르게 나타나는 이유를 정리했다.",
      sechuk: "대기와 해양의 상호작용 개념을 기후 자료 분석에 적용하고, 그래프를 활용해 지역별 차이를 설명함."
    },
    3: {
      self: "지구과학Ⅱ에서 판 구조론을 배운 뒤 지진 위험 지역의 공간 분포를 조사했다. 판 경계와 진앙 분포 자료를 비교하고, 재해 대응에서는 과학적 예측과 사회적 대비가 함께 필요함을 정리했다.",
      sechuk: "판 구조론과 지진 분포 자료를 연결해 분석하고, 자연 현상 이해를 재해 대응 관점으로 확장함."
    }
  },
  정보: {
    1: {
      self: "정보 수업에서 문제를 작은 절차로 나누는 방법을 배우며 일상적인 반복 작업도 알고리즘으로 표현할 수 있음을 알게 되었다. 등교 준비 과정을 조건과 순서로 나누어 흐름도로 작성했다.",
      sechuk: "생활 속 문제를 절차와 조건으로 나누어 표현하고, 알고리즘적 사고의 기본 구조를 이해함."
    },
    2: {
      self: "인공지능 기초에서 데이터 편향을 배우며 학습 데이터가 결과에 영향을 줄 수 있다는 점이 궁금해졌다. 얼굴 인식 사례를 통해 데이터의 다양성과 오류 가능성을 정리했다.",
      sechuk: "데이터 편향 사례를 분석하고, AI 결과를 비판적으로 해석하며 데이터 구성의 중요성을 설명함."
    },
    3: {
      self: "정보 과목에서 분류 알고리즘을 조사하며 AI가 제시한 설명을 그대로 쓰지 않고, 내가 이해한 정규분포 개념으로 모델의 의미를 다시 설명했다. 설명 가능한 탐구의 중요성을 느꼈다.",
      sechuk: "AI 자료를 비판적으로 점검하고, 수업 개념을 바탕으로 알고리즘의 의미를 자신의 언어로 재구성함."
    }
  },
  체육: {
    1: {
      self: "체육 수업에서 기본 자세가 기록에 미치는 영향을 확인했다. 달리기 출발 자세를 바꾸기 전후의 기록을 비교하고, 팔 동작과 중심 이동이 속도에 영향을 줄 수 있음을 정리했다.",
      sechuk: "기본 동작과 수행 기록을 비교하며 움직임 개선 요인을 찾고, 피드백을 반영해 자세를 조정함."
    },
    2: {
      self: "구기 활동에서 팀 전략과 역할 분담이 경기 흐름에 미치는 영향을 관찰했다. 패스 성공률과 위치 선정이 공격 기회와 연결된다는 점을 팀원들과 피드백하며 수정했다.",
      sechuk: "팀 경기에서 역할 분담과 전략의 효과를 관찰하고, 피드백을 바탕으로 협력적 수행을 개선함."
    },
    3: {
      self: "체육 수업에서 개인 운동 계획을 세우며 심박수와 회복 시간을 기록했다. 단순 운동량보다 강도와 회복의 균형이 중요하다는 점을 확인하고, 지속 가능한 건강 관리 계획을 조정했다.",
      sechuk: "운동 강도와 회복 자료를 바탕으로 자기 관리 계획을 조정하고, 건강 관리 역량을 구체적으로 드러냄."
    }
  },
  예술: {
    1: {
      self: "미술 수업에서 색의 대비가 작품 분위기를 바꾸는 과정을 실험했다. 같은 구도에 다른 색 조합을 적용해보고 친구들의 감상 차이를 비교했다.",
      sechuk: "색채와 분위기의 관계를 탐색하고, 감상 피드백을 바탕으로 표현 효과를 이해함."
    },
    2: {
      self: "음악 수업에서 같은 선율도 빠르기와 악기 편성에 따라 감정이 달라진다는 점을 비교했다. 표현 의도에 맞게 리듬과 강약을 조정하며 발표를 준비했다.",
      sechuk: "음악 요소의 변화가 표현 효과에 미치는 영향을 분석하고, 의도에 맞게 수행을 조정함."
    },
    3: {
      self: "예술 수업에서 사회 문제를 주제로 한 작품을 기획하며 상징과 매체 선택을 고민했다. 관객이 작품을 어떻게 해석할지 예상하고 피드백을 반영해 표현 방식을 수정했다.",
      sechuk: "표현 의도, 매체, 관객 반응을 종합적으로 고려해 작품을 수정하며 예술적 사고와 성찰을 심화함."
    }
  },
  교양: {
    1: {
      self: "진로 수업에서 나의 관심 분야와 강점을 정리했다. 좋아하는 활동을 직업명으로 바로 연결하기보다, 그 활동에서 사용한 역량이 무엇인지 생각했다.",
      sechuk: "자신의 관심 활동을 역량 중심으로 분석하고, 진로 탐색의 기초를 구체화함."
    },
    2: {
      self: "진로 활동에서 관심 직업의 실제 업무를 조사한 뒤, 필요한 역량을 현재 과목 활동과 연결했다. 자료 해석과 의사소통 경험이 부족하다고 판단해 다음 활동에서 보완할 계획을 세웠다.",
      sechuk: "진로 정보를 자신의 학습 경험과 연결해 필요한 역량을 분석하고, 후속 활동 계획을 구체화함."
    },
    3: {
      self: "진로 수업에서 희망 전공과 관련한 사회 문제를 정리하고, 지금까지의 과목 활동이 어떤 질문으로 이어졌는지 돌아보았다. 단순한 전공 희망보다 내가 탐구해온 문제의 흐름을 설명하는 것이 중요하다고 느꼈다.",
      sechuk: "희망 전공과 과목 활동의 연결성을 성찰하고, 자신의 탐구 흐름을 진로 계획으로 구체화함."
    }
  }
};

const selfEvalAdvancedTransformExamples = [
  {
    title: "진로 끼워 넣기에서 수업 출발로",
    weak: "간호사가 되고 싶어서 삼투 현상을 조사했다.",
    better: "생명과학 수업에서 삼투 현상을 배우며 수액 농도가 세포의 수분 이동에 영향을 준다는 점이 궁금해졌다. 이후 생리식염수 농도와 세포 형태 변화 자료를 찾아 수업 개념과 연결했다.",
    sechuk: "삼투 현상 학습 후 수액 농도와 세포 내외 수분 이동의 관계를 질문하고, 생리식염수 사례를 통해 항상성 개념으로 확장함."
  },
  {
    title: "활동 나열에서 탐구 과정으로",
    weak: "최저임금에 대해 조사하고 발표했다.",
    better: "시장 균형 단원에서 임금 결정 원리를 배운 뒤, 최저임금 인상이 고용과 소득에 미치는 영향을 노동자, 자영업자, 기업의 관점으로 나누어 비교했다.",
    sechuk: "시장 균형 개념을 바탕으로 최저임금 정책의 효과를 고용, 소득, 기업 비용 측면에서 비교하고, 상반된 자료를 해석하며 경제 현상을 단일 원인으로 판단하지 않으려는 태도를 보임."
  },
  {
    title: "감상에서 근거 있는 결과로",
    weak: "자료를 보니 환경 문제가 심각하다는 것을 알았다.",
    better: "지역별 미세먼지 농도와 교통량 자료를 비교하며, 같은 도시 안에서도 생활권과 이동량에 따라 노출 정도가 달라질 수 있음을 확인했다.",
    sechuk: "환경 문제를 일반적 감상으로 다루지 않고, 미세먼지 농도와 교통량 자료를 비교해 생활권별 차이를 근거 있게 설명함."
  },
  {
    title: "마무리에서 후속 탐구로",
    weak: "앞으로 더 공부해야겠다고 생각했다.",
    better: "이번 탐구에서 자료 단위가 커 세부 지역 차이를 보기 어려웠기 때문에, 다음에는 동별 자료와 대중교통 접근성을 함께 비교해보고 싶다.",
    sechuk: "탐구 자료의 한계를 인식하고, 세부 지역 자료와 접근성 지표를 추가해 후속 탐구를 설계하려는 태도를 보임."
  },
  {
    title: "AI 요약에서 설명 가능한 탐구로",
    weak: "AI가 알려준 SVM 알고리즘과 가우시안 혼합 모델을 조사했다.",
    better: "대수 수업에서 정규분포 그래프를 배운 뒤, 뇌파 신호처럼 복잡한 자료를 분류할 때 확률밀도함수가 어떻게 활용되는지 궁금해졌다. AI가 제시한 설명을 그대로 쓰지 않고, 내가 이해한 정규분포 개념으로 알고리즘의 의미를 다시 설명해보았다.",
    sechuk: "정규분포와 확률밀도함수 개념을 바탕으로 뇌파 신호 분석 모델의 의미를 이해하려고 시도하고, 생성형 AI 자료를 비판적으로 점검해 자신의 언어로 재구성함."
  }
];

const trackLabels = {
  engineering: "공학",
  medical: "의학·보건",
  humanities: "인문·사회",
  media: "국어·미디어"
};

const subjectRelationGroups = [
  {
    title: "국어 기초 과목",
    subjects: ["공통국어1", "공통국어2", "공통국어", "국어"],
    description: "읽기, 쓰기, 말하기, 자료 해석 능력은 인문·사회·교육·미디어 분야의 기본 토대가 됩니다.",
    tracks: [
      { name: "인문계열", majors: ["국어국문학과", "문예창작학과", "철학과", "사학과"] },
      { name: "교육계열", majors: ["국어교육과", "초등교육과", "교육학과"] },
      { name: "사회과학계열", majors: ["언론정보학과", "미디어커뮤니케이션학과", "행정학과", "법학과"] },
      { name: "문화콘텐츠계열", majors: ["문화콘텐츠학과", "출판콘텐츠학과", "스토리텔링 관련 학과"] }
    ]
  },
  {
    title: "독서·탐구 독서",
    subjects: ["독서", "독서와 작문", "주제 탐구 독서"],
    description: "자료를 읽고 질문을 세우는 힘은 전공 탐색, 보고서 작성, 세특 흐름을 만드는 데 직접 연결됩니다.",
    tracks: [
      { name: "인문계열", majors: ["국어국문학과", "철학과", "사학과", "문헌정보학과"] },
      { name: "교육계열", majors: ["국어교육과", "교육학과"] },
      { name: "사회과학계열", majors: ["사회학과", "정치외교학과", "행정학과", "법학과"] },
      { name: "융합탐구계열", majors: ["자율전공학부", "자유전공학부", "융합학부"] }
    ]
  },
  {
    title: "문학·영상",
    subjects: ["문학", "문학과 영상"],
    description: "작품을 해석하고 시대·사회·표현 방식을 읽어내는 활동은 언어, 문화, 영상 콘텐츠 분야와 잘 이어집니다.",
    tracks: [
      { name: "인문계열", majors: ["국어국문학과", "문예창작학과", "영어영문학과", "비교문학 관련 학과"] },
      { name: "예술·콘텐츠계열", majors: ["영화영상학과", "영상콘텐츠학과", "공연예술학과"] },
      { name: "미디어계열", majors: ["미디어커뮤니케이션학과", "광고홍보학과", "디지털콘텐츠학과"] },
      { name: "교육계열", majors: ["국어교육과", "문화예술교육 관련 학과"] }
    ]
  },
  {
    title: "화법·작문·매체",
    subjects: ["화법과 작문", "화법과 언어", "매체 의사소통", "언어와 매체", "언어생활 탐구"],
    description: "말과 글, 매체 표현을 분석하는 과목은 커뮤니케이션, 언론, 법·정책, 교육 분야와 연결됩니다.",
    tracks: [
      { name: "미디어계열", majors: ["언론정보학과", "미디어커뮤니케이션학과", "광고홍보학과"] },
      { name: "인문언어계열", majors: ["국어국문학과", "언어학과", "문헌정보학과"] },
      { name: "사회과학계열", majors: ["정치외교학과", "행정학과", "법학과"] },
      { name: "교육·상담계열", majors: ["국어교육과", "교육학과", "상담심리학과"] }
    ]
  },
  {
    title: "수학 기초 과목",
    subjects: ["공통수학1", "공통수학2", "공통수학", "수학"],
    description: "수학의 기본 개념은 자연과학, 공학, 데이터, 상경 분야를 이해하는 공통 언어가 됩니다.",
    tracks: [
      { name: "자연과학계열", majors: ["수학과", "통계학과", "물리학과"] },
      { name: "공학계열", majors: ["기계공학과", "전기전자공학과", "컴퓨터공학과", "산업공학과"] },
      { name: "상경계열", majors: ["경제학과", "경영학과", "금융학과"] },
      { name: "데이터계열", majors: ["데이터사이언스학과", "인공지능학과", "빅데이터융합학과"] }
    ]
  },
  {
    title: "대수·수학Ⅰ·수학Ⅱ",
    subjects: ["대수", "수학Ⅰ", "수학Ⅱ"],
    description: "함수와 변화, 수식 구조를 다루는 과목은 공학·자연과학·상경·사회과학의 기초 계산력과 연결됩니다.",
    tracks: [
      { name: "자연과학계열", majors: ["수학과", "통계학과", "물리학과"] },
      { name: "공학계열", majors: ["전기전자공학과", "컴퓨터공학과", "기계공학과", "산업공학과"] },
      { name: "상경계열", majors: ["경제학과", "경영학과", "회계학과", "금융공학 관련 학과"] },
      { name: "사회과학계열", majors: ["심리학과", "사회학과", "행정학과"] }
    ]
  },
  {
    title: "미적분 계열",
    subjects: ["미적분Ⅰ", "미적분Ⅱ", "미적분", "미적분1", "미적분2"],
    keywords: ["미적분"],
    description: "변화량과 누적량을 다루는 미적분은 공학, 자연과학, 의약·보건, AI 분야의 핵심 기초가 됩니다.",
    tracks: [
      { name: "공학계열", majors: ["기계공학과", "전기전자공학과", "화학공학과", "건축공학과", "산업공학과"] },
      { name: "자연과학계열", majors: ["수학과", "물리학과", "화학과", "생명과학과"] },
      { name: "컴퓨터·AI계열", majors: ["컴퓨터공학과", "인공지능학과", "데이터사이언스학과"] },
      { name: "의약·보건계열", majors: ["의예과", "약학과", "간호학과", "보건계열"] },
      { name: "환경·에너지계열", majors: ["에너지공학과", "환경공학과", "신재생에너지 관련 학과"] }
    ]
  },
  {
    title: "기하",
    subjects: ["기하"],
    description: "공간, 도형, 벡터적 사고는 기계, 건축, 로봇, 컴퓨터 그래픽처럼 공간을 다루는 분야와 잘 연결됩니다.",
    tracks: [
      { name: "공학계열", majors: ["기계공학과", "전기전자공학과", "로봇공학과", "항공우주공학과"] },
      { name: "건축·공간계열", majors: ["건축학과", "건축공학과", "도시공학과", "공간정보공학과"] },
      { name: "컴퓨터·그래픽계열", majors: ["컴퓨터공학과", "게임공학과", "영상공학과", "인공지능학과"] },
      { name: "자연과학계열", majors: ["수학과", "물리학과", "천문우주학과"] },
      { name: "디자인공학계열", majors: ["산업디자인학과", "제품디자인학과", "융합디자인학과"] }
    ]
  },
  {
    title: "확률과 통계",
    subjects: ["확률과 통계"],
    description: "자료를 해석하고 불확실성을 다루는 능력은 데이터, 사회조사, 상경, 보건 분야에서 폭넓게 쓰입니다.",
    tracks: [
      { name: "데이터계열", majors: ["통계학과", "데이터사이언스학과", "빅데이터융합학과"] },
      { name: "상경계열", majors: ["경영학과", "경제학과", "회계학과", "금융학과"] },
      { name: "사회과학계열", majors: ["심리학과", "사회학과", "행정학과", "정치외교학과"] },
      { name: "보건계열", majors: ["보건행정학과", "간호학과", "공중보건 관련 학과"] },
      { name: "미디어·조사계열", majors: ["미디어커뮤니케이션학과", "광고홍보학과", "소비자학과"] }
    ]
  },
  {
    title: "수학 탐구 과목",
    subjects: ["수학과제 탐구", "수학 과제 탐구", "수학과 문화"],
    description: "수학 개념을 주제로 탐구를 설계하는 과목은 수학, 교육, 데이터, 융합 전공 탐색에 활용하기 좋습니다.",
    tracks: [
      { name: "자연과학계열", majors: ["수학과", "통계학과"] },
      { name: "교육계열", majors: ["수학교육과", "교육학과"] },
      { name: "데이터계열", majors: ["데이터사이언스학과", "인공지능학과"] },
      { name: "융합계열", majors: ["자율전공학부", "자유전공학부", "융합학부"] }
    ]
  },
  {
    title: "정보·인공지능",
    subjects: ["정보", "정보 처리와 관리", "인공지능 기초", "인공지능 수학"],
    keywords: ["인공지능", "정보"],
    description: "정보와 AI 과목은 문제를 데이터와 알고리즘으로 해결하는 사고를 기르는 데 도움이 됩니다.",
    tracks: [
      { name: "컴퓨터·AI계열", majors: ["컴퓨터공학과", "소프트웨어학과", "인공지능학과"] },
      { name: "데이터계열", majors: ["데이터사이언스학과", "빅데이터융합학과", "통계학과"] },
      { name: "보안계열", majors: ["정보보안학과", "사이버보안학과"] },
      { name: "산업·경영계열", majors: ["산업공학과", "경영정보학과", "스마트팩토리 관련 학과"] },
      { name: "미디어기술계열", majors: ["디지털미디어학과", "게임공학과", "콘텐츠IT 관련 학과"] }
    ]
  },
  {
    title: "영어 기초 과목",
    subjects: ["공통영어1", "공통영어2", "공통영어", "영어", "영어Ⅰ", "영어Ⅱ"],
    description: "영어는 어문뿐 아니라 국제, 경영, 관광, 미디어 분야에서 자료 읽기와 의사소통 역량으로 이어집니다.",
    tracks: [
      { name: "어문계열", majors: ["영어영문학과", "영어학과", "통번역학과"] },
      { name: "국제계열", majors: ["국제학부", "국제관계학과", "국제통상학과"] },
      { name: "교육계열", majors: ["영어교육과", "초등교육과"] },
      { name: "상경계열", majors: ["경영학과", "경제학과", "무역학과"] },
      { name: "관광·서비스계열", majors: ["관광학과", "호텔경영학과", "항공서비스학과"] }
    ]
  },
  {
    title: "영어 독해·작문",
    subjects: ["영어 독해와 작문", "심화 영어", "심화 영어 독해와 작문"],
    description: "영어 자료를 읽고 글로 정리하는 능력은 국제 이슈, 전공 원서, 정책·사회 자료 탐구에 활용됩니다.",
    tracks: [
      { name: "어문계열", majors: ["영어영문학과", "언어학과", "통번역학과"] },
      { name: "국제계열", majors: ["국제학부", "국제관계학과", "국제통상학과"] },
      { name: "사회과학계열", majors: ["정치외교학과", "행정학과", "법학과"] },
      { name: "상경계열", majors: ["경영학과", "경제학과", "금융학과"] },
      { name: "연구·융합계열", majors: ["자율전공학부", "글로벌융합학부"] }
    ]
  },
  {
    title: "영어 회화·발표·토론",
    subjects: ["영어 회화", "실생활 영어 회화", "영어 발표와 토론"],
    description: "영어로 설명하고 토론하는 경험은 국제, 교육, 서비스, 미디어 분야의 의사소통 역량과 이어집니다.",
    tracks: [
      { name: "국제계열", majors: ["국제학부", "국제관계학과", "국제통상학과"] },
      { name: "교육계열", majors: ["영어교육과", "교육학과"] },
      { name: "경영·서비스계열", majors: ["경영학과", "관광학과", "호텔경영학과", "항공서비스학과"] },
      { name: "미디어계열", majors: ["미디어커뮤니케이션학과", "광고홍보학과"] },
      { name: "사회과학계열", majors: ["정치외교학과", "행정학과"] }
    ]
  },
  {
    title: "영미 문학·영어권 문화",
    subjects: ["영미 문학 읽기", "영어권 문화", "세계 문화와 영어"],
    description: "언어와 문화를 함께 읽는 과목은 어문, 국제지역, 콘텐츠, 다문화 교육과 연결됩니다.",
    tracks: [
      { name: "어문계열", majors: ["영어영문학과", "문화비평 관련 학과", "언어학과"] },
      { name: "국제지역계열", majors: ["국제학부", "지역학과", "문화인류학과"] },
      { name: "문화콘텐츠계열", majors: ["문화콘텐츠학과", "영상콘텐츠학과"] },
      { name: "교육계열", majors: ["영어교육과", "다문화교육 관련 학과"] }
    ]
  },
  {
    title: "미디어 영어",
    subjects: ["미디어 영어"],
    description: "영어 매체를 분석하고 활용하는 활동은 미디어, 광고, 콘텐츠, 국제 커뮤니케이션 분야와 이어집니다.",
    tracks: [
      { name: "미디어계열", majors: ["미디어커뮤니케이션학과", "언론정보학과", "광고홍보학과"] },
      { name: "콘텐츠계열", majors: ["디지털콘텐츠학과", "영상콘텐츠학과", "문화콘텐츠학과"] },
      { name: "국제커뮤니케이션계열", majors: ["국제학부", "글로벌커뮤니케이션학과"] }
    ]
  },
  {
    title: "사회 기초 과목",
    subjects: ["통합사회", "통합사회1", "통합사회2", "사회"],
    description: "사회 현상을 여러 관점에서 보는 과목은 사회과학, 법·정책, 상경, 복지 분야의 기초가 됩니다.",
    tracks: [
      { name: "사회과학계열", majors: ["사회학과", "정치외교학과", "행정학과", "심리학과"] },
      { name: "법·정책계열", majors: ["법학과", "정책학과", "공공인재학부"] },
      { name: "상경계열", majors: ["경영학과", "경제학과", "회계학과"] },
      { name: "복지·교육계열", majors: ["사회복지학과", "교육학과", "상담심리학과"] },
      { name: "국제계열", majors: ["국제학부", "국제관계학과"] }
    ]
  },
  {
    title: "역사",
    subjects: ["한국사", "한국사1", "한국사2", "세계사", "동아시아사", "역사로 탐구하는 현대 세계", "동아시아 역사 기행"],
    keywords: ["역사"],
    description: "역사 과목은 시대 변화와 사회 구조를 읽는 힘을 길러 인문, 교육, 문화유산, 국제지역 분야와 연결됩니다.",
    tracks: [
      { name: "인문계열", majors: ["사학과", "역사문화학과", "고고학과"] },
      { name: "교육계열", majors: ["역사교육과", "사회교육과"] },
      { name: "국제지역계열", majors: ["국제학부", "지역학과", "문화인류학과"] },
      { name: "문화유산계열", majors: ["문화재학과", "박물관학 관련 학과"] },
      { name: "콘텐츠계열", majors: ["문화콘텐츠학과", "스토리텔링 관련 학과"] }
    ]
  },
  {
    title: "지리·공간",
    subjects: ["세계지리", "한국지리", "세계시민과 지리", "한국지리 탐구"],
    keywords: ["지리"],
    description: "공간, 지역, 환경을 분석하는 지리 과목은 도시, 공간정보, 환경, 관광 분야와 잘 이어집니다.",
    tracks: [
      { name: "지리·공간계열", majors: ["지리학과", "지리교육과", "공간정보공학과"] },
      { name: "도시·지역계열", majors: ["도시공학과", "도시행정학과", "지역개발학과"] },
      { name: "환경계열", majors: ["환경학과", "환경공학과", "기후환경학과"] },
      { name: "관광계열", majors: ["관광학과", "문화관광학과"] },
      { name: "국제계열", majors: ["국제학부", "국제관계학과"] }
    ]
  },
  {
    title: "정치·법",
    subjects: ["정치와 법", "정치", "법과 사회"],
    description: "정치 제도, 법, 정책을 다루는 과목은 공공 문제를 분석하고 제도를 이해하는 분야와 연결됩니다.",
    tracks: [
      { name: "법·정책계열", majors: ["법학과", "정책학과", "공공인재학부"] },
      { name: "사회과학계열", majors: ["정치외교학과", "행정학과", "사회학과"] },
      { name: "국제계열", majors: ["국제관계학과", "국제학부"] },
      { name: "미디어계열", majors: ["미디어커뮤니케이션학과", "언론정보학과"] },
      { name: "교육계열", majors: ["사회교육과", "일반사회교육과"] }
    ]
  },
  {
    title: "사회문화·사회문제",
    subjects: ["사회와 문화", "사회·문화", "사회문화", "사회문제 탐구"],
    description: "사회 구조, 문화, 집단, 불평등을 탐구하는 과목은 사회과학과 복지, 미디어, 교육 분야와 연결됩니다.",
    tracks: [
      { name: "사회과학계열", majors: ["사회학과", "심리학과", "정치외교학과"] },
      { name: "복지계열", majors: ["사회복지학과", "아동학과", "청소년학과"] },
      { name: "미디어계열", majors: ["미디어커뮤니케이션학과", "광고홍보학과"] },
      { name: "교육계열", majors: ["교육학과", "상담심리학과"] },
      { name: "데이터사회계열", majors: ["사회조사·통계 관련 학과", "빅데이터융합학과"] }
    ]
  },
  {
    title: "경제",
    subjects: ["경제", "인간과 경제활동", "실용경제", "경제 수학", "경제수학"],
    keywords: ["경제"],
    description: "자원 배분, 시장, 선택을 다루는 경제 과목은 상경, 국제통상, 정책, 데이터 분야와 연결됩니다.",
    tracks: [
      { name: "상경계열", majors: ["경제학과", "경영학과", "회계학과", "금융학과"] },
      { name: "국제통상계열", majors: ["무역학과", "국제통상학과", "국제학부"] },
      { name: "정책계열", majors: ["행정학과", "정책학과", "세무학과"] },
      { name: "데이터계열", majors: ["통계학과", "데이터사이언스학과", "경영정보학과"] },
      { name: "소비자·산업계열", majors: ["소비자학과", "산업공학과"] }
    ]
  },
  {
    title: "윤리·철학·논리",
    subjects: ["현대사회와 윤리", "윤리문제 탐구", "인간과 철학", "논리와 사고", "논리학", "논술", "윤리와 사상", "삶과 종교"],
    keywords: ["윤리", "철학", "논리", "종교"],
    description: "가치 판단과 논리적 사고를 다루는 과목은 인문, 교육, 법·정책, 상담, AI 윤리 분야와 이어집니다.",
    tracks: [
      { name: "인문계열", majors: ["철학과", "윤리학과", "종교학과"] },
      { name: "교육계열", majors: ["윤리교육과", "교육학과", "상담심리학과"] },
      { name: "법·정책계열", majors: ["법학과", "행정학과", "정책학과"] },
      { name: "보건·생명윤리계열", majors: ["보건학과", "간호학과", "생명윤리 관련 학과"] },
      { name: "AI·정보윤리계열", majors: ["인공지능학과", "정보보안학과", "데이터윤리 관련 융합학과"] }
    ]
  },
  {
    title: "기후·환경·도시",
    subjects: ["기후변화와 지속가능한 세계", "도시의 미래 탐구", "여행지리", "기후변화와 환경생태", "생태와 환경", "환경", "생활과 과학"],
    keywords: ["기후", "환경", "생태"],
    description: "환경 변화와 지속가능성을 다루는 과목은 환경, 생태, 도시정책, 에너지 분야와 연결됩니다.",
    tracks: [
      { name: "환경계열", majors: ["환경학과", "환경공학과", "기후환경학과"] },
      { name: "생태계열", majors: ["생태학 관련 학과", "산림환경학과", "해양생태 관련 학과"] },
      { name: "도시·정책계열", majors: ["도시공학과", "환경정책학과", "행정학과"] },
      { name: "관광·지역계열", majors: ["관광학과", "문화관광학과", "지역개발학과"] },
      { name: "에너지계열", majors: ["에너지공학과", "신재생에너지 관련 학과"] }
    ]
  },
  {
    title: "과학 기초·융합 과목",
    subjects: ["통합과학", "통합과학1", "통합과학2", "과학탐구실험", "과학탐구실험1", "과학탐구실험2", "융합과학", "융합과학 탐구", "과학의 역사와 문화", "과학사", "과학과제 연구"],
    description: "과학의 기본 개념과 탐구 방법은 자연과학, 공학, 보건, 환경 분야를 두루 살펴보는 출발점입니다.",
    tracks: [
      { name: "자연과학계열", majors: ["물리학과", "화학과", "생명과학과", "지구과학과"] },
      { name: "공학계열", majors: ["기계공학과", "전기전자공학과", "화학공학과", "컴퓨터공학과"] },
      { name: "융합과학계열", majors: ["자율전공학부", "융합과학부", "과학기술학 관련 학과"] },
      { name: "교육계열", majors: ["과학교육과", "물리교육과", "화학교육과", "생물교육과", "지구과학교육과"] },
      { name: "보건·환경계열", majors: ["보건학과", "환경학과", "식품영양학과"] }
    ]
  },
  {
    title: "물리 계열 과목",
    subjects: ["물리학", "물리학Ⅰ", "물리학Ⅱ", "물리학1", "물리학2", "역학과 에너지", "전자기와 양자"],
    keywords: ["물리", "역학", "전자기", "양자"],
    description: "힘, 운동, 에너지, 전자기 개념은 기계·전자·반도체·에너지·우주 분야를 이해하는 데 중요합니다.",
    tracks: [
      { name: "공학계열", majors: ["기계공학과", "전기전자공학과", "로봇공학과", "항공우주공학과"] },
      { name: "반도체·전자계열", majors: ["반도체공학과", "전자공학과", "디스플레이공학과"] },
      { name: "에너지계열", majors: ["에너지공학과", "원자력공학과", "신재생에너지 관련 학과"] },
      { name: "건축·토목계열", majors: ["건축공학과", "토목공학과", "사회환경시스템공학과"] },
      { name: "자연과학계열", majors: ["물리학과", "천문우주학과"] },
      { name: "컴퓨터융합계열", majors: ["컴퓨터공학과", "인공지능학과", "로봇AI 관련 학과"] }
    ]
  },
  {
    title: "화학 계열 과목",
    subjects: ["화학", "화학Ⅰ", "화학Ⅱ", "화학1", "화학2", "물질과 에너지", "화학 반응의 세계", "화학반응의 세계"],
    keywords: ["화학", "물질", "반응"],
    description: "물질의 구조와 반응을 다루는 화학 과목은 소재, 의약, 식품, 환경 분야와 폭넓게 연결됩니다.",
    tracks: [
      { name: "자연과학계열", majors: ["화학과", "응용화학과"] },
      { name: "소재·화공계열", majors: ["화학공학과", "신소재공학과", "고분자공학과", "에너지소재 관련 학과"] },
      { name: "의약계열", majors: ["약학과", "제약공학과", "의예과", "치의예과"] },
      { name: "생명·식품계열", majors: ["생명공학과", "식품공학과", "식품영양학과"] },
      { name: "환경계열", majors: ["환경공학과", "환경화학 관련 학과"] },
      { name: "화장품·생활소재계열", majors: ["화장품학과", "바이오화장품 관련 학과"] }
    ]
  },
  {
    title: "생명과학 계열 과목",
    subjects: ["생명과학", "생명과학Ⅰ", "생명과학Ⅱ", "생명과학1", "생명과학2", "세포와 물질대사", "생물의 유전"],
    keywords: ["생명", "세포", "유전"],
    description: "생명 현상, 세포, 유전 개념은 생명과학, 의약·보건, 식품, 생태 분야의 기초가 됩니다.",
    tracks: [
      { name: "생명과학계열", majors: ["생명과학과", "생명공학과", "분자생명과학과"] },
      { name: "의약·보건계열", majors: ["의예과", "간호학과", "약학과", "임상병리학과", "물리치료학과"] },
      { name: "식품·영양계열", majors: ["식품영양학과", "식품공학과"] },
      { name: "환경·생태계열", majors: ["환경생태공학과", "생태학 관련 학과"] },
      { name: "농생명계열", majors: ["농생명과학과", "동물자원학과", "식물생명과학과"] },
      { name: "바이오융합계열", majors: ["바이오의공학과", "바이오시스템공학과"] }
    ]
  },
  {
    title: "지구과학 계열 과목",
    subjects: ["지구과학", "지구과학Ⅰ", "지구과학Ⅱ", "지구과학1", "지구과학2", "지구시스템과학", "행성우주과학"],
    keywords: ["지구", "우주", "행성", "대기"],
    description: "지구, 대기, 해양, 우주를 다루는 과목은 환경과학, 천문우주, 기후, 공간정보 분야와 연결됩니다.",
    tracks: [
      { name: "지구과학계열", majors: ["지구과학과", "지질학과", "해양학과"] },
      { name: "천문·우주계열", majors: ["천문우주학과", "우주과학과"] },
      { name: "대기·기후계열", majors: ["대기과학과", "기후환경학과"] },
      { name: "환경계열", majors: ["환경공학과", "지구환경과학과"] },
      { name: "공간정보계열", majors: ["공간정보공학과", "지리정보시스템 관련 학과"] },
      { name: "자원·에너지계열", majors: ["자원공학과", "에너지자원공학과"] }
    ]
  },
  {
    title: "음악",
    subjects: ["음악", "음악 연주와 창작", "음악 감상과 비평"],
    description: "음악 과목은 표현, 감상, 창작 경험을 바탕으로 예술, 공연, 교육, 문화기획 분야와 연결됩니다.",
    tracks: [
      { name: "예술계열", majors: ["음악학과", "작곡과", "성악과", "기악과"] },
      { name: "공연예술계열", majors: ["공연예술학과", "뮤지컬학과", "실용음악학과"] },
      { name: "교육계열", majors: ["음악교육과", "문화예술교육 관련 학과"] },
      { name: "콘텐츠계열", majors: ["음향제작학과", "영상음악 관련 학과"] },
      { name: "문화경영계열", majors: ["문화예술경영학과", "공연기획 관련 학과"] }
    ]
  },
  {
    title: "미술",
    subjects: ["미술", "미술 창작", "미술 감상과 비평"],
    description: "미술 과목은 시각 표현과 해석 능력을 바탕으로 예술, 디자인, 영상, 공간 분야와 연결됩니다.",
    tracks: [
      { name: "예술계열", majors: ["회화과", "조소과", "미술학과"] },
      { name: "디자인계열", majors: ["시각디자인학과", "산업디자인학과", "패션디자인학과"] },
      { name: "영상·콘텐츠계열", majors: ["애니메이션학과", "영상디자인학과", "게임그래픽 관련 학과"] },
      { name: "건축·공간계열", majors: ["건축학과", "실내건축학과", "공간디자인학과"] },
      { name: "교육계열", majors: ["미술교육과", "문화예술교육 관련 학과"] }
    ]
  },
  {
    title: "체육·운동",
    subjects: ["체육", "체육1", "체육2", "체육1·2", "스포츠 생활", "스포츠 생활1", "스포츠 생활2", "운동과 건강", "스포츠 문화", "체육 탐구"],
    keywords: ["체육", "스포츠", "운동"],
    description: "체육 과목은 신체 활동, 건강, 운동 분석을 바탕으로 체육, 재활, 스포츠산업 분야와 연결됩니다.",
    tracks: [
      { name: "체육계열", majors: ["체육학과", "스포츠과학과", "생활체육학과"] },
      { name: "교육계열", majors: ["체육교육과"] },
      { name: "보건·재활계열", majors: ["물리치료학과", "운동재활학과", "작업치료학과"] },
      { name: "산업계열", majors: ["스포츠산업학과", "스포츠마케팅 관련 학과"] },
      { name: "심리·상담계열", majors: ["스포츠심리학 관련 학과", "상담심리학과"] },
      { name: "의생명융합계열", majors: ["운동생리학", "스포츠의학 관련 학과"] }
    ]
  },
  {
    title: "보건",
    subjects: ["보건"],
    description: "보건 과목은 건강, 질병 예방, 지역사회 문제를 이해하며 보건·간호·복지 분야와 연결됩니다.",
    tracks: [
      { name: "보건계열", majors: ["보건학과", "보건행정학과", "공중보건학과"] },
      { name: "의약·간호계열", majors: ["간호학과", "임상병리학과", "물리치료학과", "작업치료학과"] },
      { name: "복지계열", majors: ["사회복지학과", "아동학과"] },
      { name: "교육계열", majors: ["보건교육 관련 학과", "교육학과"] }
    ]
  },
  {
    title: "교육의 이해",
    subjects: ["교육의 이해"],
    description: "교육의 목적과 학습자를 이해하는 과목은 교육, 상담, 교과교육 분야와 연결됩니다.",
    tracks: [
      { name: "교육계열", majors: ["교육학과", "초등교육과", "유아교육과", "특수교육과"] },
      { name: "상담계열", majors: ["상담심리학과", "청소년학과"] },
      { name: "교과교육계열", majors: ["국어교육과", "수학교육과", "영어교육과", "사회교육과", "과학교육과"] }
    ]
  },
  {
    title: "진로와 직업",
    subjects: ["진로와 직업", "진로 주제 프로젝트"],
    description: "진로 탐색 과목은 특정 학과보다 자신의 관심 분야를 좁히고, 과목 선택과 활동 흐름을 설계하는 데 도움이 됩니다.",
    tracks: [
      { name: "진로탐색계열", majors: ["자율전공학부", "자유전공학부", "융합학부"] },
      { name: "교육·상담계열", majors: ["교육학과", "상담심리학과", "청소년학과"] },
      { name: "경영·창업계열", majors: ["경영학과", "창업학과", "벤처경영 관련 학과"] }
    ]
  }
];

const state = {
  activeView: "home",
  activeGrade: "1",
  activeCurriculumPlan: "current2026",
  activeCurriculumScope: "semester",
  curriculumSorts: [],
  recommendationMode: "major",
  topicArea: "all",
  topicSubject: "all",
  topicLevel: "all",
  topicGrade: "all",
  topicQuery: "",
  courseDesignerPlan: "incoming2026",
  courseDesignerStatus: "considering",
  courseDesignerInterests: [],
  courseDesignerOption: "balanced",
  courseDesignerGenerated: false,
  courseDesignerNeedsReview: false,
  plannerMode: "fixed",
  plannerPlan: "incoming2026",
  plannerPlans: {},
  selfEvalPlan: "incoming2026",
  selfEvalSemester: "all",
  selfEvalSubject: "",
  selfEvalEntries: {},
  selfEvalMode: "subject",
  selfEvalStudentNumber: "",
  creativeEvalPlan: "incoming2026",
  creativeEvalGrade: "1",
  creativeEvalType: "autonomy",
  creativeEvalDomain: "korean",
  creativeEvalSeries: "representative",
  creativeEvalEntries: {},
  selectedTrack: "engineering",
  targetMajor: "",
  plan: createEmptyPlan(),
  creative: {
    autonomy: "",
    career: "",
    club: ""
  },
  inquiry: {
    motivation: "",
    question: "",
    result: "",
    followUp: ""
  }
};

function createEmptyPlan() {
  const plan = {};
  ["1", "2", "3"].forEach((grade) => {
    plan[grade] = {};
    ["1", "2"].forEach((semester) => {
      plan[grade][semester] = {
        regular: [],
        joint: [],
        additional: []
      };
    });
  });
  return plan;
}

function $(selector) {
  return document.querySelector(selector);
}

function $all(selector) {
  return [...document.querySelectorAll(selector)];
}

function getCourse(id) {
  return courses.find((course) => course.id === id);
}

function init() {
  loadState();
  loadCourseDesignerState();
  loadSelfEvaluationState();
  loadCreativeEvaluationState();
  loadSelfEvaluationIdentity();
  bindNavigation();
  bindControls();
  bindCourseDesignerControls();
  bindCurriculumSortHeaders();
  bindCurriculumHelp();
  bindSechukFlow();
  bindSubjectInfoPopup();
  renderCurriculumPlanOptions();
  renderCurriculum();
  renderCourseDesignerPlanOptions();
  renderCourseDesigner();
  renderRecommendationExplorer();
  renderTopicExplorerOptions();
  renderTopicExplorer();
  renderPlannerPlanOptions();
  renderSelfEvaluationPlanOptions();
  renderSelfEvaluationSemesterOptions();
  renderSelfEvaluationSubjectOptions();
  renderCreativeEvaluationPlanOptions();
  renderSelfEvaluationIdentity();
  renderCoursePool();
  renderPlanner();
  renderSelfEvaluation();
  renderCreativeEvaluation();
  renderSelfEvaluationMode();
  updateFormValues();
  setView(getInitialViewFromHash() || "home");
}

function bindNavigation() {
  $all("[data-view]").forEach((button) => {
    button.addEventListener("click", () => setView(button.dataset.view));
  });
}

function setView(viewId) {
  if (!$(`#${viewId}`)?.classList.contains("view")) viewId = "home";
  state.activeView = viewId;
  if (viewId === "planner") {
    state.plannerMode = "fixed";
    renderPlannerModeState();
  }
  if (viewId === "courseDesigner") renderCourseDesigner();
  $all(".view").forEach((view) => view.classList.toggle("active", view.id === viewId));
  $all(".nav-button").forEach((button) => button.classList.toggle("active", button.dataset.view === viewId));
  window.scrollTo({ top: 0, behavior: "smooth" });
}

function getInitialViewFromHash() {
  const hash = decodeURIComponent(window.location.hash || "").replace(/^#/, "");
  if (!hash) return "";
  if ($(`#${hash}`)?.classList.contains("view")) return hash;
  return "";
}

function bindControls() {
  $("#curriculumPlan").addEventListener("change", (event) => {
    state.activeCurriculumPlan = event.target.value;
    saveState(false);
    renderCurriculum();
  });
  $("#curriculumScope").addEventListener("change", (event) => {
    state.activeCurriculumScope = event.target.value;
    saveState(false);
    renderCurriculum();
  });
  $("#curriculumGrade").addEventListener("change", renderCurriculum);
  $("#curriculumSemester").addEventListener("change", renderCurriculum);
  $("#curriculumSearch").addEventListener("input", renderCurriculum);

  bindRecommendationControls();
  bindTopicExplorerControls();

  const plannerPlanSelect = $("#plannerPlanSelect");
  if (plannerPlanSelect) {
    plannerPlanSelect.addEventListener("change", (event) => switchPlannerPlan(event.target.value));
  }

  $("#plannerFixedModeButton")?.addEventListener("click", () => setPlannerMode("fixed"));
  $("#plannerChoiceModeButton")?.addEventListener("click", () => setPlannerMode("choice"));
  $("#plannerMineModeButton")?.addEventListener("click", () => setPlannerMode("mine"));
  $("#returnFixedButton")?.addEventListener("click", () => setPlannerMode("fixed"));

  $("#courseSearch")?.addEventListener("input", renderCoursePool);

  $all(".year-tab").forEach((button) => {
    button.addEventListener("click", () => {
      state.activeGrade = button.dataset.grade;
      $all(".year-tab").forEach((tab) => tab.classList.toggle("active", tab.dataset.grade === state.activeGrade));
      renderCoursePool();
      renderPlanner();
    });
  });

  $("#savePlanButton")?.addEventListener("click", () => saveState(true));
  $("#loadPlanButton")?.addEventListener("click", loadSavedPlannerSelection);
  $("#resetPlanButton")?.addEventListener("click", resetCurrentPlannerPlan);

  bindSelfEvaluationControls();
}

function bindSelfEvaluationControls() {
  $("#selfEvalSubjectModeButton")?.addEventListener("click", () => setSelfEvaluationMode("subject"));
  $("#selfEvalCreativeModeButton")?.addEventListener("click", () => setSelfEvaluationMode("creative"));

  $("#selfEvalStudentNumber")?.addEventListener("input", (event) => {
    const normalized = normalizeStudentNumber(event.target.value);
    event.target.value = normalized;
    state.selfEvalStudentNumber = normalized;
    saveSelfEvaluationIdentity();
  });

  $("#clearSelfEvalStudentNumberButton")?.addEventListener("click", clearSelfEvaluationStudentNumber);

  $("#selfEvalPlanSelect")?.addEventListener("change", (event) => {
    updateSelfEvaluationEntryFromForm();
    state.selfEvalPlan = event.target.value;
    state.selfEvalSemester = "all";
    state.selfEvalSubject = "";
    ensureSelfEvaluationSubject();
    saveSelfEvaluationState(false);
    renderSelfEvaluationSemesterOptions();
    renderSelfEvaluationSubjectOptions();
    renderSelfEvaluation();
  });

  $("#selfEvalSemesterSelect")?.addEventListener("change", (event) => {
    updateSelfEvaluationEntryFromForm();
    state.selfEvalSemester = event.target.value;
    ensureSelfEvaluationSubject();
    saveSelfEvaluationState(false);
    renderSelfEvaluationSubjectOptions();
    renderSelfEvaluation();
  });

  $("#selfEvalSubjectSelect")?.addEventListener("change", (event) => {
    updateSelfEvaluationEntryFromForm();
    state.selfEvalSubject = event.target.value;
    saveSelfEvaluationState(false);
    renderSelfEvaluation();
  });

  $("#saveSelfEvalButton")?.addEventListener("click", () => {
    updateSelfEvaluationEntryFromForm();
    saveSelfEvaluationState(true);
  });

  $("#loadSelfEvalButton")?.addEventListener("click", () => {
    loadSelfEvaluationState();
    renderSelfEvaluationPlanOptions();
    renderSelfEvaluationSemesterOptions();
    renderSelfEvaluationSubjectOptions();
    renderSelfEvaluation();
    showToast("저장된 자기평가서를 불러왔습니다.");
  });

  $("#resetSelfEvalButton")?.addEventListener("click", resetCurrentSelfEvaluationEntry);

  $("#exportSelfEvalButton")?.addEventListener("click", exportSelfEvaluationPdf);

  $("#selfEvalForm")?.addEventListener("input", (event) => {
    const target = event.target;
    if (!(target instanceof HTMLTextAreaElement)) return;
    resizeSelfEvaluationTextarea(target);
    updateSelfEvaluationEntryFromForm();
    saveSelfEvaluationState(false);
  });

  $all("[data-self-eval-competency]").forEach((checkbox) => {
    checkbox.addEventListener("change", () => {
      updateSelfEvaluationEntryFromForm();
      saveSelfEvaluationState(false);
      renderSelfEvaluationStatus();
    });
  });

  $("#selfEvalCompetencyNote")?.addEventListener("input", () => {
    updateSelfEvaluationEntryFromForm();
    saveSelfEvaluationState(false);
    renderSelfEvaluationStatus();
  });

  $("#creativeEvalPlanSelect")?.addEventListener("change", (event) => {
    updateCreativeEvaluationEntryFromForm();
    state.creativeEvalPlan = event.target.value;
    saveCreativeEvaluationState(false);
    renderCreativeEvaluation();
  });

  $("#creativeEvalGradeSelect")?.addEventListener("change", (event) => {
    updateCreativeEvaluationEntryFromForm();
    state.creativeEvalGrade = event.target.value;
    saveCreativeEvaluationState(false);
    renderCreativeEvaluation();
  });

  $("#creativeEvalDomainSelect")?.addEventListener("change", (event) => {
    state.creativeEvalDomain = event.target.value;
    state.creativeEvalSeries = "representative";
    renderCreativeEvaluationSeriesOptions();
    saveCreativeEvaluationState(false);
  });

  $("#creativeEvalSeriesSelect")?.addEventListener("change", (event) => {
    state.creativeEvalSeries = event.target.value;
    saveCreativeEvaluationState(false);
  });

  $("#creativeEvalFlowButton")?.addEventListener("click", openCreativeEvalFlowPopup);

  $all("[data-creative-eval-type]").forEach((button) => {
    button.addEventListener("click", () => {
      updateCreativeEvaluationEntryFromForm();
      state.creativeEvalType = button.dataset.creativeEvalType || "autonomy";
      state.creativeEvalSeries = "";
      renderCreativeEvaluationSeriesOptions();
      saveCreativeEvaluationState(false);
      renderCreativeEvaluation();
    });
  });

  $("#saveCreativeEvalButton")?.addEventListener("click", () => {
    updateCreativeEvaluationEntryFromForm();
    saveCreativeEvaluationState(true);
  });

  $("#loadCreativeEvalButton")?.addEventListener("click", () => {
    loadCreativeEvaluationState();
    renderCreativeEvaluationPlanOptions();
    renderCreativeEvaluation();
    showToast("저장된 창의적 체험활동을 불러왔습니다.");
  });

  $("#resetCreativeEvalButton")?.addEventListener("click", resetCurrentCreativeEvaluationEntry);

  $("#exportCreativeEvalButton")?.addEventListener("click", exportCreativeEvaluationPdf);

  $("#creativeEvalForm")?.addEventListener("input", (event) => {
    const target = event.target;
    if (target instanceof HTMLTextAreaElement) resizeSelfEvaluationTextarea(target);
    updateCreativeEvaluationEntryFromForm();
    saveCreativeEvaluationState(false);
    renderCreativeEvaluationStatus();
  });

  $all("[data-creative-eval-competency]").forEach((checkbox) => {
    checkbox.addEventListener("change", () => {
      updateCreativeEvaluationEntryFromForm();
      saveCreativeEvaluationState(false);
      renderCreativeEvaluationStatus();
    });
  });

  $("#creativeEvalCompetencyNote")?.addEventListener("input", () => {
    updateCreativeEvaluationEntryFromForm();
    saveCreativeEvaluationState(false);
    renderCreativeEvaluationStatus();
  });
}

function bindSubjectInfoPopup() {
  document.addEventListener("click", (event) => {
    const clicked = event.target instanceof Element ? event.target : null;
    const infoButton = clicked?.closest("[data-subject-info]");
    if (infoButton) {
      event.preventDefault();
      event.stopPropagation();
      openSubjectInfoPopup({
        name: infoButton.dataset.subjectName || "",
        area: infoButton.dataset.subjectArea || "",
        category: infoButton.dataset.subjectCategory || ""
      });
      return;
    }

    const selfHelpButton = clicked?.closest("[data-self-help]");
    if (selfHelpButton) {
      event.preventDefault();
      event.stopPropagation();
      openSelfEvalHelpPopup(selfHelpButton.dataset.selfHelp || "");
      return;
    }

    const creativeHelpButton = clicked?.closest("[data-creative-help]");
    if (creativeHelpButton) {
      event.preventDefault();
      event.stopPropagation();
      openCreativeEvalHelpPopup(creativeHelpButton.dataset.creativeHelp || "");
      return;
    }

    if (clicked?.closest("[data-close-subject-info]")) {
      closeSubjectInfoPopup();
    }
  });

  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape") closeSubjectInfoPopup();
  });
}

function bindRecommendationControls() {
  const majorSelect = $("#majorRecommendationSelect");
  const majorSearch = $("#majorRecommendationSearch");
  const subjectSelect = $("#subjectRecommendationSelect");
  const planFilter = $("#recommendationPlanFilter");
  const universitySearch = $("#universityRecommendationSearch");
  const universityMajorSearch = $("#universityMajorSearch");

  if (majorSelect && majorSelect.dataset.bound !== "true") {
    majorSelect.addEventListener("change", (event) => {
      if (majorSearch) majorSearch.value = event.target.value;
      renderRecommendationExplorer();
    });
    majorSelect.dataset.bound = "true";
  }

  if (majorSearch && majorSearch.dataset.bound !== "true") {
    majorSearch.addEventListener("input", () => {
      if (majorSelect) majorSelect.value = "";
      renderRecommendationExplorer();
    });
    majorSearch.dataset.bound = "true";
  }

  if (subjectSelect && subjectSelect.dataset.bound !== "true") {
    subjectSelect.addEventListener("change", renderRecommendationExplorer);
    subjectSelect.dataset.bound = "true";
  }

  if (universitySearch && universitySearch.dataset.bound !== "true") {
    universitySearch.addEventListener("input", renderRecommendationExplorer);
    universitySearch.dataset.bound = "true";
  }

  if (universityMajorSearch && universityMajorSearch.dataset.bound !== "true") {
    universityMajorSearch.addEventListener("input", renderRecommendationExplorer);
    universityMajorSearch.dataset.bound = "true";
  }

  if (planFilter && planFilter.dataset.bound !== "true") {
    planFilter.addEventListener("change", renderRecommendationExplorer);
    planFilter.dataset.bound = "true";
  }

  $all("[data-recommendation-mode]").forEach((button) => {
    if (button.dataset.bound === "true") return;
    button.addEventListener("click", () => {
      state.recommendationMode = button.dataset.recommendationMode || "major";
      renderRecommendationExplorer();
      scrollRecommendationSectionIntoView();
    });
    button.dataset.bound = "true";
  });
}

function bindTopicExplorerControls() {
  const areaFilter = $("#topicAreaFilter");
  const subjectFilter = $("#topicSubjectFilter");
  const levelFilter = $("#topicLevelFilter");
  const gradeFilter = $("#topicGradeFilter");
  const searchInput = $("#topicSearch");
  const resetButton = $("#topicResetButton");
  if (!areaFilter || !subjectFilter || !levelFilter || !searchInput) return;

  areaFilter.addEventListener("change", (event) => {
    state.topicArea = event.target.value;
    state.topicSubject = "all";
    renderTopicSubjectOptions();
    renderTopicExplorer();
  });

  subjectFilter.addEventListener("change", (event) => {
    state.topicSubject = event.target.value;
    renderTopicExplorer();
  });

  levelFilter.addEventListener("change", (event) => {
    state.topicLevel = event.target.value;
    renderTopicExplorer();
  });

  gradeFilter?.addEventListener("change", (event) => {
    state.topicGrade = event.target.value;
    renderTopicExplorer();
  });

  searchInput.addEventListener("input", (event) => {
    state.topicQuery = event.target.value;
    renderTopicExplorer();
  });

  resetButton?.addEventListener("click", () => {
    state.topicArea = "all";
    state.topicSubject = "all";
    state.topicLevel = "all";
    state.topicGrade = "all";
    state.topicQuery = "";
    areaFilter.value = "all";
    levelFilter.value = "all";
    if (gradeFilter) gradeFilter.value = "all";
    searchInput.value = "";
    renderTopicSubjectOptions();
    renderTopicExplorer();
  });
}

function renderTopicExplorerOptions() {
  const areaFilter = $("#topicAreaFilter");
  const levelFilter = $("#topicLevelFilter");
  const gradeFilter = $("#topicGradeFilter");
  const searchInput = $("#topicSearch");
  if (!areaFilter) return;

  const areas = getTopicAreas();
  areaFilter.innerHTML = `
    <option value="all">전체 교과군</option>
    ${areas.map((area) => `<option value="${escapeAttribute(area)}">${escapeHtml(area)}</option>`).join("")}
  `;
  areaFilter.value = state.topicArea || "all";
  if (levelFilter) levelFilter.value = state.topicLevel || "all";
  if (gradeFilter) gradeFilter.value = state.topicGrade || "all";
  if (searchInput) searchInput.value = state.topicQuery || "";
  renderTopicSubjectOptions();
}

function renderTopicSubjectOptions() {
  const subjectFilter = $("#topicSubjectFilter");
  if (!subjectFilter) return;
  const subjects = getTopicSubjects(state.topicArea);
  if (!subjects.includes(state.topicSubject)) state.topicSubject = "all";
  subjectFilter.innerHTML = `
    <option value="all">전체 과목</option>
    ${subjects.map((subject) => `<option value="${escapeAttribute(subject)}">${escapeHtml(subject)}</option>`).join("")}
  `;
  subjectFilter.value = state.topicSubject || "all";
}

function renderTopicExplorer() {
  const resultList = $("#topicResultList");
  const resultCount = $("#topicResultCount");
  if (!resultList) return;

  const areaFilter = $("#topicAreaFilter");
  const subjectFilter = $("#topicSubjectFilter");
  const levelFilter = $("#topicLevelFilter");
  const gradeFilter = $("#topicGradeFilter");
  const searchInput = $("#topicSearch");
  const area = areaFilter?.value || state.topicArea || "all";
  const subject = subjectFilter?.value || state.topicSubject || "all";
  const level = levelFilter?.value || state.topicLevel || "all";
  const grade = gradeFilter?.value || state.topicGrade || "all";
  const query = searchInput?.value || state.topicQuery || "";
  state.topicArea = area;
  state.topicSubject = subject;
  state.topicLevel = level;
  state.topicGrade = grade;
  state.topicQuery = query;

  const topics = getFilteredTopicRecords({ area, subject, level, grade, query });
  const visibleLimit = area === "all" && subject === "all" && level === "all" && grade === "all" && !query.trim() ? 18 : 36;
  const visibleTopics = topics.slice(0, visibleLimit);
  const emptyTopicMessage = level !== "all"
    ? `${level} 난이도에 맞는 예시가 아직 없습니다. 과목을 넓히거나 전체 난이도로 다시 확인해 보세요.`
    : "검색 조건에 맞는 탐구 주제를 찾지 못했습니다. 과목을 넓히거나 검색어를 짧게 입력해 보세요.";
  if (resultCount) resultCount.textContent = topics.length > visibleTopics.length ? `${topics.length}개 중 ${visibleTopics.length}개` : `${topics.length}개`;
  resultList.innerHTML = topics.length
    ? `${visibleTopics.map((topic) => renderTopicCard(topic)).join("")}${topics.length > visibleTopics.length ? `<div class="empty-note topic-more-note">교과군·과목·학년을 선택하면 나머지 주제를 더 정확하게 확인할 수 있습니다.</div>` : ""}`
    : `<div class="empty-note topic-empty">${escapeHtml(emptyTopicMessage)}</div>`;
}

function getTopicRecords() {
  return Array.isArray(topicData.topics) ? topicData.topics : [];
}

function getTopicAreas() {
  const preferredOrder = ["국어", "영어", "수학", "사회", "과학", "정보", "기술·가정", "예술", "체육", "교양"];
  const areas = [...new Set(getTopicRecords().map((topic) => topic.area).filter(Boolean))];
  return areas.sort((a, b) => {
    const aIndex = preferredOrder.indexOf(a);
    const bIndex = preferredOrder.indexOf(b);
    if (aIndex !== -1 || bIndex !== -1) {
      return (aIndex === -1 ? 99 : aIndex) - (bIndex === -1 ? 99 : bIndex);
    }
    return a.localeCompare(b, "ko");
  });
}

function getTopicSubjects(area = "all") {
  const records = getTopicRecords().filter((topic) => area === "all" || topic.area === area);
  return [...new Set(records.map((topic) => topic.subject).filter(Boolean))]
    .sort((a, b) => a.localeCompare(b, "ko"));
}

function getFilteredTopicRecords({ area, subject, level, grade, query }) {
  const normalizedQuery = normalizeText(query);
  return getTopicRecords().filter((topic) => {
    if (area !== "all" && topic.area !== area) return false;
    if (subject !== "all" && topic.subject !== subject) return false;
    if (level !== "all" && topic.level !== level) return false;
    if (grade !== "all" && !getTopicGrades(topic).includes(Number(grade))) return false;
    if (!normalizedQuery) return true;
    const haystack = normalizeText([
      topic.area,
      topic.subject,
      topic.level,
      getTopicGradeLabel(topic),
      topic.title,
      topic.question,
      topic.method,
      topic.output,
      ...(topic.keywords || []),
      ...(topic.majors || [])
    ].join(" "));
    return haystack.includes(normalizedQuery);
  });
}

function getTopicGrades(topic) {
  if (Array.isArray(topic.grades) && topic.grades.length) return topic.grades.map(Number).filter(Boolean);
  if (topic.grade) return [Number(topic.grade)].filter(Boolean);
  return topic.level === "심화" ? [2, 3] : [1, 2];
}

function getTopicGradeLabel(topic) {
  return `${getTopicGrades(topic).join("·")}학년`;
}

function getTopicDevelopmentText(topic) {
  if (topic.nextStep) return topic.nextStep;
  return topic.level === "심화"
    ? "자료의 한계와 다른 해석 가능성을 점검하고, 새로운 자료로 재검증하거나 대안을 제안해 봅니다."
    : "비교할 자료와 기준을 늘리고, 변수를 구분해 다음 학년의 분석 탐구로 발전시켜 봅니다.";
}

function renderTopicCard(topic) {
  const keywords = Array.isArray(topic.keywords) ? topic.keywords : [];
  const majors = Array.isArray(topic.majors) ? topic.majors : [];
  return `
    <article class="topic-card">
      <div class="topic-card-head">
        <div>
          <span class="topic-subject-badge">${escapeHtml(topic.area)} · ${escapeHtml(topic.subject)}</span>
          <h4>${escapeHtml(topic.title)}</h4>
        </div>
        <div class="topic-badge-stack">
          <span class="topic-grade-badge">${escapeHtml(getTopicGradeLabel(topic))}</span>
          <span class="topic-level-badge ${topic.level === "심화" ? "advanced" : "basic"}">${escapeHtml(topic.level)}</span>
        </div>
      </div>
      <div class="topic-card-section">
        <b>탐구 질문</b>
        <p>${escapeHtml(topic.question)}</p>
      </div>
      <div class="topic-card-section">
        <b>진행 방법</b>
        <p>${escapeHtml(topic.method)}</p>
      </div>
      <div class="topic-card-footer">
        <div>
          <b>결과물</b>
          <p>${escapeHtml(topic.output)}</p>
        </div>
        <div>
          <b>연결 학과 예시</b>
          <p>${majors.map((major) => escapeHtml(major)).join(" · ")}</p>
        </div>
      </div>
      <div class="topic-card-section topic-development">
        <b>다음 학년 발전 방향</b>
        <p>${escapeHtml(getTopicDevelopmentText(topic))}</p>
      </div>
      <div class="topic-keyword-row">
        ${keywords.map((keyword) => `<span>${escapeHtml(keyword)}</span>`).join("")}
      </div>
    </article>
  `;
}

function bindCurriculumSortHeaders() {
  $all("[data-curriculum-sort]").forEach((button) => {
    button.addEventListener("click", () => cycleCurriculumSort(button.dataset.curriculumSort));
  });
}

function cycleCurriculumSort(key) {
  const existing = state.curriculumSorts.find((sort) => sort.key === key);
  if (!existing) {
    if (state.curriculumSorts.length >= 2) state.curriculumSorts.shift();
    state.curriculumSorts.push({ key, direction: "asc" });
  } else if (existing.direction === "asc") {
    existing.direction = "desc";
  } else {
    state.curriculumSorts = state.curriculumSorts.filter((sort) => sort.key !== key);
  }
  renderCurriculum();
}

function updateCurriculumSortHeaders() {
  $all("[data-curriculum-sort]").forEach((button) => {
    const sortIndex = state.curriculumSorts.findIndex((sort) => sort.key === button.dataset.curriculumSort);
    const indicator = button.querySelector(".sort-indicator");
    button.classList.toggle("active", sortIndex >= 0);
    if (!indicator) return;
    if (sortIndex < 0) {
      indicator.textContent = "";
      button.removeAttribute("aria-label");
      return;
    }

    const sort = state.curriculumSorts[sortIndex];
    const directionLabel = sort.direction === "asc" ? "오름차순" : "내림차순";
    const label = button.childNodes[0]?.textContent.trim() || button.textContent.trim();
    indicator.textContent = `${sortIndex + 1}${sort.direction === "asc" ? "↑" : "↓"}`;
    button.setAttribute("aria-label", `${label} ${sortIndex + 1}순위 ${directionLabel}`);
  });
}

function bindCurriculumHelp() {
  const button = $("#choiceHelpButton");
  const panel = $("#choiceHelpPanel");
  if (!button || !panel) return;

  button.addEventListener("click", () => {
    const willOpen = panel.hidden;
    panel.hidden = !willOpen;
    button.setAttribute("aria-expanded", String(willOpen));
  });
}

function bindSechukFlow() {
  const buttons = $all("[data-sechuk-step]");
  if (!buttons.length) return;

  buttons.forEach((button) => {
    button.addEventListener("click", () => renderSechukExample(button.dataset.sechukStep));
  });
  renderSechukExample(buttons[0].dataset.sechukStep);
}

function renderSechukExample(stepKey) {
  const data = sechukExamples[stepKey];
  const title = $("#sechukExampleTitle");
  const text = $("#sechukExampleText");
  if (!data || !title || !text) return;

  $all("[data-sechuk-step]").forEach((button) => {
    button.classList.toggle("active", button.dataset.sechukStep === stepKey);
  });
  title.textContent = data.title;
  text.textContent = data.text;
}

function renderCompetencies() {
  $("#competencyCards").innerHTML = competencies
    .map(
      (card) => `
        <article class="competency-card">
          <span class="label">${card.label}</span>
          <h3>${card.title}</h3>
          <p>${card.body}</p>
        </article>
      `
    )
    .join("");
}

function renderCurriculumPlanOptions() {
  const select = $("#curriculumPlan");
  if (!select) return;
  const options = curriculumPlanOrder
    .filter((key) => curriculumData.plans[key])
    .map((key) => `<option value="${key}">${getCurriculumPlanLabel(key)}</option>`)
    .join("");
  select.innerHTML = options;
  if (!curriculumData.plans[state.activeCurriculumPlan]) {
    state.activeCurriculumPlan = curriculumPlanOrder.find((key) => curriculumData.plans[key]) || "";
  }
  select.value = state.activeCurriculumPlan;
}

function renderPlannerPlanOptions() {
  const select = $("#plannerPlanSelect");
  if (!select) return;
  const options = plannerPlanOrder
    .filter((key) => curriculumData.plans[key])
    .map((key) => `<option value="${key}">${getCurriculumPlanLabel(key)}</option>`)
    .join("");
  select.innerHTML = options;
  if (!curriculumData.plans[state.plannerPlan]) {
    state.plannerPlan = plannerPlanOrder.find((key) => curriculumData.plans[key]) || state.plannerPlan;
  }
  select.value = state.plannerPlan;
}

function getCurriculumPlanLabel(key) {
  return curriculumPlanLabels[key] || curriculumData.plans[key]?.label || key;
}

function bindCourseDesignerControls() {
  $("#courseDesignerPlanSelect")?.addEventListener("change", (event) => {
    state.courseDesignerPlan = event.target.value;
    state.courseDesignerGenerated = false;
    state.courseDesignerNeedsReview = false;
    saveCourseDesignerState();
    renderCourseDesigner();
  });

  $("#courseDesignerStatusSwitch")?.addEventListener("click", (event) => {
    const button = event.target instanceof Element ? event.target.closest("[data-course-designer-status]") : null;
    if (!button) return;
    state.courseDesignerStatus = button.dataset.courseDesignerStatus;
    state.courseDesignerOption = getCourseDesignerDefaultOption(state.courseDesignerStatus);
    state.courseDesignerNeedsReview = false;
    saveCourseDesignerState();
    renderCourseDesigner();
  });

  $("#courseDesignerInterestGroups")?.addEventListener("click", (event) => {
    const button = event.target instanceof Element ? event.target.closest("[data-course-designer-interest]") : null;
    if (!button) return;
    toggleCourseDesignerInterest(button.dataset.courseDesignerInterest);
  });

  $("#courseDesignerOptionTabs")?.addEventListener("click", (event) => {
    const button = event.target instanceof Element ? event.target.closest("[data-course-designer-option]") : null;
    if (!button) return;
    state.courseDesignerOption = button.dataset.courseDesignerOption;
    saveCourseDesignerState();
    renderCourseDesignerResults();
  });

  $("#courseDesignerResults")?.addEventListener("click", (event) => {
    const button = event.target instanceof Element ? event.target.closest("[data-course-designer-add-course]") : null;
    if (!button) return;
    addCourseDesignerCourseToPlanner(button);
  });

  $("#courseDesignerOpenPlannerButton")?.addEventListener("click", openCourseDesignerPlanner);

  $("#courseDesignerGenerateButton")?.addEventListener("click", generateCourseDesignerOptions);
  $("#courseDesignerResetButton")?.addEventListener("click", resetCourseDesigner);
}

function renderCourseDesignerPlanOptions() {
  const select = $("#courseDesignerPlanSelect");
  if (!select) return;
  select.innerHTML = plannerPlanOrder
    .filter((key) => curriculumData.plans?.[key])
    .map((key) => `<option value="${key}">${escapeHtml(getCurriculumPlanLabel(key))}</option>`)
    .join("");
  if (!curriculumData.plans?.[state.courseDesignerPlan]) {
    state.courseDesignerPlan = plannerPlanOrder.find((key) => curriculumData.plans?.[key]) || "incoming2026";
  }
  select.value = state.courseDesignerPlan;
}

function renderCourseDesigner() {
  const root = $("#courseDesigner");
  if (!root) return;
  renderCourseDesignerPlanOptions();
  renderCourseDesignerStatus();
  renderCourseDesignerInterestGroups();
  renderCourseDesignerVersionNotice();

  const selected = getSelectedCourseDesignerInterests();
  const count = $("#courseDesignerInterestCount");
  const summary = $("#courseDesignerSelectionSummary");
  const results = $("#courseDesignerResults");
  if (count) count.textContent = `${selected.length} / 3`;
  if (summary) {
    summary.textContent = selected.length
      ? `내가 고른 분야: ${selected.map((interest) => interest.label).join(" · ")}`
      : "관심 있는 분야를 한 개 이상 골라 보세요.";
  }
  if (results) results.hidden = !state.courseDesignerGenerated || !selected.length || state.courseDesignerNeedsReview;
  if (!results?.hidden) renderCourseDesignerResults();
}

function renderCourseDesignerStatus() {
  $all("[data-course-designer-status]").forEach((button) => {
    const active = button.dataset.courseDesignerStatus === state.courseDesignerStatus;
    button.classList.toggle("active", active);
    button.setAttribute("aria-pressed", String(active));
  });
}

function renderCourseDesignerInterestGroups() {
  const target = $("#courseDesignerInterestGroups");
  if (!target) return;
  const selected = new Set(state.courseDesignerInterests);
  const groups = new Map();
  (courseDesignerData.interests || []).forEach((interest) => {
    if (!groups.has(interest.group)) groups.set(interest.group, []);
    groups.get(interest.group).push(interest);
  });
  target.innerHTML = [...groups.entries()].map(([group, interests]) => `
    <section class="course-designer-interest-group">
      <h4>${escapeHtml(group)}</h4>
      <div>
        ${interests.map((interest) => {
          const active = selected.has(interest.id);
          return `
            <button class="course-designer-interest-button${active ? " active" : ""}" type="button"
              data-course-designer-interest="${escapeAttribute(interest.id)}" aria-pressed="${active}">
              <b>${escapeHtml(interest.label)}</b>
              <small>${escapeHtml(interest.summary)}</small>
            </button>
          `;
        }).join("")}
      </div>
    </section>
  `).join("");
}

function renderCourseDesignerVersionNotice() {
  const notice = $("#courseDesignerVersionNotice");
  if (!notice) return;
  notice.hidden = !state.courseDesignerNeedsReview;
  if (notice.hidden) {
    notice.innerHTML = "";
    return;
  }
  notice.innerHTML = `
    <b>우리학교 교육과정이 바뀌었습니다.</b>
    <span>고른 관심 분야는 그대로 두고 이전 결과만 숨겼습니다. 바뀐 교육과정으로 과목 선택안을 다시 확인해 주세요.</span>
  `;
}

function toggleCourseDesignerInterest(interestId) {
  const validIds = new Set((courseDesignerData.interests || []).map((interest) => interest.id));
  if (!validIds.has(interestId)) return;
  const selected = new Set(state.courseDesignerInterests);
  if (selected.has(interestId)) {
    selected.delete(interestId);
  } else {
    if (selected.size >= 3) {
      showToast("관심 분야는 한 화면에서 최대 3개까지 비교할 수 있습니다.");
      return;
    }
    selected.add(interestId);
  }
  state.courseDesignerInterests = [...selected];
  state.courseDesignerGenerated = false;
  state.courseDesignerNeedsReview = false;
  saveCourseDesignerState();
  renderCourseDesigner();
}

function generateCourseDesignerOptions() {
  if (!getSelectedCourseDesignerInterests().length) {
    showToast("관심 있는 분야를 한 개 이상 골라 주세요.");
    return;
  }
  state.courseDesignerOption = getCourseDesignerDefaultOption(state.courseDesignerStatus);
  state.courseDesignerGenerated = true;
  state.courseDesignerNeedsReview = false;
  saveCourseDesignerState();
  renderCourseDesigner();
  window.requestAnimationFrame(() => {
    $("#courseDesignerResults")?.scrollIntoView({ behavior: "smooth", block: "start" });
  });
}

function resetCourseDesigner() {
  state.courseDesignerStatus = "considering";
  state.courseDesignerInterests = [];
  state.courseDesignerOption = "balanced";
  state.courseDesignerGenerated = false;
  state.courseDesignerNeedsReview = false;
  saveCourseDesignerState();
  renderCourseDesigner();
  showToast("고른 관심 분야와 과목 선택안을 모두 지웠습니다.");
}

function getCourseDesignerDefaultOption(status) {
  if (status === "clear") return "deep";
  if (status === "exploring") return "explore";
  return "balanced";
}

function getSelectedCourseDesignerInterests() {
  const selected = new Set(state.courseDesignerInterests);
  return (courseDesignerData.interests || []).filter((interest) => selected.has(interest.id));
}

function getCourseDesignerOptionProfile(optionId) {
  return (courseDesignerData.optionProfiles || []).find((profile) => profile.id === optionId)
    || (courseDesignerData.optionProfiles || [])[0]
    || { id: "balanced", code: "균형", label: "복수 관심 균형형", description: "", caution: "" };
}

function saveCourseDesignerState() {
  const payload = {
    plan: state.courseDesignerPlan,
    status: state.courseDesignerStatus,
    interests: state.courseDesignerInterests,
    option: state.courseDesignerOption,
    generated: state.courseDesignerGenerated,
    curriculumVersion: curriculumData.updated || ""
  };
  localStorage.setItem(COURSE_DESIGNER_STORAGE_KEY, JSON.stringify(payload));
}

function loadCourseDesignerState() {
  try {
    const saved = JSON.parse(localStorage.getItem(COURSE_DESIGNER_STORAGE_KEY));
    if (!saved) return;
    const planKeys = new Set(plannerPlanOrder.filter((key) => curriculumData.plans?.[key]));
    const statusKeys = new Set(["clear", "considering", "exploring"]);
    const interestIds = new Set((courseDesignerData.interests || []).map((interest) => interest.id));
    const optionIds = new Set((courseDesignerData.optionProfiles || []).map((profile) => profile.id));
    if (planKeys.has(saved.plan)) state.courseDesignerPlan = saved.plan;
    if (statusKeys.has(saved.status)) state.courseDesignerStatus = saved.status;
    state.courseDesignerInterests = Array.isArray(saved.interests)
      ? saved.interests.filter((id) => interestIds.has(id)).slice(0, 3)
      : [];
    if (optionIds.has(saved.option)) state.courseDesignerOption = saved.option;
    state.courseDesignerGenerated = Boolean(saved.generated);
    state.courseDesignerNeedsReview = Boolean(
      saved.curriculumVersion
      && curriculumData.updated
      && saved.curriculumVersion !== curriculumData.updated
    );
    if (state.courseDesignerNeedsReview) state.courseDesignerGenerated = false;
  } catch (error) {
    localStorage.removeItem(COURSE_DESIGNER_STORAGE_KEY);
  }
}

function buildCourseDesignerSubjectCandidates() {
  const candidates = new Map();
  const recordMap = new Map((recommendationData.records || []).map((record) => [record.id, record]));
  const selectedInterests = getSelectedCourseDesignerInterests();
  const officialEvidenceMap = buildCourseDesignerOfficialEvidenceMap(selectedInterests, recordMap);
  const ensureCandidate = (subject) => {
    const key = normalizeText(subject);
    if (!key) return null;
    if (!candidates.has(key)) {
      candidates.set(key, {
        subject,
        sources: new Set(),
        interests: new Set(),
        recordMajors: new Set(),
        levelsByInterest: new Map(),
        officialEvidence: []
      });
    }
    return candidates.get(key);
  };

  selectedInterests.forEach((interest) => {
    (interest.recordIds || []).forEach((recordId) => {
      const record = recordMap.get(recordId);
      if (!record) return;
      [
        ["core", getRecordCoreSubjects(record)],
        ["recommended", getRecordRecommendedSubjects(record)],
        ["suggested", getRecordSuggestedSubjects(record)]
      ].forEach(([source, subjects]) => {
        subjects.forEach((subject) => {
          const candidate = ensureCandidate(subject);
          if (!candidate) return;
          candidate.sources.add(source);
          candidate.interests.add(interest.label);
          candidate.recordMajors.add(record.major);
          if (!candidate.levelsByInterest.has(interest.label)) {
            candidate.levelsByInterest.set(interest.label, new Set());
          }
          candidate.levelsByInterest.get(interest.label).add(source);
        });
      });
    });
  });

  officialEvidenceMap.forEach((evidenceList) => {
    evidenceList.forEach((evidence) => {
      const candidate = candidates.get(normalizeText(evidence.subject));
      if (!candidate) return;
      const matchesGuideLevel = evidence.level === "core"
        ? candidate.sources.has("core")
        : candidate.sources.has("recommended");
      if (!matchesGuideLevel) return;
      addCourseDesignerOfficialEvidence(candidate.officialEvidence, evidence);
    });
  });

  return [...candidates.values()];
}

function buildCourseDesignerOfficialEvidenceMap(selectedInterests, recordMap) {
  const evidenceMap = new Map();
  const universityRecords = getUniversityRecommendationRecords();
  selectedInterests.forEach((interest) => {
    (interest.recordIds || []).forEach((recordId) => {
      const guideRecord = recordMap.get(recordId);
      if (!guideRecord) return;
      universityRecords
        .filter((record) => courseDesignerGuideMatchesUniversityRecord(guideRecord, record))
        .forEach((record) => {
          [
            ["core", filterRecommendationDisplaySubjects(record.coreSubjects || [])],
            ["recommended", filterRecommendationDisplaySubjects(record.recommendedSubjects || [])]
          ].forEach(([level, subjects]) => {
            subjects.forEach((subject) => {
              const key = normalizeText(subject);
              if (!key) return;
              if (!evidenceMap.has(key)) evidenceMap.set(key, []);
              addCourseDesignerOfficialEvidence(evidenceMap.get(key), {
                subject,
                level,
                university: record.university || "대학명 확인",
                department: record.department || "모집단위 확인",
                interest: interest.label,
                major: guideRecord.major
              });
            });
          });
        });
    });
  });
  return evidenceMap;
}

function courseDesignerGuideMatchesUniversityRecord(guideRecord, universityRecord) {
  const target = normalizeMajorSearchTerm(universityRecord.department || "");
  if (!target) return false;
  return (guideRecord.departments || [])
    .map((department) => normalizeMajorSearchTerm(department))
    .filter(Boolean)
    .some((term) => target === term || target.endsWith(term));
}

function addCourseDesignerOfficialEvidence(target, evidence) {
  const key = [evidence.university, evidence.department, evidence.level, normalizeText(evidence.subject)].join("|");
  if (!target.some((item) => item.key === key)) target.push({ ...evidence, key });
}

function buildCourseDesignerContext() {
  const plan = curriculumData.plans?.[state.courseDesignerPlan] || { courses: [] };
  const subjectCandidates = buildCourseDesignerSubjectCandidates();
  const choiceGroups = buildCurriculumChoiceGroups(plan.courses || [], curriculumSemesterOrder);
  const groupMap = buildCourseChoiceGroupMap(choiceGroups);
  const availableMap = new Map();
  const fixedMap = new Map();
  const unmatched = [];

  subjectCandidates.forEach((subjectCandidate) => {
    const matches = getCourseDesignerCourseMatches(subjectCandidate.subject, state.courseDesignerPlan);
    if (!matches.length) {
      unmatched.push(subjectCandidate);
      return;
    }
    matches.forEach((course) => {
      const targetMap = isPlannerChoiceCourse(course) ? availableMap : fixedMap;
      mergeCourseDesignerCourseCandidate(targetMap, course, subjectCandidate, groupMap.get(course.id) || []);
    });
  });

  const availableCourses = [...availableMap.values()];
  const fixedCourses = [...fixedMap.values()];
  const relevantAreas = new Set([...availableCourses, ...fixedCourses].map((item) => item.course.area).filter(Boolean));
  return {
    plan,
    subjectCandidates,
    availableCourses,
    fixedCourses,
    unmatched,
    relevantAreas
  };
}

function mergeCourseDesignerCourseCandidate(targetMap, course, subjectCandidate, groupInfos) {
  if (!targetMap.has(course.id)) {
    targetMap.set(course.id, {
      course,
      sources: new Set(),
      interests: new Set(),
      subjects: new Set(),
      recordMajors: new Set(),
      levelsByInterest: new Map(),
      officialEvidence: [],
      groupInfos
    });
  }
  const target = targetMap.get(course.id);
  subjectCandidate.sources.forEach((source) => target.sources.add(source));
  subjectCandidate.interests.forEach((interest) => target.interests.add(interest));
  subjectCandidate.recordMajors.forEach((major) => target.recordMajors.add(major));
  subjectCandidate.levelsByInterest.forEach((levels, interest) => {
    if (!target.levelsByInterest.has(interest)) target.levelsByInterest.set(interest, new Set());
    levels.forEach((level) => target.levelsByInterest.get(interest).add(level));
  });
  (subjectCandidate.officialEvidence || []).forEach((evidence) => addCourseDesignerOfficialEvidence(target.officialEvidence, evidence));
  target.subjects.add(subjectCandidate.subject);
}

function getCourseDesignerCourseMatches(subject, planKey) {
  const plan = curriculumData.plans?.[planKey];
  if (!plan) return [];
  const names = new Set(getCourseDesignerCompareNames(subject, planKey).map((name) => normalizeText(name)));
  const seen = new Set();
  return (plan.courses || []).filter((course) => {
    if (!names.has(normalizeText(course.name)) || seen.has(course.id)) return false;
    seen.add(course.id);
    return true;
  });
}

function getCourseDesignerCompareNames(subject, planKey) {
  const planMeta = recommendationCurriculumPlans.find((plan) => plan.key === planKey);
  const names = new Set(getSubjectCompareNames(subject, planKey));
  const aliases2022 = {
    "정치와 법": ["정치", "법과 사회"],
    "생활과 윤리": ["현대사회와 윤리"],
    "한국지리": ["한국지리 탐구"],
    "세계지리": ["세계시민과 지리"]
  };
  if (planMeta?.standard === "2022") {
    (aliases2022[subject] || []).forEach((name) => names.add(name));
  }
  return [...names];
}

function getCourseDesignerOptionSelections(context, optionId) {
  const selectedByGrade = { "1": [], "2": [], "3": [] };
  const selectedIds = new Set();
  const groupUsage = new Map();
  const areaUsage = new Map();
  const interestUsage = new Map();
  const limit = optionId === "explore" ? 4 : 3;
  const selectedInterestLabels = getSelectedCourseDesignerInterests().map((interest) => interest.label);

  ["1", "2", "3"].forEach((grade) => {
    const pool = context.availableCourses.filter((candidate) => courseDesignerCourseHasGrade(candidate.course, grade));
    const getViable = (interestLabel = "") => pool
      .filter((candidate) => !selectedIds.has(candidate.course.id))
      .filter((candidate) => !interestLabel || candidate.interests.has(interestLabel))
      .filter((candidate) => canUseCourseDesignerChoiceGroups(candidate, groupUsage, grade))
      .map((candidate) => {
        const interestMetrics = getCourseDesignerInterestMetrics(candidate, interestLabel);
        return {
          candidate,
          score: getCourseDesignerCandidateScore(candidate, optionId, areaUsage, interestUsage),
          ...interestMetrics
        };
      })
      .sort((a, b) => {
        if (interestLabel) {
          return b.interestLevel - a.interestLevel
            || b.coreBreadth - a.coreBreadth
            || b.recommendedBreadth - a.recommendedBreadth
            || b.score - a.score
            || a.candidate.course.row - b.candidate.course.row;
        }
        return b.score - a.score || a.candidate.course.row - b.candidate.course.row;
      });
    const selectCandidate = (candidate) => {
      selectedByGrade[grade].push(candidate);
      selectedIds.add(candidate.course.id);
      areaUsage.set(candidate.course.area, (areaUsage.get(candidate.course.area) || 0) + 1);
      candidate.interests.forEach((interest) => interestUsage.set(interest, (interestUsage.get(interest) || 0) + 1));
      markCourseDesignerChoiceGroups(candidate, groupUsage, grade);
    };

    if (optionId !== "deep" && selectedInterestLabels.length > 1) {
      selectedInterestLabels.forEach((interestLabel) => {
        if (selectedByGrade[grade].length >= limit) return;
        const candidate = getViable(interestLabel)[0]?.candidate;
        if (candidate) selectCandidate(candidate);
      });
    }

    while (selectedByGrade[grade].length < limit) {
      const viable = getViable();
      if (!viable.length) break;
      selectCandidate(viable[0].candidate);
    }

    selectedByGrade[grade] = selectedByGrade[grade]
      .map((candidate, recommendationOrder) => ({ candidate, recommendationOrder }))
      .sort((a, b) => getCourseDesignerSemesterIndex(a.candidate, grade) - getCourseDesignerSemesterIndex(b.candidate, grade)
        || a.recommendationOrder - b.recommendationOrder)
      .map(({ candidate }) => candidate);
  });
  return selectedByGrade;
}

function getCourseDesignerSemesterIndex(candidate, grade) {
  const indexes = (candidate.course?.semesters || [])
    .filter((semesterKey) => String(semesterKey).startsWith(`${grade}-`))
    .map(getSemesterSortIndex);
  return indexes.length ? Math.min(...indexes) : Number.MAX_SAFE_INTEGER;
}

function getCourseDesignerCandidateScore(candidate, optionId, areaUsage = new Map(), interestUsage = new Map()) {
  const sourceWeights = {
    deep: { core: 54, recommended: 38, suggested: 12 },
    balanced: { core: 44, recommended: 38, suggested: 24 },
    explore: { core: 28, recommended: 32, suggested: 40 }
  };
  const weights = sourceWeights[optionId] || sourceWeights.balanced;
  const sourceScore = Math.max(...[...candidate.sources].map((source) => weights[source] || 0), 0);
  const officialEvidence = candidate.officialEvidence || [];
  const officialScore = officialEvidence.some((evidence) => evidence.level === "core")
    ? 16
    : officialEvidence.length
      ? 10
      : 0;
  const sharedMultiplier = optionId === "deep" ? 4 : optionId === "balanced" ? 9 : 11;
  const sharedScore = Math.max(0, candidate.interests.size - 1) * sharedMultiplier;
  const category = String(candidate.course?.category || "");
  const categoryScore = optionId === "deep" && category.includes("진로")
    ? 6
    : optionId === "explore" && category.includes("융합")
      ? 10
      : optionId === "balanced" && category.includes("일반")
        ? 5
        : 0;
  const areaPenalty = (areaUsage.get(candidate.course?.area) || 0) * (optionId === "deep" ? 2 : 7);
  const interestCounts = [...candidate.interests].map((interest) => interestUsage.get(interest) || 0);
  const leastUsedInterest = interestCounts.length ? Math.min(...interestCounts) : 0;
  const interestBalanceScore = optionId === "deep" ? 0 : Math.max(0, 8 - leastUsedInterest * 4);
  return sourceScore + officialScore + sharedScore + categoryScore + interestBalanceScore - areaPenalty;
}

function getCourseDesignerInterestMetrics(candidate, interestLabel) {
  if (!interestLabel) return { interestLevel: 0, coreBreadth: 0, recommendedBreadth: 0 };
  const currentLevels = candidate.levelsByInterest?.get(interestLabel) || new Set();
  const interestLevel = currentLevels.has("core")
    ? 3
    : currentLevels.has("recommended")
      ? 2
      : currentLevels.has("suggested")
        ? 1
        : 0;
  const levelSets = [...(candidate.levelsByInterest?.values() || [])];
  const coreBreadth = levelSets.filter((levels) => levels.has("core")).length;
  const recommendedBreadth = levelSets.filter((levels) => levels.has("recommended")).length;
  return { interestLevel, coreBreadth, recommendedBreadth };
}

function courseDesignerCourseHasGrade(course, grade) {
  return (course.semesters || []).some((semesterKey) => String(semesterKey).startsWith(`${grade}-`));
}

function canUseCourseDesignerChoiceGroups(candidate, usage, grade) {
  return candidate.groupInfos
    .filter((group) => String(group.semesterKey).startsWith(`${grade}-`))
    .every((group) => (usage.get(group.id) || 0) < getCourseDesignerChoiceLimit(group));
}

function markCourseDesignerChoiceGroups(candidate, usage, grade) {
  candidate.groupInfos
    .filter((group) => String(group.semesterKey).startsWith(`${grade}-`))
    .forEach((group) => usage.set(group.id, (usage.get(group.id) || 0) + 1));
}

function getCourseDesignerChoiceLimit(group) {
  const match = String(group.marker || "").match(/택\s*(\d+)/);
  return match ? Number(match[1]) : 1;
}

function getCourseDesignerFoundationCourses(context, grade) {
  const direct = context.fixedCourses
    .filter((candidate) => courseDesignerCourseHasGrade(candidate.course, grade))
    .map((candidate) => candidate.course);
  const common = (context.plan.courses || [])
    .filter((course) => !isPlannerChoiceCourse(course))
    .filter((course) => courseDesignerCourseHasGrade(course, grade))
    .filter((course) => String(course.category || "").includes("공통"))
    .filter((course) => !context.relevantAreas.size || context.relevantAreas.has(course.area));
  const seen = new Set();
  return [...direct, ...common].filter((course) => {
    const key = normalizeText(course.name);
    if (seen.has(key)) return false;
    seen.add(key);
    return true;
  }).slice(0, 4);
}

function getCourseDesignerAlternatives(context, optionId) {
  const fakeAreaUsage = new Map();
  const fakeInterestUsage = new Map();
  return [...context.unmatched]
    .map((candidate) => ({
      ...candidate,
      course: { area: "", category: "" },
      score: getCourseDesignerCandidateScore({ ...candidate, course: { area: "", category: "" } }, optionId, fakeAreaUsage, fakeInterestUsage)
    }))
    .sort((a, b) => b.score - a.score || String(a.subject).localeCompare(String(b.subject), "ko"))
    .slice(0, 6);
}

function renderCourseDesignerResults() {
  const results = $("#courseDesignerResults");
  if (!results || results.hidden) return;
  const profiles = courseDesignerData.optionProfiles || [];
  if (!profiles.some((profile) => profile.id === state.courseDesignerOption)) {
    state.courseDesignerOption = getCourseDesignerDefaultOption(state.courseDesignerStatus);
  }
  const context = buildCourseDesignerContext();
  const selectedInterests = getSelectedCourseDesignerInterests();
  const profile = getCourseDesignerOptionProfile(state.courseDesignerOption);
  const selections = getCourseDesignerOptionSelections(context, profile.id);
  const alternatives = getCourseDesignerAlternatives(context, profile.id);
  const planMeta = recommendationCurriculumPlans.find((plan) => plan.key === state.courseDesignerPlan);
  const basis = $("#courseDesignerResultBasis");
  if (basis) {
    basis.textContent = `${getCurriculumPlanLabel(state.courseDesignerPlan)} · ${planMeta?.standard || ""} 개정 · 교육과정 ${formatDateLabel(curriculumData.updated)}`;
  }
  renderCourseDesignerOptionTabs(context);
  renderCourseDesignerOptionIntro(profile, context, selections, selectedInterests);
  renderCourseDesignerThreeYearMap(context, selections);
  renderCourseDesignerAlternativeList(alternatives, planMeta);
}

function renderCourseDesignerOptionTabs(context) {
  const target = $("#courseDesignerOptionTabs");
  if (!target) return;
  target.innerHTML = (courseDesignerData.optionProfiles || []).map((profile) => {
    const selections = getCourseDesignerOptionSelections(context, profile.id);
    const selectedCourses = Object.values(selections).flat();
    const preview = selectedCourses.slice(0, 3).map((candidate) => candidate.course.name).join(" · ");
    const active = profile.id === state.courseDesignerOption;
    return `
      <button class="course-designer-option-tab${active ? " active" : ""}" type="button" role="tab"
        aria-selected="${active}" data-course-designer-option="${escapeAttribute(profile.id)}">
        <span>${escapeHtml(profile.code)}</span>
        <b>${escapeHtml(profile.label)}</b>
        <small>${selectedCourses.length ? `살펴볼 과목 ${selectedCourses.length}개 · ${escapeHtml(preview)}` : "학교 선택과목 찾아보기"}</small>
      </button>
    `;
  }).join("");
}

function renderCourseDesignerOptionIntro(profile, context, selections, interests) {
  const target = $("#courseDesignerOptionIntro");
  if (!target) return;
  const selectedCount = Object.values(selections).flat().length;
  const fixedCount = context.fixedCourses.length;
  target.innerHTML = `
    <div>
      <span class="course-designer-option-code">${escapeHtml(profile.code)}</span>
      <div>
        <h3>${escapeHtml(profile.label)}</h3>
        <p>${escapeHtml(profile.description)}</p>
      </div>
    </div>
    <dl>
      <div><dt>관심 분야</dt><dd>${interests.length}</dd></div>
      <div><dt>살펴볼 과목</dt><dd>${selectedCount}</dd></div>
      <div><dt>기본 과목</dt><dd>${fixedCount}</dd></div>
    </dl>
    <p class="course-designer-option-caution">${escapeHtml(profile.caution)}</p>
  `;
}

function renderCourseDesignerThreeYearMap(context, selections) {
  const target = $("#courseDesignerMap");
  if (!target) return;
  target.innerHTML = `
    <div class="course-designer-map-head">
      <div>
        <span class="label">학년별로 살펴보기</span>
        <h3>1학년부터 3학년까지 어떤 과목을 배우고 선택하는지 살펴봅시다</h3>
      </div>
      <span>이미 배운 과목은 관심 분야와 어떻게 이어지는지 돌아보고, 앞으로 선택할 과목은 개설 학기와 선택 묶음을 함께 확인해 보세요.</span>
    </div>
    <div class="course-designer-grade-grid">
      ${["1", "2", "3"].map((grade) => renderCourseDesignerGradeCard(context, selections[grade] || [], grade)).join("")}
    </div>
  `;
}

function renderCourseDesignerGradeCard(context, selectedCourses, grade) {
  const foundations = getCourseDesignerFoundationCourses(context, grade);
  return `
    <article class="course-designer-grade-card grade-${grade}">
      <header>
        <span>${grade}</span>
        <div>
          <h4>${grade}학년</h4>
          <small>${getCourseDesignerGradeFocus(grade)}</small>
        </div>
      </header>
      <div class="course-designer-foundation">
        <b>학교에서 기본으로 배우는 과목 <small>따로 고르지 않음</small></b>
        <div>
          ${foundations.length
            ? foundations.map((course) => `<span>${escapeHtml(course.name)}</span>`).join("")
            : `<span class="empty">선택한 분야와 바로 연결해 보여줄 기본 과목은 없지만, 다른 기본 과목도 학업 역량을 쌓는 데 중요합니다.</span>`}
        </div>
      </div>
      <div class="course-designer-grade-courses">
        <b>관심 분야와 이어서 살펴볼 선택과목</b>
        ${selectedCourses.length
          ? selectedCourses.map((candidate) => renderCourseDesignerCourseCard(candidate, grade)).join("")
          : `<p class="course-designer-grade-empty">이 화면에서 먼저 보여줄 선택과목이 없습니다. 다음 학년 과목을 살펴보거나, 기본 과목에서 관심 분야와 이어지는 질문을 찾아보세요.</p>`}
      </div>
    </article>
  `;
}

function getCourseDesignerGradeFocus(grade) {
  return `우리학교에서 ${grade}학년에 배우거나 선택하는 과목`;
}

function renderCourseDesignerCourseCard(candidate, grade) {
  const course = candidate.course;
  const primarySource = getCourseDesignerPrimarySource(candidate);
  const choiceLabel = getCourseDesignerChoiceLabel(candidate, grade);
  const linkedSubject = isCourseDesignerLinkedSubject(candidate);
  return `
    <article class="course-designer-course-card source-${primarySource}">
      <div class="course-designer-course-head">
        ${renderCourseNameWithInfo(course)}
        <span class="course-designer-source-badge">${escapeHtml(getCourseDesignerSourceLabel(primarySource))}</span>
      </div>
      <div class="course-designer-course-meta">
        <span>${escapeHtml(course.area || "교과군 확인")}</span>
        <span>${escapeHtml(formatSubjectCategory(course.category || ""))}</span>
        <span>${escapeHtml(choiceLabel)}</span>
        ${linkedSubject ? `<span class="course-designer-link-candidate">과목명 연결 확인</span>` : ""}
      </div>
      <p>${escapeHtml(getCourseDesignerCourseReason(candidate))}</p>
      ${renderCourseDesignerOfficialEvidence(candidate)}
      ${renderCourseDesignerPlannerActions(course, grade)}
    </article>
  `;
}

function renderCourseDesignerPlannerActions(course, grade) {
  const semesters = (course.semesters || [])
    .filter((semesterKey) => String(semesterKey).startsWith(`${grade}-`))
    .sort((a, b) => getSemesterSortIndex(a) - getSemesterSortIndex(b))
    .map((semesterKey) => String(semesterKey).split("-")[1])
    .filter((semester) => semester === "1" || semester === "2");

  if (!semesters.length) return "";
  const zone = course.section?.includes("공동교육") ? "joint" : "regular";
  return `
    <div class="course-designer-planner-actions" aria-label="${escapeAttribute(course.name)} 내 교육과정 반영">
      ${semesters.map((semester) => {
        const selected = isCourseDesignerCoursePlanned(course.id, grade, semester);
        const availableInMultipleSemesters = semesters.length > 1;
        const label = selected
          ? `${availableInMultipleSemesters ? `${semester}학기 ` : ""}반영됨`
          : availableInMultipleSemesters
            ? `${semester}학기 반영`
            : "내 교육과정에 반영";
        return `
          <button class="course-designer-add-button${selected ? " selected" : ""}" type="button"
            data-course-designer-add-course="${escapeAttribute(course.id)}"
            data-grade="${escapeAttribute(grade)}"
            data-semester="${escapeAttribute(semester)}"
            data-zone="${escapeAttribute(zone)}"
            ${selected ? "disabled" : ""}>${escapeHtml(label)}</button>
        `;
      }).join("")}
    </div>
  `;
}

function isCourseDesignerCoursePlanned(courseId, grade, semester) {
  const plan = state.plannerPlan === state.courseDesignerPlan
    ? state.plan
    : state.plannerPlans[state.courseDesignerPlan];
  const semesterPlan = plan?.[grade]?.[semester];
  if (!semesterPlan) return false;
  return [...(semesterPlan.regular || []), ...(semesterPlan.joint || [])]
    .some((item) => item.id === courseId);
}

function addCourseDesignerCourseToPlanner(button) {
  const courseId = button.dataset.courseDesignerAddCourse;
  const grade = button.dataset.grade;
  const semester = button.dataset.semester;
  const zone = button.dataset.zone || "regular";

  if (state.plannerPlan !== state.courseDesignerPlan) {
    switchPlannerPlan(state.courseDesignerPlan);
  }

  const course = getPlannerCourse(courseId);
  const added = addCourseToPlan(courseId, grade, semester, zone);
  renderCourseDesignerResults();
  if (added) {
    showToast(`${course?.name || "과목"}을 ${grade}학년 ${semester}학기 내 교육과정에 반영했습니다.`);
  }
}

function openCourseDesignerPlanner() {
  if (state.plannerPlan !== state.courseDesignerPlan) {
    switchPlannerPlan(state.courseDesignerPlan);
  }
  setView("planner");
  setPlannerMode("mine");
}

function getCourseDesignerChoiceLabel(candidate, grade) {
  const group = candidate.groupInfos
    .filter((item) => String(item.semesterKey).startsWith(`${grade}-`))
    .sort((a, b) => getSemesterSortIndex(a.semesterKey) - getSemesterSortIndex(b.semesterKey))[0];
  if (group) return `${group.semesterLabel} · ${group.shortLabel} 중 ${group.choiceText}`;
  const labels = (candidate.course.semesters || [])
    .filter((semesterKey) => String(semesterKey).startsWith(`${grade}-`))
    .map(getSemesterLabel);
  return labels.join(" · ") || `${grade}학년`;
}

function getCourseDesignerCourseReason(candidate) {
  const primarySource = getCourseDesignerPrimarySource(candidate);
  const interests = [...candidate.interests];
  const interestText = interests.slice(0, 3).join("·") || "선택한 관심";
  const sourceText = primarySource === "official-core"
    ? "2028학년도 대학별 자료에서 일부 학과가 핵심과목으로 제시했습니다. 모든 대학이 반드시 요구하는 과목은 아닙니다."
    : primarySource === "official-recommended"
      ? "2028학년도 대학별 자료에서 일부 학과가 권장과목으로 제시했습니다. 희망 대학이 정해지면 그 대학의 최신 안내를 다시 확인하세요."
      : primarySource === "field-guide"
        ? "관련 학과의 과목 선택 흐름을 참고해 함께 살펴볼 과목입니다. 특정 대학이 공식적으로 지정한 핵심·권장과목은 아닙니다."
        : "관심 분야를 넓혀 볼 때 함께 살펴볼 과목입니다. 대학이 공식 핵심·권장과목으로 제시한 과목은 아닙니다.";
  const sourceSubjects = [...candidate.subjects];
  const renamed = isCourseDesignerLinkedSubject(candidate)
    ? ` 자료에 나온 ${sourceSubjects.slice(0, 2).join("·")}와 우리학교의 ${candidate.course.name}을 비슷한 흐름의 과목으로 연결했습니다. 두 과목의 내용이 완전히 같다는 뜻은 아닙니다.`
    : "";
  return `선택한 ${interestText} 분야를 공부할 때 도움이 될 수 있는 과목입니다. ${sourceText}${renamed}`;
}

function isCourseDesignerLinkedSubject(candidate) {
  const courseName = normalizeText(candidate.course?.name || "");
  const sourceSubjects = [...(candidate.subjects || [])];
  return Boolean(sourceSubjects.length && !sourceSubjects.some((subject) => normalizeText(subject) === courseName));
}

function getCourseDesignerPrimarySource(candidate) {
  const evidence = candidate.officialEvidence || [];
  if (candidate.sources?.has("core") && evidence.some((item) => item.level === "core")) return "official-core";
  if (candidate.sources?.has("recommended") && evidence.some((item) => item.level === "recommended")) return "official-recommended";
  if (candidate.sources?.has("core") || candidate.sources?.has("recommended")) return "field-guide";
  return "suggested";
}

function getCourseDesignerSourceLabel(source) {
  if (source === "official-core") return "대학 자료: 핵심";
  if (source === "official-recommended") return "대학 자료: 권장";
  if (source === "field-guide") return "분야 탐색";
  return "함께 살펴보기";
}

function renderCourseDesignerOfficialEvidence(candidate) {
  const evidence = [...(candidate.officialEvidence || [])]
    .sort((a, b) => (a.level === "core" ? -1 : 1) - (b.level === "core" ? -1 : 1)
      || String(a.university).localeCompare(String(b.university), "ko")
      || String(a.department).localeCompare(String(b.department), "ko"));
  if (!evidence.length) return "";
  const basis = courseDesignerData.officialBasis || {};
  const visible = evidence.slice(0, 4);
  return `
    <details class="course-designer-evidence">
      <summary>대학별 제시 사례 보기 (${evidence.length}개 학과·모집단위)</summary>
      <ul>
        ${visible.map((item) => `
          <li><b>${escapeHtml(item.university)}</b> ${escapeHtml(item.department)} <span>${item.level === "core" ? "핵심" : "권장"}</span></li>
        `).join("")}
      </ul>
      ${evidence.length > visible.length ? `<p>이 밖에도 ${evidence.length - visible.length}개 학과·모집단위에서 제시했습니다.</p>` : ""}
      <small>${escapeHtml(basis.caution || "대학마다 과목을 제시하는 방법이 다르므로 희망 대학의 최신 안내를 다시 확인하세요.")}</small>
    </details>
  `;
}

function renderCourseDesignerAlternativeList(alternatives, planMeta) {
  const target = $("#courseDesignerAlternatives");
  if (!target) return;
  const standard = planMeta?.standard === "2015" ? "15" : "22";
  target.innerHTML = `
    <div class="course-designer-alternative-head">
      <div>
        <span class="label">우리학교에 없는 과목도 살펴보기</span>
        <h3>학교에서 열리지 않는 과목은 다른 수강 방법을 알아봅시다</h3>
      </div>
      <p>공동교육과정이나 온라인학교에서 들을 수 있는지 알아보고, 내 진로와 학습 계획에 정말 필요한 과목인지 선생님과 함께 살펴보세요.</p>
    </div>
    ${alternatives.length ? `
      <div class="course-designer-alternative-grid">
        ${alternatives.map((candidate) => {
          const source = getCourseDesignerPrimarySource(candidate);
          return `
            <article class="course-designer-alternative-card source-${source}">
              <div>
                <b>${escapeHtml(candidate.subject)}</b>
                <span>미개설(${standard})</span>
              </div>
              <small>${escapeHtml(getCourseDesignerSourceLabel(source))}</small>
              <p>${escapeHtml([...candidate.interests].join(" · "))} 분야를 더 알아볼 때 도움이 될 수 있는 과목입니다. 실제로 들을 수 있는지와 신청 방법은 선생님께 확인하세요.</p>
            </article>
          `;
        }).join("")}
      </div>
    ` : `<div class="empty-note">선택한 관심 분야와 연결되는 주요 과목을 우리학교 교육과정에서 모두 찾았습니다.</div>`}
  `;
}

function renderCurriculum() {
  const plan = curriculumData.plans[state.activeCurriculumPlan];
  if (!plan) return;

  $("#curriculumPlan").value = state.activeCurriculumPlan;
  $("#curriculumScope").value = state.activeCurriculumScope;
  $("#curriculumPlanTitle").textContent = getCurriculumPlanLabel(state.activeCurriculumPlan);
  $("#curriculumPlanDescription").textContent = plan.description;
  const updatedTarget = $("#curriculumUpdatedDate");
  if (updatedTarget) updatedTarget.textContent = formatDateLabel(curriculumData.updated);

  const scope = $("#curriculumScope").value;
  $("#curriculumCourseSectionTitle").textContent = getCurriculumCourseSectionTitle(scope);
  const grade = $("#curriculumGrade").value;
  const semester = $("#curriculumSemester").value;
  const semesterKeys = getCurriculumSemesterKeys(scope, grade, semester);
  const query = normalizeText($("#curriculumSearch").value);
  updateCurriculumScopeControls(scope);

  const allScopeCourses = plan.courses
    .filter((course) => course.semesters.some((semesterKey) => semesterKeys.includes(semesterKey)))
    .sort((a, b) => a.row - b.row);
  const semesterCourses = allScopeCourses
    .filter((course) => {
      if (!query) return true;
      return normalizeText(`${course.name} ${course.area} ${course.category} ${course.section}`).includes(query);
    });
  const choiceGroups = buildCurriculumChoiceGroups(plan.courses, semesterKeys);
  const courseGroupMap = buildCourseChoiceGroupMap(choiceGroups);
  const visibleCourses = sortCurriculumCourses(semesterCourses, semesterKeys, courseGroupMap);

  renderCurriculumSummary(plan, semesterKeys, semesterCourses);
  updateCurriculumSortHeaders();
  $("#curriculumTableBody").innerHTML = semesterCourses.length
    ? visibleCourses.map((course) => renderCurriculumRow(course, semesterKeys, courseGroupMap.get(course.id) || [])).join("")
    : `<tr><td colspan="6">해당 조건에 맞는 과목이 없습니다.</td></tr>`;
  renderCurriculumChoiceGroups(choiceGroups, semesterCourses, Boolean(query));
}

function renderCurriculumSummary(plan, semesterKeys, semesterCourses) {
  const summary = getCurriculumSummaryForScope(plan, semesterKeys);
  const fixedCount = semesterCourses.filter((course) => course.section.includes("학교지정")).length;
  const choiceCount = semesterCourses.filter((course) => course.section.includes("학생선택")).length;
  const jointCount = semesterCourses.filter((course) => course.section.includes("공동교육")).length;
  const totalLabel = semesterKeys.length === 1 ? "학기 총학점" : "범위 총학점";
  $("#curriculumSummary").innerHTML = `
    <span><b>${summary.courseCredits || "-"}</b>교과 이수학점</span>
    <span><b>${summary.creativeCredits || "3"}</b>창체</span>
    <span><b>${summary.totalCredits || "-"}</b>${totalLabel}</span>
    <span><b>${semesterKeys.length}</b>조회 학기</span>
    <span><b>${semesterCourses.length}</b>표시 과목</span>
    <span><b>${fixedCount}/${choiceCount}/${jointCount}</b>지정·선택·공동</span>
  `;
}

function renderCurriculumRow(course, semesterKeys, groupInfos) {
  const rowClass = groupInfos.length ? ` class="choice-linked-row ${groupInfos[0].colorClass}"` : "";
  const sectionClass = [
    getCurriculumSectionClass(course, semesterKeys),
    getCurriculumGradeClass(course, semesterKeys),
    getCurriculumGradeLabel(course, semesterKeys) ? "has-grade" : ""
  ].filter(Boolean).join(" ");
  return `
    <tr${rowClass}>
      <td><span class="section-badge ${sectionClass}">${formatCurriculumSectionLabel(course, semesterKeys)}</span></td>
      <td>${renderSemesterChoiceBadges(course, semesterKeys, groupInfos)}</td>
      <td>${escapeHtml(course.area)}</td>
      <td>${escapeHtml(course.category)}</td>
      <td>${renderCourseNameWithInfo(course)}</td>
      <td>${course.credits || "-"}</td>
    </tr>
  `;
}

function buildCurriculumChoiceGroups(allCourses, semesterKeys) {
  return semesterKeys.flatMap((semesterKey) => buildCurriculumChoiceGroupsForSemester(allCourses, semesterKey));
}

function getPlanKeyForCourseList(allCourses) {
  return Object.entries(curriculumData.plans || {})
    .find(([, plan]) => plan.courses === allCourses)?.[0] || "";
}

function isChoiceGroupStartOverride(allCourses, semesterKey, course) {
  const planKey = getPlanKeyForCourseList(allCourses);
  const startRows = choiceGroupStartOverrides[planKey]?.[semesterKey] || [];
  return startRows.includes(course.row);
}

function buildCurriculumChoiceGroupsForSemester(allCourses, semesterKey) {
  const semesterCourses = allCourses
    .filter((course) => course.semesters.includes(semesterKey))
    .sort((a, b) => a.row - b.row);
  const groups = [];
  let currentGroup = null;
  let previousKey = "";
  let previousRow = null;

  semesterCourses.forEach((course) => {
    const marker = course.markers[semesterKey] || "";
    if (!marker.includes("택")) {
      currentGroup = null;
      previousKey = "";
      previousRow = null;
      return;
    }
    const key = `${marker} · ${course.section}`;
    const isSeparated = previousRow !== null && course.row !== previousRow + 1;
    const isOverrideStart = isChoiceGroupStartOverride(allCourses, semesterKey, course);
    if (!currentGroup || key !== previousKey || isSeparated || isOverrideStart) {
      currentGroup = {
        label: key,
        marker,
        section: course.section,
        semesterKey,
        courses: [],
        ...getChoiceMarkerMeta(marker)
      };
      groups.push(currentGroup);
    }
    currentGroup.courses.push(course);
    previousKey = key;
    previousRow = course.row;
  });

  return groups.map((group, index) => ({
    id: `${group.semesterKey}-choice-${index + 1}`,
    label: group.label,
    shortLabel: `선택 ${getChoiceGroupLetter(index)}`,
    choiceIndex: index,
    colorClass: `choice-group-${(index % 6) + 1}`,
    marker: group.marker,
    section: group.section,
    semesterKey: group.semesterKey,
    semesterLabel: getSemesterLabel(group.semesterKey),
    choiceText: group.choiceText,
    creditText: group.creditText,
    courses: group.courses
  }));
}

function buildCourseChoiceGroupMap(choiceGroups) {
  const groupMap = new Map();
  choiceGroups.forEach((group) => {
    group.courses.forEach((course) => {
      if (!groupMap.has(course.id)) groupMap.set(course.id, []);
      groupMap.get(course.id).push(group);
    });
  });
  return groupMap;
}

function sortCurriculumCourses(courses, semesterKeys, courseGroupMap) {
  return [...courses].sort((a, b) => {
    for (const sort of state.curriculumSorts) {
      const result = compareCurriculumBySort(a, b, sort, semesterKeys, courseGroupMap);
      if (result !== 0) return result;
    }
    return compareDefaultCurriculumCourses(a, b, semesterKeys, courseGroupMap);
  });
}

function compareCurriculumBySort(a, b, sort, semesterKeys, courseGroupMap) {
  if (sort.key === "section") {
    return compareSortTuples(
      [getCurriculumSectionSortOrder(a, semesterKeys), getCourseSortGrade(a, semesterKeys)],
      [getCurriculumSectionSortOrder(b, semesterKeys), getCourseSortGrade(b, semesterKeys)],
      sort.direction
    );
  }
  if (sort.key === "choice") return compareChoiceSort(a, b, sort.direction, courseGroupMap);
  if (sort.key === "area") return compareTextSort(a.area, b.area, sort.direction);
  if (sort.key === "category") return compareTextSort(a.category, b.category, sort.direction);
  if (sort.key === "name") return compareTextSort(a.name, b.name, sort.direction);
  if (sort.key === "credits") return compareNumberSort(a.credits, b.credits, sort.direction);
  return 0;
}

function compareDefaultCurriculumCourses(a, b, semesterKeys, courseGroupMap) {
  const sectionCompare = getCurriculumSectionSortOrder(a, semesterKeys) - getCurriculumSectionSortOrder(b, semesterKeys);
  if (sectionCompare !== 0) return sectionCompare;

  const gradeCompare = getCourseSortGrade(a, semesterKeys) - getCourseSortGrade(b, semesterKeys);
  if (gradeCompare !== 0) return gradeCompare;

  const semesterCompare = getCourseSortSemesterIndex(a, semesterKeys, courseGroupMap) - getCourseSortSemesterIndex(b, semesterKeys, courseGroupMap);
  if (semesterCompare !== 0) return semesterCompare;

  const choiceCompare = getCourseSortChoiceIndex(a, courseGroupMap) - getCourseSortChoiceIndex(b, courseGroupMap);
  if (choiceCompare !== 0) return choiceCompare;

  if (a.row !== b.row) return a.row - b.row;
  return String(a.name).localeCompare(String(b.name), "ko", { numeric: true });
}

function compareChoiceSort(a, b, direction, courseGroupMap) {
  const aGroup = getPrimaryChoiceGroup(courseGroupMap.get(a.id) || []);
  const bGroup = getPrimaryChoiceGroup(courseGroupMap.get(b.id) || []);
  if (!aGroup && !bGroup) return 0;
  if (!aGroup) return 1;
  if (!bGroup) return -1;
  return compareSortTuples(
    [getSemesterSortIndex(aGroup.semesterKey), aGroup.choiceIndex ?? 999],
    [getSemesterSortIndex(bGroup.semesterKey), bGroup.choiceIndex ?? 999],
    direction
  );
}

function compareTextSort(a, b, direction) {
  const aValue = String(a || "").trim();
  const bValue = String(b || "").trim();
  if (!aValue && !bValue) return 0;
  if (!aValue) return 1;
  if (!bValue) return -1;
  const result = aValue.localeCompare(bValue, "ko", { numeric: true });
  return direction === "asc" ? result : -result;
}

function compareNumberSort(a, b, direction) {
  const aValue = Number(a || 0);
  const bValue = Number(b || 0);
  const result = aValue - bValue;
  return direction === "asc" ? result : -result;
}

function compareSortTuples(aValues, bValues, direction) {
  for (let index = 0; index < aValues.length; index += 1) {
    const aValue = aValues[index];
    const bValue = bValues[index];
    const result = typeof aValue === "string" || typeof bValue === "string"
      ? String(aValue || "").localeCompare(String(bValue || ""), "ko", { numeric: true })
      : Number(aValue || 0) - Number(bValue || 0);
    if (result !== 0) return direction === "asc" ? result : -result;
  }
  return 0;
}

function getCurriculumSectionSortOrder(course, semesterKeys) {
  if (course.section.includes("공동교육") || course.section.includes("추가")) return 3;
  if (course.section.includes("학생선택")) return 2;
  if (course.section.includes("학교지정") && courseHasChoiceMarker(course, semesterKeys)) return 1;
  if (course.section.includes("학교지정")) return 0;
  return 4;
}

function getCourseSortGrade(course, semesterKeys) {
  const gradeLabel = getCurriculumGradeLabel(course, semesterKeys);
  const match = gradeLabel.match(/([123])학년/);
  return match ? Number(match[1]) : 9;
}

function getCourseSortSemesterIndex(course, semesterKeys, courseGroupMap) {
  const group = getPrimaryChoiceGroup(courseGroupMap.get(course.id) || []);
  const semesterKey = group?.semesterKey || getCoursePrimarySemesterKey(course, semesterKeys);
  return getSemesterSortIndex(semesterKey);
}

function getCourseSortChoiceIndex(course, courseGroupMap) {
  const group = getPrimaryChoiceGroup(courseGroupMap.get(course.id) || []);
  return group ? group.choiceIndex ?? 999 : 999;
}

function getPrimaryChoiceGroup(groups) {
  return [...groups].sort((a, b) => {
    const semesterCompare = getSemesterSortIndex(a.semesterKey) - getSemesterSortIndex(b.semesterKey);
    if (semesterCompare !== 0) return semesterCompare;
    return (a.choiceIndex ?? 999) - (b.choiceIndex ?? 999);
  })[0] || null;
}

function getSemesterSortIndex(semesterKey) {
  const index = curriculumSemesterOrder.indexOf(semesterKey);
  return index === -1 ? 99 : index;
}

function renderCurriculumChoiceGroups(choiceGroups, visibleCourses, hasQuery) {
  const visibleIds = new Set(visibleCourses.map((course) => course.id));
  const visibleGroups = hasQuery
    ? choiceGroups.filter((group) => group.courses.some((course) => visibleIds.has(course.id)))
    : choiceGroups;
  const displayGroups = mergeYearLinkedChoiceGroups(visibleGroups);

  $("#curriculumChoiceGroups").innerHTML = displayGroups.length
    ? displayGroups
        .map(
          (group) => `
            <article class="choice-group-card ${group.colorClass}">
              <div class="choice-group-title">
                ${renderChoiceBadge(group)}
                <b>${escapeHtml(getChoiceGroupSectionLabel(group))}</b>
                ${group.creditText ? `<small>${escapeHtml(`${group.creditText} 편성`)}</small>` : ""}
              </div>
              <div class="tag-list">
                ${group.courses.map((course) => `<span class="tag ${visibleIds.has(course.id) ? "selected" : ""}">${escapeHtml(course.name)} · ${course.credits}학점</span>`).join("")}
              </div>
            </article>
          `
        )
        .join("")
    : `<div class="empty-note">${hasQuery ? "검색된 과목에 연결된 선택 묶음이 없습니다." : "이 학기에는 선택 묶음 표시가 없습니다."}</div>`;
}

function getCurriculumSemesterKeys(scope, grade, semester) {
  if (scope === "plan") return curriculumSemesterOrder;
  if (scope === "semesterAllGrades") return ["1", "2", "3"].map((gradeKey) => `${gradeKey}-${semester}`);
  if (scope === "grade") return [`${grade}-1`, `${grade}-2`];
  return [`${grade}-${semester}`];
}

function getCurriculumCourseSectionTitle(scope) {
  if (scope === "plan") return "전체 교육과정 과목";
  if (scope === "semesterAllGrades") return "선택 학기 전학년 과목";
  if (scope === "grade") return "해당 학년 전체 과목";
  return "해당 학기 과목";
}

function updateCurriculumScopeControls(scope) {
  $("#curriculumGrade").disabled = scope === "plan" || scope === "semesterAllGrades";
  $("#curriculumSemester").disabled = scope === "plan" || scope === "grade";
}

function getCurriculumSummaryForScope(plan, semesterKeys) {
  if (semesterKeys.length === 1) return plan.summary[semesterKeys[0]] || {};
  return semesterKeys.reduce(
    (total, semesterKey) => {
      const summary = plan.summary[semesterKey] || {};
      total.courseCredits += Number(summary.courseCredits || 0);
      total.creativeCredits += Number(summary.creativeCredits || 0);
      total.totalCredits += Number(summary.totalCredits || 0);
      return total;
    },
    { courseCredits: 0, creativeCredits: 0, totalCredits: 0 }
  );
}

function renderSemesterChoiceBadges(course, semesterKeys, groupInfos) {
  if (!groupInfos.length) return renderSemesterBadges(course, semesterKeys);
  const displayGroups = mergeCourseChoiceGroupsForDisplay(course, semesterKeys, groupInfos);
  return `
    <div class="choice-badge-list">
      ${displayGroups
        .map((group) => renderChoiceBadge(group))
        .join("")}
    </div>
  `;
}

function renderSemesterBadges(course, semesterKeys) {
  const semesterLabels = getCourseSemesterLabels(course, semesterKeys);
  if (!semesterLabels.length) return `<span class="choice-badge none">-</span>`;
  return `
    <div class="choice-badge-list">
      ${semesterLabels
        .map((label) => `<span class="choice-badge semester-badge">${escapeHtml(label)}</span>`)
        .join("")}
    </div>
  `;
}

function renderChoiceBadge(group) {
  return `
    <span class="choice-badge ${group.colorClass} has-detail">
      <span>${escapeHtml(group.semesterLabel)}</span>
      <small>${escapeHtml(`${group.shortLabel} 중 ${group.choiceText}`)}</small>
    </span>
  `;
}

function getCourseSemesterLabels(course, semesterKeys) {
  const labels = [];
  const handledSemesters = new Set();

  ["1", "2", "3"].forEach((grade) => {
    const firstSemester = `${grade}-1`;
    const secondSemester = `${grade}-2`;
    if (isCourseYearLinked(course, semesterKeys, firstSemester, secondSemester)) {
      labels.push(getYearLinkedChoiceSemesterLabel(firstSemester));
      handledSemesters.add(firstSemester);
      handledSemesters.add(secondSemester);
    }
  });

  curriculumSemesterOrder.forEach((semesterKey) => {
    if (!semesterKeys.includes(semesterKey) || !course.semesters.includes(semesterKey) || handledSemesters.has(semesterKey)) return;
    labels.push(getSemesterLabel(semesterKey));
  });

  return labels;
}

function mergeCourseChoiceGroupsForDisplay(course, semesterKeys, groups) {
  const usedIds = new Set();
  const merged = [];

  groups.forEach((group) => {
    if (usedIds.has(group.id)) return;
    const pair = findCourseYearLinkedChoicePair(course, semesterKeys, group, groups);
    if (!pair) {
      merged.push(group);
      return;
    }

    const [firstGroup, secondGroup] = [group, pair].sort((a, b) => getSemesterSortIndex(a.semesterKey) - getSemesterSortIndex(b.semesterKey));
    usedIds.add(firstGroup.id);
    usedIds.add(secondGroup.id);
    merged.push({
      ...firstGroup,
      id: `${firstGroup.id}-course-year-linked`,
      semesterLabel: getYearLinkedChoiceSemesterLabel(firstGroup.semesterKey),
      isYearLinked: true
    });
  });

  return merged;
}

function mergeYearLinkedChoiceGroups(groups) {
  const usedIds = new Set();
  const merged = [];

  groups.forEach((group) => {
    if (usedIds.has(group.id)) return;
    const pair = findYearLinkedChoicePair(group, groups);
    if (!pair) {
      merged.push(group);
      return;
    }

    const [firstGroup, secondGroup] = [group, pair].sort((a, b) => getSemesterSortIndex(a.semesterKey) - getSemesterSortIndex(b.semesterKey));
    usedIds.add(firstGroup.id);
    usedIds.add(secondGroup.id);
    merged.push({
      ...firstGroup,
      id: `${firstGroup.id}-year-linked`,
      semesterLabel: getYearLinkedChoiceSemesterLabel(firstGroup.semesterKey),
      isYearLinked: true
    });
  });

  return merged;
}

function findYearLinkedChoicePair(group, groups) {
  const [grade, semester] = group.semesterKey.split("-");
  const otherSemesterKey = `${grade}-${semester === "1" ? "2" : "1"}`;
  const courseKey = getChoiceGroupCourseKey(group);
  return groups.find((candidate) => (
    candidate.id !== group.id &&
    candidate.semesterKey === otherSemesterKey &&
    candidate.marker === group.marker &&
    candidate.section === group.section &&
    getChoiceGroupCourseKey(candidate) === courseKey
  ));
}

function findCourseYearLinkedChoicePair(course, semesterKeys, group, groups) {
  const [grade, semester] = group.semesterKey.split("-");
  const otherSemesterKey = `${grade}-${semester === "1" ? "2" : "1"}`;
  if (!isCourseYearLinked(course, semesterKeys, group.semesterKey, otherSemesterKey)) return null;
  return groups.find((candidate) => (
    candidate.id !== group.id &&
    candidate.semesterKey === otherSemesterKey &&
    candidate.marker === group.marker &&
    candidate.section === group.section &&
    candidate.shortLabel === group.shortLabel &&
    candidate.courses.some((candidateCourse) => candidateCourse.id === course.id)
  ));
}

function getChoiceGroupCourseKey(group) {
  return group.courses.map((course) => course.id).sort().join("|");
}

function isCourseYearLinked(course, semesterKeys, firstSemester, secondSemester) {
  if (!semesterKeys.includes(firstSemester) || !semesterKeys.includes(secondSemester)) return false;
  if (!course.semesters.includes(firstSemester) || !course.semesters.includes(secondSemester)) return false;
  const firstMarker = String(course.markers?.[firstSemester] || "").trim();
  const secondMarker = String(course.markers?.[secondSemester] || "").trim();
  return Boolean(firstMarker && firstMarker === secondMarker);
}

function getYearLinkedChoiceSemesterLabel(semesterKey) {
  const [grade] = semesterKey.split("-");
  return `${grade}학년(학년제 편성)`;
}

function getSemesterLabel(semesterKey) {
  const [grade, semester] = semesterKey.split("-");
  return `${grade}학년 ${semester}학기`;
}

function getChoiceGroupLetter(index) {
  return String.fromCharCode(65 + index);
}

function getChoiceMarkerMeta(marker) {
  const match = marker.match(/\[?\s*택\s*(\d+)\s*\]?\s*\/\s*(\d+)/);
  if (!match) {
    return {
      choiceText: marker || "선택",
      creditText: ""
    };
  }
  return {
    choiceText: `${match[1]}개 선택`,
    creditText: `${match[2]}학점`
  };
}

function getChoiceGroupSectionLabel(group) {
  const base = group.section.replace(/\s*\(.+?\)\s*/g, "").replace(/\s+/g, " ").trim();
  if (base.includes("학교지정") && String(group.marker || "").includes("택")) return "지정선택";
  return base;
}

function getCurriculumSectionClass(course, semesterKeys) {
  if (course.section.includes("공동교육") || course.section.includes("추가")) return "joint";
  if (course.section.includes("학생선택")) return "choice";
  if (course.section.includes("학교지정") && courseHasChoiceMarker(course, semesterKeys)) return "fixed fixed-choice";
  return "fixed";
}

function getCurriculumGradeClass(course, semesterKeys) {
  const sectionGrade = course.section.match(/([123])학년/);
  if (sectionGrade) return `grade-${sectionGrade[1]}`;
  const activeSemester = getCoursePrimarySemesterKey(course, semesterKeys);
  if (!activeSemester) return "";
  return `grade-${activeSemester.split("-")[0]}`;
}

function formatCurriculumSectionLabel(course, semesterKeys) {
  const baseLabel = getCurriculumSectionBaseLabel(course, semesterKeys);
  const gradeLabel = getCurriculumGradeLabel(course, semesterKeys);
  if (!gradeLabel) return escapeHtml(baseLabel);
  return `${escapeHtml(baseLabel)}<small>(${escapeHtml(gradeLabel)})</small>`;
}

function getCurriculumSectionBaseLabel(course, semesterKeys) {
  const base = course.section.replace(/\s*\(.+?\)\s*/g, "").replace(/\s+/g, " ").trim();
  if (base.includes("학교지정") && courseHasChoiceMarker(course, semesterKeys)) return "지정선택";
  return base;
}

function getCurriculumGradeLabel(course, semesterKeys) {
  const sectionGrade = course.section.match(/([123])학년/);
  if (sectionGrade) return `${sectionGrade[1]}학년`;
  const activeSemester = getCoursePrimarySemesterKey(course, semesterKeys);
  if (!activeSemester) return "";
  return `${activeSemester.split("-")[0]}학년`;
}

function getCoursePrimarySemesterKey(course, semesterKeys) {
  return curriculumSemesterOrder.find((semesterKey) => semesterKeys.includes(semesterKey) && course.semesters.includes(semesterKey)) || "";
}

function courseHasChoiceMarker(course, semesterKeys) {
  return semesterKeys.some((semesterKey) => String(course.markers?.[semesterKey] || "").includes("택"));
}

function renderRecommendations() {
  const recommended = courses.filter((course) => course.tracks.includes(state.selectedTrack));
  $("#recommendBox").innerHTML = `
    <strong>${trackLabels[state.selectedTrack]} 계열 추천 과목</strong>
    <div class="tag-list">
      ${recommended
        .slice(0, 8)
        .map((course) => `<span class="tag">${course.name}</span>`)
        .join("")}
    </div>
  `;
}

function renderRecommendationExplorer() {
  const searchInput = $("#majorRecommendationSearch");
  const majorSelect = $("#majorRecommendationSelect");
  const subjectSelect = $("#subjectRecommendationSelect");
  const universitySearchInput = $("#universityRecommendationSearch");
  const universityMajorInput = $("#universityMajorSearch");
  if (!searchInput || !majorSelect) return;

  renderMajorRecommendationOptions(majorSelect);
  renderMajorSuggestionList();
  if (subjectSelect) renderSubjectRecommendationOptions(subjectSelect);
  renderQuickMajorButtons();
  renderRecommendationModeState();

  const mode = state.recommendationMode || "major";
  const isSubjectMode = mode === "subject";
  const isAdvancedMode = mode === "advanced";
  const isUniversityMode = mode === "university";
  const majorResultTarget = $("#majorRecommendationResults");
  const subjectResultTarget = $("#subjectRecommendationResults");
  const quickMajorTarget = $("#quickMajorButtons");

  const query = normalizeText(searchInput.value);
  const records = getRecommendationRecords();
  const majorMatches = query
    ? records.filter((record) => recordMatchesMajor(record, query))
    : getFeaturedRecommendationRecords();

  if (majorResultTarget) {
    majorResultTarget.hidden = isSubjectMode;
    if (isSubjectMode) {
      majorResultTarget.innerHTML = "";
    } else if (isUniversityMode) {
      majorResultTarget.innerHTML = renderUniversityRecommendationResults(
        universitySearchInput?.value || "",
        universityMajorInput?.value || ""
      );
    } else {
      majorResultTarget.innerHTML = `
        <div class="recommendation-result-head">
          <h4>${query ? "학과·계열 검색 결과" : "먼저 보면 좋은 학과·계열 예시"}</h4>
          <span>${majorMatches.length}개</span>
        </div>
        ${majorMatches.length ? majorMatches.map((record) => renderRecommendationCard(record)).join("") : renderEmptyRecommendation("검색어와 맞는 학과·계열을 찾지 못했습니다. 간호학과, 의학 보건계열, 지리교육과처럼 학과명 또는 넓은 계열명으로 다시 입력해 보세요.")}
      `;
    }
  }

  if (quickMajorTarget) {
    quickMajorTarget.hidden = isSubjectMode || isUniversityMode;
  }

  if (!subjectResultTarget) {
    bindMajorQueryButtons();
    return;
  }

  subjectResultTarget.hidden = !isSubjectMode && !isAdvancedMode;
  if (!isSubjectMode && !isAdvancedMode) {
    subjectResultTarget.innerHTML = "";
    bindMajorQueryButtons();
    return;
  }

  const selectedSubject = subjectSelect?.value || "";
  const subjectMatches = selectedSubject
    ? records.filter((record) => recordHasSubject(record, selectedSubject))
    : [];
  subjectResultTarget.innerHTML = selectedSubject
    ? `
      <div class="recommendation-result-head">
        <h4>${escapeHtml(selectedSubject)} 과목과 연결되는 학과·계열</h4>
        <span>${subjectMatches.length}개</span>
      </div>
      ${subjectMatches.length ? subjectMatches.map((record) => renderSubjectMatchCard(record, selectedSubject)).join("") : renderEmptyRecommendation("이 과목과 직접 연결된 학과·계열 데이터가 아직 없습니다.")}
    `
    : `
      <div class="empty-note">과목을 선택하면 그 과목을 핵심·권장·추천으로 연결한 학과·계열이 나타납니다.</div>
    `;
  bindMajorQueryButtons();
}

function renderRecommendationModeState() {
  const mode = state.recommendationMode || "major";
  const tool = $(".recommendation-tool");
  if (tool) {
    tool.classList.toggle("major-mode", mode === "major");
    tool.classList.toggle("subject-mode", mode === "subject");
    tool.classList.toggle("advanced-mode", mode === "advanced");
    tool.classList.toggle("university-mode", mode === "university");
  }

  $all("[data-recommendation-mode]").forEach((button) => {
    button.classList.toggle("active", button.dataset.recommendationMode === mode);
  });

  const help = $("#recommendationModeHelp");
  if (!help) return;
  if (mode === "subject") {
    help.textContent = "관심 과목을 고르면 그 과목과 연결해 생각해 볼 수 있는 학과·계열이 나타납니다.";
    return;
  }
  if (mode === "university") {
    help.textContent = "대학명과 학과명을 입력하면 대학별 권장과목 자료에서 확인되는 과목을 보여줍니다.";
    return;
  }
  if (mode === "advanced") {
    help.textContent = "학과 기준 조회와 과목 기준 역조회를 함께 확인합니다.";
    return;
  }
  help.textContent = "희망 학과나 계열을 고르면 연결 과목과 우리학교 개설 여부를 함께 볼 수 있습니다.";
}

function renderMajorSuggestionList() {
  const datalist = $("#majorSuggestionList");
  if (!datalist) return;
  const names = new Set();
  getRecommendationRecords().forEach((record) => {
    names.add(record.major);
    (record.aliases || []).forEach((alias) => names.add(alias));
    (record.departments || []).forEach((department) => names.add(department));
  });
  datalist.innerHTML = [...names].sort((a, b) => a.localeCompare(b, "ko")).map((name) => `<option value="${escapeHtml(name)}"></option>`).join("");
}

function renderMajorRecommendationOptions(select) {
  const currentValue = select.value;
  const optionValues = getAllMajorOptionValues();
  select.innerHTML = `
    <option value="">학과·계열 선택</option>
    ${majorOptionGroups
      .map(
        (group) => `
          <optgroup label="${escapeHtml(group.label)}">
            ${group.options.map((option) => `<option value="${escapeHtml(option)}">${escapeHtml(option)}</option>`).join("")}
          </optgroup>
        `
      )
      .join("")}
  `;
  if (optionValues.includes(currentValue)) {
    select.value = currentValue;
  }
}

function renderSubjectRecommendationOptions(select) {
  const currentValue = select.value;
  const subjects = getAllRecommendationSubjects();
  select.innerHTML = `<option value="">과목 선택</option>${subjects.map((subject) => `<option value="${escapeHtml(subject)}">${escapeHtml(subject)}</option>`).join("")}`;
  if (subjects.includes(currentValue)) select.value = currentValue;
}

function renderQuickMajorButtons() {
  const target = $("#quickMajorButtons");
  if (!target || target.dataset.bound === "true") return;
  const quickQueries = [
    { label: "공학", query: "공학" },
    { label: "의학·보건", query: "의학 보건계열" },
    { label: "간호", query: "간호학과" },
    { label: "보건·재활", query: "물리치료학과" },
    { label: "컴퓨터·AI", query: "컴퓨터" },
    { label: "반도체", query: "반도체" },
    { label: "생명·바이오", query: "생명" },
    { label: "인문·사회", query: "인문" },
    { label: "경영·법·행정", query: "경영" },
    { label: "지리·도시", query: "지리학과" },
    { label: "미디어·어문", query: "미디어" },
    { label: "교육", query: "교육학과" },
    { label: "지리교육", query: "지리교육과" }
  ];
  target.innerHTML = `<span>주요 계열</span>${quickQueries.map((item) => `<button type="button" data-major-query="${escapeHtml(item.query)}">${escapeHtml(item.label)}</button>`).join("")}`;
  target.dataset.bound = "true";
}

function bindMajorQueryButtons() {
  $all("[data-major-query]").forEach((button) => {
    button.onclick = () => {
      const query = button.dataset.majorQuery;
      const searchInput = $("#majorRecommendationSearch");
      const majorSelect = $("#majorRecommendationSelect");
      if (button.dataset.recommendationModeTarget) {
        state.recommendationMode = button.dataset.recommendationModeTarget;
      }
      searchInput.value = query;
      if (majorSelect) {
        majorSelect.value = getAllMajorOptionValues().includes(query) ? query : "";
      }
      renderRecommendationExplorer();
      if (button.dataset.recommendationModeTarget) {
        scrollRecommendationSectionIntoView();
      }
    };
  });
}

function scrollRecommendationSectionIntoView() {
  const target = $("#holistic-subjects") || $(".advanced-recommendation-tool");
  if (!target) return;
  window.requestAnimationFrame(() => {
    target.scrollIntoView({ block: "start", behavior: "smooth" });
  });
}

function getAllMajorOptionValues() {
  return majorOptionGroups.flatMap((group) => group.options);
}

function getRecordCoreSubjects(record) {
  return filterRecommendationDisplaySubjects(record.coreSubjects || []);
}

function getRecordRecommendedSubjects(record) {
  return filterRecommendationDisplaySubjects(record.recommendedSubjects || []);
}

function getRecordSuggestedSubjects(record) {
  const official = new Set([...getRecordCoreSubjects(record), ...getRecordRecommendedSubjects(record)].map((subject) => normalizeText(subject)));
  return filterRecommendationDisplaySubjects(record.suggestedSubjects || [])
    .filter((subject) => !official.has(normalizeText(subject)));
}

function filterRecommendationDisplaySubjects(subjects) {
  const seen = new Set();
  return (subjects || [])
    .filter((subject) => subject && !isCommonRecommendationSubject(subject) && !isGenericRecommendationSubject(subject))
    .filter((subject) => {
      const key = normalizeText(subject);
      if (seen.has(key)) return false;
      seen.add(key);
      return true;
    });
}

function isCommonRecommendationSubject(subject) {
  const target = normalizeText(subject);
  return commonRecommendationSubjects.some((commonSubject) => normalizeText(commonSubject) === target);
}

function isGenericRecommendationSubject(subject) {
  const target = normalizeText(subject);
  if (!target) return true;
  if (genericRecommendationSubjectLabels.some((label) => normalizeText(label) === target)) return true;
  return genericRecommendationSubjectPhrases.some((phrase) => target.includes(normalizeText(phrase)));
}

function getAllRecommendationSubjects() {
  const subjects = new Set();
  getRecommendationRecords().forEach((record) => {
    [...getRecordCoreSubjects(record), ...getRecordRecommendedSubjects(record), ...getRecordSuggestedSubjects(record)].forEach((subject) => subjects.add(subject));
  });
  return [...subjects].sort((a, b) => a.localeCompare(b, "ko"));
}

function getFeaturedRecommendationRecords() {
  const featuredIds = ["engineering_general_2028", "nursing_2028", "computer_ai_data_2028", "business_economics_2028", "education_general_2028", "life_food_environment_2028"];
  const records = getRecommendationRecords();
  return featuredIds.map((id) => records.find((record) => record.id === id)).filter(Boolean);
}

function getRecommendationRecords() {
  return recommendationData.records.filter((record) => {
    const haystack = normalizeText(getRecordMajorSearchTerms(record).join(" "));
    return !["사관", "육군사관", "국방", "군사"].some((word) => haystack.includes(normalizeText(word)));
  });
}

function getUniversityRecommendationRecords() {
  return (universityRecommendationData.records || []).filter((record) => {
    const hasSubjects = filterRecommendationDisplaySubjects([
      ...(record.coreSubjects || []),
      ...(record.recommendedSubjects || [])
    ]).length > 0;
    const haystack = normalizeText([record.university, record.department].join(" "));
    return hasSubjects && !["사관", "육군사관", "국방", "군사"].some((word) => haystack.includes(normalizeText(word)));
  });
}

function renderUniversityRecommendationResults(universityQuery, departmentQuery) {
  const normalizedUniversityQuery = normalizeUniversitySearchTerm(universityQuery);
  const normalizedDepartmentQuery = normalizeMajorSearchTerm(departmentQuery);
  if (!normalizedUniversityQuery && !normalizedDepartmentQuery) {
    return `
      <div class="empty-note">
        대학별 자료를 보려면 대학명과 학과명을 입력해 보세요. 예: 대학명 서울대 / 학과명 간호, 대학명 부산대 / 학과명 기계
      </div>
    `;
  }

  const matches = getUniversityRecommendationRecords()
    .filter((record) => recordMatchesUniversity(record, normalizedUniversityQuery, normalizedDepartmentQuery))
    .sort(compareUniversityRecommendationRecords);
  const visibleMatches = matches.slice(0, 30);

  return `
    <div class="recommendation-result-head">
      <h4>대학별 권장과목 검색 결과</h4>
      <span>${matches.length}개</span>
    </div>
    ${matches.length ? visibleMatches.map((record) => renderUniversityRecommendationCard(record)).join("") : renderEmptyRecommendation("조건에 맞는 대학별 권장과목을 찾지 못했습니다. 대학명은 줄이고, 학과명은 간호·경영·기계처럼 짧게 입력해 보세요. 자료에 구체 과목이 없는 학과는 학과·계열로 찾기도 함께 확인합니다.")}
    ${matches.length > visibleMatches.length ? `<div class="empty-note compact">결과가 많아 처음 ${visibleMatches.length}개만 보여줍니다. 대학명이나 학과명을 더 구체적으로 입력해 보세요.</div>` : ""}
  `;
}

function normalizeUniversitySearchTerm(value) {
  return normalizeText(value)
    .replace(/대학교/g, "대")
    .replace(/캠퍼스/g, "")
    .replace(/[()]/g, "");
}

function recordMatchesUniversity(record, universityQuery, departmentQuery) {
  const universityTarget = normalizeUniversitySearchTerm([record.university, record.local, record.region].join(" "));
  const departmentTarget = normalizeMajorSearchTerm(record.department || "");
  const combinedTarget = `${normalizeUniversitySearchTerm(record.university || "")}${departmentTarget}`;
  if (universityQuery && !departmentQuery && combinedTarget.includes(universityQuery)) return true;
  const universityMatch = !universityQuery || universityTarget.includes(universityQuery) || normalizeUniversitySearchTerm(record.university || "").includes(universityQuery);
  const departmentMatch = !departmentQuery || departmentTarget.includes(departmentQuery) || departmentQuery.includes(departmentTarget);
  return universityMatch && departmentMatch;
}

function compareUniversityRecommendationRecords(a, b) {
  return String(a.university || "").localeCompare(String(b.university || ""), "ko")
    || String(a.department || "").localeCompare(String(b.department || ""), "ko");
}

function renderUniversityRecommendationCard(record) {
  const coreSubjects = filterRecommendationDisplaySubjects(record.coreSubjects || []);
  const recommendedSubjects = filterRecommendationDisplaySubjects(record.recommendedSubjects || []);
  const note = getUniversityRecommendationNote(record);
  return `
    <article class="recommendation-card university-recommendation-card">
      <div class="recommendation-card-head">
        <div>
          <span class="label">${escapeHtml([record.region, record.local].filter(Boolean).join(" · "))}</span>
          <h4>${escapeHtml(record.university)} <span>${escapeHtml(record.department)}</span></h4>
        </div>
      </div>
      <div class="subject-badge-section">
        <b>핵심 과목</b>
        ${renderSubjectBadgeRow(coreSubjects, "core", "자료상 별도 핵심 과목이 확인되지 않았습니다.")}
      </div>
      <div class="subject-badge-section">
        <b>권장 과목</b>
        ${renderSubjectBadgeRow(recommendedSubjects, "recommended", "자료상 별도 권장 과목이 확인되지 않았습니다.")}
      </div>
      ${note ? `<p class="university-recommendation-note">${escapeHtml(note)}</p>` : ""}
    </article>
  `;
}

function getUniversityRecommendationNote(record) {
  const note = String(record.note || "").trim();
  if (!note || isGenericRecommendationSubject(note)) return "";
  return note;
}

function recordMatchesMajor(record, query) {
  const rawQuery = normalizeText(query);
  const normalizedQuery = normalizeMajorSearchTerm(query);
  if (!rawQuery && !normalizedQuery) return false;

  return getRecordMajorSearchTerms(record).some((term) => {
    const rawTerm = normalizeText(term);
    const normalizedTerm = normalizeMajorSearchTerm(term);
    const reverseRawMatch = rawTerm.length >= 4 && rawQuery.includes(rawTerm);
    const reverseNormalizedMatch = normalizedTerm.length >= 4 && normalizedQuery.includes(normalizedTerm);
    const forwardNormalizedMatch = normalizedQuery.length >= 3 && normalizedTerm.includes(normalizedQuery);
    return rawTerm.includes(rawQuery)
      || reverseRawMatch
      || (normalizedTerm && normalizedQuery && forwardNormalizedMatch)
      || (normalizedTerm && normalizedQuery && reverseNormalizedMatch);
  });
}

function getRecordMajorSearchTerms(record) {
  return [
    record.id,
    record.major,
    record.field,
    ...(record.aliases || []),
    ...(record.departments || [])
  ].filter(Boolean);
}

function normalizeMajorSearchTerm(value) {
  return normalizeText(value)
    .replace(/학과/g, "")
    .replace(/학부/g, "")
    .replace(/전공/g, "")
    .replace(/계열/g, "")
    .replace(/대학/g, "")
    .replace(/교육과/g, "교육")
    .replace(/교과교육/g, "교육");
}

function recordHasSubject(record, subject) {
  const target = normalizeText(subject);
  return [...getRecordCoreSubjects(record), ...getRecordRecommendedSubjects(record), ...getRecordSuggestedSubjects(record)].some((item) => normalizeText(item) === target);
}

function getRecordSubjectMatchType(record, subject) {
  const target = normalizeText(subject);
  if (getRecordCoreSubjects(record).some((item) => normalizeText(item) === target)) return "핵심 과목";
  if (getRecordRecommendedSubjects(record).some((item) => normalizeText(item) === target)) return "권장 과목";
  if (getRecordSuggestedSubjects(record).some((item) => normalizeText(item) === target)) return "추천과목";
  return "연결 과목";
}

function renderCourseNameWithInfo(course) {
  return `
    <span class="course-title-with-info">
      <strong>${escapeHtml(course.name)}</strong>
      <button class="subject-info-button" type="button"
        aria-label="${escapeHtml(`${course.name} 과목 안내 보기`)}"
        title="과목 안내 보기"
        data-subject-info
        data-subject-name="${escapeHtml(course.name)}"
        data-subject-area="${escapeHtml(course.area || "")}"
        data-subject-category="${escapeHtml(course.category || "")}">?</button>
    </span>
  `;
}

function openSubjectInfoPopup(course) {
  const modal = $("#subjectInfoModal");
  const label = $("#subjectInfoLabel");
  const title = $("#subjectInfoTitle");
  const content = $("#subjectInfoContent");
  if (!modal || !title || !content || !course.name) return;

  const relation = getSubjectRelationInfo(course);
  const guide = getSubjectGuideInfo(course, relation);
  const offerings = getSubjectGuideOfferings(course.name);
  if (label) label.textContent = "과목 선택 안내";
  title.textContent = course.name;
  content.innerHTML = `
    <div class="subject-guide-meta" aria-label="과목 기본 정보">
      <span>${escapeHtml(course.area || "교과 확인")}</span>
      <span>${escapeHtml(formatSubjectCategory(course.category))}</span>
      ${(guide.badges || []).map((badge) => `<span class="subject-guide-badge">${escapeHtml(badge)}</span>`).join("")}
    </div>
    <section class="subject-guide-opening">
      <b>우리학교 개설 정보</b>
      ${renderSubjectGuideOfferings(offerings, course.name)}
    </section>
    <div class="subject-guide-summary-grid">
      <section class="subject-guide-summary-card is-learning">
        <span>무엇을 배우나요</span>
        <p>${escapeHtml(guide.learning)}</p>
      </section>
      <section class="subject-guide-summary-card">
        <span>준비하면 좋은 역량</span>
        <div class="subject-guide-chip-list">
          ${guide.competencies.map((item) => `<span>${escapeHtml(item)}</span>`).join("")}
        </div>
      </section>
    </div>
    <section class="subject-guide-detail-section">
      <b>수업에서 해볼 수 있는 활동</b>
      <ul>${guide.activities.map((item) => `<li>${escapeHtml(item)}</li>`).join("")}</ul>
    </section>
    ${guide.relatedSubjects.length ? `
      <section class="subject-guide-detail-section">
        <b>함께 살펴볼 과목</b>
        <div class="subject-guide-related-list">
          ${guide.relatedSubjects.map((subject) => `<span>${escapeHtml(subject)}</span>`).join("")}
        </div>
      </section>
    ` : ""}
    <details class="subject-guide-major-details">
      <summary>관련 계열·학과 예시 보기</summary>
      <div class="subject-info-track-list">
        ${relation.tracks.map((track) => `
          <section class="subject-info-track-card">
            <strong>${escapeHtml(track.name)}</strong>
            <div class="subject-info-major-list">
              ${track.majors.map((major) => `<span>${escapeHtml(major)}</span>`).join("")}
            </div>
          </section>
        `).join("")}
      </div>
    </details>
    <p class="subject-guide-basis">안내 기준 · ${escapeHtml(guide.basisLabel)} · ${escapeHtml(formatDateLabel(subjectGuideData.updated))}</p>
    <p class="subject-info-caution">
      이 안내는 과목 선택을 돕는 참고 자료이며 대학의 공식 핵심·권장 과목을 뜻하지 않습니다. 실제 수업 내용과 개설 여부는 학교 안내를, 대학 반영 여부는 해당 학년도 대학 자료를 확인하세요. 희망 진로와 직접 연결되지 않는 과목도 질문·자료 해석·토론·협업을 통해 학업역량과 공동체역량을 보여줄 수 있습니다.
    </p>
  `;

  modal.hidden = false;
  document.body.classList.add("modal-open");
  $(".subject-info-close")?.focus();
}

function getSubjectGuideInfo(course, relation) {
  const areaProfile = subjectGuideData.areaProfiles?.[course.area] || {};
  const groupProfile = subjectGuideData.groupProfiles?.[relation.title] || {};
  return {
    learning: groupProfile.learning || relation.description || areaProfile.learning || getSubjectConnectionPoint(course),
    activities: groupProfile.activities || areaProfile.activities || ["수업 개념에서 질문 만들기", "자료를 찾아 근거 비교하기", "결과와 한계를 자신의 말로 정리하기"],
    competencies: groupProfile.competencies || areaProfile.competencies || ["질문 만들기", "자료 해석", "성찰"],
    relatedSubjects: groupProfile.relatedSubjects || [],
    badges: groupProfile.badges || ["과목 탐색"],
    basisLabel: subjectGuideData.basisLabel || "교육과정·전공 연계 참고"
  };
}

function formatSubjectCategory(category) {
  const value = String(category || "").trim();
  if (!value) return "과목 유형 확인";
  if (value.endsWith("선택") || value === "공통") return value;
  return `${value}선택`;
}

function getSubjectGuideOfferings(subject) {
  return recommendationCurriculumPlans.flatMap((plan) => {
    return getSubjectCurriculumMatches(subject, plan.key).map((match) => ({
      ...plan,
      name: match.name,
      semesters: match.semesters || []
    }));
  });
}

function renderSubjectGuideOfferings(offerings, subject) {
  if (!offerings.length) {
    return `<p class="subject-guide-opening-empty">우리학교 교육과정에서 개설 정보를 찾지 못했습니다. 공동교육과정과 학교 밖 교육 가능 여부를 상담해 보세요.</p>`;
  }

  return `
    <div class="subject-guide-offering-list">
      ${offerings.map((offering) => {
        const semesterLabel = offering.semesters.map((semesterKey) => getSemesterLabel(semesterKey)).join(" · ") || "학년·학기 확인";
        const actualName = normalizeText(offering.name) === normalizeText(subject) ? "" : ` · ${offering.name}`;
        return `<span class="subject-guide-offering ${offering.className}"><b>${escapeHtml(offering.shortLabel)}</b> · ${escapeHtml(semesterLabel)}${escapeHtml(actualName)}</span>`;
      }).join("")}
    </div>
  `;
}

function openSelfEvalHelpPopup(helpKey) {
  if (helpKey === "advanced") {
    openSelfEvalAdvancedHelpPopup();
    return;
  }

  const modal = $("#subjectInfoModal");
  const label = $("#subjectInfoLabel");
  const title = $("#subjectInfoTitle");
  const content = $("#subjectInfoContent");
  const help = selfEvalHelp[helpKey];
  if (!modal || !title || !content || !help) return;

  const course = getSelfEvaluationCourse();
  const areaExample = getSelfEvalSubjectAreaExample(course);

  if (label) label.textContent = "자기평가서 도움말";
  title.textContent = help.title;
  content.innerHTML = `
    <p class="subject-info-copy">${escapeHtml(help.guide)}</p>
    <div class="self-help-example-grid">
      <article>
        <b>좋은 예시</b>
        <p>${escapeHtml(help.good)}</p>
      </article>
      <article>
        <b>피해야 할 예시</b>
        <p>${escapeHtml(help.avoid)}</p>
      </article>
    </div>
    <div class="self-help-section">
      <b>세특 반영 예시</b>
      <p>${escapeHtml(help.sechuk)}</p>
      <span>${escapeHtml(help.how)}</span>
    </div>
    <div class="self-help-section subject-case">
      <b>${escapeHtml(areaExample.title)}</b>
      <p><strong>초점:</strong> ${escapeHtml(areaExample.focus)}</p>
      <p><strong>자기평가 예시:</strong> ${escapeHtml(areaExample.self)}</p>
      <p><strong>세특형 예시:</strong> ${escapeHtml(areaExample.sechuk)}</p>
    </div>
    ${renderSelfEvalConceptHelpSection(course)}
    <div class="self-help-deep-row">
      <button class="ghost-button self-eval-guide-button" type="button" data-self-help="advanced">학년별 심화 도움말 보기</button>
    </div>
  `;

  modal.hidden = false;
  document.body.classList.add("modal-open");
  $(".subject-info-close")?.focus();
}

function openSelfEvalAdvancedHelpPopup() {
  const modal = $("#subjectInfoModal");
  const label = $("#subjectInfoLabel");
  const title = $("#subjectInfoTitle");
  const content = $("#subjectInfoContent");
  if (!modal || !title || !content) return;

  const course = getSelfEvaluationCourse();

  if (label) label.textContent = "심화 도움말";
  title.textContent = "좋은 자기평가서로 바꾸는 과정";
  content.innerHTML = `
    <p class="subject-info-copy">
      좋은 세특은 활동을 많이 했다는 말보다, 수업 개념에서 생긴 질문이 탐구 과정과 결과, 후속 활동으로 이어졌다는 흐름을 보여줍니다.
      아래 예시는 그대로 옮겨 쓰는 문장이 아니라, 내 활동을 어떻게 구체화할지 보는 참고 틀입니다.
    </p>
    ${renderSelfEvalAdvancedGradeExamples(course)}
    <div class="advanced-sechuk-list">
      ${selfEvalAdvancedTransformExamples.map((item) => `
        <article>
          <span class="label">${escapeHtml(item.title)}</span>
          <div>
            <b>아쉬운 표현</b>
            <p>${escapeHtml(item.weak)}</p>
          </div>
          <div>
            <b>자기평가서에서 이렇게</b>
            <p>${escapeHtml(item.better)}</p>
          </div>
          <div>
            <b>세특형 표현 예시</b>
            <p>${escapeHtml(item.sechuk)}</p>
          </div>
        </article>
      `).join("")}
    </div>
    <p class="subject-info-caution">
      세특은 교사가 학생의 실제 수업 참여와 탐구 과정을 관찰해 기록하는 자료입니다. 이 예시는 학생이 자기평가서에 자신의 경험을 구체적으로 정리하도록 돕는 참고용입니다.
    </p>
  `;

  modal.hidden = false;
  document.body.classList.add("modal-open");
  $(".subject-info-close")?.focus();
}

function getSelfEvalSubjectAreaExample(course) {
  const area = course?.area || "";
  return selfEvalSubjectAreaExamples[area] || selfEvalSubjectAreaExamples.교양;
}

function renderSelfEvalConceptHelpSection(course) {
  const profile = getSelfEvalConceptProfile(course);
  const grade = getSelfEvalCourseGrade(course);
  const gradeLabel = grade ? `${grade}학년` : "현재 과목";
  const currentExample = getSelfEvalGradeExample(profile, grade);
  const currentDepth = profile.gradeDepth[grade] || profile.gradeDepth[1] || "";

  return `
    <div class="self-help-section concept-case">
      <b>개념 기반 탐구로 바꾸면</b>
      <p><strong>${escapeHtml(profile.label)} 핵심 개념:</strong> ${escapeHtml(profile.concept)}</p>
      <p><strong>탐구 질문 예시:</strong> ${escapeHtml(profile.question)}</p>
      <p><strong>${escapeHtml(gradeLabel)} 심화 방향:</strong> ${escapeHtml(currentDepth)}</p>
      <p><strong>${escapeHtml(gradeLabel)} 자기평가 예시:</strong> ${escapeHtml(currentExample.self)}</p>
      <p><strong>세특형 예시:</strong> ${escapeHtml(currentExample.sechuk)}</p>
      <span>수업·수행평가 내용을 먼저 바탕으로 삼고, 추가 탐구는 교과 선생님과 상의해 수업 개념에서 작게 확장하는 방식이 좋습니다.</span>
    </div>
  `;
}

function renderSelfEvalAdvancedGradeExamples(course) {
  const profile = getSelfEvalConceptProfile(course);
  const currentGrade = getSelfEvalCourseGrade(course);

  return `
    <div class="self-help-section concept-case advanced-grade-case">
      <b>${escapeHtml(profile.label)} 계열 1·2·3학년 심화 예시</b>
      <p class="advanced-grade-intro">
        같은 교과군이라도 학년이 올라갈수록 단순 이해에서 자료 비교, 한계 분석, 후속 탐구 설계로 깊어져야 합니다.
        학생은 문장을 베끼기보다 아래 구조를 자기 수업 활동에 맞게 바꾸어 쓰면 됩니다.
      </p>
      <div class="concept-depth-list detailed" aria-label="${escapeHtml(profile.label)} 학년별 자기평가 예시">
        ${[1, 2, 3].map((grade) => {
          const example = getSelfEvalGradeExample(profile, grade);
          return `
            <article class="${grade === currentGrade ? "active" : ""}">
              <b>${grade}학년</b>
              <p><strong>심화 방향</strong> ${escapeHtml(profile.gradeDepth[grade] || "")}</p>
              <p><strong>자기평가</strong> ${escapeHtml(example.self)}</p>
              <p><strong>세특형</strong> ${escapeHtml(example.sechuk)}</p>
            </article>
          `;
        }).join("")}
      </div>
      <div class="self-help-convert-box">
        <b>내 활동으로 바꾸는 순서</b>
        <p>수업 개념 1개를 고른 뒤, 수업·수행평가에서 실제로 한 활동을 붙입니다. 그다음 자료나 관찰 결과를 하나 적고, 알게 된 점과 한계를 구분하면 자기평가서가 훨씬 자연스러워집니다.</p>
      </div>
    </div>
  `;
}

function getSelfEvalConceptProfile(course) {
  const key = getSelfEvalConceptProfileKey(course);
  return { key, ...(selfEvalConceptProfiles[key] || selfEvalConceptProfiles.교양) };
}

function getSelfEvalConceptProfileKey(course) {
  const courseName = normalizeText(course?.name || "");
  const matchedRule = selfEvalConceptKeywordRules.find((rule) =>
    rule.keywords.some((keyword) => courseName.includes(normalizeText(keyword)))
  );
  if (matchedRule) return matchedRule.profile;

  const area = course?.area || "";
  return selfEvalConceptProfiles[area] ? area : "교양";
}

function getSelfEvalGradeExample(profile, grade) {
  const examples = selfEvalProfileGradeExamples[profile.key] || {};
  return examples[grade] || examples[1] || {
    self: profile.self || "",
    sechuk: profile.sechuk || ""
  };
}

function getSelfEvalCourseGrade(course) {
  const grades = (course?.semesters || [])
    .map((semesterKey) => Number(String(semesterKey).split("-")[0]))
    .filter((grade) => Number.isFinite(grade))
    .sort((a, b) => a - b);
  return grades[0] || 1;
}

function closeSubjectInfoPopup() {
  const modal = $("#subjectInfoModal");
  if (!modal || modal.hidden) return;
  modal.hidden = true;
  document.body.classList.remove("modal-open");
}

function getSubjectRelationInfo(course) {
  const subjectNames = getSubjectRelationCompareNames(course.name);
  const exactGroup = subjectRelationGroups.find((group) => {
    const groupSubjects = group.subjects.map((subject) => normalizeText(subject));
    return groupSubjects.some((subject) => subjectNames.includes(subject));
  });

  if (exactGroup) return exactGroup;

  const courseName = normalizeText(course.name);
  const keywordGroup = subjectRelationGroups.find((group) => {
    const keywords = group.keywords || [];
    return keywords.some((keyword) => courseName.includes(normalizeText(keyword)));
  });

  if (keywordGroup) return keywordGroup;

  return getFallbackRelatedTrackGroup(course);
}

function getFallbackRelatedTrackGroup(course) {
  const areaTracks = {
    국어: [
      { name: "인문계열", majors: ["국어국문학과", "문예창작학과", "철학과"] },
      { name: "교육·미디어계열", majors: ["국어교육과", "미디어커뮤니케이션학과", "문화콘텐츠학과"] }
    ],
    수학: [
      { name: "자연과학계열", majors: ["수학과", "통계학과", "물리학과"] },
      { name: "공학·데이터계열", majors: ["컴퓨터공학과", "인공지능학과", "산업공학과"] }
    ],
    영어: [
      { name: "어문·국제계열", majors: ["영어영문학과", "국제학부", "국제통상학과"] },
      { name: "미디어·서비스계열", majors: ["미디어커뮤니케이션학과", "관광학과", "호텔경영학과"] }
    ],
    사회: [
      { name: "사회과학계열", majors: ["정치외교학과", "행정학과", "사회학과"] },
      { name: "상경·복지계열", majors: ["경영학과", "경제학과", "사회복지학과"] }
    ],
    과학: [
      { name: "자연과학계열", majors: ["물리학과", "화학과", "생명과학과", "지구과학과"] },
      { name: "공학·보건계열", majors: ["기계공학과", "화학공학과", "간호학과", "환경공학과"] }
    ],
    체육: [
      { name: "체육계열", majors: ["체육학과", "스포츠과학과", "체육교육과"] },
      { name: "보건·재활계열", majors: ["물리치료학과", "운동재활학과", "스포츠의학 관련 학과"] }
    ],
    예술: [
      { name: "예술·디자인계열", majors: ["미술학과", "음악학과", "디자인학과"] },
      { name: "콘텐츠계열", majors: ["영상콘텐츠학과", "문화콘텐츠학과", "공연예술학과"] }
    ],
    교양: [
      { name: "융합·진로탐색계열", majors: ["자율전공학부", "자유전공학부", "융합학부"] },
      { name: "교육·상담계열", majors: ["교육학과", "상담심리학과", "청소년학과"] }
    ]
  };

  return {
    title: `${course.area || "교과"} 관련 과목`,
    subjects: [course.name],
    description: getSubjectConnectionPoint(course),
    tracks: areaTracks[course.area] || [
      { name: "융합계열", majors: ["자율전공학부", "자유전공학부", "융합학부"] },
      { name: "탐구계열", majors: ["교육학과", "사회과학계열", "문화콘텐츠학과"] }
    ]
  };
}

function getSubjectRelationCompareNames(subject) {
  const names = new Set([subject]);
  recommendationCurriculumPlans.forEach((plan) => {
    getSubjectCompareNames(subject, plan.key).forEach((name) => names.add(name));
  });
  return [...names].map((name) => normalizeText(name)).filter(Boolean);
}

function getSubjectConnectionPoint(course) {
  const name = course.name || "이 과목";
  const area = course.area || "교과";
  const category = course.category || "과목";
  return `${name}은 ${area} ${category}의 관점에서 진로 관심을 넓혀 볼 수 있는 과목입니다. 아래 학과들은 이 과목에서 다루는 개념이나 탐구 방식과 연결해 생각해볼 수 있는 예시입니다.`;
}

function renderRecommendationCard(record) {
  return `
    <article class="recommendation-card">
      <div class="recommendation-card-head">
        <div>
          <span class="label">${escapeHtml(record.field)}</span>
          <h4>${escapeHtml(record.major)}</h4>
        </div>
      </div>
      <div class="subject-badge-section">
        <b>핵심 과목</b>
        ${renderSubjectBadgeRow(getRecordCoreSubjects(record), "core", "자료상 별도 핵심 과목이 확인되지 않았습니다.")}
      </div>
      <div class="subject-badge-section">
        <b>권장 과목</b>
        ${renderSubjectBadgeRow(getRecordRecommendedSubjects(record), "recommended", "자료상 별도 권장 과목이 확인되지 않았습니다.")}
      </div>
      <div class="subject-badge-section suggested">
        <b>추천과목 <span>참고 과목</span></b>
        ${renderSubjectBadgeRow(getRecordSuggestedSubjects(record), "suggested", "추가 추천과목은 상담 과정에서 보완합니다.")}
      </div>
      ${renderRecommendationDepartmentExamples(record)}
      <p>${escapeHtml(record.note)}</p>
    </article>
  `;
}

function renderRecommendationDepartmentExamples(record) {
  const departments = (record.departments || []).filter(Boolean).slice(0, 8);
  if (!departments.length) return "";
  return `
    <div class="recommendation-department-examples">
      <b>조회 가능한 학과 예시</b>
      <div>${departments.map((department) => `<span>${escapeHtml(department)}</span>`).join("")}</div>
    </div>
  `;
}

function renderSubjectBadgeRow(subjects, importance, emptyMessage) {
  if (!subjects.length) return `<div class="empty-note compact">${escapeHtml(emptyMessage)}</div>`;
  const groups = groupRecommendationSubjects(subjects);
  return `
    <div class="subject-badge-groups">
      ${groups.map((group) => `
        <div class="subject-badge-group ${escapeHtml(group.key)}">
          <span class="subject-badge-group-title">${escapeHtml(group.label)}</span>
          <div class="subject-badge-row">${renderRecommendedSubjectBadges(group.subjects, importance)}</div>
        </div>
      `).join("")}
    </div>
  `;
}

function renderSubjectMatchCard(record, subject) {
  const type = getRecordSubjectMatchType(record, subject);
  const note = type === "추천과목"
    ? `${subject}은 학과 탐색을 넓혀 보기 위한 참고 연결입니다. 대학 공식 핵심·권장 과목은 학과별 자료에서 따로 확인합니다.`
    : record.note;
  return `
    <article class="subject-match-card">
      <div>
        <span class="label">${escapeHtml(type)}</span>
        <h4>${escapeHtml(record.major)}</h4>
        <p>${escapeHtml(note)}</p>
      </div>
      <button type="button" data-major-query="${escapeHtml(record.major)}" data-recommendation-mode-target="major">학과로 보기</button>
    </article>
  `;
}

function groupRecommendationSubjects(subjects) {
  const grouped = recommendationSubjectCategories.map((category) => ({ ...category, subjects: [] }));
  subjects.forEach((subject) => {
    const categoryKey = getRecommendationSubjectCategory(subject);
    const group = grouped.find((item) => item.key === categoryKey) || grouped[grouped.length - 1];
    group.subjects.push(subject);
  });
  return grouped.filter((group) => group.subjects.length);
}

function getRecommendationSubjectCategory(subject) {
  const target = normalizeText(subject);
  const matched = recommendationSubjectCategories.find((category) => {
    if (category.key === "other") return false;
    return category.keywords.some((keyword) => target.includes(normalizeText(keyword)));
  });
  return matched?.key || "other";
}

function renderRecommendedSubjectBadges(subjects, importance) {
  return subjects
    .map((subject) => {
      const availability = getSubjectCurriculumAvailability(subject);
      if (availability.length) {
        return renderSubjectAvailabilityBadges(subject, availability);
      }

      const missingClass = importance === "core" ? "missing-core" : "missing-recommended";
      return `<span class="subject-badge ${missingClass}">${escapeHtml(subject)}<small>${getMissingSubjectLabel()}</small></span>`;
    })
    .join("");
}

function renderSubjectAvailabilityBadges(subject, availability) {
  return groupSubjectAvailabilityByMatchedName(subject, availability)
    .map((group) => {
      const badgeClass = group.plans.length === 1 ? group.plans[0].className : "grade-multi";
      return `<span class="subject-badge ${badgeClass}">${escapeHtml(group.name)}<small>${renderMatchedSubjectAvailabilityLabel(group.plans)}</small></span>`;
    })
    .join("");
}

function groupSubjectAvailabilityByMatchedName(subject, availability) {
  const groups = new Map();
  const sourceKey = normalizeText(subject);

  availability.forEach((plan) => {
    plan.matches.forEach((match) => {
      const matchedName = match.name;
      const key = `${normalizeText(matchedName)}-${plan.standard}`;
      if (!groups.has(key)) {
        groups.set(key, {
          name: matchedName,
          exact: normalizeText(matchedName) === sourceKey,
          plans: []
        });
      }
      groups.get(key).plans.push({
        ...plan,
        courseGradeLabel: getCourseGradeLabelFromSemesters(match.semesters)
      });
    });
  });

  return [...groups.values()]
    .map((group) => ({
      ...group,
      plans: dedupePlans(group.plans)
    }))
    .sort((a, b) => {
      if (a.exact !== b.exact) return a.exact ? -1 : 1;
      return getFirstPlanOrder(a.plans) - getFirstPlanOrder(b.plans) || a.name.localeCompare(b.name, "ko");
    });
}

function dedupePlans(plans) {
  const seen = new Set();
  return plans.filter((plan) => {
    const key = `${plan.key}-${plan.courseGradeLabel}`;
    if (seen.has(key)) return false;
    seen.add(key);
    return true;
  });
}

function getFirstPlanOrder(plans) {
  return Math.min(...plans.map((plan) => recommendationCurriculumPlans.findIndex((item) => item.key === plan.key)).filter((index) => index >= 0));
}

function getMissingSubjectLabel() {
  const selectedPlan = $("#recommendationPlanFilter")?.value || "all";
  if (selectedPlan === "incoming2024") return "미개설(15)";
  if (selectedPlan === "incoming2025" || selectedPlan === "incoming2026") return "미개설(22)";
  return "미개설(22·15)";
}

function renderEmptyRecommendation(message) {
  return `<div class="empty-note">${escapeHtml(message)}</div>`;
}

function getSubjectCurriculumAvailability(subject) {
  const selectedPlan = $("#recommendationPlanFilter")?.value || "all";
  const plans = selectedPlan === "all"
    ? recommendationCurriculumPlans
    : recommendationCurriculumPlans.filter((plan) => plan.key === selectedPlan);
  return plans
    .map((plan) => ({ ...plan, matches: getSubjectCurriculumMatches(subject, plan.key) }))
    .filter((plan) => plan.matches.length);
}

function renderMatchedSubjectAvailabilityLabel(plans) {
  if (plans.length === 1) return renderPlanCourseLabel(plans[0]);
  return `<span class="availability-marks">${plans.map((plan) => `<span class="availability-mark ${plan.className}">${renderPlanCourseLabel(plan)}</span>`).join("")}</span>`;
}

function renderPlanCourseLabel(plan) {
  return escapeHtml(`${plan.shortLabel} · ${plan.courseGradeLabel || "학년 확인"}`);
}

function getSubjectCurriculumMatches(subject, planKey) {
  const schoolSubjects = getSchoolSubjectMap(planKey);
  const matched = getSubjectCompareNames(subject, planKey)
    .flatMap((name) => schoolSubjects.get(normalizeText(name)) || [])
    .map((course) => ({
      name: course.name,
      semesters: course.semesters || []
    }));
  return dedupeSubjectMatches(matched);
}

function dedupeSubjectMatches(matches) {
  const seen = new Set();
  return matches.filter((match) => {
    const key = `${normalizeText(match.name)}-${(match.semesters || []).join("|")}`;
    if (seen.has(key)) return false;
    seen.add(key);
    return true;
  });
}

function getCourseGradeLabelFromSemesters(semesters) {
  const grades = [...new Set((semesters || []).map((semesterKey) => String(semesterKey).split("-")[0]).filter(Boolean))];
  if (!grades.length) return "";
  if (grades.length === 1) return `${grades[0]}학년`;
  return `${grades.join("·")}학년`;
}

function getSchoolSubjectMap(planKey) {
  if (!getSchoolSubjectMap.cache) getSchoolSubjectMap.cache = {};
  if (getSchoolSubjectMap.cache[planKey]) return getSchoolSubjectMap.cache[planKey];
  const subjects = new Map();
  const plan = curriculumData.plans?.[planKey];
  if (plan) {
    plan.courses.forEach((course) => {
      const key = normalizeText(course.name);
      if (!subjects.has(key)) subjects.set(key, []);
      subjects.get(key).push(course);
    });
  }
  getSchoolSubjectMap.cache[planKey] = subjects;
  return subjects;
}

function getSubjectCompareNames(subject, planKey) {
  const plan = recommendationCurriculumPlans.find((item) => item.key === planKey);
  const to2022 = {
    "미적분": ["미적분Ⅰ", "미적분Ⅱ"],
    "수학Ⅰ": ["대수"],
    "수학Ⅱ": ["미적분Ⅰ"],
    "물리학Ⅰ": ["물리학"],
    "화학Ⅰ": ["화학"],
    "생명과학Ⅰ": ["생명과학"],
    "지구과학Ⅰ": ["지구과학"],
    "화법과 작문": ["화법과 언어", "독서와 작문"],
    "독서": ["독서와 작문", "주제 탐구 독서"],
    "영어Ⅰ": ["공통영어1", "공통영어2"],
    "영어Ⅱ": ["공통영어1", "공통영어2"],
    "세계사(지리)": ["세계사", "세계시민과 지리"],
    "세계지리": ["세계시민과 지리"],
    "심리학": ["인간과 심리"]
  };
  const to2015 = {
    "대수": ["수학Ⅰ"],
    "미적분Ⅰ": ["수학Ⅱ"],
    "미적분Ⅱ": ["미적분"],
    "물리학": ["물리학Ⅰ", "물리학Ⅱ"],
    "화학": ["화학Ⅰ", "화학Ⅱ"],
    "생명과학": ["생명과학Ⅰ", "생명과학Ⅱ"],
    "지구과학": ["지구과학Ⅰ", "지구과학Ⅱ"],
    "화법과 언어": ["화법과 작문"],
    "독서와 작문": ["독서"],
    "주제 탐구 독서": ["독서"],
    "공통영어1": ["영어Ⅰ"],
    "공통영어2": ["영어Ⅱ"],
    "세계시민과 지리": ["세계지리"],
    "인간과 심리": ["심리학"]
  };
  const equivalents = plan?.standard === "2015" ? to2015 : to2022;
  return [...new Set([subject, ...(equivalents[subject] || [])])];
}

function normalizeText(value) {
  return String(value || "")
    .normalize("NFC")
    .replaceAll("Ⅰ", "1")
    .replaceAll("ⅰ", "1")
    .replaceAll("Ⅱ", "2")
    .replaceAll("ⅱ", "2")
    .replaceAll("Ⅲ", "3")
    .replaceAll("ⅲ", "3")
    .toLowerCase()
    .replace(/[^0-9a-z가-힣]/g, "");
}

function getPlannerCurriculumPlan() {
  return curriculumData.plans?.[state.plannerPlan] || curriculumData.plans?.incoming2026 || { courses: [] };
}

function getPlannerSelectableCourses() {
  const plan = getPlannerCurriculumPlan();
  return (plan.courses || [])
    .filter((course) => isPlannerChoiceCourse(course))
    .sort((a, b) => a.row - b.row);
}

function getPlannerFixedCourses() {
  const plan = getPlannerCurriculumPlan();
  return (plan.courses || [])
    .filter((course) => isPlannerFixedCourse(course))
    .sort((a, b) => a.row - b.row);
}

function isPlannerFixedCourse(course) {
  return course.section.includes("학교지정") && !courseHasChoiceMarker(course, course.semesters || []);
}

function isPlannerChoiceCourse(course) {
  return (
    course.section.includes("선택") ||
    course.section.includes("공동교육") ||
    course.section.includes("추가교육") ||
    courseHasChoiceMarker(course, course.semesters || [])
  );
}

function getCourseSemestersForGrade(course, grade) {
  return [...new Set((course.semesters || [])
    .filter((semesterKey) => semesterKey.startsWith(`${grade}-`))
    .map((semesterKey) => semesterKey.split("-")[1])
    .filter(Boolean))]
    .sort();
}

function getPlannerCourse(courseId) {
  const activePlanCourse = getPlannerCurriculumPlan().courses?.find((course) => course.id === courseId);
  if (activePlanCourse) return activePlanCourse;

  for (const plan of Object.values(curriculumData.plans || {})) {
    const matched = plan.courses?.find((course) => course.id === courseId);
    if (matched) return matched;
  }

  return getCourse(courseId);
}

function getPlannerCourseCredits(course, grade, semester) {
  const semesterKey = `${grade}-${semester}`;
  const markerCredit = Number(course.markers?.[semesterKey]);
  if (Number.isFinite(markerCredit) && markerCredit > 0) return markerCredit;

  const semesterCount = Math.max(1, course.semesters?.length || 1);
  return Math.max(1, Math.round(Number(course.credits || 1) / semesterCount));
}

function getPlannerSectionLabel(section = "") {
  if (section.includes("학생선택")) return "학생선택";
  if (section.includes("지정선택") || section.includes("학교지정선택")) return "지정선택";
  if (section.includes("학교지정")) return "지정선택";
  if (section.includes("공동교육")) return "공동교육과정";
  if (section.includes("추가교육")) return "추가교육과정";
  return section || "선택과목";
}

function getAdditionalCourseCategoryLabel(category = "") {
  const value = String(category || "").trim();
  if (!value) return "선택과목";
  if (value.includes("선택") || value === "공통") return value;
  if (["일반", "진로", "융합"].includes(value)) return `${value}선택`;
  return value;
}

function getAdditionalCourseCatalog() {
  const coursesFromCurriculum = Object.values(curriculumData.plans || {})
    .flatMap((plan) => plan.courses || [])
    .filter((course) => course.name)
    .filter((course) => !commonRecommendationSubjects.some((subject) => normalizeText(subject) === normalizeText(course.name)))
    .filter((course) => !String(course.category || "").includes("공통"))
    .map((course) => ({
      name: course.name,
      area: course.area || "기타",
      category: getAdditionalCourseCategoryLabel(course.category || course.section)
    }));

  const merged = [...coursesFromCurriculum, ...additionalCourseCatalogSeed];
  const seen = new Map();

  merged.forEach((course) => {
    const name = String(course.name || "").trim();
    if (!name) return;
    const item = {
      name,
      area: String(course.area || "기타").trim(),
      category: getAdditionalCourseCategoryLabel(course.category)
    };
    const key = normalizeText(`${item.name}|${item.area}|${item.category}`);
    if (!seen.has(key)) seen.set(key, item);
  });

  return [...seen.values()].sort((a, b) => {
    const areaCompare = a.area.localeCompare(b.area, "ko");
    if (areaCompare) return areaCompare;
    const categoryCompare = a.category.localeCompare(b.category, "ko");
    if (categoryCompare) return categoryCompare;
    return a.name.localeCompare(b.name, "ko");
  });
}

function getAdditionalCourseByName(name) {
  const target = normalizeText(name);
  if (!target) return null;
  return getAdditionalCourseCatalog().find((course) => normalizeText(course.name) === target) || null;
}

function renderAdditionalCourseOptions() {
  return getAdditionalCourseCatalog()
    .map((course) => `
      <option value="${escapeAttribute(course.name)}" label="${escapeAttribute(`${course.area} · ${course.category}`)}"></option>
    `)
    .join("");
}

function getPlannerChoiceGroupsForGrade(grade) {
  const plan = getPlannerCurriculumPlan();
  return ["1", "2"].flatMap((semester) => buildCurriculumChoiceGroupsForSemester(plan.courses || [], `${grade}-${semester}`));
}

function getPlannerChoiceGroupForCourse(course, semesterKey) {
  return buildCurriculumChoiceGroupsForSemester(getPlannerCurriculumPlan().courses || [], semesterKey)
    .find((group) => group.courses.some((item) => item.id === course.id)) || null;
}

function getPlannerChoiceLimit(group) {
  if (!group) return Infinity;
  const match = String(group.choiceText || "").match(/(\d+)/);
  return match ? Number(match[1]) : Infinity;
}

function getPlannedItemsForSemester(grade, semester) {
  const semesterPlan = state.plan[grade]?.[semester];
  if (!semesterPlan) return [];
  return [...semesterPlan.regular, ...semesterPlan.joint];
}

function isCoursePlanned(courseId, grade, semester) {
  return getPlannedItemsForSemester(grade, semester).some((item) => item.id === courseId);
}

function getPlannedCountForChoiceGroup(group) {
  if (!group) return 0;
  const [grade, semester] = group.semesterKey.split("-");
  const courseIds = new Set(group.courses.map((course) => course.id));
  return getPlannedItemsForSemester(grade, semester).filter((item) => courseIds.has(item.id)).length;
}

function getPlannerCourseActionState(course, grade, semester) {
  const semesterKey = `${grade}-${semester}`;
  const group = getPlannerChoiceGroupForCourse(course, semesterKey);
  const selected = isCoursePlanned(course.id, grade, semester);
  const selectedCount = getPlannedCountForChoiceGroup(group);
  const limit = getPlannerChoiceLimit(group);
  const limitReached = Number.isFinite(limit) && selectedCount >= limit;
  const disabledByLimit = Boolean(group) && limitReached && !selected;

  return {
    group,
    selected,
    selectedCount,
    limit,
    disabledByLimit,
    addDisabled: selected || disabledByLimit,
    removeDisabled: !selected
  };
}

function setPlannerMode(mode) {
  state.plannerMode = ["choice", "mine"].includes(mode) ? mode : "fixed";
  renderPlannerModeState();
  saveState(false);
  const target = $("#planner");
  if (target) target.scrollIntoView({ block: "start", behavior: "smooth" });
}

function renderPlannerModeState() {
  const isChoice = state.plannerMode === "choice";
  const isMine = state.plannerMode === "mine";
  const fixedView = $("#plannerFixedView");
  const mineView = $("#plannerMineView");
  const choiceView = $("#plannerChoiceView");
  if (fixedView) fixedView.hidden = isChoice || isMine;
  if (mineView) mineView.hidden = !isMine;
  if (choiceView) choiceView.hidden = !isChoice;

  $("#plannerFixedModeButton")?.classList.toggle("active", !isChoice && !isMine);
  $("#plannerChoiceModeButton")?.classList.toggle("active", isChoice);
  $("#plannerMineModeButton")?.classList.toggle("active", isMine);
}

function renderPlannerFixedView() {
  const target = $("#plannerFixedView");
  if (!target) return;
  const fixedCourses = getPlannerFixedCourses();

  target.innerHTML = `
    <section class="planner-fixed-panel">
      <div class="planner-fixed-head">
        <div>
          <span class="label">학교지정 과목</span>
          <h3>${escapeHtml(getCurriculumPlanLabel(state.plannerPlan))}</h3>
          <p>선택하지 않아도 기본으로 편성된 과목입니다. 먼저 이 흐름을 확인한 뒤 선택과목을 고릅니다.</p>
        </div>
        <button class="primary-button" id="fixedToChoiceButton" type="button">선택과목 고르기</button>
      </div>
      <div class="fixed-curriculum-grid">
        ${["1", "2", "3"].map((grade) => renderFixedGradeBlock(fixedCourses, grade)).join("")}
      </div>
    </section>
  `;

  $("#fixedToChoiceButton")?.addEventListener("click", () => setPlannerMode("choice"));
}

function renderPlannerMineView() {
  const target = $("#plannerMineView");
  if (!target) return;
  const fixedCourses = getPlannerFixedCourses();

  target.innerHTML = `
    <section class="planner-mine-panel">
      <div class="planner-fixed-head">
        <div>
          <span class="label">내 교육과정</span>
          <h3>${escapeHtml(getCurriculumPlanLabel(state.plannerPlan))}</h3>
          <p>학교지정 과목과 내가 선택한 과목을 학년·학기별로 함께 확인합니다.</p>
        </div>
      </div>
      <div class="mine-curriculum-grid">
        ${["1", "2", "3"].map((grade) => renderMineGradeBlock(fixedCourses, grade)).join("")}
      </div>
      <datalist id="additionalCourseOptions">
        ${renderAdditionalCourseOptions()}
      </datalist>
      <div class="mine-export-actions">
        <button class="ghost-button" id="mineSaveButton" type="button">저장하기</button>
        <button class="primary-button" id="minePdfButton" type="button">PDF 내보내기</button>
      </div>
    </section>
  `;

  $("#mineSaveButton")?.addEventListener("click", () => saveState(true));
  $("#minePdfButton")?.addEventListener("click", exportMineCurriculumPdf);
  bindAdditionalCourseControls();
}

function renderMineGradeBlock(fixedCourses, grade) {
  return `
    <article class="mine-grade-card">
      <h4>${grade}학년 과정</h4>
      <div class="fixed-semester-grid">
        ${["1", "2"].map((semester) => renderMineSemesterBlock(fixedCourses, grade, semester)).join("")}
      </div>
    </article>
  `;
}

function renderMineSemesterBlock(fixedCourses, grade, semester) {
  const semesterKey = `${grade}-${semester}`;
  const fixedSemesterCourses = fixedCourses.filter((course) => course.semesters.includes(semesterKey));
  const semesterPlan = state.plan[grade]?.[semester] || { regular: [], joint: [], additional: [] };
  const selectedItems = [
    ...semesterPlan.regular.map((item) => ({ ...item, zone: "regular" })),
    ...semesterPlan.joint.map((item) => ({ ...item, zone: "joint" }))
  ];
  const additionalItems = Array.isArray(semesterPlan.additional) ? semesterPlan.additional : [];
  const fixedCredits = fixedSemesterCourses.reduce((sum, course) => sum + getPlannerCourseCredits(course, grade, semester), 0);
  const selectedCredits = sumCredits(selectedItems);
  const totalCourses = fixedSemesterCourses.length + selectedItems.length;
  const totalCredits = fixedCredits + selectedCredits;

  return `
    <div class="mine-semester-card">
      <div class="mine-semester-head">
        <b>${grade}학년 ${semester}학기</b>
        <span>기본·선택 ${totalCourses}과목 · ${totalCredits}학점${additionalItems.length ? ` · 추가 ${additionalItems.length}과목` : ""}</span>
      </div>
      <div class="mine-course-section">
        <span class="label">학교지정</span>
        ${
          fixedSemesterCourses.length
            ? `<div class="mine-course-list">${fixedSemesterCourses.map((course) => renderMineFixedCourseChip(course, grade, semester)).join("")}</div>`
            : `<p class="empty-note compact">학교지정 과목이 없습니다.</p>`
        }
      </div>
      <div class="mine-course-section">
        <span class="label">내 선택과목</span>
        ${
          selectedItems.length
            ? `<div class="mine-course-list">${selectedItems.map((item) => renderMineSelectedCourseChip(item, grade, semester)).join("")}</div>`
            : `<p class="empty-note compact">아직 선택한 과목이 없습니다.</p>`
        }
      </div>
      <div class="mine-course-section additional-course-section">
        <div class="additional-course-head">
          <span class="label">추가 교육과정</span>
          <small>온라인·오프라인 등 학기별 최대 2과목</small>
        </div>
        ${
          additionalItems.length
            ? `<div class="additional-course-list">${additionalItems.map((item, index) => renderAdditionalCourseItem(item, grade, semester, index)).join("")}</div>`
            : `<p class="empty-note compact">학교 밖이나 온라인에서 추가로 듣고 싶은 과목이 있으면 아래에 적습니다.</p>`
        }
        ${renderAdditionalCourseForm(grade, semester, additionalItems.length)}
      </div>
    </div>
  `;
}

function renderMineFixedCourseChip(course, grade, semester) {
  return renderMineCourseChip({
    course,
    credit: getPlannerCourseCredits(course, grade, semester),
    label: "학교지정",
    type: "fixed"
  });
}

function renderMineSelectedCourseChip(item, grade, semester) {
  const course = getPlannerCourse(item.id);
  if (!course) return "";
  return renderMineCourseChip({
    course,
    credit: item.credits || getPlannerCourseCredits(course, grade, semester),
    label: getPlannerSectionLabel(course.section || "선택과목"),
    type: item.zone === "joint" ? "joint" : "selected"
  });
}

function renderMineCourseChip({ course, credit, label, type }) {
  return `
    <span class="mine-course-chip ${type}">
      ${renderCourseNameWithInfo(course)}
      <small>${escapeHtml(course.area)} · ${escapeHtml(label)} · ${credit}학점</small>
    </span>
  `;
}

function renderAdditionalCourseItem(item, grade, semester, index) {
  return `
    <div class="additional-course-item">
      <div>
        <strong>${escapeHtml(item.name)}</strong>
        <div class="additional-course-meta">
          <span>${escapeHtml(item.area || "기타")}</span>
          <span>${escapeHtml(item.category || "선택과목")}</span>
        </div>
        ${item.reason ? `<p class="additional-course-reason">${escapeHtml(item.reason)}</p>` : ""}
      </div>
      <button class="additional-course-remove" type="button"
        data-remove-additional-course
        data-grade="${grade}"
        data-semester="${semester}"
        data-index="${index}"
        aria-label="${escapeAttribute(item.name)} 삭제">삭제</button>
    </div>
  `;
}

function renderAdditionalCourseForm(grade, semester, selectedCount) {
  const isFull = selectedCount >= 2;
  return `
    <div class="additional-course-form ${isFull ? "is-full" : ""}" data-additional-form data-grade="${grade}" data-semester="${semester}">
      <label>
        과목 검색
        <input type="search"
          list="additionalCourseOptions"
          placeholder="예: 경제 수학, 데이터 과학"
          data-additional-course-name
          ${isFull ? "disabled" : ""} />
      </label>
      <label class="additional-course-reason-field">
        필요성
        <input type="text"
          maxlength="90"
          placeholder="왜 듣고 싶은지 한 문장으로 적기"
          data-additional-course-reason
          ${isFull ? "disabled" : ""} />
      </label>
      <button class="ghost-button" type="button" data-add-additional-course ${isFull ? "disabled" : ""}>추가</button>
      <p class="additional-course-preview" data-additional-course-preview>
        ${isFull ? "이 학기에는 추가 교육과정을 2과목까지 입력했습니다." : "과목을 선택하면 교과군과 성격이 표시됩니다."}
      </p>
    </div>
  `;
}

function exportMineCurriculumPdf() {
  state.plannerMode = "mine";
  renderPlannerModeState();
  saveState(false);

  const previousTitle = document.title;
  document.title = `${getCurriculumPlanLabel(state.plannerPlan)} 내 교육과정`;
  document.body.classList.add("print-mine-curriculum");

  const cleanup = () => {
    document.body.classList.remove("print-mine-curriculum");
    document.title = previousTitle;
    window.removeEventListener("afterprint", cleanup);
  };

  window.addEventListener("afterprint", cleanup);
  window.print();
}

function setSelfEvaluationMode(mode) {
  if (!["subject", "creative"].includes(mode)) return;
  if (state.selfEvalMode === "subject") updateSelfEvaluationEntryFromForm();
  if (state.selfEvalMode === "creative") updateCreativeEvaluationEntryFromForm();
  state.selfEvalMode = mode;
  renderSelfEvaluationMode();
}

function renderSelfEvaluationMode() {
  const isCreative = state.selfEvalMode === "creative";
  const subjectButton = $("#selfEvalSubjectModeButton");
  const creativeButton = $("#selfEvalCreativeModeButton");
  const subjectTargets = ["#selfEvalSubjectControls", "#selfEvalSubjectGuide", "#selfEvalSubjectWorkspace"];
  const creativeTargets = ["#creativeEvalControls", "#creativeEvalGuide", "#creativeEvalWorkspace"];

  subjectButton?.classList.toggle("active", !isCreative);
  creativeButton?.classList.toggle("active", isCreative);
  subjectButton?.setAttribute("aria-selected", String(!isCreative));
  creativeButton?.setAttribute("aria-selected", String(isCreative));
  subjectTargets.forEach((selector) => {
    const element = $(selector);
    if (element) element.hidden = isCreative;
  });
  creativeTargets.forEach((selector) => {
    const element = $(selector);
    if (element) element.hidden = !isCreative;
  });

  if (isCreative) renderCreativeEvaluation();
  else renderSelfEvaluation();
}

function normalizeStudentNumber(value) {
  return String(value || "").replace(/[^0-9]/g, "").slice(0, 12);
}

function loadSelfEvaluationIdentity() {
  state.selfEvalStudentNumber = normalizeStudentNumber(localStorage.getItem(SELF_EVAL_IDENTITY_STORAGE_KEY));
}

function saveSelfEvaluationIdentity() {
  if (state.selfEvalStudentNumber) {
    localStorage.setItem(SELF_EVAL_IDENTITY_STORAGE_KEY, state.selfEvalStudentNumber);
  } else {
    localStorage.removeItem(SELF_EVAL_IDENTITY_STORAGE_KEY);
  }
}

function renderSelfEvaluationIdentity() {
  const input = $("#selfEvalStudentNumber");
  if (input) input.value = state.selfEvalStudentNumber;
}

function clearSelfEvaluationStudentNumber() {
  if (!state.selfEvalStudentNumber) {
    showToast("지울 학번이 없습니다.");
    return;
  }
  if (!window.confirm("학번만 지웁니다. 작성한 자기평가서 내용은 유지됩니다.")) return;
  state.selfEvalStudentNumber = "";
  saveSelfEvaluationIdentity();
  renderSelfEvaluationIdentity();
  $("#selfEvalStudentNumber")?.focus();
  showToast("학번을 지웠습니다.");
}

function requireSelfEvaluationStudentNumber() {
  const input = $("#selfEvalStudentNumber");
  const studentNumber = normalizeStudentNumber(input?.value || state.selfEvalStudentNumber);
  state.selfEvalStudentNumber = studentNumber;
  saveSelfEvaluationIdentity();
  renderSelfEvaluationIdentity();
  if (studentNumber) return studentNumber;
  showToast("PDF를 내보내려면 학번을 먼저 입력해 주세요.");
  input?.focus();
  return "";
}

function createLocalExportTime(date = new Date()) {
  const pad = (value) => String(value).padStart(2, "0");
  const year = date.getFullYear();
  const month = pad(date.getMonth() + 1);
  const day = pad(date.getDate());
  const hour = pad(date.getHours());
  const minute = pad(date.getMinutes());
  return {
    compact: `${year}${month}${day}${hour}${minute}`,
    display: `${year}.${month}.${day} ${hour}:${minute}`
  };
}

function sanitizePdfFilePart(value) {
  return String(value || "")
    .trim()
    .replace(/[\\/:*?"<>|]/g, "")
    .replace(/\s+/g, "_") || "자료";
}

function getCreativeEvaluationTypeConfig(type = state.creativeEvalType) {
  return creativeEvalTypes[type] || creativeEvalTypes.autonomy;
}

function createEmptyCreativeEvaluationEntry(type = state.creativeEvalType) {
  const entry = { competencies: [], competencyNote: "" };
  getCreativeEvaluationTypeConfig(type).fields.forEach((field) => {
    entry[field.key] = "";
  });
  return entry;
}

function getCreativeEvaluationEntry(type = state.creativeEvalType) {
  const planKey = state.creativeEvalPlan;
  const grade = state.creativeEvalGrade;
  if (!state.creativeEvalEntries[planKey]) state.creativeEvalEntries[planKey] = {};
  if (!state.creativeEvalEntries[planKey][grade]) state.creativeEvalEntries[planKey][grade] = {};
  if (!state.creativeEvalEntries[planKey][grade][type]) {
    state.creativeEvalEntries[planKey][grade][type] = createEmptyCreativeEvaluationEntry(type);
  }
  return state.creativeEvalEntries[planKey][grade][type];
}

function renderCreativeEvaluationPlanOptions() {
  const select = $("#creativeEvalPlanSelect");
  if (!select) return;
  select.innerHTML = plannerPlanOrder
    .map((key) => `<option value="${key}">${escapeHtml(getCurriculumPlanLabel(key))}</option>`)
    .join("");
  if (!plannerPlanOrder.includes(state.creativeEvalPlan)) state.creativeEvalPlan = "incoming2026";
  select.value = state.creativeEvalPlan;
  const gradeSelect = $("#creativeEvalGradeSelect");
  if (gradeSelect) gradeSelect.value = state.creativeEvalGrade;
  const domainSelect = $("#creativeEvalDomainSelect");
  if (domainSelect) {
    domainSelect.innerHTML = Object.entries(creativeEvalDomains)
      .map(([key, domain]) => `<option value="${key}">${escapeHtml(domain.label)}</option>`)
      .join("");
    if (!creativeEvalDomains[state.creativeEvalDomain]) state.creativeEvalDomain = "korean";
    domainSelect.value = state.creativeEvalDomain;
  }
  renderCreativeEvaluationSeriesOptions();
}

function renderCreativeEvaluationSeriesOptions() {
  const select = $("#creativeEvalSeriesSelect");
  if (!select) return;
  const seriesList = getCreativeEvalSeries(state.creativeEvalDomain, state.creativeEvalType);
  if (!seriesList.some((series) => series.id === state.creativeEvalSeries)) {
    state.creativeEvalSeries = seriesList[0]?.id || "representative";
  }
  select.innerHTML = seriesList
    .map((series) => `<option value="${escapeAttribute(series.id)}">${escapeHtml(series.label)}</option>`)
    .join("");
  select.value = state.creativeEvalSeries;
}

function renderCreativeEvaluation() {
  const formTarget = $("#creativeEvalFields");
  if (!formTarget) return;
  const type = state.creativeEvalType;
  const config = getCreativeEvaluationTypeConfig(type);
  const entry = getCreativeEvaluationEntry(type);

  $all("[data-creative-eval-type]").forEach((button) => {
    const active = button.dataset.creativeEvalType === type;
    button.classList.toggle("active", active);
    button.setAttribute("aria-selected", String(active));
  });
  const gradeSelect = $("#creativeEvalGradeSelect");
  if (gradeSelect) gradeSelect.value = state.creativeEvalGrade;
  const gradeLabel = $("#creativeEvalCurrentGrade");
  const title = $("#creativeEvalCurrentType");
  const guide = $("#creativeEvalCurrentGuide");
  if (gradeLabel) gradeLabel.textContent = `${state.creativeEvalGrade}학년 창체`;
  if (title) title.textContent = config.label;
  if (guide) guide.textContent = config.shortGuide;
  const domainSelect = $("#creativeEvalDomainSelect");
  if (domainSelect) domainSelect.value = state.creativeEvalDomain;
  const seriesSelect = $("#creativeEvalSeriesSelect");
  if (seriesSelect) seriesSelect.value = state.creativeEvalSeries;

  formTarget.innerHTML = config.fields.map((field) => renderCreativeEvaluationField(field, entry[field.key] || "")).join("");
  renderCreativeEvaluationCompetencies(entry);
  renderCreativeEvaluationStatus();
  $all("#creativeEvalForm textarea").forEach((textarea) => resizeSelfEvaluationTextarea(textarea));
}

function renderCreativeEvaluationField(field, value) {
  return `
    <label class="self-eval-field creative-eval-field">
      <span>
        <b>${escapeHtml(field.label)}</b>
        <button class="subject-info-button self-help-button" type="button"
          aria-label="${escapeHtml(`${field.label} 도움말`)}"
          title="작성 도움말"
          data-creative-help="${escapeHtml(field.key)}">?</button>
      </span>
      <textarea data-creative-eval-field="${escapeHtml(field.key)}" rows="${field.rows || 4}" placeholder="${escapeAttribute(field.placeholder)}">${escapeTextareaValue(value)}</textarea>
    </label>
  `;
}

function renderCreativeEvaluationCompetencies(entry) {
  const selected = new Set(entry.competencies || []);
  $all("[data-creative-eval-competency]").forEach((checkbox) => {
    checkbox.checked = selected.has(checkbox.value);
  });
  const note = $("#creativeEvalCompetencyNote");
  if (note) note.value = entry.competencyNote || "";
}

function renderCreativeEvaluationStatus() {
  const status = $("#creativeEvalSaveStatus");
  if (!status) return;
  const config = getCreativeEvaluationTypeConfig();
  const entry = getCreativeEvaluationEntry();
  const filledCount = config.fields.filter((field) => String(entry[field.key] || "").trim()).length;
  const competencyText = entry.competencies?.length ? ` · ${entry.competencies.join(", ")}` : "";
  status.textContent = `작성 항목 ${filledCount}/${config.fields.length}${competencyText}`;
}

function updateCreativeEvaluationEntryFromForm() {
  const form = $("#creativeEvalForm");
  if (!form) return;
  const config = getCreativeEvaluationTypeConfig();
  const entry = getCreativeEvaluationEntry();
  config.fields.forEach((field) => {
    const input = form.querySelector(`[data-creative-eval-field="${field.key}"]`);
    if (input) entry[field.key] = input.value;
  });
  entry.competencies = $all("[data-creative-eval-competency]")
    .filter((checkbox) => checkbox.checked)
    .map((checkbox) => checkbox.value);
  entry.competencyNote = $("#creativeEvalCompetencyNote")?.value.trim() || "";
}

function saveCreativeEvaluationState(showMessage) {
  const data = {
    creativeEvalPlan: state.creativeEvalPlan,
    creativeEvalGrade: state.creativeEvalGrade,
    creativeEvalType: state.creativeEvalType,
    creativeEvalDomain: state.creativeEvalDomain,
    creativeEvalSeries: state.creativeEvalSeries,
    creativeEvalEntries: state.creativeEvalEntries
  };
  localStorage.setItem(CREATIVE_EVAL_STORAGE_KEY, JSON.stringify(data));
  if (showMessage) showToast("창의적 체험활동을 저장했습니다.");
}

function resetCurrentCreativeEvaluationEntry() {
  updateCreativeEvaluationEntryFromForm();
  const config = getCreativeEvaluationTypeConfig();
  const label = `${state.creativeEvalGrade}학년 ${config.label}`;
  if (!window.confirm(`${label} 작성 내용만 초기화합니다. 다른 학년과 활동 내용은 유지됩니다.`)) return;

  const planEntries = state.creativeEvalEntries[state.creativeEvalPlan];
  const gradeEntries = planEntries?.[state.creativeEvalGrade];
  if (gradeEntries) delete gradeEntries[state.creativeEvalType];
  if (gradeEntries && !Object.keys(gradeEntries).length) delete planEntries[state.creativeEvalGrade];
  if (planEntries && !Object.keys(planEntries).length) delete state.creativeEvalEntries[state.creativeEvalPlan];

  saveCreativeEvaluationState(false);
  renderCreativeEvaluation();
  showToast(`${label} 작성 내용을 초기화했습니다.`);
}

function loadCreativeEvaluationState() {
  try {
    const saved = JSON.parse(localStorage.getItem(CREATIVE_EVAL_STORAGE_KEY));
    if (!saved) return;
    state.creativeEvalPlan = saved.creativeEvalPlan || state.creativeEvalPlan;
    state.creativeEvalGrade = ["1", "2", "3"].includes(saved.creativeEvalGrade) ? saved.creativeEvalGrade : "1";
    state.creativeEvalType = creativeEvalTypes[saved.creativeEvalType] ? saved.creativeEvalType : "autonomy";
    state.creativeEvalDomain = creativeEvalDomains[saved.creativeEvalDomain] ? saved.creativeEvalDomain : "korean";
    const savedSeries = saved.creativeEvalSeries || "representative";
    state.creativeEvalSeries = getCreativeEvalSeries(state.creativeEvalDomain, state.creativeEvalType).some((series) => series.id === savedSeries)
      ? savedSeries
      : "representative";
    state.creativeEvalEntries = saved.creativeEvalEntries || {};
  } catch {
    localStorage.removeItem(CREATIVE_EVAL_STORAGE_KEY);
    state.creativeEvalEntries = {};
  }
}

function openCreativeEvalHelpPopup(fieldKey) {
  const modal = $("#subjectInfoModal");
  const label = $("#subjectInfoLabel");
  const title = $("#subjectInfoTitle");
  const content = $("#subjectInfoContent");
  const config = getCreativeEvaluationTypeConfig();
  const field = config.fields.find((item) => item.key === fieldKey);
  if (!modal || !title || !content || !field) return;

  const domain = creativeEvalDomains[state.creativeEvalDomain] || creativeEvalDomains.korean;
  const examples = buildCreativeEvalSeriesExamples(state.creativeEvalType, state.creativeEvalDomain, state.creativeEvalSeries)
    .filter((item) => item.values[fieldKey]);
  if (label) label.textContent = `${state.creativeEvalGrade}학년 ${config.label} 도움말`;
  title.textContent = field.label;
  content.innerHTML = `
    <p class="subject-info-copy">${escapeHtml(field.help)}</p>
    <div class="creative-help-principle">
      <b>작성할 때 확인하세요</b>
      <p>실제로 한 활동만 적고, 활동명보다 내가 판단하고 실행한 과정이 보이게 씁니다. 아래는 ${escapeHtml(domain.label)} 분야의 하나의 질문이 3년 동안 심화되는 재구성 사례입니다.</p>
    </div>
    <div class="creative-help-example-list">
      ${examples.map((example) => `
        <article class="${String(example.grade) === state.creativeEvalGrade ? "active" : ""}">
          <span>${example.grade}학년 사례</span>
          <b>${escapeHtml(example.title)}</b>
          <p>${escapeHtml(example.values[fieldKey])}</p>
        </article>
      `).join("")}
    </div>
    <p class="subject-info-caution">
      학교생활기록부는 교사가 실제 관찰과 학교 교육활동을 바탕으로 기록합니다. 학생은 사실을 과장하거나 학생부 문장을 대신 만들기보다 자신의 역할, 과정, 결과를 상담 자료로 정리해야 합니다.
    </p>
  `;
  modal.hidden = false;
  document.body.classList.add("modal-open");
  $(".subject-info-close")?.focus();
}

function openCreativeEvalFlowPopup() {
  const modal = $("#subjectInfoModal");
  const label = $("#subjectInfoLabel");
  const title = $("#subjectInfoTitle");
  const content = $("#subjectInfoContent");
  const config = getCreativeEvaluationTypeConfig();
  const domain = creativeEvalDomains[state.creativeEvalDomain] || creativeEvalDomains.korean;
  if (!modal || !title || !content) return;

  const series = getCreativeEvalSeries(state.creativeEvalDomain, state.creativeEvalType)
    .find((item) => item.id === state.creativeEvalSeries);
  const examples = buildCreativeEvalSeriesExamples(state.creativeEvalType, state.creativeEvalDomain, state.creativeEvalSeries);
  if (label) label.textContent = `${config.label} · ${domain.label}`;
  title.textContent = series?.label || "3개년 성장 흐름";
  content.innerHTML = `
    <p class="subject-info-copy">
      같은 활동을 반복하는 흐름이 아닙니다. 1학년의 관찰과 질문을 2학년의 비교·분석으로 발전시키고,
      3학년에는 검증·한계·대안까지 다루는 방식으로 심화합니다.
    </p>
    <div class="creative-flow-summary">
      ${examples.map((example) => {
        const values = example.values;
        const process = values.process || values.learningConnection || "";
        const result = values.result || values.insight || values.change || "";
        return `
          <article class="${String(example.grade) === state.creativeEvalGrade ? "active" : ""}">
            <div class="creative-flow-heading">
              <span>${example.grade}학년</span>
              <b>${escapeHtml(example.title)}</b>
            </div>
            <dl>
              <div><dt>출발</dt><dd>${escapeHtml(values.motivation || "")}</dd></div>
              <div><dt>과정</dt><dd>${escapeHtml(process)}</dd></div>
              <div><dt>확인</dt><dd>${escapeHtml(result)}</dd></div>
              ${values.book ? `<div><dt>독서</dt><dd>${escapeHtml(values.book)}<br>${escapeHtml(values.bookReflection || "")}</dd></div>` : ""}
              <div><dt>다음 단계</dt><dd>${escapeHtml(values.followUp || "")}</dd></div>
            </dl>
          </article>
        `;
      }).join("")}
    </div>
    <div class="creative-help-principle">
      <b>내 활동으로 바꾸는 방법</b>
      <p>사례의 주제를 가져오는 것이 아니라 내가 실제로 발견한 문제, 사용한 자료, 맡은 역할, 확인한 변화와 한계를 같은 구조로 정리합니다.</p>
    </div>
    <p class="subject-info-caution">
      아래 흐름은 공식 평가 관점과 학교 교육활동의 기록 원칙을 참고해 만든 가상 사례입니다. 실제 학생의 기록을 복제한 자료가 아닙니다.
    </p>
  `;
  modal.hidden = false;
  document.body.classList.add("modal-open");
  $(".subject-info-close")?.focus();
}

function exportCreativeEvaluationPdf() {
  updateCreativeEvaluationEntryFromForm();
  saveCreativeEvaluationState(false);
  const studentNumber = requireSelfEvaluationStudentNumber();
  if (!studentNumber) return;
  const exportTime = createLocalExportTime();
  const printWindow = window.open("", "_blank");
  if (!printWindow) {
    showToast("PDF 창을 열 수 없습니다. 브라우저의 팝업 허용 여부를 확인해 주세요.");
    return;
  }
  printWindow.document.open();
  printWindow.document.write(buildCreativeEvaluationPrintDocument(studentNumber, exportTime));
  printWindow.document.close();
  const runPrint = () => {
    printWindow.focus();
    printWindow.print();
  };
  if (printWindow.document.readyState === "complete") window.setTimeout(runPrint, 250);
  else printWindow.addEventListener("load", () => window.setTimeout(runPrint, 250), { once: true });
}

function buildCreativeEvaluationPrintDocument(studentNumber, exportTime) {
  const planLabel = getCurriculumPlanLabel(state.creativeEvalPlan);
  const grade = state.creativeEvalGrade;
  const fileTitle = `${sanitizePdfFilePart(studentNumber)}_${grade}학년_창체자기평가서_${exportTime.compact}`;
  const sections = Object.entries(creativeEvalTypes).map(([type, config]) => {
    const entry = getCreativeEvaluationEntry(type);
    const competencies = entry.competencies?.length ? entry.competencies.join(", ") : "작성 내용 없음";
    return `
      <section class="activity-section">
        <h2>${escapeHtml(config.label)}</h2>
        <p class="competency"><b>강조하고 싶은 역량</b> ${escapeHtml(competencies)}${entry.competencyNote ? ` · ${escapeHtml(entry.competencyNote)}` : ""}</p>
        ${config.fields.map((field) => `
          <article>
            <b>${escapeHtml(field.label)}</b>
            <p>${escapeHtml(entry[field.key] || "작성 내용 없음")}</p>
          </article>
        `).join("")}
      </section>
    `;
  }).join("");

  return `<!doctype html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>${escapeHtml(fileTitle)}</title>
  <style>
    @page { size: A4; margin: 12mm; }
    * { box-sizing: border-box; }
    body { margin: 0; color: #111; font-family: Pretendard, "Apple SD Gothic Neo", "Noto Sans KR", sans-serif; font-size: 11px; line-height: 1.55; }
    header { border-bottom: 2px solid #111; margin-bottom: 12px; padding-bottom: 10px; }
    header span { border: 1px solid #111; border-radius: 999px; padding: 2px 8px; font-weight: 800; }
    h1 { margin: 7px 0 3px; font-size: 22px; }
    h2 { margin: 0 0 8px; font-size: 17px; }
    p { margin: 0; white-space: pre-wrap; overflow-wrap: anywhere; }
    .activity-section { margin-bottom: 14px; break-before: auto; }
    .activity-section + .activity-section { break-before: page; }
    .competency { border: 1px solid #bcc9c0; background: #f5f8f6; margin-bottom: 8px; padding: 7px; }
    article { border: 1px solid #c9d3cc; margin-bottom: 6px; padding: 7px; break-inside: avoid; }
    article b { display: block; margin-bottom: 3px; }
    footer { border-top: 1px solid #c9d3cc; margin-top: 12px; padding-top: 8px; color: #555; font-size: 10px; }
    .export-time { margin-top: 4px; text-align: right; color: #222; font-weight: 700; }
  </style>
</head>
<body>
  <header>
    <span>상담용 자료</span>
    <h1>${escapeHtml(`${grade}학년 창의적 체험활동 자기평가서`)}</h1>
    <p>학번 ${escapeHtml(studentNumber)} · ${escapeHtml(planLabel)}</p>
  </header>
  ${sections}
  <footer>
    <p>이 자료는 학생부 문장을 대신 작성하는 자료가 아니라, 실제 학교 활동과 성장 과정을 상담 전에 정리하기 위한 초안입니다.</p>
    <p class="export-time">저장 시각: ${escapeHtml(exportTime.display)}</p>
  </footer>
</body>
</html>`;
}

function getSelfEvaluationPlan() {
  return curriculumData.plans?.[state.selfEvalPlan] || curriculumData.plans?.incoming2026 || { courses: [] };
}

function getSelfEvaluationCourses() {
  const plan = getSelfEvaluationPlan();
  return (plan.courses || [])
    .filter((course) => course.name)
    .filter((course) => state.selfEvalSemester === "all" || course.semesters?.includes(state.selfEvalSemester))
    .sort((a, b) => getCourseFirstSemesterSort(a) - getCourseFirstSemesterSort(b) || a.row - b.row || a.name.localeCompare(b.name, "ko"));
}

function getCourseFirstSemesterSort(course) {
  const firstSemester = curriculumSemesterOrder.find((semesterKey) => course.semesters?.includes(semesterKey));
  return firstSemester ? getSemesterSortIndex(firstSemester) : 999;
}

function ensureSelfEvaluationSubject() {
  const coursesForPlan = getSelfEvaluationCourses();
  if (!coursesForPlan.length) {
    state.selfEvalSubject = "";
    return;
  }
  if (!coursesForPlan.some((course) => course.id === state.selfEvalSubject)) {
    state.selfEvalSubject = coursesForPlan[0].id;
  }
}

function getSelfEvaluationCourse() {
  ensureSelfEvaluationSubject();
  return getSelfEvaluationCourses().find((course) => course.id === state.selfEvalSubject) || null;
}

function getSelfEvaluationEntryKey() {
  ensureSelfEvaluationSubject();
  return state.selfEvalSubject || "";
}

function createEmptySelfEvaluationEntry() {
  return {
    motivation: "",
    question: "",
    concept: "",
    process: "",
    result: "",
    limit: "",
    followUp: "",
    book: "",
    bookReflection: "",
    bookConnection: "",
    competencies: [],
    competencyNote: ""
  };
}

function getSelfEvaluationEntry() {
  const planKey = state.selfEvalPlan;
  const entryKey = getSelfEvaluationEntryKey();
  if (!planKey || !entryKey) return createEmptySelfEvaluationEntry();
  if (!state.selfEvalEntries[planKey]) state.selfEvalEntries[planKey] = {};
  if (!state.selfEvalEntries[planKey][entryKey]) {
    state.selfEvalEntries[planKey][entryKey] = createEmptySelfEvaluationEntry();
  }
  return state.selfEvalEntries[planKey][entryKey];
}

function renderSelfEvaluationPlanOptions() {
  const select = $("#selfEvalPlanSelect");
  if (!select) return;
  select.innerHTML = plannerPlanOrder
    .map((key) => `<option value="${key}">${escapeHtml(getCurriculumPlanLabel(key))}</option>`)
    .join("");
  if (plannerPlanOrder.includes(state.selfEvalPlan)) {
    select.value = state.selfEvalPlan;
  }
}

function renderSelfEvaluationSemesterOptions() {
  const select = $("#selfEvalSemesterSelect");
  if (!select) return;
  const optionValues = ["all", ...curriculumSemesterOrder];
  if (!optionValues.includes(state.selfEvalSemester)) state.selfEvalSemester = "all";
  select.innerHTML = `
    <option value="all">전체 과목</option>
    ${curriculumSemesterOrder.map((semesterKey) => `<option value="${semesterKey}">${escapeHtml(getSemesterLabel(semesterKey))}</option>`).join("")}
  `;
  select.value = state.selfEvalSemester;
}

function renderSelfEvaluationSubjectOptions() {
  const select = $("#selfEvalSubjectSelect");
  if (!select) return;
  ensureSelfEvaluationSubject();
  const coursesForPlan = getSelfEvaluationCourses();
  select.innerHTML = coursesForPlan.length
    ? coursesForPlan.map((course) => `<option value="${escapeHtml(course.id)}">${escapeHtml(renderSelfEvaluationCourseOptionLabel(course))}</option>`).join("")
    : `<option value="">과목 없음</option>`;
  select.value = state.selfEvalSubject;
}

function renderSelfEvaluationCourseOptionLabel(course) {
  const semesters = (course.semesters || []).map((semesterKey) => getSemesterLabel(semesterKey)).join(", ");
  return `${course.name} · ${course.area}${semesters ? ` · ${semesters}` : ""}`;
}


function resizeSelfEvaluationTextarea(textarea) {
  if (!(textarea instanceof HTMLTextAreaElement)) return;
  textarea.style.height = "auto";
  textarea.style.height = `${textarea.scrollHeight}px`;
}

function resizeSelfEvaluationTextareas() {
  $all("#selfEvalForm textarea, #selfEvalCompetencyNote").forEach((textarea) => resizeSelfEvaluationTextarea(textarea));
}

function renderSelfEvaluation() {
  const formTarget = $("#selfEvalFields");
  if (!formTarget) return;
  ensureSelfEvaluationSubject();
  const course = getSelfEvaluationCourse();
  const entry = getSelfEvaluationEntry();
  const currentSubject = $("#selfEvalCurrentSubject");
  const currentMeta = $("#selfEvalCurrentMeta");

  if (currentSubject) currentSubject.textContent = course?.name || "과목을 선택하세요";
  if (currentMeta) {
    currentMeta.textContent = course
      ? `${getCurriculumPlanLabel(state.selfEvalPlan)} · ${course.area} · ${course.category || "과목"} · ${(course.semesters || []).map((semesterKey) => getSemesterLabel(semesterKey)).join(", ")}`
      : "학년도와 과목을 선택하면 과목별 자기평가서를 작성할 수 있습니다.";
  }

  formTarget.innerHTML = selfEvalFields.map((field) => renderSelfEvaluationField(field, entry[field.key] || "")).join("");
  renderSelfEvaluationCompetencies(entry);
  renderSelfEvaluationStatus();
  resizeSelfEvaluationTextareas();
}

function renderSelfEvaluationField(field, value) {
  const rows = field.rows || 4;
  return `
    <label class="self-eval-field">
      <span>
        <b>${escapeHtml(field.label)}</b>
        <button class="subject-info-button self-help-button" type="button"
          aria-label="${escapeHtml(`${field.label} 도움말`)}"
          title="작성 도움말"
          data-self-help="${escapeHtml(field.key)}">?</button>
      </span>
      <textarea data-self-eval-field="${escapeHtml(field.key)}" rows="${rows}" placeholder="${escapeAttribute(field.placeholder)}">${escapeTextareaValue(value)}</textarea>
    </label>
  `;
}

function renderSelfEvaluationCompetencies(entry) {
  const selected = new Set(entry.competencies || []);
  $all("[data-self-eval-competency]").forEach((checkbox) => {
    checkbox.checked = selected.has(checkbox.value);
  });
  const noteInput = $("#selfEvalCompetencyNote");
  if (noteInput) noteInput.value = entry.competencyNote || "";
}

function renderSelfEvaluationStatus() {
  const status = $("#selfEvalSaveStatus");
  if (!status) return;
  const entry = getSelfEvaluationEntry();
  const filledCount = selfEvalFields.filter((field) => String(entry[field.key] || "").trim()).length;
  const competencyText = (entry.competencies || []).length ? ` · ${entry.competencies.join(", ")}` : "";
  const noteText = String(entry.competencyNote || "").trim() ? " · 강조 장면 작성" : "";
  status.textContent = `작성 항목 ${filledCount}/${selfEvalFields.length}${competencyText}${noteText}`;
}

function updateSelfEvaluationEntryFromForm() {
  const form = $("#selfEvalForm");
  if (!form || !state.selfEvalSubject) return;
  const entry = getSelfEvaluationEntry();
  selfEvalFields.forEach((field) => {
    const input = form.querySelector(`[data-self-eval-field="${field.key}"]`);
    if (input) entry[field.key] = input.value;
  });
  entry.competencies = $all("[data-self-eval-competency]")
    .filter((checkbox) => checkbox.checked)
    .map((checkbox) => checkbox.value);
  entry.competencyNote = $("#selfEvalCompetencyNote")?.value.trim() || "";
}

function saveSelfEvaluationState(showMessage) {
  const data = {
    selfEvalPlan: state.selfEvalPlan,
    selfEvalSemester: state.selfEvalSemester,
    selfEvalSubject: state.selfEvalSubject,
    selfEvalEntries: state.selfEvalEntries
  };
  localStorage.setItem(SELF_EVAL_STORAGE_KEY, JSON.stringify(data));
  if (showMessage) showToast("자기평가서를 저장했습니다.");
}

function resetCurrentSelfEvaluationEntry() {
  updateSelfEvaluationEntryFromForm();
  const course = getSelfEvaluationCourse();
  const entryKey = getSelfEvaluationEntryKey();
  if (!entryKey) {
    showToast("초기화할 과목을 먼저 선택해 주세요.");
    return;
  }
  const label = course?.name || "현재 과목";
  if (!window.confirm(`${label} 자기평가서만 초기화합니다. 다른 과목의 작성 내용은 유지됩니다.`)) return;

  const planEntries = state.selfEvalEntries[state.selfEvalPlan];
  if (planEntries) delete planEntries[entryKey];
  if (planEntries && !Object.keys(planEntries).length) delete state.selfEvalEntries[state.selfEvalPlan];

  saveSelfEvaluationState(false);
  renderSelfEvaluation();
  showToast(`${label} 자기평가서를 초기화했습니다.`);
}

function loadSelfEvaluationState() {
  try {
    const saved = JSON.parse(localStorage.getItem(SELF_EVAL_STORAGE_KEY));
    if (!saved) {
      ensureSelfEvaluationSubject();
      return;
    }
    state.selfEvalPlan = saved.selfEvalPlan || state.selfEvalPlan;
    state.selfEvalSemester = saved.selfEvalSemester || "all";
    state.selfEvalSubject = saved.selfEvalSubject || "";
    state.selfEvalEntries = saved.selfEvalEntries || {};
    ensureSelfEvaluationSubject();
  } catch {
    localStorage.removeItem(SELF_EVAL_STORAGE_KEY);
    state.selfEvalEntries = {};
    ensureSelfEvaluationSubject();
  }
}

function exportSelfEvaluationPdf() {
  updateSelfEvaluationEntryFromForm();
  saveSelfEvaluationState(false);
  const studentNumber = requireSelfEvaluationStudentNumber();
  if (!studentNumber) return;
  const exportTime = createLocalExportTime();
  const course = getSelfEvaluationCourse();
  const fileTitle = `${sanitizePdfFilePart(studentNumber)}_${sanitizePdfFilePart(course?.name || "과목별")}_자기평가서_${exportTime.compact}`;
  renderSelfEvaluationPrintView(studentNumber, exportTime);

  const printDocument = buildSelfEvaluationPrintDocument(fileTitle);
  const printWindow = window.open("", "_blank");

  if (!printWindow) {
    printSelfEvaluationInCurrentWindow(fileTitle);
    return;
  }

  printWindow.document.open();
  printWindow.document.write(printDocument);
  printWindow.document.close();

  const runPrint = () => {
    try {
      printWindow.focus();
      printWindow.print();
    } catch (error) {
      printSelfEvaluationInCurrentWindow(fileTitle);
    }
  };

  if (printWindow.document.readyState === "complete") {
    window.setTimeout(runPrint, 250);
  } else {
    printWindow.addEventListener("load", () => window.setTimeout(runPrint, 250), { once: true });
  }
}

function printSelfEvaluationInCurrentWindow(fileTitle) {
  const previousTitle = document.title;
  document.title = fileTitle;
  document.body.classList.add("print-self-evaluation");

  const cleanup = () => {
    document.body.classList.remove("print-self-evaluation");
    document.title = previousTitle;
    window.removeEventListener("afterprint", cleanup);
  };

  window.addEventListener("afterprint", cleanup);
  window.print();
}

function buildSelfEvaluationPrintDocument(fileTitle) {
  const target = $("#selfEvalPrintView");
  const content = target?.innerHTML || "";

  return `<!doctype html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>${escapeHtml(fileTitle)}</title>
  <style>
    @page {
      size: A4;
      margin: 12mm;
    }

    * {
      box-sizing: border-box;
    }

    html,
    body {
      margin: 0;
      padding: 0;
      background: #fff;
      color: #111;
      font-family: Pretendard, "Apple SD Gothic Neo", "Noto Sans KR", system-ui, sans-serif;
      font-size: 12px;
      line-height: 1.55;
    }

    .self-eval-print-paper {
      width: 100%;
      height: auto;
      max-height: none;
      overflow: visible;
    }

    .print-paper-head {
      border-bottom: 2px solid #111;
      margin-bottom: 12px;
      padding-bottom: 10px;
    }

    .print-paper-head span {
      display: inline-block;
      border: 1px solid #111;
      border-radius: 999px;
      padding: 2px 8px;
      font-weight: 800;
    }

    .print-paper-head h2 {
      margin: 6px 0 3px;
      font-size: 22px;
      line-height: 1.25;
    }

    .print-paper-head p,
    .print-meta-grid p,
    .print-competency-box p,
    .print-field-list p,
    .print-note {
      margin: 0;
    }

    .print-meta-grid {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      border: 1px solid #c9d3cc;
      margin-bottom: 10px;
    }

    .print-meta-grid > div {
      border-right: 1px solid #c9d3cc;
      padding: 8px;
      min-width: 0;
    }

    .print-meta-grid > div:last-child {
      border-right: 0;
    }

    .print-meta-grid b,
    .print-competency-box b,
    .print-field-list b,
    .teacher-memo-box b {
      display: block;
      margin-bottom: 4px;
      font-size: 11px;
      color: #111;
    }

    .print-meta-grid,
    .print-competency-box,
    .teacher-memo-box {
      break-inside: avoid;
      page-break-inside: avoid;
    }

    .print-competency-box,
    .teacher-memo-box {
      border: 1px solid #c9d3cc;
      margin-bottom: 10px;
      padding: 8px;
      height: auto;
      max-height: none;
      overflow: visible;
    }

    .print-field-list {
      display: block;
    }

    .print-field-list article {
      display: block;
      width: 100%;
      border: 1px solid #c9d3cc;
      min-height: 0;
      height: auto;
      max-height: none;
      margin: 0 0 8px;
      padding: 8px;
      overflow: visible;
      break-inside: avoid;
      page-break-inside: avoid;
    }

    .print-field-list p,
    .print-competency-box p,
    .print-meta-grid p,
    .print-note {
      display: block;
      width: 100%;
      height: auto;
      max-height: none;
      overflow: visible;
      white-space: pre-wrap;
      word-break: break-all;
      overflow-wrap: anywhere;
      line-break: anywhere;
    }

    .teacher-memo-box {
      margin-top: 10px;
    }

    .teacher-memo-box div {
      min-height: 70px;
      border-top: 1px solid #d8dfda;
    }

    .print-note {
      color: #444;
      font-size: 10px;
    }

    .print-export-time {
      margin-top: 5px;
      color: #222;
      font-size: 10px;
      font-weight: 700;
      text-align: right;
    }
  </style>
</head>
<body>
  ${content}
</body>
</html>`;
}

function renderSelfEvaluationPrintView(studentNumber, exportTime) {
  const target = $("#selfEvalPrintView");
  if (!target) return;

  const course = getSelfEvaluationCourse();
  const entry = getSelfEvaluationEntry();
  const planLabel = getCurriculumPlanLabel(state.selfEvalPlan);
  const semesterLabel = course?.semesters?.length
    ? course.semesters.map((semesterKey) => getSemesterLabel(semesterKey)).join(", ")
    : "학기 확인";
  const competencyText = entry.competencies?.length ? entry.competencies.join(", ") : "아직 체크하지 않음";
  const competencyNoteText = entry.competencyNote || "작성 내용 없음";

  target.innerHTML = `
    <section class="self-eval-print-paper">
      <div class="print-paper-head">
        <span>상담용 자료</span>
        <h2>과목별 자기평가서</h2>
        <p>학번 ${escapeHtml(studentNumber)} · ${escapeHtml(planLabel)} · ${escapeHtml(course?.name || "과목 미선택")}</p>
      </div>
      <div class="print-meta-grid">
        <div>
          <b>과목</b>
          <p>${escapeHtml(course?.name || "과목 미선택")}</p>
        </div>
        <div>
          <b>교과군</b>
          <p>${escapeHtml(course?.area || "확인 필요")}</p>
        </div>
        <div>
          <b>유형</b>
          <p>${escapeHtml(course?.category || "확인 필요")}</p>
        </div>
        <div>
          <b>학기</b>
          <p>${escapeHtml(semesterLabel)}</p>
        </div>
      </div>
      <div class="print-competency-box">
        <b>강조하고 싶은 역량</b>
        <p>${escapeHtml(competencyText)}</p>
        <p>${escapeHtml(competencyNoteText)}</p>
      </div>
      <div class="print-field-list">
        ${selfEvalFields.map((field) => `
          <article>
            <b>${escapeHtml(field.label)}</b>
            <p>${escapeHtml(entry[field.key] || "작성 내용 없음")}</p>
          </article>
        `).join("")}
      </div>
      <div class="teacher-memo-box">
        <b>담임교사 상담 메모</b>
        <div></div>
      </div>
      <p class="print-note">
        이 자기평가서는 세특 문장을 대신 작성하는 자료가 아니라, 수업 속 배움과 탐구 과정을 정리하기 위한 상담 자료입니다.
      </p>
      <p class="print-export-time">저장 시각: ${escapeHtml(exportTime.display)}</p>
    </section>
  `;
}

function renderFixedGradeBlock(courses, grade) {
  return `
    <article class="fixed-grade-card">
      <h4>${grade}학년 과정</h4>
      <div class="fixed-semester-grid">
        ${["1", "2"].map((semester) => renderFixedSemesterBlock(courses, grade, semester)).join("")}
      </div>
    </article>
  `;
}

function renderFixedSemesterBlock(courses, grade, semester) {
  const semesterKey = `${grade}-${semester}`;
  const semesterCourses = courses.filter((course) => course.semesters.includes(semesterKey));
  return `
    <div class="fixed-semester-card">
      <b>${grade}학년 ${semester}학기</b>
      ${
        semesterCourses.length
          ? `<div class="fixed-course-list">${semesterCourses.map((course) => renderFixedCourseChip(course, grade, semester)).join("")}</div>`
          : `<p class="empty-note compact">학교지정 과목이 없습니다.</p>`
      }
    </div>
  `;
}

function renderFixedCourseChip(course, grade, semester) {
  return `
    <span class="fixed-course-chip">
      ${renderCourseNameWithInfo(course)}
      <small>${escapeHtml(course.area)} · ${getPlannerCourseCredits(course, grade, semester)}학점</small>
    </span>
  `;
}

function renderCoursePool() {
  const pool = $("#coursePool");
  const guide = $("#recommendBox");
  if (!pool) return;

  const query = normalizeText($("#courseSearch")?.value || "");
  const selectableCourses = getPlannerSelectableCourses()
    .filter((course) => course.semesters.some((semesterKey) => semesterKey.startsWith(`${state.activeGrade}-`)))
    .filter((course) => {
      if (!query) return true;
      return normalizeText(`${course.name} ${course.area} ${course.category} ${course.section}`).includes(query);
    });

  if (guide) {
    guide.innerHTML = `
      <strong>${escapeHtml(getCurriculumPlanLabel(state.plannerPlan))}</strong>
      <p>${state.activeGrade}학년 과정에서 고를 수 있는 선택과목을 보여줍니다. 과목을 누르거나 학기 버튼을 눌러 수강 희망 목록에 넣습니다.</p>
    `;
  }

  pool.innerHTML = selectableCourses.length ? selectableCourses
    .map((course) => {
      const semesterButtons = getCourseSemestersForGrade(course, state.activeGrade)
        .map((semester) => renderCourseActionPair(course, state.activeGrade, semester))
        .join("");
      const disabledForAllSemesters = getCourseSemestersForGrade(course, state.activeGrade)
        .every((semester) => getPlannerCourseActionState(course, state.activeGrade, semester).disabledByLimit);
      return `
        <div class="course-card ${course.section.includes("공동교육") ? "extra" : ""} ${disabledForAllSemesters ? "disabled-choice" : ""}"
          draggable="${disabledForAllSemesters ? "false" : "true"}"
          data-course-id="${escapeHtml(course.id)}"
          data-course-zone="${course.section.includes("공동교육") ? "joint" : "regular"}">
          ${renderCourseNameWithInfo(course)}
          <small>${escapeHtml(course.area)} · ${escapeHtml(course.category)} · ${escapeHtml(getPlannerSectionLabel(course.section))}${renderCourseChoiceStatus(course)}</small>
          <div class="quick-add-row">
            ${semesterButtons}
          </div>
        </div>
      `;
    })
    .join("") : `<div class="empty-note">해당 조건에 맞는 선택과목이 없습니다.</div>`;

  $all(".course-card").forEach((card) => {
    card.addEventListener("dragstart", (event) => {
      event.dataTransfer.setData("text/plain", card.dataset.courseId);
      event.dataTransfer.setData("application/x-planner-zone", card.dataset.courseZone || "regular");
    });
  });

  $all("[data-add-course]").forEach((button) => {
    button.addEventListener("click", () => {
      addCourseToPlan(button.dataset.addCourse, state.activeGrade, button.dataset.semester, button.dataset.zone || "regular");
    });
  });

  $all("[data-remove-course]").forEach((button) => {
    button.addEventListener("click", () => {
      removeCourseFromPlan(button.dataset.removeCourse, state.activeGrade, button.dataset.semester, button.dataset.zone || "regular");
    });
  });
}

function renderCourseActionPair(course, grade, semester) {
  const zone = course.section.includes("공동교육") ? "joint" : "regular";
  const stateForCourse = getPlannerCourseActionState(course, grade, semester);
  const addLabel = stateForCourse.selected ? "추가됨" : `${semester}학기 추가`;
  const limitTitle = stateForCourse.disabledByLimit ? `선택 묶음에서 ${stateForCourse.limit}과목을 이미 선택했습니다.` : "";
  return `
    <div class="quick-action-pair">
      <button type="button"
        data-add-course="${escapeHtml(course.id)}"
        data-semester="${semester}"
        data-zone="${zone}"
        ${stateForCourse.addDisabled ? "disabled" : ""}
        title="${escapeHtml(limitTitle)}">${addLabel}</button>
      <button type="button"
        class="cancel-choice-button"
        data-remove-course="${escapeHtml(course.id)}"
        data-semester="${semester}"
        data-zone="${zone}"
        ${stateForCourse.removeDisabled ? "disabled" : ""}>취소</button>
    </div>
  `;
}

function renderCourseChoiceStatus(course) {
  const statuses = getCourseSemestersForGrade(course, state.activeGrade)
    .map((semester) => {
      const actionState = getPlannerCourseActionState(course, state.activeGrade, semester);
      if (!actionState.group || !Number.isFinite(actionState.limit)) return "";
      return ` · ${semester}학기 ${actionState.selectedCount}/${actionState.limit}`;
    })
    .filter(Boolean);
  return statuses.join("");
}

function renderPlanner() {
  renderPlannerFixedView();
  renderPlannerMineView();
  $("#semesterGrid").innerHTML = ["1", "2"].map((semester) => renderSemesterBoard(state.activeGrade, semester)).join("");
  bindDropZones();
  bindRemoveButtons();
  renderPlannerSavedSummary();
  renderPlannerModeState();
  updateFormValues();
}

function renderPlannerSavedSummary() {
  const target = $("#plannerSavedSummary");
  if (!target) return;

  const rows = [];
  ["1", "2", "3"].forEach((grade) => {
    ["1", "2"].forEach((semester) => {
      const semesterPlan = state.plan[grade][semester];
      const items = [...semesterPlan.regular, ...semesterPlan.joint];
      const additionalItems = Array.isArray(semesterPlan.additional) ? semesterPlan.additional : [];
      if (!items.length && !additionalItems.length) return;
      const names = items
        .map((item) => {
          const course = getPlannerCourse(item.id);
          return course ? `${escapeHtml(course.name)} <small>${item.credits}학점</small>` : "";
        })
        .filter(Boolean)
        .join(", ");
      const additionalNames = additionalItems
        .map((item) => `${escapeHtml(item.name)} <small>${escapeHtml(item.area)} · ${escapeHtml(item.category)} · 추가</small>`)
        .join(", ");
      const combinedNames = [names, additionalNames].filter(Boolean).join("<br>");
      rows.push(`
        <tr>
          <th>${grade}학년 ${semester}학기</th>
          <td>${combinedNames}</td>
        </tr>
      `);
    });
  });

  target.innerHTML = rows.length
    ? `
      <table class="planner-summary-table">
        <tbody>${rows.join("")}</tbody>
      </table>
    `
    : `<div class="empty-note">아직 선택한 과목이 없습니다. 선택과목 또는 추가 교육과정을 입력해 보세요.</div>`;
}

function renderSemesterBoard(grade, semester) {
  const semesterPlan = state.plan[grade][semester];
  const plannedCourses = [...semesterPlan.regular, ...semesterPlan.joint];
  const selectedCredits = sumCredits(plannedCourses);

  return `
    <section class="semester-board">
      <div class="semester-head">
        <div>
          <h3>${grade}학년 ${semester}학기</h3>
          <p>${escapeHtml(getCurriculumPlanLabel(state.plannerPlan))} 기준 선택 희망 과목</p>
        </div>
        <span class="credit-badge ok">${plannedCourses.length}과목</span>
      </div>
      <div class="drop-zone" data-grade="${grade}" data-semester="${semester}" data-zone="regular">
        <p class="board-label">수강하고 싶은 선택과목</p>
        ${semesterPlan.regular.map((item, index) => renderPlannedCourse(item, grade, semester, "regular", index)).join("")}
        ${semesterPlan.joint.map((item, index) => renderPlannedCourse(item, grade, semester, "joint", index)).join("")}
      </div>
      <div class="semester-summary">
        <span class="summary-chip"><b>${plannedCourses.length}</b>선택 과목</span>
        <span class="summary-chip"><b>${selectedCredits}</b>선택 학점</span>
        <span class="summary-chip"><b>${semesterPlan.joint.length}</b>공동교육</span>
      </div>
    </section>
  `;
}

function renderPlannedCourse(item, grade, semester, zone, index) {
  const course = getPlannerCourse(item.id);
  if (!course) return "";
  const sectionLabel = getPlannerSectionLabel(course.section || (course.offered ? "학교 개설" : "추가 희망"));
  return `
    <div class="planned-course ${zone === "joint" ? "extra" : ""}">
      <div>
        ${renderCourseNameWithInfo(course)}
        <small>${escapeHtml(course.area)} · ${escapeHtml(sectionLabel)}</small>
      </div>
      <span class="planned-credit">${item.credits}학점</span>
      <button class="remove-course" type="button" aria-label="${course.name} 삭제"
        data-grade="${grade}" data-semester="${semester}" data-zone="${zone}" data-index="${index}">×</button>
    </div>
  `;
}

function bindDropZones() {
  $all(".drop-zone").forEach((zone) => {
    zone.addEventListener("dragover", (event) => {
      event.preventDefault();
      zone.classList.add("drag-over");
    });
    zone.addEventListener("dragleave", () => zone.classList.remove("drag-over"));
    zone.addEventListener("drop", (event) => {
      event.preventDefault();
      zone.classList.remove("drag-over");
      const courseId = event.dataTransfer.getData("text/plain");
      const courseZone = event.dataTransfer.getData("application/x-planner-zone") || zone.dataset.zone;
      addCourseToPlan(courseId, zone.dataset.grade, zone.dataset.semester, courseZone);
    });
  });
}

function bindRemoveButtons() {
  $all(".remove-course").forEach((button) => {
    button.addEventListener("click", () => {
      const list = state.plan[button.dataset.grade][button.dataset.semester][button.dataset.zone];
      list.splice(Number(button.dataset.index), 1);
      saveState(false);
      renderCoursePool();
      renderPlanner();
    });
  });
}

function bindAdditionalCourseControls() {
  $all("[data-additional-form]").forEach((form) => {
    const input = form.querySelector("[data-additional-course-name]");
    const preview = form.querySelector("[data-additional-course-preview]");
    if (!input || !preview) return;

    const updatePreview = () => {
      const course = getAdditionalCourseByName(input.value);
      preview.classList.toggle("valid", Boolean(course));
      preview.textContent = course
        ? `${course.area} · ${course.category}`
        : input.value.trim()
          ? "목록에 있는 과목명을 정확히 선택해 주세요."
          : "과목을 선택하면 교과군과 성격이 표시됩니다.";
    };

    input.addEventListener("input", updatePreview);
    updatePreview();
  });

  $all("[data-add-additional-course]").forEach((button) => {
    button.addEventListener("click", () => {
      const form = button.closest("[data-additional-form]");
      if (!form) return;
      addAdditionalCourseToPlan(form.dataset.grade, form.dataset.semester, form);
    });
  });

  $all("[data-remove-additional-course]").forEach((button) => {
    button.addEventListener("click", () => {
      removeAdditionalCourseFromPlan(button.dataset.grade, button.dataset.semester, Number(button.dataset.index));
    });
  });
}

function addAdditionalCourseToPlan(grade, semester, form) {
  const semesterPlan = state.plan[grade]?.[semester];
  if (!semesterPlan) return;
  if (!Array.isArray(semesterPlan.additional)) semesterPlan.additional = [];

  if (semesterPlan.additional.length >= 2) {
    showToast("추가 교육과정은 학기별 최대 2과목까지 입력합니다.");
    return;
  }

  const nameInput = form.querySelector("[data-additional-course-name]");
  const reasonInput = form.querySelector("[data-additional-course-reason]");
  const selectedCourse = getAdditionalCourseByName(nameInput?.value || "");
  const reason = String(reasonInput?.value || "").trim();

  if (!selectedCourse) {
    showToast("목록에서 과목명을 정확히 선택해 주세요.");
    return;
  }

  if (!reason) {
    showToast("그 과목이 필요한 이유를 한 문장으로 적어 주세요.");
    return;
  }

  if (semesterPlan.additional.some((item) => normalizeText(item.name) === normalizeText(selectedCourse.name))) {
    showToast("이미 이 학기에 추가한 과목입니다.");
    return;
  }

  semesterPlan.additional.push({
    name: selectedCourse.name,
    area: selectedCourse.area,
    category: selectedCourse.category,
    reason
  });

  saveState(false);
  renderPlannerMineView();
  renderPlannerSavedSummary();
}

function removeAdditionalCourseFromPlan(grade, semester, index) {
  const additional = state.plan[grade]?.[semester]?.additional;
  if (!Array.isArray(additional) || !additional[index]) return;
  additional.splice(index, 1);
  saveState(false);
  renderPlannerMineView();
  renderPlannerSavedSummary();
}

function addCourseToPlan(courseId, grade, semester, zone) {
  const course = getPlannerCourse(courseId);
  if (!course) return false;
  const semesterKey = `${grade}-${semester}`;

  if (course.semesters?.length && !course.semesters.includes(semesterKey)) {
    showToast("이 과목은 해당 학기에 편성된 과목이 아닙니다.");
    return false;
  }

  const list = state.plan[grade][semester][zone];
  if (list.some((item) => item.id === course.id)) {
    showToast("이미 이 학기에 추가한 과목입니다.");
    return false;
  }

  const actionState = getPlannerCourseActionState(course, grade, semester);
  if (actionState.disabledByLimit) {
    showToast(`이 선택 묶음은 ${actionState.limit}과목까지만 선택할 수 있습니다.`);
    return false;
  }

  list.push({ id: course.id, credits: getPlannerCourseCredits(course, grade, semester) });

  const jointCredits = sumCredits(state.plan[grade][semester].joint);
  if (zone === "joint" && jointCredits > 6) {
    list.pop();
    showToast("공동교육과정은 학기당 최대 6학점까지 입력합니다.");
    return false;
  }

  saveState(false);
  renderCoursePool();
  renderPlanner();
  return true;
}

function removeCourseFromPlan(courseId, grade, semester, zone) {
  const semesterPlan = state.plan[grade]?.[semester];
  if (!semesterPlan) return;
  const zones = zone ? [zone] : ["regular", "joint"];
  let removed = false;

  zones.forEach((zoneKey) => {
    const list = semesterPlan[zoneKey] || [];
    const index = list.findIndex((item) => item.id === courseId);
    if (index >= 0) {
      list.splice(index, 1);
      removed = true;
    }
  });

  if (!removed) return;
  saveState(false);
  renderCoursePool();
  renderPlanner();
}

function sumCredits(list) {
  return list.reduce((sum, item) => sum + Number(item.credits || 0), 0);
}

function updateFormValues() {
  if ($("#plannerPlanSelect")) $("#plannerPlanSelect").value = state.plannerPlan;
}

function saveState(showMessage) {
  rememberActivePlannerPlan();
  const data = {
    activeGrade: state.activeGrade,
    activeCurriculumPlan: state.activeCurriculumPlan,
    activeCurriculumScope: state.activeCurriculumScope,
    plannerPlan: state.plannerPlan,
    plannerPlans: state.plannerPlans,
    selectedTrack: state.selectedTrack,
    targetMajor: state.targetMajor,
    plan: state.plan,
    creative: state.creative,
    inquiry: state.inquiry
  };
  localStorage.setItem(STORAGE_KEY, JSON.stringify(data));
  if (showMessage) showToast("저장했습니다. 이 브라우저에서 선택한 과목을 다시 불러올 수 있습니다.");
}

function loadState() {
  try {
    const saved = JSON.parse(localStorage.getItem(STORAGE_KEY));
    if (!saved) return;
    state.activeGrade = saved.activeGrade || state.activeGrade;
    state.activeCurriculumPlan = saved.activeCurriculumPlan || state.activeCurriculumPlan;
    state.activeCurriculumScope = saved.activeCurriculumScope || state.activeCurriculumScope;
    state.plannerPlan = saved.plannerPlan || state.plannerPlan;
    state.plannerPlans = saved.plannerPlans || {};
    state.selectedTrack = saved.selectedTrack || state.selectedTrack;
    state.targetMajor = saved.targetMajor || "";
    if (!state.plannerPlans[state.plannerPlan] && saved.plan) {
      state.plannerPlans[state.plannerPlan] = saved.plan;
    }
    state.plan = clonePlannerPlan(state.plannerPlans[state.plannerPlan] || saved.plan || createEmptyPlan());
    state.creative = { ...state.creative, ...(saved.creative || {}) };
    state.inquiry = { ...state.inquiry, ...(saved.inquiry || {}) };
  } catch {
    localStorage.removeItem(STORAGE_KEY);
  }
}

function rememberActivePlannerPlan() {
  state.plannerPlans[state.plannerPlan] = clonePlannerPlan(state.plan);
}

function clonePlannerPlan(plan) {
  return normalizePlannerPlan(JSON.parse(JSON.stringify(plan || createEmptyPlan())));
}

function normalizePlannerPlan(plan) {
  const normalized = createEmptyPlan();
  ["1", "2", "3"].forEach((grade) => {
    ["1", "2"].forEach((semester) => {
      const semesterPlan = plan?.[grade]?.[semester] || {};
      normalized[grade][semester] = {
        regular: Array.isArray(semesterPlan.regular) ? semesterPlan.regular : [],
        joint: Array.isArray(semesterPlan.joint) ? semesterPlan.joint : [],
        additional: Array.isArray(semesterPlan.additional)
          ? semesterPlan.additional.map(normalizeAdditionalCourseItem).filter(Boolean)
          : []
      };
    });
  });
  return normalized;
}

function normalizeAdditionalCourseItem(item) {
  if (!item || !item.name) return null;
  const matchedCourse = getAdditionalCourseByName(item.name);
  return {
    name: matchedCourse?.name || String(item.name).trim(),
    area: matchedCourse?.area || String(item.area || "기타").trim(),
    category: matchedCourse?.category || getAdditionalCourseCategoryLabel(item.category),
    reason: String(item.reason || "").trim()
  };
}

function switchPlannerPlan(planKey) {
  if (!curriculumData.plans?.[planKey]) return;
  rememberActivePlannerPlan();
  state.plannerPlan = planKey;
  state.plan = clonePlannerPlan(state.plannerPlans[planKey] || createEmptyPlan());
  saveState(false);
  renderCoursePool();
  renderPlanner();
}

function loadSavedPlannerSelection() {
  loadState();
  renderPlannerPlanOptions();
  renderCoursePool();
  renderPlanner();
  showToast("저장된 선택 과목을 불러왔습니다.");
}

function resetCurrentPlannerPlan() {
  const label = getCurriculumPlanLabel(state.plannerPlan);
  if (!window.confirm(`${label}에서 선택한 과목과 추가 교육과정만 초기화합니다. 다른 학년도의 선택 내용은 유지됩니다.`)) return;

  state.plan = createEmptyPlan();
  state.plannerPlans[state.plannerPlan] = clonePlannerPlan(state.plan);
  saveState(false);
  renderCoursePool();
  renderPlanner();
  showToast(`${label} 선택 내용을 초기화했습니다.`);
}

function escapeHtml(value) {
  return String(value || "")
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#039;")
    .replaceAll("\n", "<br>");
}

function escapeAttribute(value) {
  return String(value || "")
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#039;");
}

function escapeTextareaValue(value) {
  return escapeAttribute(value);
}

function formatDateLabel(value) {
  if (!value) return "확인 중";
  const [year, month, day] = String(value).split("-");
  if (!year || !month || !day) return String(value);
  return `${year}.${month}.${day}`;
}

function showToast(message) {
  const toast = $("#toast");
  toast.textContent = message;
  toast.classList.add("show");
  window.setTimeout(() => toast.classList.remove("show"), 2200);
}

function initAdmissionDetailPage() {
  const root = $("#admissionDetailPage");
  const pageKey = root?.dataset.admissionPage || "";
  const admissionPages = window.ANJWA_ADMISSION_PAGES || { pages: {}, groups: {} };
  const page = admissionPages.pages?.[pageKey];
  if (!root || !page) return;

  document.title = `${page.title} | 진로진학 플랫폼`;
  root.innerHTML = renderAdmissionDetailPage(pageKey, page, admissionPages.groups?.[page.group] || []);

  if (page.feature === "recommendations") {
    state.recommendationMode = "major";
    bindRecommendationControls();
    renderRecommendationExplorer();
  }
  if (page.feature === "competencies") {
    renderCompetencies();
  }
  if (page.feature === "sechuk") {
    bindSechukFlow();
  }
}

function renderAdmissionDetailPage(pageKey, page, tabs) {
  const toneClass = page.tone === "holistic" ? "admission-tone-holistic" : "admission-tone-subject";
  return `
    <div class="section-head subject-section-head admission-detail-head ${toneClass}">
      <div class="section-title-wrap">
        <div class="section-title-row">
          <h1>
            ${renderAdmissionTitleIcon(page.tone)}
            <span>${escapeHtml(page.title)}</span>
          </h1>
          <a class="ghost-button back-home-button" href="./index.html">첫 화면으로</a>
        </div>
        <p>${escapeHtml(page.summary)}</p>
      </div>
    </div>

    <nav class="in-page-tabs admission-page-tabs ${page.tone === "holistic" ? "holistic-tabs" : ""}" aria-label="${escapeHtml(page.title)} 세부 안내">
      ${tabs.map((tab) => `<a class="${tab.key === pageKey ? "active" : ""}" href="${escapeAttribute(tab.href)}">${escapeHtml(tab.label)}</a>`).join("")}
    </nav>

    ${page.basis ? `<aside class="admission-basis-note"><strong>자료 기준</strong><span>${escapeHtml(page.basis)}</span><small>최근 확인 2026.07.18</small></aside>` : ""}

    <div class="detail-hero ${page.tone === "holistic" ? "holistic-hero" : "subject-hero"}">
      <div>
        <span class="label">${escapeHtml(page.heroLabel || "핵심 이해")}</span>
        <h3>${escapeHtml(page.heroTitle)}</h3>
        <p>${escapeHtml(page.heroText)}</p>
      </div>
      <div class="admission-detail-guide">
        ${renderAdmissionDetailGuide(pageKey, page)}
      </div>
    </div>

    ${page.body || ""}
  `;
}

function renderAdmissionDetailGuide(pageKey, page) {
  const guide = getAdmissionDetailGuide(pageKey, page);
  return `
    <b>${escapeHtml(guide.title)}</b>
    <ul>
      ${guide.items.map((item) => `<li>${escapeHtml(item)}</li>`).join("")}
    </ul>
  `;
}

function getAdmissionDetailGuide(pageKey, page) {
  const guideMap = {
    subject: {
      title: "먼저 확인할 것",
      items: ["교과전형은 내신 평균만 보는 전형이 아니라는 점을 잡습니다.", "성적, 학점수, 대학별 계산법, 수능최저, 서류 반영을 나누어 봅니다."]
    },
    "subject-flow": {
      title: "읽는 순서",
      items: ["내 성적을 먼저 읽고, 대학 반영 방식으로 다시 계산합니다.", "마지막에 수능최저와 추천 가능 여부를 확인합니다."]
    },
    "subject-score": {
      title: "성적표에서 볼 것",
      items: ["등급 개수만 보지 말고 과목별 학점수 비중을 함께 봅니다.", "대학 환산점수는 평균등급과 다를 수 있음을 확인합니다."]
    },
    "subject-calculation": {
      title: "대학 사례를 보는 법",
      items: ["사례의 숫자를 외우기보다 대학마다 계산 방식이 다르다는 점을 확인합니다.", "지원 전에는 반드시 해당 연도 모집요강의 산출식을 다시 확인합니다."]
    },
    "subject-minimum": {
      title: "수능최저 확인",
      items: ["수능최저는 합격 가능성을 가르는 문턱입니다.", "대학, 전형, 모집단위별 기준이 다르므로 학과 단위로 확인합니다."]
    },
    "subject-document": {
      title: "서류 반영 확인",
      items: ["교과전형이어도 서류평가가 들어가면 과목 이수와 학생부 흐름이 중요합니다.", "전공 관련 과목을 들었는지, 수업 기록이 연결되는지 봅니다."]
    },
    "subject-misunderstanding": {
      title: "오해를 풀며 점검",
      items: ["내신 평균, 과목 선택, 수능최저, 추천, 출결에 대한 오해를 하나씩 확인합니다.", "상담 전에는 내가 해당되는 질문을 표시해 두면 좋습니다."]
    },
    holistic: {
      title: "종합전형의 중심",
      items: ["활동량보다 수업 속 질문과 성장 흐름을 봅니다.", "과목 선택, 세특, 탐구, 창체, 면접이 자연스럽게 이어지는지 확인합니다."]
    },
    "holistic-flow": {
      title: "읽는 순서",
      items: ["전형 방식, 학생부 항목, 평가역량, 과목 선택, 세특, 면접 순서로 봅니다.", "각 단계가 따로 놀지 않고 하나의 성장 흐름이 되는지 확인합니다."]
    },
    "holistic-record": {
      title: "학생부 읽는 법",
      items: ["학생부 항목을 따로 외우기보다 항목 사이의 연결을 봅니다.", "독서와 탐구는 세특, 창체, 면접 설명으로 이어질 때 의미가 커집니다."]
    },
    "holistic-competency": {
      title: "역량 확인",
      items: ["학업역량이 중심이고, 진로역량과 공동체역량은 수업 안에서 함께 드러납니다.", "희망 진로와 직접 관련 없는 과목도 학업역량을 보여줄 수 있습니다."]
    },
    "holistic-subjects": {
      title: "추천 과목 사용법",
      items: ["희망 학과가 정해졌다면 학과명으로, 아직 고민 중이라면 계열명으로 찾아봅니다.", "대학별로 확인하고 싶을 때는 대학명과 학과명을 함께 넣어 좁혀 봅니다."]
    },
    "holistic-sechuk": {
      title: "세특 흐름 만들기",
      items: ["진로를 억지로 붙이지 말고 수업 개념에서 질문을 시작합니다.", "동기, 질문, 개념, 과정, 결과, 한계, 후속 탐구가 이어지는지 봅니다."]
    },
    "holistic-ai": {
      title: "AI 사용 기준",
      items: ["AI는 학생부 문장을 대신 쓰는 도구가 아니라 질문을 넓히는 도구입니다.", "주제 후보, 반론, 한계, 추가 자료를 찾는 용도로 사용합니다."]
    },
    "holistic-interview": {
      title: "면접 준비",
      items: ["학생부에 적힌 내용을 외우기보다 왜 했는지와 무엇을 배웠는지 설명합니다.", "결과뿐 아니라 과정, 한계, 다음 탐구까지 말할 수 있어야 합니다."]
    }
  };
  return guideMap[pageKey] || {
    title: "이 페이지에서 할 일",
    items: [page.summary || "상단 탭으로 필요한 내용을 나누어 확인합니다."]
  };
}

function renderAdmissionTitleIcon(tone) {
  if (tone === "holistic") {
    return `
      <svg class="title-holistic-icon" viewBox="0 0 36 36" aria-hidden="true" focusable="false">
        <rect x="8" y="7" width="20" height="23" rx="4" />
        <path d="M13 14H23M13 19H20" />
        <circle cx="13" cy="25" r="2.5" />
        <circle cx="23" cy="25" r="2.5" />
        <path class="holistic-link" d="M15.5 25H20.5" />
      </svg>
    `;
  }
  return `
    <svg class="title-subject-icon" viewBox="0 0 36 36" aria-hidden="true" focusable="false">
      <rect x="8" y="6" width="20" height="24" rx="4" />
      <path d="M13 14H22M13 20H19" />
      <path class="subject-check" d="M20 25L23 28L29 20" />
    </svg>
  `;
}

function initRecommendationOnly(defaultMode = "advanced") {
  state.recommendationMode = defaultMode;
  bindRecommendationControls();
  renderRecommendationExplorer();
}

document.addEventListener("DOMContentLoaded", () => {
  if ($("#admissionDetailPage")) {
    initAdmissionDetailPage();
    return;
  }

  if ($("#home")) {
    init();
    return;
  }

  if ($("#advancedRecommendationPage")) {
    initRecommendationOnly("major");
  }
});
